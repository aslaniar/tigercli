# The Layer-1 live console launcher (the reviewer's game plan).
# One window: the server + the client logs merged, with the ladder panel, the
# bootflow strip, the trap translations, and the census feed.
# The old recording tails: live_server_tail.ps1.v1 + live_client_tail.ps1.v1.
python -X utf8 "C:\Users\rasla\Downloads\destiny-preservation\RE_scripts\live_console.py"
