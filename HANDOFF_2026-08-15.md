STATUS: historical - superseded by HANDOFF_OPENCODE_2026-08-25_EVENING.md and STATE.md (marked 2026-08-26). Kept for archaeology.

# SESSION HANDOFF — 2026-08-15 (read after AGENTS.md + GAME_PLAN_2026-08-14.md)

Written at the depth-era close, before a model switch. Everything here is
conversational state that exists NOWHERE else. Read fully; do not skim.

## STANDING HARD RULES (user-set, do not re-litigate)

1. **ALL subagents run deepseek-v4-flash** (`opencode-go/deepseek-v4-flash`, max
   effort). GLM 5.3 ONLY when the user explicitly asks. The user is cost-conscious;
   the main session is the expensive one — delegate everything delegable.
2. **Delegate-don't-act**: if a subagent CAN do the work, it does. Main session
   executes only: Ghidra passes, live RPM reads, dotnet runs, and VERIFICATION of
   agent claims. If an agent's work is wrong, delegate the fix back — don't
   hand-fix it in the main session (the vendor-walk compile-fix loop of 8.23-8.27
   burned significant money doing exactly this; user called it out twice).
3. **Verification discipline**: every agent claim gets mechanically checked against
   Ghidra/live reads before entering the map. The gate has caught 5+ wrong-but-
   confident claims (GCM normalization, retry bound, class-vs-type, the 12,699
   artifact, the "third key" false alarm). It also catches MAIN-SESSION errors —
   the coldrun lane caught my own 12,699-count artifact. The gate is symmetric.
4. **No benchmarking / no A/B unless asked.** User judges model quality by eye.
5. **Cost reporting**: user watches spend. When grinding long, report where the
   money is going. Never edit-compile-inspect loop in the main session — batch it,
   delegate it, or write it once correctly.
6. **WINDOWS SHELL RULE** (added 2026-08-15, after a session of PowerShell quote
   mangling): anything longer than one line, or containing quotes inside quotes,
   goes in a FILE (RE_output/claims/*.py or *.cmd) and the file is run. Inline
   `python -X utf8 -c` is for single-line, quote-free snippets only. PowerShell
   stays the orchestration shell — Start-Process -Verb RunAs (elevated server
   cycles), Get-NetTCPConnection/Get-Process/Copy-Item all work fine. Do NOT
   layer `bash -c` under PowerShell for anything complex. The config-level shell
   switch to Git Bash (opencode.json "shell") was considered and REJECTED: the
   RunAs/port/process ops are PS-only, and the file-form pattern has been ~20/20
   clean this session while inline content was the only mangling source.

## AGENT CONFIGURATION (working, do not break)

- Agent files: `.opencode/agent/*.md` (project) + `~/.config/opencode/agent/*.md`
  (global; the TUI reads the GLOBAL one — this took three restarts to learn).
- **`steps: 0` is INVALID** (schema wants PositiveInt) — it silently drops the
  agent from the registry with no error. "No cap" = OMIT the field entirely.
- `edit:` scoped as an object HIDES edit tools entirely (v1 visibleTools filter).
  Working pattern: `edit: allow` + `external_directory: deny`.
- Agent registry caches at startup: new/edited agent files need an opencode
  restart to register. A "Unknown agent" error = stale registry, not a bad file.
- Silent deaths happen (2 of ~30 flash runs died with no final text; claims files
  were intact both times — recoverable by reading the file and continuing; the
  parent picks up the REQUEST queue). GLM at steps:80 hit its cap every time but
  always delivered structured handoffs.
- Current lanes (all flash, no cap): `utility` (general executor),
  `coldrun-triage`, `content-extraction`, `decoder-runbook`, `family3-roster`,
  `opcode-sweep`, `bap-dispatch`, `deadorbit-pipeline`, `entity-combatants`,
  `family4-decoders`, `signon-schema` (the original five still exist; stale
  step:40 caps on those are harmless). GLM: `schema-namejoin`,
  `family7-closeout`, `entity-archetypes`.

## TECHNICAL ORCHESTRATION MEMORY (nowhere else)

- **Python ctypes BCrypt wrapper is BROKEN** (marshaling bug, hit twice — spawn
  sets and vendor walk). ALL AES-GCM goes through the C# net9 tool:
  `RE_tools/TagDecode.cs` (project `RE_tools/gcmtool.csproj`, `dotnet run`).
  Its `DecodeTag(uint tag)` resolves any .pkg tag end-to-end. Keys:
  `RE_output/content/key_table.bin` (layout: alternateKey[0:16],
  identityConstant[16:32], nonceBase[32:44]; primary = ASCII
  "2MFioXto7iAUN4Qj" byte-added to identityConstant; nonce = nonceBase[12],
  [0]^=pid>>8, [1]=0xF9, [11]^=pid&0xFF; blocks live in ANY patch file of the
  family, decimal patch suffix).
- **find_pid zombie bug (FIXED 2026-08-14 evening)**: a crashed leftover
  destiny2.exe shadows the real one; find_pid now prefers an openable pid.
- **Live-read one-offs**: inline `python -X utf8 -c` snippets via
  `dump_sunrise_memory.py` (RPM). The game must be PAST character select
  (SignOn sets bootstrapTokenPresent). At a DESTINATION = best state (cold runs
  + full schema nodes live). Never ask the user to relaunch just for a read —
  batch reads for when they're playing anyway.
- **Ghidra projects**: `d2_arrivals` (94% image) + `d2_full` (100% image,
  4,000 noise functions PURGED 2026-08-14). Automation = PYTHON (PyGhidra:
  `pyghidraRun.bat -H RE_output/ghidra d2_full -process destiny2_unpacked_full.exe
  -noanalysis -scriptPath RE_output/content -postScript <script>.py`), never Java —
  the AGENTS.md GHIDRA TOOLING RULE (2026-08-16) is the pattern. Output to
  RE_output/export/phaseN/ or the lane raw files.
- **Content extraction pipeline (proven)**: pkg header v38 → entry table (16B
  records, reference = class) → block table (48B records) → per-patch AES-GCM →
  Oodle (game's own oo2core, threadPhase=3) → records. Generic tool = TagDecode.
  386 spawn sets extracted (spawn_sets_full.json, 8,779 points). Activity defs
  confirmed decodable (first payload w/ child tags). REDACTED blocks (0x8) exist
  in the format but were a false alarm for investment (flags are all 0x3).

## IN-FLIGHT STATE (as of this writing)

1. **Vendor walk** — flash lane working R8-fix (build_data.bin record layout was
   misparsed: names shifted one byte, lengths read as 128-cap; TRUE layout must
   come from RE_build/Sunrise-021/Sunrise/src/state/content_manifest/cache/
   format.h). The cache itself is a goldmine: 2,285 named records, 15,424 items,
   466 scenarios, 2,090 spawn name-hashes — Sunrise's own extraction catalog.
   Container question: which tag holds the investment root (R1/R2 both zero-hit;
   FNV-1("investment_globals")=0x6F7125CB confirmed correct).
2. **Decoder post-read** — needs user to Change Character in-game (one command
   after; SPEC 2B in live_session_reads.py). Expect NEW [3] only; zero-yield is
   a valid complete negative.
3. **Schema field names** — registry at 0x141FBCD08 = CVAR registry (eliminated
   with evidence). Oracle candidates left: row-chain col2 aux pointers (raw is
   banked in schema_streams2_raw.bin, 29.6MB — offline testable forever) and the
   phase9 gap-A list-heads. Schema itself: 8 nodes/362 fields captured at
   destination; grammar + tree-cut-points closed; names are the last mile.
4. **Cold-run batches** — 8 ranked requests in claims/coldrun-naming.md; runs
   2-6 of 8 remain (batch A/B done; purge done).

## THE REASONING PATTERNS THAT GOT US HERE (the method, preserved)

- **Swarm-and-verify loop**: frozen corpus → parallel flash lanes with
  evidence-gated claims (addr + quoted evidence + confidence) → main-session
  mechanical verification → targeted requery → repeat. Four waves built the
  entire protocol map this way.
- **Distillation into context** (RE_scripts/domain_brief.md): the teacher-student
  trick, prompt-side. Ground truth + conventions + TRAP LIST + gold-standard
  pointer. Zero gate-catches on lane claims the entire post-brief wave.
- **Trap list maintenance**: every gate-caught error becomes a numbered trap in
  the domain brief. Traps are why flash quality rose — the model didn't get
  smarter, the mistakes got pre-empted.
- **Write-early-and-often rule**: a full wave was once lost to a silent cap
  death. Every brief carries the rule; every lane writes claims as it goes.
- **REQUEST gate**: agents with no Ghidra/bash file REQUESTS; the main session
  executes and the corpus grows. One 5-min Ghidra pass produces corpus that
  would cost millions of tokens to rediscover.
- **Expansion-first mandate** (user-set): RE targets are what Sunrise's source
  cannot answer. Its layouts are prior context, not the deliverable.

## NEXT ERA: S0 (the build phase)

S0 = extract Sunrise's in-process server into a standalone binary via the
external_server seam. ZERO open RE questions block it. References: the Proton
gist CMake build (FINDINGS 8.19), our v143 build (8.11), the closed client
contract (five claims specs). The user will likely open this next.

## IN-FLIGHT UPDATE (2026-08-15, final action of the founding session)
Supersedes the in-flight section above where they conflict:
- Vendor walk advanced through waves 3-5 (FINDINGS 8.29-8.32). Container identified
  by cache+FNV: tag 0x80B754B4 (pid 0x1BA) is the best candidate but the R16 gate
  read the WRONG ROW (off-by-one or layout difference in the entry-table read;
  idx 5301 is the suspect). The lane also caught TWO tool bugs: (1) TagDecode.cs
  line 64 appends RAW bytes on oodle failure - the 1304B container-fit was
  arithmetic on corrupted data - fix before ANY further decode; (2) the cache-tag
  byte-swap theory is refuted (cache tags are clean). 1.4MB blob = index table
  (item-index candidate: 0x80807BEA x15424 = cache itemCount).
- NEXT SESSION EXECUTION ORDER: R19 (entry-table row dumps: 0x1BA patch5 rows
  5288-5312, 0x59B patch3 rows 4496-4520, 0x914 patch3 rows 4706-4730, + header
  rows + full-table 0x80805BB1 scan) -> R21 (decode idx 5299/5300/5301 three-way +
  structural scan of the 1.4MB blob) -> R20 (block-1172 cross-test both patch
  files + re-gate 0x81A2926E) -> re-run the gate on the corrected row. Full specs
  in RE_output/claims/vendor-walk.md (sections 11-12).
- FIRST ACTIONS for a new session (before resuming the walk):
  1. Fix TagDecode.cs line 64: throw on oodle failure, never append raw bytes.
  2. winget install Rustlang.Rustup; cargo install tiger-pkg. Use tiger-pkg
     natively for pkg decode (its d2_prebl block/patch selection is battle-tested;
     likely clears block-1172 for free).
  3. Enable PyGhidra in Ghidra (kills the Java-script compile tax).
  4. Optionally fix the python BCrypt marshaling or just standardize on tiger-pkg.
- Bubble CSVs (3 files, workspace root; FINDINGS 8.30): sunrise_bubbles.csv x
  class_80809162_entries.json cross-join = zero-cost semantic spawn labels.
  Queued behind the vendor item.
