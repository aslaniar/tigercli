# FINDINGS - 2026-08-16

## 10.1 THE S2-0 PROBE DEPLOY (the carrier experiment armed)

- The S2 v0 lane's deliverable (claims/s2v0-carrier-spawn.md): the honest negative on
  R2 (the carrier = NOT corpus-resolvable; narrowed to {40,41,54,55} + the live-capture
  plan) + the built probe machinery: Hook B (the log-only detour at FUN_140E0F000 that
  logs name/payloadLen/first-bytes for EVERY decoded push), the flag-gated emission
  (the baseline builder with the F3 property order + the carrier push + the type-52
  epoch bump, appended AFTER the unchanged join burst), and the two knobs
  (world_population_carrier, world_population_schema_hash - defaults 54 + n1019's
  placeholder lo32 0xCEC26333).
- DEPLOYED + boot-verified (this session): the new server exe (9d6498e4b5c19b79,
  7,569,920 B) + the cache re-stamped to its PE identity (ts 1786864899, size 20197376;
  the checksum chain preserved = 0x1480C7C0EBBA16DB - the subclass-fixed chain) + the
  server settings world_population=true + the new client DLL (e1d4bf0abd4d9caf,
  7,933,440 B - contains BOTH the package_validator strings AND the Hook B strings -
  the validator hook = preserved in the S2 build). The server boot = clean (the swaps
  match, the cache accepted). The backups: .bak_s2v0 x4 (the exe, the cache, the
  settings, the DLL).
- THE PROBE BOOT (the user's next launch): the join burst unchanged -> the emission
  (ev=world_population stage=push type=54 + stage=epoch type=52) -> Hook B's client-side
  log (ev=handle_message stage=push name=.. type=..) -> THE VISUAL GATE: one entity
  anywhere in the Tower. If the carrier fails: cycle the knob 40/41/55 (one boot each).


## 10.2 THE S2-0 LIVE CAPTURE (the carrier narrowed further + the ASLR trap)

- THE ASLR RE-ROLL (new trap for the record): this boot's game base = 0x7FF795BD0000,
  NOT the pinned 0x7FF641BF0000. The base is NOT stable across launches within a
  session (the earlier boots' identical base = a long coincidence). ALL the "RPM
  blocked / everything MEM_FREE" readings = the stale-base artifact. RULE: every live
  tool resolves the module base via find_module_base (with the loader-lock retry) +
  verifies the MZ header before ANY read. The s2v0_capture.py = fixed accordingly.
- THE CAPTURE (the Tower, the live base): the schema root = 0x1F882C669B8 (the heap;
  the groups captured with the "loop" name-hash visible). The 15-object table's
  type getters = mov eax,imm; ret stubs with the types:
  {123, 124, 9, 8, 160, 161, 6, 150, 47, 100, 301, 51, 27, 20, 28}.
  CORRECTION to the lane's V1: the message-type space = WIDER than the 59-name enum,
  and the join-burst types {0,1,4,5} = NOT in the 15-object set (the lane's "the
  join burst proves the 15-object table" = wrong; the join types = a different path).
  The type-54 = NOT in the set + its state-table entry = 0 -> the carrier-54 crash
  = the unhandled-type path, consistent.
- The state table (DAT_14280E3E0): types 6..51 = populated (the state objects @
  RVA 0x1FBF2xx); types 0..5 + 52..58 = ZERO (the type-52 patch_epoch = unregistered
  too - the lane's epoch push = also unhandled; harmless by itself, but noted).
- The decoders (the vtable+0x18 of the state objects): type 40 (script_state) =
  0x140E0DC10 (the shared generic decode); type 41 (script_event) = 0x140106E9E0
  (a UNIQUE decoder); type 47 = 0x140106E620; type 51 = 0x140E74450. NONE of the
  unique decoders = in the corpus (the grep = zero hits) -> the payload shape needs
  a fresh Ghidra decompile pass on those RVAs (the d2_full project = free).
- NEXT (the carrier close-out): (a) the Ghidra decompile of the type-41 decoder
  (0x140106E9E0) = the script_event payload shape - the strongest candidate (a
  unique decode, the "script" family = the entity-adjacent); (b) then the probe
  boot with the carrier = 41 + the shape-matched baseline; (c) the 15-set's other
  members (123/124/150/160/161/301 - the unnamed ids) = the fallback probes.


## 10.3 THE ARMOR RENDERS (01:1x, the repick-fix boot)

The user's first goal of the night = CLOSED: the armor repick (the 15 equipped items ->
the socket-having authored-era Scatterhorn/exotic armor) + the eqHash re-stamp =
deployed and the boot shows the REAL armor meshes instead of the space placeholder.
The chain: the repick-fix lane's root-cause (the cache's configuredEquipmentHash
fingerprints the account's equipped items - Phase A changed 15 hashes so the identity
had to be re-stamped to 0x111D8144D0F77456, not preserved) + the fixed script's
server-gate verification (fresh apply -> loaded, all swaps match at the boot).
THE SUB-CLASS INVENTORY = deferred (its lane proved the DB rows alone are inert: the
fork's persistence only consumes equipped items and write_back wipes the rest - the
fix = a fork capability change; the six class-locked picks stay banked).
REMAINING TONIGHT: the S2 carrier (the type-41 decoder decompile -> the payload shape
-> the probe boot) and the Tower population.


## 10.4 DECODER-41 LANE (the decompile verdict - the carrier probe refined, NOT booted)

- The type-41 decoder (FUN_14106E9E0, 82 B) = decompiled: the payload = a PROTOTUF
  (LEB128 varint keys, wire types 0/1/2/5, field 1 = the u32 varint, field 2 = the
  length-delimited nested {u64, u64, <=176-B blob}); the empty payload = accepted; the
  decoded-size = 0xF0. The APPLY thunk = "return 0xffffffff" (a no-op success) - the
  type-41 = proves the decode path but does NOT render an entity. The F3-shaped entity
  baseline = the WRONG family for 41 (it belongs to the reader-table family: the
  carriers 54/40).
- The lane's stage probes (for the next session): stage 1 = the zero-length payload
  (the guaranteed-accept decode test + the Hook B readout); stage 2 = 08 00 12 04 08
  00 10 00; stage 3b = the 16-B nested blob. The boot signal: Hook B
  "ev=handle_message result=ok name=script_event type=41" + no fail-arm + no crash.
- The carrier hunt's next target = the reader-table family (the type-54/40 shapes),
  NOT the script family. The type-54's crash = the payload overran ITS decoder (the
  join's own 1-byte type-54 = the bubble-host state = the working reference).
- The night = closed: the -87 dead, S1 accepted, the armor rendering, the carrier
  narrowed + the decode grammar in hand. The probe boot = the next session's first
  boot (the stage-1 accept test, then the reader-table baseline).

## 10.5 THE INVENTORY MILESTONE LAUNCHES (the wave-1 lane board, ~10:5x)

- The goal framing (with the outside reviewer): full inventory working � subclasses,
  weapons, armor, shaders � with real persistence. The client supports all of it; the
  gap = 100% server-side (the fork publishes equipped-only at join; write_back wipes
  non-equipped rows). The three layers: skeleton (the request/mutate/publish/persist
  loop, built once), plumbing (per-family wire grammar; F4 done), policy (per-verb
  rules, mostly extracted data). The equip verb first = its policy is pure extracted
  data (49 bucket descriptors, 14 socket lists, 9 socket tables ARE the rules).
- PRE-FLIGHT: the fork baseline committed = cb4c0fb (RE_build/Sunrise-fork master,
  clean tree). The S2 lanes on disk: vendor-first + sobject landed (10:54/10:35).
- WAVE 1 LAUNCHED (5 parallel lanes, READ-ONLY, zero boots):
  L2 snapshot census / L4 policy spec / L5 transaction design / L6 soid+write_back /
  L7 perk-bank. Briefs: claims/inventory-l2..l7-*-brief.md. Deliverables:
  claims/inventory-l2..l7-*.md. Logs: content/lane_inv_*.log.
  L1 equip-message RE + L3 delta-encoder RE = QUEUED (the d2_full Ghidra lock;
  sequence L1 ? L3; briefs written and ready).
- The disk-validated corrections baked into the briefs: the subclass-inventory.md �3
  census names the snapshot-gap sites (persistence.cpp:624-627/1199-1212,
  character_encoder.cpp:157-169, loadout_resolver.cpp:101, account_state.h:62-94) �
  L2/L6 EXTEND it, not re-derive; the snapshot fix's hidden third prerequisite = the
  cache rebuild (itemDetails dense 1189/1190/1192/1193/1195/1196 + abilityBuckets
  for lists 3/6/10), without which the resolve fails the whole loadout.
- NEXT: integrate the five deliverables as they land (liveness check = the mtimes);
  then wave 2 = the snapshot-generalization build lane ? the first boot (the 6
  subclass picks visible), gated by the main session's boot rights.


## 10.5 THE VENDOR PROBE (the first logged boot) - no Zavala, the black-screen wrinkle, the REAL schema hash

- The vendor-first stack deployed (the commit cb4c0fb + the exe a765b9fd + the DLL
  6c955b64 + the re-stamped cache) and the probe booted: the emission FIRED (the
  server log: world_population type=7 body=137 + the epoch, after the join burst)
  but NO vendor rendered. The client = silent (no crash, no entity) - the failure =
  the client-side decode chain (the lane's ranked risks).
- The client-side logging = enabled (the file_sink + the client:info byte-edits) and
  the second boot produced the observability:
  * Hook B = live: the type-9 pushes decode ("name=BAP ID 1 desc:OUT FAH type=9").
  * THE SCHEMA-CAPTURE HOOK = FIRED: "ev=schema_capture stage=decode result=ok
    hash=0x80806AC0 bucket=3 row=2752 codec=0" - THE REAL schemaTagHash =
    0x80806AC0 (the tag-handle family, the bucket 3), NOT the 0x7F6000 guess (the
    bucket-1019 theory = dead; the mine's own trap caught the shape issue).
  * The type-7 sobject push = NO Hook B line (the sobject channel = a different
    path than FUN_140E0F000 - consistent with the lane's finding).
- THE NEW WRINKLE: the same boot = the PERMANENT BLACK SCREEN at the Tower launch
  (the game = alive - the inventory/UI work; the world state = up; the bootflow =
  completed through the fade_release - NOT the spawn-hold). The prime suspect =
  the type-7 push with the wrong hash/shape side-effecting the render (the Tower
  rendered fine in the pre-emission boots).
- NEXT: the emission off (the control) or the payload refined with the REAL hash
  0x80806AC0 + the shape corrections. The schema-hash question = CLOSED by the
  hook (the knob value = 0x80806AC0).

## 10.6 L5 TRANSACTION DESIGN LANDED (the skeleton's blueprint; spot-verified on disk)

- The census (VERIFIED, source-exact): write_back = delete-all + re-seed (persistence.cpp:
  1199-1200), harness-only trigger (account_memcmp_test.cpp:404), WAL single connection,
  no busy_timeout; the request surface = 205/206/501/503/504/601 + echo, the 504
  staged-outcome pattern = the mutation-handler template; exactly TWO state mutators
  (set_primary_soid / set_selected_character), both candidate-copy + valid() + swap; no
  inventory API, no non-equipped container.
- THE KEY FINDING: the fork ALREADY ships proven live family-4 INCREMENTAL pushes �
  queuez_change_character.cpp:22-49 (17-B tagReflection selection patch, version+1,
  flags 0) + queuez_select_character.cpp (the 504 move, 3 object ops) + the deferred
  re-push (queuez_deferred_push.cpp:101-151). The delta publisher's transport EXISTS;
  only the per-verb payload grammar is L3-dependent. Fallback until then = the full
  snapshot preparer (proven).
- THE LOOP: decode -> validate (L4 + resolvability) -> mutate (candidate-copy, single-
  writer) -> PERSIST (L6 publish-scoped, ONE sqlite txn = the commit point) -> publish
  delta (after the correlated reply) -> re-stamp (deferred to shutdown). Persist-before-
  publish makes the crash window self-healing (the next boot's snapshot converges).
- THE RE-STAMP PROTOCOL (settled from the armor evidence): eqHash fingerprints
  {characterCount, per-char 5 ability bytes, equipped slots' presence+hash+level+socket
  policy+plugs}. Equip/unequip/plugs/level/ability = re-stamp; quantity/soid/profile/
  non-equipped rows = NOT. OPEN: stamp timing (shutdown recommended) + the shader verb's
  stamp class (L1/L3 evidence).
- F1-F8 failure table covers every abort point (busy -> rollback+revert; publish-fail ->
  deferred re-push; stamp-fail -> pending + the proven repair script; never a hand edit).

## 10.7 L2 SNAPSHOT CENSUS LANDED (the snapshot fix is fully specified; spot-verified)

- The census (VERIFIED, hash-verified): upstream (Sunrise @ 2ce592c) and fork (cb4c0fb)
  are BYTE-IDENTICAL across the family-4 publish chain � 11 files, SHA256-matched
  (character_encoder.cpp re-verified this session: 7CC09465... both trees). Upstream
  publishes EQUIPPED-ONLY (16 authored slots); non-equipped inventory = a NEW capability
  in both trees. The only fork divergence = server/persistence/* (upstream has none).
- Bucket layout: the 49-descriptor inventoryBuckets domain IS the placement rule
  {bucketId, arraySelector, firstSlot, slotCount}; bucket 16 = character rows 110..119
  (measured in s1_domains_inventoryBuckets.json; oracle_count 49, cache matches row-for-
  row). place_item fills the lowest free row in the run; rows are positional, the client
  maps row -> tab via its own copy of the domain.
- THE GAP SET (G1-G8): loader (persistence.cpp:624-627) -> state (CharacterState array)
  -> resolver (second pass reusing resolve_item + place_item) -> encoder (second write
  loop + validator relaxation + nextInventorySerial = total rows) -> kObjectCapacity
  50 -> 55-object frame (snapshot.h:21-23; the preparer/staging need NO code change) ->
  write_back must re-seed in_equipment=0 rows instead of wiping. G8 = the cache rebuild
  (six itemDetails rows, dense 1189/1190/1192/1193/1195/1196) = the hard blocker
  (no-fallback lookup fails the whole loadout resolve -> boot break).
- EXPECTED FIRST BOOT: boot lines unchanged except family=4 objects=49 -> 55 (six new
  416-B subclass instances; the character object stays 46,928 B; subclass rows at 110..119).
- OPEN for the build lane: capacity params (334 max/char; start 2 each), mutationSerial
  semantics for non-equipped rows, per-row flag marks (newItemFlags/watermarks), the
  equip-opcode surface (still RE; L1), multi-bucket run-fill (the armor lane's territory).

## 10.8 L6 SOID + WRITE_BACK LANDED (both designs settled; spot-verified)

- SOID census (VERIFIED): account 0x9EAA300100100100 + chars +1..+3 (derived at runtime,
  state_account_runtime.cpp:23); 48 equipped instances 0x6000...01..0x30 (S1-6 script
  soid_base 0x6000000000000000, dense char-blocked); 6 additions 0x31..36 (flat
  continuation); 7 profile rows NULL. The ONLY soid structure the fork names = the activity
  session form (kAccountMask 0xFFFFFFFF00000000 | kSessionClass 0x00200000 | counter �
  verified internal.h). Item soids = opaque; two hi32 prefixes (0x4000 settings-era,
  0x6000 S1-6-era) both booted green � no client hi-bit requirement evidenced.
- MINTING POLICY (verdict a): opaque counter 0x60000000_00000000 | counter; HWM in the
  meta table (key/value, exists) seeded max(DB soids)+1 at boot; mint+bump INSIDE the
  mutation transaction; UNIQUE(account_id, instance_soid) arbiter with one rescan-retry;
  never reuse (dismantle retires; the client store may hold stale soid-keyed slots); NULL
  soid for stackables.
- WRITE_BACK DESIGN (verdict b): scoped-diff � DELETE ... WHERE account_id = ? AND
  in_equipment = 1 (equipped scope) + character_index IS NULL (profile scope) +
  account-scoped full-state tables; re-seed functions unchanged; one transaction (PHR-proof;
  WAL crash atomicity); NO schema migration (meta key only, user_version stays 1).
  Ownership-flag and side-table options rejected with evidence.
- ORDERING RECONCILIATION (this session's call, for the skeleton): L6's per-verb
  ordering (persist after the delta publish) was written against the brief; L5's
  deliverable settled persist-BEFORE-publish (client delivery after durability; the crash
  window self-heals at next boot). The skeleton follows L5's order; L6's scope predicates
  and mint procedure are the persist-step spec.
- KEY OPEN handed to the skeleton: mutation_serial � the DB column is always 0 and the
  wire value is encoder-derived; L5/L3 must settle whether per-verb mutations persist the
  column as the wire source (the client's row match key; a 0-serial row may diff as
  'no change' on re-publish). Plus: account-half item-soid question (capture-only), the
  runtime write_back trigger (L5), multi-account LIMIT-1 load (out of scope).


## 10.6 THE CONTROL BOOT - the emission = the black-screen cause, CONFIRMED

- Path A executed: the world_population flag = off, the server restarted, the fresh
  boot -> THE TOWER RENDERED PROPERLY. The type-7 sobject push (the wrong-hash/
  wrong-shape payload) = the black screen's cause, isolated and confirmed.
- The schema-hash = 0x80806AC0 (the schema-capture hook's live value) = the refined
  payload's first correction. The next = the shape corrections per the vendor
  lane's risk list (the tree misalignment / the session-index / the entity-index
  semantics - the entity-index field = the render-poison candidate: the client-
  global DAT_141F91300 semantics could clobber the player's own entity).

## 10.9 L4 POLICY SPEC LANDED (24 rules; one cross-lane discrepancy flagged)

- The deliverable (claims/inventory-l4-policy-spec.md): 24 numbered rules � EQUIP E1-E11,
  UNEQUIP U1-U4, SOCKET S1-S5, SUBCLASS SC1-SC4 � every rule citing a measured value or
  file:line. The rules mirror the proven resolve_item gate chain (E1-E11 = exactly its
  gate list), so implementing them in order reproduces working behavior; divergence = bug
  by construction.
- Census (re-measured): 49 bucket descriptors CONFIRMED (IDs 0..50 minus {30,39};
  slotCount == manifest itemCount for every named bucket; category-3 = equippable);
  class-lock table complete � bucket 16 fully locked (9 subclasses, 3/class), armor
  736-759 per bucket, 6 class swords (bucket 2), 370 class mods (bucket 13), emblems;
  15,343/15,424 manifest-matched, 81 unknown hashes fail-closed.
- TOP-3 rules for the skeleton: E2 class-lock (the only gate the fork's resolver lacks �
  its runtime has no classType field), E4 slot occupancy (native-slot bijection + DB
  partial unique), E3+E5 bucket/detail gate (the no-fallback resolve chain; a missing
  detail = boot-class failure).
- THE READYMASK DISCREPANCY (flagged, not settled): L4's JSON-domain readyMask = 0xF7FBFC
  (16251900) for ALL nine entryCount-24 lists � clear bits {0,1,10,19}, self-consistent
  with the lane table (the no-plug lanes + the super lane start absent). The subclassfix
  lane's cache measurement = 0x...F7FB low word + per-list high bits (0x1C/0x5C/0xA8 for
  lists 1/5/11). The healthy boot fed the client the CACHE's value; the JSON domain is
  swap-gated against it. The build lane must reconcile cache-vs-JSON before the roll
  computation is trusted (a record-offset misread on one side is the likely cause).
- Boundary (explicit): purchases/quest gates, beyond-initial plug placement, plug costs,
  and UNEQUIP-subclass (U3: data cannot settle; recommend authored reject) = authored-
  later policy. The equip REQUEST surface itself is L1's lane.

## 10.10 L7 PERK-BANK LANDED (0x0226 settled; wave 1 complete) + the exe re-deploy observation

- 0x0226 SETTLED: a u16 index into the client's sandbox-perk investment table (not a hash
  or slot). Arrivals-era (2020-07-16) table index 550 = hash 0xCB7A2F77 � nameless,
  isDisplayable=false, damageType 0; carried by exactly the six D1-lineage subclasses.
  The 2026 manifest's index 550 = 'Arc Conductor' is a RENUMBERING ARTIFACT (the tables
  renumber per release; 0xCB7A2F77 persists at index 612 today, still unnamed).
- Preview-bank fill order SETTLED (source-exact): apply_perk_banks walks native slots
  0..19, weapons diverted to per-weapon banks, per item = its own sandboxPerks then each
  plug lane's; the 32-hash overflow bank is hash-based/priority-ordered, unaffected.
  Plug-lane perks ARE required for spec parity � the wave-2 patch is exactly those lanes.
- '28 rows' REFINED: dcv = 30 rows (2 subclass + 28); deployed cache (90 details) residual
  gap = 26 rows (10 dcv-spec-verified, 16 raw-blob-verified) � full patch table in the
  deliverable �3. Fix shape = data-only cache patch + FNV-1a checksum; P2 (family-3
  preview surface, zero family-4 wire impact).
- REBASE OVERTAKEN: the subclass bundle (68 rows) is superseded; the disk cache (951,582 B
  / 90 details) already carries the subclass patch (idx 1191/1194 = 0x0226). Wave-2 cache
  rebuilds base on the disk file.
- EXE RE-DEPLOY OBSERVATION (this session, disk-verified): the S2 session's vendor-first
  build (10:54) replaced the deployed sunrise-server.exe AND re-stamped the cache � the
  header now carries ts 0x6A81F950 (1786902864) / size 0x1343000, matching the exe's PE
  identity exactly; the content chain (checksum 0x3535F4BE964B6EA3, eqHash 0x111D8144D0F77456)
  is UNCHANGED. STATE-inventory.md corrected accordingly (the old exe identity 0x6A816503
  was stale).
- WAVE 1 = COMPLETE: all five lanes landed + disk-verified. L1/L3 remain queued behind
  the Ghidra lock. Wave 2 awaits the main session's scheduling: the snapshot-generalization
  worktree branch + ONE cache rebuild carrying the six itemDetails rows + abilityBuckets
  for lists 3/6/10 + L7's 26-row perk patch.


## 10.7 THE REFINED PROBE - the poison cured, the entity's the last mile

- The refined payload (the hash 0x80806AC0 + the width corrections: the identity
  24, the vector3d 37, the forward_and_up 29) deployed + booted: THE TOWER
  RENDERS, NO ZAVALA. The black screen = CURED (the 0x7F6000 garbage-row heap
  corruption = the proven poison, now fixed). The payload = decodes cleanly
  (no corruption, no crash) but the entity = not created/rendered.
- The remaining gap (for the next owner): the codec body's field VALUES vs the
  real schema tree's rows (the walker over the now-valid hash), the anchor/lease
  semantics, the position quantization (the undumped direction table
  FUN_1403830E0 + the descriptor DAT_141BA5F90).
- PARKED HERE per the user (the handoff to the inventory session).

## 10.11 RIGHTS HANDOVER + TASK 0 CLOSED (the readyMask reconciliation)

- THE HANDOVER (STATE.md, read this session): the boot rights + the build rights = THIS
  session's (handed over ~11:4x). The S2 session = parked (no lanes; the refined-payload
  lane died after its study). THE SUBAGENT MORATORIUM = IN EFFECT (the user directive) -
  the remaining edit/build work = main-session hands. L1/L3 stay queued.
- THE FORK TREE (disk-verified): HEAD = cb4c0fb, but 5 UNCOMMITTED S2 files sit in the
  main tree (default_settings.json, server/definition.h, activity_entity_baseline.{cpp,h},
  activity_world_population_push.cpp - the refined-payload edits). Left untouched; the
  inventory worktree branches off cb4c0fb and excludes them. Folding S2's parked edits into
  the boot-gate deploy = a pre-deploy question for the user (the deploy replaces S2's live
  stack).
- THE DEPLOYED STACK (disk-verified 12:2x): the cache header ts 0x6A8207CE (1786906574) /
  size 0x1343000 == the deployed exe's PE identity (built 11:56, the refined build); the
  content chain unchanged (checksum 0x3535F4BE964B6EA3, eqHash 0x111D8144D0F77456).
- TASK 0 = CLOSED (claims/inventory-readyMask-reconciliation.md): the canonical readyMask =
  0x0000000000F7FBFC for all nine entryCount-24 lists (lists 4/8/12 = 0x4, 0/13 = 0).
  THREE sources agree: the deployed cache (selists @469602, '<IHBBQ' grammar), the old
  68-detail bundle, and the JSON domain. The subclassfix lane's 0x1C/0x5C/0xA8...F7FB
  values = a probe artifact, forensically reconstructed: subclassfix_probe4.py:51 unpacked
  '<IHHBQ' (17 B) from a 16-B record - the extra B ate mask byte 0, and the u64 captured
  the NEXT record's first hash byte (0xDF3B971C -> 0x1C, 0x64FC475C -> 0x5C, 0xEDF8DEA8 ->
  0xA8). Start-absent entries = {0,1,10,19} (bit set = ready, subclass_socket_selection.
  cpp:68; entry 10 = the kind-34 super lane, activated by rule not mask). The swap gate
  (content_loader.cpp:1035) replaces the runtime rows with the JSON parse regardless - so
  the JSON value is what the roll uses; NO cache change needed for the mask.
- NEXT (under the moratorium, main-session hands): the snapshot-generalization build in a
  worktree branch (G1-G7 per L2) -> the ONE cache rebuild -> the boot gate.

## 10.12 THE SNAPSHOT BUILD LANDS (G1-G7, main-session hands under the moratorium)

- THE WORKTREE: RE_build/Sunrise-fork-inventory @ inventory/snapshot-generalization (commit
  6a95ec2, 8 files, 331 insertions). Built with MSBuild VS2022 + /p:PlatformToolset=v143 (the
  vcxproj declares v145 which is not installed; the override matches the S2 lanes' build path).
  The S2 session's 5 uncommitted main-tree files are excluded by construction.
- THE CHANGE SET: G1 loader (load_storage: in_equipment=0 + plugs); G2 state (CharacterState.
  storageItems[32] + storageItemCount; account::valid extended: storage shape + account-wide
  soid uniqueness); G3 resolver (second pass reusing resolve_item + place_item; ResolvedItem.
  equipped flag; no semantic/native slot for storage); G4 encoder (bijection only for equipped;
  summary = equipped-count; equippedInstanceSoids guarded; newItemFlags/watermarks marked for
  every row); G5 capacity (kItemCapacity 16+32, kObjectCapacity 146); G6 staging unchanged;
  G7 write_back scoped deletes (equipped + profile + characters WHERE account_id; the global
  item_plugs delete dropped - FK cascade; flags/objectives/family5/entitlements stay global =
  single-account-equivalent, full L6 scoping = the skeleton's step) + storage re-seed
  (in_equipment=0, equipment_slot NULL).
- PHASE A (scratch deploy inventory_build_test_base, the live tree untouched; the cache
  re-stamped to MY exe identity with eqHash preserved): the full boot chain + the S1 harness =
  EXIT 0. Swap gates all match; load 3 chars / 7 profile; resolve items=16 x3; character
  encode ok; write_back + second-boot account/family5/character all EQUAL. ZERO regression.
- PHASE B (same + the six subclass rows applied to the COPY): load result=ok (the storage
  loader works; the valid() storage extension holds) then stage=resolve result=fail - the
  EXACT G8 prediction (no itemDetails rows for dense 1189/1190/1192/1193/1195/1196). The fork
  capability is proven complete; the cache rebuild is the only remaining piece.
- MY exe identity (for the next re-stamps): PE ts 1786907841, SizeOfImage 20234240. Claims:
  claims/inventory-snapshot-build.md.
- NEXT (step 3, in progress): the cache rebuild on the scratch deploys - six itemDetails rows
  + abilityBuckets lists 3/6/10 + L7's 26-row perk patch -> phase B must show items=18 x3 +
  exit 0 -> then the deploy + boot gate (the user's call; the S2 fold question documented in
  STATE-inventory.md).

## 10.13 THE CACHE REBUILD LANDS + PHASE C GREEN (the server-side milestone proven off-game)

- THE REBUILD (RE_output/content/inventory_cache_rebuild/rebuild_inventory_cache.py): six new
  itemDetails rows (dense 1189/1190/1192/1193/1195/1196 via the verified S1-6 jsonl_detail
  machinery; sandboxPerks 0x0226 for Gunslinger/Nightstalker/Sunbreaker/Voidwalker, none for
  Sentinel/Dawnblade) + L7's 26-row perk patch, spliced over the disk cache -> 953,310 B / 96
  details, checksum round-trips, swap-gate domains byte-identical. The abilityBuckets rows for
  lists 3/6/10 = DEFERRED (family-3 preview of an EQUIPPED Nightstalker/Sentinel/Voidwalker;
  not needed for the snapshot boot; they ride the equip-milestone rebuild).
- TWO BUGS CAUGHT + FIXED (both mine, documented for the record): (1) the rebuild sort placed
  existing rows by array position instead of dense index -> the C++ reader's strictly_ordered
  rejected the file (diagnosed via temporary cache_diag instrumentation naming
  step=details_order; the checksum-equality printout also proved the packed 288-B record
  layout matches the python FMT byte-for-byte); (2) the scoped write_back preserved storage
  rows then re-seeded them -> UNIQUE(account_id, instance_soid) clash; the corrected scope =
  the whole seed account's items (storage rows are now publish-owned via load_storage; other
  accounts' rows stay preserved vs the old delete-all). The diag code was removed after the
  diagnosis - the committed tree carries no instrumentation.
- PHASE C = EXIT 0 (the scratch deploy, 61-item DB + rebuilt cache + my exe): swap gates all
  match, load 61 rows, resolve items=18 x3 characters, character encode ok, write_back ok,
  second-boot account/family5/character EQUAL; the post-harness DB census = all 9 bucket-16
  rows intact (3 equipped + 6 storage, in_equipment=0, equipment_slot NULL). PHASE A (55-row
  regression) = EXIT 0. The fork branch = inventory/snapshot-generalization @ 201a66c (two
  commits, clean).
- THE PRE-DEPLOY QUESTION (for the user, recorded): the boot-gate deploy replaces the S2
  refined-payload exe; fold the S2 session's 5 parked main-tree files (the refined carrier/
  hash/width edits) into the boot-gate build, or boot on cb4c0fb + the inventory branch only?
  The boot gate (step 4) = the user's call: deploy exe + cache + the live-DB apply script +
  identity re-stamp, batched in ONE game session; acceptance = family=4 objects=49 -> 55 +
  the six subclass picks visible + the ability-lock probe.

## 10.14 THE EQUIP VERB LANDS (opcode 403) + THE HOLISTIC PASS (2026-08-16)

- THE CAPTURE (boot 1.5): the swap verbs named live � 701 (2463-B state dump) -> 702 (4800-B
  dump carrying the account soid) -> 104 (1 B 0x40) -> 403 = THE EQUIP REQUEST {u64 BE item
  soid, u8 flag} -> 801 (inspect: {u64 BE soid, 2 bytes}); 2100 = the ability change (3 hits
  in the first session, body still uncaptured). 403/701/702/801 = statusPair routes; 2100 =
  statusOnly. The upstream Sunrise surface = IDENTICAL (no handlers for any of them) - the
  generic statusPair replies are the in-process-era contract.
- THE VERB (built on the worktree branch, main-session hands): validate in consume() (storage
  membership + bucket-16) -> mutate (candidate-copy swap slot 11 <-> storage) -> persist
  (displace-then-equip, one transaction) -> push the character after-image (one oodle object,
  version+1, flags 0; the prepare_selection_move shape) -> nonce advance + SessionState.
- TWO BUGS FOUND + FIXED (the holistic pass): (1) equip-before-displace hit the slot-11
  partial UNIQUE (UNIQUE constraint failed: account_id, character_index, equipment_slot) -
  the release must run BEFORE the claim; (2) the first build left memory divergent after the
  failed persist (the swap landed in memory but not the DB) - which is exactly why the retry
  'did nothing' (the storage-membership check then failed). The F4 handling (revert the swap
  on persist failure) is now in. LESSON FOR THE SKELETON: release-then-claim for slot-indexed
  rows + mutate-revert-on-persist-fail, for every future verb.
- THE PROOF CHAIN (both logs): server stage=subclass_equip result=ok objects=1 bytes=824
  recorded=1; the client received the frame (handle_message push result=ok len=796 family
  byte 0x04 = the frame applied at the type-dispatched handler); AND THE DB SWAP PERSISTED
  (char 0 = Gunslinger 0xD8B8D1FC equipped, Arcstrider in storage). The cache eqHash was
  re-stamped to the post-swap fingerprint (0x48E8C744E324D45A) - EVERY successful swap
  session obligates this before the next SERVER boot until the shutdown-stamp hook lands.
- THE DOWNSTREAM GAP (the remaining failure surface, for the generalization stage): the
  client's UI swap transaction still rolls back visually - the store updated (the push
  applied) but the UI's reconciliation verbs (801 inspect, 701/702 sync, 2100 ability) get
  only the generic replies, and the inspect UI breaks after a swap (801 sent with the OLD
  soid). The response grammars = the client-side RE targets (Ghidra, main-session hands; the
  L1/L3 briefs). Claims: claims/inventory-equip-verb.md.

## 10.15 THE UI-MODEL MECHANISM (user observation) + the capture-driven lane targets

- THE OBSERVATION (user, in-game): the press-and-hold confirmation cue (the little loading
  bar) COMPLETES for both the ability swap AND the collections item pull - then nothing
  happens. The cue = the client's local 'request in flight' UI; the commit never lands. The
  SAME pattern across verbs implies ONE client-side transaction mechanism: the UI plays the
  cue optimistically, sends the verb, and commits only from the response (or a
  reconciliation payload). THE GENERALIZATION: the collections verbs (1820 collections
  reacquisition / 1901 plug mutation, both statusPair routes per the opcode sweep) share
  the pattern - their response grammars are as unhandled as 801/701/702/2100. The sweep's
  'NOT IN CORPUS' for 1820/1901 + the unhandled handlers = the same repair family.
- THE SESSION EVIDENCE (this session's capture): the 801 inspect now targets the NEW
  equipped soid (0x31 = the persisted Gunslinger) - the client's family-4 store DID absorb
  the last session's swap; the 403 (0x32 Nightstalker) fired + result=ok + the frame arrived
  client-side (len=796 family 04, result=ok); the 702 payloads carry a DYNAMIC state hash
  (E2E2E2C8 341E25ABB / D83FB8A9ACB / 8268693137 - three variants across the syncs) +
  the account soid. NO 2100 was sent this session - the ability change is gated BEFORE the
  wire (the client-side ability lock; the request never exists).
- THE PROTOCOL SNAG (named, for the record): the swap's wire = complete (403 -> frame ->
  client apply -> persistence all proven); the client's UI commit = driven by the 701/702
  sync pair + 801 inspect responses (all generic statusPair today); the ability verb = never
  emitted (client-side gate). THE REPAIR = the client-side response decoders for
  403/701/702/801/2100 + the ability gate's flag family - the Ghidra lanes (L1/L3), now
  launched with the capture data as targets.

## 10.16 THE BANNER REFRESH + THE FOLDED BRANCH (2026-08-16)

- THE BANNER REFRESH (the family-3/0 hypothesis, built): the equip push now ALSO re-publishes
  the family-0 banner pair (the anchor + the character record) as a same-key refresh after the
  family-4 increment - stage_family0_refresh + append_banner_refresh_notification, reusing
  prepare_banner verbatim (the select move's proven pair). The hypothesis: the client's
  subclass UI reads the banner record (the re-encoded ability buckets), not only the family-4
  store. Deployed to s1_accept (ts 1786913185). THE STAMP RACE (the shutdown-stamp hook's
  case study): the Nightstalker swap from the last session persisted -> the DB fingerprint
  moved AGAIN -> the deployed cache went stale -> the server refused to boot (cache=0). The
  re-stamp now = DB-driven (0x6FC29EB4EE4D7275, char 0 slot 11 = 0xC0483D8B Nightstalker).
  This is exactly L5 open item 6 (the shutdown-stamp hook) biting twice in one day - it moves
  UP the priority list.
- THE FOLDED BRANCH (the user's two-branch plan): inventory-folded-s2 = the S2 parked files
  on top of the equip-verb commit (b954169). Zero file overlap, built clean, scratch harness
  EXIT 0. NOT deployed - the user's fold decision after the inventory boot tests.
- NEXT BOOTS: (1) the swap retest on the banner-refresh build (the family-0 pair rides the
  403 now); (2) a collections pull (press-and-hold) - the capture logs 1820/1901 for free.

## 10.17 THE CREATE-SCREEN INCIDENT (RISK-3 bites) + the fix (2026-08-16)

- THE INCIDENT: the user's boot after the Nightstalker swap (list 3) landed on the character-
  CREATE screen. The chain (fully diagnosed): the swap persisted -> the roster record encode
  for the equipped list-3 subclass failed (apply_ability_buckets returns FALSE on the missing
  abilityBuckets row - subclass-inventory.md RISK-3, deferred in the cache rebuild) -> the
  family-3 snapshot went out with objects=0 (server log: 'snapshot result=ok family=3
  objects=0') -> the client's signin screen had no characters -> the create screen. The stamp
  and the banner refresh were EXONERATED (the cache loaded, the swap gates matched).
- THE FIX: (1) the 3 abilityBuckets rows computed by the rosterfix machinery (list 3 kinds
  [37,2,8,3,12,255,...,43], list 6 [34,39,6,10,12,255,42...], list 10 [22,11,15,1,12,...,44]);
  (2) the cache rebuilt 6 -> 9 rows + the content JSON regenerated (the swap gate stops the
  boot on a mismatch); (3) the fork's abilities::kDefinitionCapacity 8 -> 16 (the parse buffer
  + the cache-reader fit were BOTH capped at 8 - a 9th row = parse fail + cache=0). Deployed:
  json=9 cache=9 match, the server healthy. The DB was reverted to the Gunslinger state
  (list 2) during the incident triage - the Nightstalker equip is now RETRYABLE.
- LESSONS: (a) the equip verb makes every deferred data prerequisite reachable - a subclass
  the client can equip MUST have its roster row, or the next boot bricks the character
  screen; (b) the DB-revert pattern (the two-row UPDATE swapped back) restored service in
  minutes; (c) the lane (L1) checked clean during the incident - its log = the Ghidra block
  scans only, no touches to the live tree (the user's ask).

## 10.18 THE COLLECTIONS CAPTURE (1820) + the session's frame evidence (2026-08-16)

- THE COLLECTIONS PULLS (the user, armor + weapon): opcode 1820 (0x71C, the collections
  reacquisition) x4, payload = 3 BYTES each - two distinct keys observed: 80 00 00 and
  93 71 00 (one per pulled item). The request = a tiny keyed lookup (a 24-bit collections
  entry key); the response = statusPair (the generic fallback today). 1901 (the plug
  mutation) did NOT fire for the two pulls. The user's UI behavior = the established
  cue-completes-then-nothing pattern - the collections verbs are the SAME response-driven
  commit family as the ability verbs (the generalization holds).
- THE BANNER-REFRESH HYPOTHESIS = FALSIFIED BY TEST: this session's swap produced BOTH the
  family-4 character after-image (client len=796 family 04) AND the family-0 banner refresh
  (client len=1050 family 00), both applied client-side (result=ok) - and the subclass UI
  still rolls back. The banner record is NOT the client's UI commit source. (The server
  log's banner_refresh line = absent from my earlier grep due to a pattern miss; the client
  log = the authoritative evidence.)
- THE REMAINING SURFACE: the client's UI commit = the 701/702 sync responses + the 801
  inspect response (the inspect = provably response-driven) + the ability gate (2100 never
  emitted client-side). The L1 lane (ses_ff3b2d1b3ffePX4QXNrw4DlCrc) = grinding the
  per-opcode decoder table (the log = 16,852 lines, active; the deliverable = pending its
  FINAL). Its pass-1 result = the decoder-registry populator FUN_140E75790 (the only writer
  of DAT_14280E3E0); FUN_140DFEC50 = exonerated (the generic decoded-body apply, no opcode
  switch inside - the opcode compares live in the decoder-class vtable methods).
- BANKED FOR THE 1820/1901 HANDLERS: the request shapes (1820 = the 3-byte key; 1901 =
  uncaptured), the statusPair response shapes, and the generalization that the response
  grammars are the client-side decoders the lane is naming.

## 10.19 THE ACQUIRED-FLAGS HYPOTHESIS + the stamp path fix (2026-08-16)

- L1 LANDED (claims/inventory-l1-equip-message.md): the per-opcode decoder table. 403 = the
  client reads the statusPair + commits from the family-4 after-image (my implementation =
  the contract); 801 = statusPair ack, the inspect UI reads the family-4 STORE; 701/702 =
  statusPair only (the real data = the follow-up pushes; the client memcmp-diffs its store
  regions); 2100 = statusOnly but the emitter has ZERO live callers (the ability verb = dead
  code); 1820/1901 = the 403 pattern (the generalization confirmed). THE ABILITY GATE =
  the account object's acquiredFlags bank (+0x742C, 12,300 B) tested by byte==2 primitives
  (FUN_140e06070/140e08cc0). The seeded bank = 4,931 rows (all value 2) + ~7,400 un-rowed
  bytes at 0 - the gate tests specific bytes for ==2, so the un-rowed regions lock.
- THE FIX (data, deployed): the flags table's account scope = all 12,300 rows value 2 (the
  'all acquired' server default). The scratch harness round-trips (EXIT 0). The boot gates =
  all green. THE TEST (the user's next boot): the ability-select UI + the subclass swap -
  if the rollback was the acquired gate, both unstick.
- THE STAMP PATH BUG (found + fixed): the stamp-on-persist wrote to a doubled-Sunrise path
  (artifact_directory already appends \Sunrise) -> OPEN_EXISTING failed -> the prior
  session's restamp result=fail (the persist + the push still succeeded). Fixed to mirror
  server_main (module_directory + \Sunrise\cache\build_data.bin). The eqHash stamping now
  works - the earlier manual re-stamps covered the gap.
- ALSO NOTE: the first flags patch attempt used INSERT OR REPLACE on a table with NO unique
  constraint -> 17,231 duplicate rows (caught + fixed with DELETE + INSERT; the loader had
  never seen the dupes because the server never restarted in between).

## 10.20 THE STAMP TRUNCATION INCIDENT + the replication-state observation (2026-08-16)

- THE STAMP TRUNCATION (found via the 96-byte cache on disk): the stamp-on-persist (after the
  path fix) read ONLY the 96-byte header, wrote the 96-byte temp, and renamed it over the full
  956-KB cache - every swap session since truncated the on-disk cache, and the next server
  boot died with cache=0. The in-memory session = unaffected (the cache loads once at boot).
  FIXED (cache_identity_patch.cpp): read the whole file (kMaximumPatchSize-bounded), patch the
  eqHash, write the full body, atomic rename. The 9-row cache restored from the rebuild
  output + re-stamped to the exe + the DB hash. THE LESSON: the stamp's temp file must carry
  the WHOLE file it replaces - the header-only temp = a one-file truncation bomb.
- THE REPLICATION-STATE OBSERVATION (the client log, the all-2-bank session): ONE new line -
  'queuez_family_update_begin: ignoring family-update due to unhandled replication state,
  family_type=ENUM(4) ... replication state=1' + 'failed to process update for [family_type
  4 ... object_count 55]'. The client REJECTED the family-4 join snapshot in the all-2-bank
  session (never before). The all-2 bank = reverted to the original 4,931 sparse rows
  (indices 26..11155); the next boot = the discriminator: the rejection gone = the all-2
  bank was the trigger (the flag processing gates the replication state machine); the
  rejection present = a separate client-side state issue.
- ALSO: the DB equipment = the Arcstrider era again (0x111D8144D0F77456) - the user's
  swap-back session persisted (the stamps each worked, truncating the cache twice). The
  flags table = the original 4,931 rows (the first INSERT OR REPLACE attempt made 17,231
  dupes - the table has NO unique constraint; fixed with DELETE + INSERT).

## 10.21 L3 LANDS: the commit exonerates the server (2026-08-16)

- THE COMMIT VERDICT (claims/inventory-l3-delta-encoder.md, full read): the UI commit =
  FUN_140e02280 (handle_response_internal) and it gates ONLY on the decode result (the
  envelope tag -> the schema-driven body decode). The per-opcode completion callback =
  a literal no-op (FUN_140b94700 = return). The 5-bit status + the 32-bit value = schema-
  decoded BODY fields, never commit gates. StatusResponse{code 0, value INT32_MIN} commits
  fine. THE SERVER'S RESPONSES ARE CORRECT BY CONSTRUCTION - the rollback = downstream.
- THE DOWNSTREAM, NAMED: (1) the 2100 ability verb's payload defaults to kNoDefinitionHash
  and NOTHING in the build resolves TO a real definition offline (300+ sites use it as the
  'definition not loaded' default) - the ability-customization gap = STRUCTURAL (the same
  offline gap existed in the in-process era); (2) the inspect-UI's soid = the UI object's
  CACHED +0x28 field (FUN_1414b95c9) - the post-swap staleness = the client's own cache
  policy, structural too; (3) the acquiredFlags gate primitives = NO code callers in the
  analyzed image (only the code-range registry entries) - the caller = the un-analyzed CUI
  region. The 701/702 responses need NO reconciliation payloads (the client decodes only
  the status pair; the reconciliation = the family-4/5 pushes - already correct).
- THE 71-CLASS TYPE MAP = recovered statically (the live-read script = unnecessary; the
  svc-10/11 decode = FUN_14051c670, the schema-driven envelope walker).
- THE IMPLICATION FOR THE MILESTONE: the server = complete + correct for every verb the
  client actually emits. The remaining failures = the client's offline definition
  resolution + the UI's cached-state policy - the CANDIDATE SERVER LEVER = the contentConfig
  manifest completeness (the package rows drive the client's content catalog; an incomplete
  package list = the 'definition not loaded' defaults). The lane's OPEN items #3/#4/#5 =
  the live-read/live-capture recipes (the CUI globals RPM + the press-and-hold flow).

## 10.22 THE OUTSIDE REVIEW (the ruling + the trap) (2026-08-16)

- THE RULING (next moves, in order): (1) THE ZERO-BOOT LEVER FIRST - the manifest
  completeness audit: the served contentConfig's field-3 = 2,199 package rows vs the
  packages dir = 2,202 .pkg files - the diff = never run for completeness as a content
  catalog (only for signature match). If the client's definition-resolver skips un-listed
  packages, three files' worth of definitions silently default to kNoDefinitionHash (the
  2100 payload gap). (2) THE DISCRIMINATOR BOOT (one session, batched): the all-2 bank =
  reverted; the next boot confirms the 55-object rejection is gone + retests the swap.
  (3) If 1+2 don't unstick: the CUI live-reads (the lane recipes #3/#4/#5 - the gate
  primitives' byte-tests in the CUI region). (4) THE REFRAME: the server-side milestone =
  DONE (every verb the client emits = handled correctly; persistence = real; the swap
  survives reboots). The remainder = a client-offline-behavior problem -> the fix space =
  data/manifest-side (server-controllable) or patch-side (the DLL, the validator-hook
  precedent), NOT protocol-side. A smaller, better-lit tunnel.
- THE TRAP TO RECORD (generalizes): bulk-mutating a client-read state bank whose reader
  lives in un-analyzed code = shooting in the dark - the all-2 bank's 55-object rejection =
  the client telling us a flag region participates in the REPLICATION STATE, not just
  gating. RULE: before bulk-writing a flags/bank region, find its reader (the CUI census
  = exactly that tool) or make the change observable in one boot with a rollback ready.
- THE MORALE NOTE: L3's 'the server is correct by construction' = the strongest
  exoneration produced - the whole interactive architecture (the skeleton, the verbs, the
  stamps) = right; the residue = a named narrow client behavior (the -87 pattern at
  one-tenth the size, boxed on three sides with the instrument specced for the fourth).

## 10.23 THE MANIFEST AUDIT (lever #1 CLEARED) (2026-08-16)

- THE AUDIT (the reviewer's zero-boot lever, executed): replicated the fork's filename
  classifier (content_manifest_package_name.cpp) over all 2,202 .pkg files in the packages
  dir. Result: 2,199 RECOGNIZED + exactly 3 EXCLUDED = w64_client_bootstrap_unp1_0.pkg /
  w64_ui_bootflow_unp1_0.pkg / w64_ui_startup_unp1_0.pkg - all three carry the _unp1_
  marker = the fork's DELIBERATE exclusion ('packages registered locally before
  ContentConfig is read'). The manifest's field-3 = 2,199 rows = the complete recognized
  set; the 2,202-vs-2,199 diff = the intended exclusions, not a gap. The manifest
  completeness hypothesis = CLEARED: the client's definition catalog is NOT missing
  server-listed packages. The 'definition not loaded' defaults = the client's own offline
  resolution state (the packages are listed), i.e. the CUI-region behavior - the live-read
  instrument (#3) = the named next tool after the discriminator boot (#2).

## 10.24 THE DISCRIMINATOR BOOT (2026-08-16)

- OBSERVATION 1 (the discriminator): the rejection = GONE (0 'failed to process update' /
  0 'ignoring family-update' in the client log) - the reverted flags bank confirmed as the
  55-object rejection's trigger; the clean stack processes the family-4 snapshot normally.
- OBSERVATION 2 (the swap): the same behavior - 403 (0x32) -> subclass_equip result=ok ->
  the family-4 after-image + the banner refresh (objects=3) all delivered + applied
  client-side; the stamp ran clean (no restamp fail). The rollback = the expected outcome
  per L1/L3: the CUI region, not the wire.
- NEW SYMPTOM: the ship is not showing in orbit (the user; might be unrelated). The ship's
  server-side data = unchanged (the same encoders + the instance pipeline), so the prime
  suspect = the same client-side offline render/resolution family - recorded, not chased.
- NEXT (the reviewer's #3): the CUI live-reads - the instrument (live_read_classmap.py +
  the lane recipes #3/#4/#5) = the named tool; one instrumented boot watches the gate
  primitives' byte-tests + the CUI component globals live.

## 10.25 M2 LANDS: the content-table channel = cleared as the substrate (2026-08-16)

- THE CONTENT-TABLE PATCH (the type-11 channel): full deliverable read. The fork has NO
  content-table machinery; the client's type-11 = a dormant OVERRIDE layer over a STATIC
  table the client builds from the served catalog (the package rows + the content). The
  availability gate (FUN_14034FF20) fails cleanly with no patch and every consumer runs the
  static path - the kNoDefinitionHash defaults = STATIC-resolution misses, not missing
  patches. The patch = a CRC32-checksummed compressed table image ({u16 tagHash, u32
  tableId, tagReflection header, raw blobs}), a hotfix tool for content the client already
  knows, NOT a teacher of new definitions. VERDICT: the content-table channel does NOT
  explain the 2100 payload gap; the lever stays = the contentConfig manifest/package rows
  (the M1 lane's verdict = the discriminator). The patch = parked as a later polish step.
- THE INSTRUMENTED BOOT = ARMED (the deployed DLL bf8435c1c483684c): the gate observers
  (the flag-test + the 2100-emitter detours with the caller-RVA logging) = deployed to the
  dcv build (.bak_gatewatch = the vendor DLL). The boot = the user's call.
- M1 = still in flight (the tigercli content inspection of the three _unp1_ packages).

## 10.26 M1 LANDS: the manifest lever DIES CLEANLY (2026-08-16)

- THE THREE EXCLUDED PACKAGES = BENIGN (claims/inventory-m1-manifest-close.md, full read):
  w64_client_bootstrap_unp1_0 = client-boot audio/containers/render params + one mode-binding
  metadata record; w64_ui_bootflow_unp1_0 + w64_ui_startup_unp1_0 = UI screens/widgets/audio
  banks/fonts. The reverse census (the full distinct-reference sets vs the 129-class
  definition catalog) = the ONLY hit = 0x80809780 (the mode-binding metadata) in bootstrap;
  ZERO item-definition/investment/sandbox/vendor/activity/spawn classes anywhere in the
  three. The _unp1_ exclusion = deliberate AND correct - nothing the subclass UI resolves
  against lives in them. NO re-inclusion fix.
- THE EXACT-ROW DIFF = CONFIRMED EXACT at the construction level: the independent Python
  port of the fork's scanner (classifier + header + grouping + fingerprint) reproduces
  2,199 recognized + 3 excluded, all canonical; the recomputed GUID = the boot-logged GUID;
  all 3 deployed caches = 2,199 rows row-for-row EXACT; all 5 served config captures = the
  exact rows (order/name/packageId/buildSignature; offerKey = the fork's kOfferKey). The
  served rows = exactly the recognized set. FINDINGS 10.23 now stands at the construction
  level (scanner -> cache -> wire), not just the classifier level.
- WITH BOTH LEVERS CLOSED (the manifest = complete-by-construction; the content-table = a
  dormant override): the 'definition not loaded' defaults = the client's own offline
  resolution state. The named path = the outside-review's #3 (the CUI live-reads) - the
  corrected gate observer (the pointer+index+byte logging) = deployed (696db23e6fe0be2a),
  awaiting the user's second instrumented boot.

## 10.27 THE GATE-OBSERVER FREEZE + THE LOCKED-DLL TRICK (2026-08-16 ~15:28)

- THE FREEZE: the gate-observer pass 2 (the pointer+index+byte logging) froze the client at
  the white load screen (past the black-screen phase, the spinner bottom-right) - twice,
  reproducibly. The cause = the observer's byte dereference (base + 0x742C + idx) with the
  args NOT being the assumed (pointer, index) pair (the register shape = unknown; the pass-1
  raw observation showed the first arg = a pointer-ish value). Pass 3 = the raw-registers
  form (RCX/RDX + result + caller RVA, NO dereference) - the safe shape, committed.
- THE ZOMBIE: the crashed destiny2 (PID 26612) lingered with an unkillable DLL handle -
  the taskkill = access denied (the same owner), the process = invisible in the user's
  Task Manager, and the deploy = blocked. THE FIX (the rename trick, recorded in AGENTS.md
  Conventions): rename the locked DLL to a .broken name (the zombie's handle stays on the
  old name; the original path frees) -> copy the known-good build in -> the zombie keeps
  its copy until it dies. The vendor DLL (6c955b649840843d) = restored. THE PRIOR ART: the
  s1-3-swap-lane's wmic process terminate = the fallback for the killable-but-denied case.
- THE RULE (generalized, the deploy toolbelt): the rename-trick FIRST for a locked deploy
  file, then wmic, then the reboot.

## 10.28 THE ESCALATION (2026-08-16 ~15:3x)

- THE TRIGGER: the clean-baseline boot (the restored vendor DLL, no instrument) = the same
  results - the subclass swap + the ability select + the collections pull all still behave
  the same (the cue completes, nothing commits; the ship still missing in orbit). The
  user's decision: ESCALATE to the outside review.
- THE ESCALATION PACKAGE (the protocol: 'STATE + incident <ts>'): the fresh digest =
  RE_output/incidents/INCIDENT_20260816_152931.md; the living state = STATE-inventory.md;
  the evidence = the claims (inventory-snapshot-build.md, inventory-equip-verb.md,
  inventory-l1-equip-message.md, inventory-l3-delta-encoder.md, inventory-m1-manifest-close.md,
  inventory-m2-content-table.md) + this FINDINGS file.
- THE STATE AT THE ESCALATION (the one-paragraph form): the server = exonerated by
  construction (the commit gates on neither the status nor the value; every verb the client
  emits = handled + persisted; the frames arrive + apply client-side); the manifest = 
  complete-by-construction (M1) + the content-table = a dormant override (M2); the failure
  = the client's own offline definition-resolution state + the UI's cached model - the
  gate family = active and failing every byte test (the pass-1 instrument: caller +0xE0880C,
  all results false, the 2100 emitter never fires); the remaining instruments = the pass-3
  observer (raw args, safe) + the CUI live-reads (live_read_classmap.py) + the L3 open
  items. THE QUESTION FOR THE REVIEWER: the next move ranking - the instrumented retry vs
  the CUI live-reads vs the client-DLL patch route (the validator-hook precedent) - and
  the bounded actions + the DO-NOT list.

## 10.29 THE MODE-PAIR PLAN ARMED + THE STATIC LANE (2026-08-16 ~16:1x)

- THE RULING EXECUTED (the outside review's ranked actions): (1) THE MODE-PAIR RESOLVER DIFF
  = the new #1; (2) the external pass-3 boot batched with (1); (3) the static emitter-
  registry lane; (4) the DLL-patch route = LAST; (5) the ship = same family, not chased.
- THE INSTRUMENT (pass 4, built + committed on inventory/snapshot-generalization):
  ff39d5c911f83c9f. THREE log-only observers now: the acquiredFlags byte test (raw RCX/RDX
  + result + caller), the 2100 emitter (payload hash + caller), and NEW = the 702 sync-
  header serializer FUN_1405308C0 (logs the 8 header u32s the original just wrote - the
  definition-hash slots: u32 0 + the two kNoDefinitionHash slots + the post-sync dynamic
  hashes). THE MODE CHANGE: install() no longer gates on externalServer.enabled - the
  observers attach in BOTH modes (the mode-pair diff needs the same instruments on each
  side). SAFETY: the 702 sampler reads ONLY the output buffer the original itself filled
  (a decompile-verified two-pointer ABI, the same class as the accepted emit-hash read);
  no assumed-shape dereferences. Also dropped the duplicated ability_gate::uninstall()
  line in client_runtime_lifecycle.cpp.
- THE DEPLOY GEOMETRY (verified, no surprises): the cache identity = GetModuleHandleW(nullptr)
  = the PROCESS MAIN MODULE (destiny2.exe in-process / sunrise-server.exe external) - the
  DLL swap touches NO identity pair. Boot A = the DLL + the settings flip only, NO re-stamp.
  THE SETTINGS DIFF (bak_s1inproc vs current) = exactly 3 keys: external_server.enabled
  (False vs True), core.logging.file_sink (False vs True), core.logging.levels.client
  (warn vs info). Boot A's settings = the current file + enabled=False + file_sink=True +
  levels.client=info (the capture knobs; the mode = the ONLY variable).
- THE STATIC LANE = LAUNCHED (inventory-l3b, the moratorium deliberately lifted for this
  ONE read-only PyGhidra lane - the ruling orders it): the emitter-registry close-out
  (claims/inventory-l3-registry-close-brief.md; the descriptor grammar at 0x141DF7BD8 +
  the reader hunt). The d2_full lock = free at launch (zero java/analyzeHeadless procs).
- THE BOOT PLAN (the user's call): BOOT A = the in-process instrumented oracle (the
  pass-4 DLL + the mode flip; the daily routine: subclass select + ability select +
  collections pull - the working side's slot/caller baseline). BOOT B = the same DLL on
  the external stack (the same actions, the failing side). THE DIFF = the caller sets +
  the slot values per side. The revert = the vendor DLL (6c955b649840843d) any time.

## 10.30 THE TRAP-18 TRIP + THE PREMISE KILL + THE CORRECTED RULING (2026-08-16 ~16:0x)

- THE TRIP (TRAP 18, re-bitten): boot A's settings flip used json.dump (indent=2) -> the
  45,179-B settings expanded to 113,070 B -> the launch died with 'problem reading game
  content' (the parser = format-sensitive: the compact spacing like 'file_sink': true , is
  load-bearing). THE FIX = the byte-exact pattern (RE_scripts/modepair_settings_fix.py):
  restore the backup + ONE raw anchor replace ('external_server': { 'enabled': true -> 
  false, +1 byte, nothing else touched). The result = 45,180 B = byte-identical to the
  known-good bak_s1inproc except the two capture knobs. THE LESSON RE-AFFIRMED: NEVER
  reserialize the game-side settings; raw byte replace or nothing. TRAP 18's first bite =
  FINDINGS 9.4 (the same fix shape).
- THE PREMISE KILL (the user's correction): THE ORIGINAL SUNRISE DLL = hardcoded - no
  subclass or ability select at all (only ONE subclass visible; class/subclass/weapons =
  hardcoded values). THE MODE-PAIR ORACLE = DEAD: there is no mode where the swap works;
  the in-process game = a static broadcaster (no 403 handler, no mutation, no persistence).
  A swap attempt there would roll back at an EARLIER stage (no after-image ever arrives).
  L3 already said the quiet part: 'the same offline gap existed in the in-process era' -
  the mode question was pre-answered statically. The reviewer's #1 = overweighted; boot A
  = CANCELLED + the staging reverted (the vendor DLL 6c955b649840843d + the external
  settings 45,179 B restored; the pass-4 observer DLL stays parked in the worktree build).
- THE CORRECTED RULING (the higher-tier model, owning the fault): the oracle = never
  in-process-vs-external; it = LIVE-2020 VS OFFLINE-2026. The client shipped expecting a
  live-service environment (real entitlements, real acquired-rights flow, real
  content-rights phases) - the state Bungie's servers populated that our wire recreation
  doesn't; both Sunrise modes lack it. THE RANKING: (1) ZERO-BOOT ON DISK - read the
  definition slots from the existing -87-era dump-pair (the healthy in-process dump vs the
  failing external dump) -> verifies L3's structural claim at the memory level, no boots;
  (2) ONE FREE OBSERVATION - the daily in-process game, attempt a swap, cue-then-nothing =
  the shared gap confirmed from the UI side; (3) THE TWO REAL DOORS - (a) the CUI decode
  (primary: L3 open #3/#4 + the live-reads + the pass-3 observer; three surgical answers:
  which acquiredFlags indices the gate tests, what would make the definition slots resolve,
  what the commit's downstream reads) and (b) the acquired-flags NEEDLE-THREAD (the all-2
  direction wasn't refuted - only the blind 12,300-row hammer broke the family-4
  replication; with the tested indices known, write those only, scalpel-not-hammer); (4)
  the DLL-hook route = LAST, unchanged. THE REFRAME: the boundary = environment-recreated
  vs environment-assumed; the task = satisfying a handful of named server-controllable
  assumptions (the GUID/entitlements/signatures/eqHash impersonations already done - the
  same trick, a different registry).

## 10.31 THE PARALLEL PROGRAM LAUNCHED + THE REGISTRY LANDS (2026-08-16 ~16:4x)

- L3B LANDED (claims/inventory-l3-registry-close.md, full read): THE READER IS FOUND -
  the gate tables are walked by VMProtect-virtualized rip-relative code (708 disp hits,
  25/25 verified; zero pointers/immediates = L3's zero-xref scans explained: Ghidra
  cannot decode VMP). The registry = 12-byte {S,D,E} records (L1's view was misaligned);
  the descriptor = {type u8, param u8, count u16}+payload nodes (0x01 descending u16
  lists / 0x19 handler+value / 0x21 {len,hi,lo} smalls + a .text range + a tail) = a DAG
  rooted at 0x141DF7BD8 shared by 3,573 .pdata holders - THE STATIC WIRE-SCHEMA CATALOG.
  The gate range [0x140E06070,0x140E0608F) + the caller @+0xE0880C = its own .pdata
  record. The 0x141E2FExx cluster = the per-verb wire-schema records. THE RIP-DISP SCAN
  = the proven hidden-reader tool (lane_inv_registry*.py).
- THE PARALLEL PROGRAM (the user's go, the clone convention: one project clone per
  Ghidra lane - 7 clones @ ~1.17 GB each, the ONE-PROJECT rule becomes one-per-lane):
  the CUI decode campaign = 4 pieces (cuia: the gate chain + the tested indices;
  cuib: the definition resolver + the slot writers; cuic: the commit's downstream;
  cuid: the catalog semantics - the node-field meanings + the per-verb records + the
  exact record count); the purchase band = 2 (purcha: the 1200-1399 emitters; purchb:
  the storefront drivers + the server requirements); the bootflow table = 2 (boota:
  the states census, corpus-only; bootb: the transition conditions). ALL 7 = launched
  + the dump-pair slot census = still running = 8 concurrent lanes total. The
  moratorium = lifted deliberately for this program (read-only RE only; the
  edit/build = main-session hands, unchanged).
- THE IMPLICATION (the extrapolation thread): the .pdata catalog = the static image of
  what the client expects per code range - the cuid decode + the cuib/cuic chains =
  the live-2020 environment question answered statically, exactly the 'see what it
  expects before we run into it' program. The finish = the three surgical answers +
  the scalpel flag patch + one instrumented boot.

## 10.32 THE DUMP-PAIR CENSUS LANDS (2026-08-16 ~17:0x)

- THE DELIVERABLE (claims/inventory-dump-pair-slots.md, full read): L3's 'structurally
  empty in both modes' = CONFIRMED at the memory level - the definition slots stay
  kNoDefinitionHash-filled in both dumps. THE TWIST: the population asymmetry runs
  OPPOSITE to the oracle hypothesis - the healthy in-process dump carries ~19.7x MORE
  defaults (612,824 vs 31,092; 94% in one 497-MB table's default bands). Real hashes
  populate in BOTH dumps but in their own hash-dense table bands (failing: the first
  80 MB at 47-78% hash-like; healthy: +84-96/+192-208 MB) - the resolver's data
  exists somewhere in both modes; the slots never get filled from it.
- THE BLOCK DAT_141FBCC00: 0/512 byte diffs (the sanity check passed); decodes as 13
  records {ptr->'unavailable', realHash, 0} with REAL hashes + two Tiger-bitpacked
  strings + pointer heads; the block holds NO kNoDefinitionHash - the canonical
  'definition not loaded' state = the named records; 0x811C9DC5 = purely the in-memory
  slot default.
- THE HEADLINE (the acquiredFlags divergence): both dumps share a byte-identical
  {0,2} bank (4,931 twos at the SAME fixed VAs, zero diffs - the seeded account
  state), but the HEALTHY dump alone carries a 100%-0x02 bank + a 4-MB+ memset-2
  fill running past +0x742C - the working in-process game's account ran ALL-2, and
  the failing external dump entirely lacks it (0 hits for the object's head
  signature). THE IMPLICATION: value-2-everywhere = what the WORKING game actually
  had in memory - the needle-thread's target state is validated by the oracle's own
  memory; the all-2 experiment's failure (10.20) = the replication rejection, NOT the
  gate semantics. THE OPEN QUESTION: how the healthy in-process path populated the
  all-2 bank without tripping the family-4 replication machinery (the seed-via-DB
  path = rejected; the healthy path = something else).
- OPEN ITEMS (5, from the lane): the getter chain FUN_140be5360/140be5780 decompiles;
  name the 497-MB table via tag-hash matching (the d2-cui-explorer methodology); the
  two packed strings (quicktag/tachyscope); bound the all-2 fill's extent; map the 40
  .data needle-delta RVAs to symbols.

## 10.33 THE BOOTFLOW STATES CENSUS LANDS (2026-08-16 ~17:0x)

- THE DELIVERABLE (claims/bootflow-states-census.md, full read): 26 bootflow: state
  names (the contiguous .rdata pool); the world-controller state enum pinned 0..39
  (14 values observed by number+name, 12 inferred from the pool order, 2 anchored by
  the fork's world_step.cpp: 33 = the first map-load step, 38 = activity:in_world).
  Three historical sequence variants across the 24 incidents: A (the stall at
  bap_signin, 14 incidents), B (through rejoin_activity), C (the current clean boot
  through cleanup -> setup:orbit). Every transition reason in every incident =
  'unavailable' (the offline default; no server-supplied reason ever arrives). The
  fork's 11 bootflow hooks = all mapped into the sequence with their observed
  fire/no-fire evidence.
- THE LIVE-ERA OBSERVATION (the reframe, visible in the bootflow): the states that
  NEVER ran in any incident = 8-15 (entitlement_warning, sms_validation,
  free_license_limit_exceeded, excessive_mtx_debt, eula, upsell, email,
  confirm_account_transfer) = literally the live-era account/entitlement gates -
  the bootflow is where the 'client expects a live environment' shows up on the
  wire-free side. Their transition CONDITIONS = exactly what the boot-b lane is
  decompiling now (GLM's #6 completes when it lands).
- OPEN ITEMS (5): state 0's name; why state 27 (character:signin) never logs its
  enter; whether the pool-adjacent tail (bubble_host/transitioning/error/
  hysteresis) = the same enum; why only 'unavailable'; the pre-tail-window
  artifacts (media_check + 8-15/17-20).

## 10.34 THE CUID SYNTHESIS (2026-08-16 ~18:5x)

- THE LANE'S DELIVERABLE = a TBD shell (the silent-cap death pattern - the pass ran, the
  synthesis died); the MAIN SESSION completed it from the raw (the deliverable rule; the
  evidence = lane_inv_cuid_raw.txt PHASES A-G, all quoted).
- THE BIG CORRECTION: the 'descriptor catalog = the static wire-schema map' reading is
  REFUTED. The evidence: (1) the 12-byte records read cleanly as {begin RVA, end RVA,
  meta RVA} = the RUNTIME_FUNCTION geometry (rec[340] = {0xF3B462, 0xF3B470,
  0x1DD89BC}; the registry lane's {S,D,E} order = the misread - its own sibling-table
  {S,E,T} = the true order); (2) every 0x19 node pairs with __GSHandlerCheck +
  a cookie-style value (0x380/0x360/0x10060/0x230/0x40/0x30/0xB8) = the /GS stack-cookie
  signature; (3) the node payloads = {size-class, offset} u16 pairs in descending offset
  order = stack-frame slot tables. CONCLUSION: the ecosystem = the unwind/scope/cookie
  metadata (the .pdata exception-directory family); its VMP readers = the unwind walkers.
  THE WIRE-SCHEMA QUESTION = RE-ROUTED to the schema-driven decoder (FUN_14051dcd0, L3's
  open #1). The definitive test (open): compare the table against the PE's own
  ExceptionDirectory.
- THE GRAMMAR WALKS COMPLETED: the root region (30+ 0x21 nodes naming 13 real functions:
  FUN_140b5e580/140b5daa0/140b6d4a0/140b65f90/140b69520/140b6afd0/140b6bc70/140b78680/
  140b5dc00/140b5da10/140b5d970/140b5db00/140b65ea0 - the 0x21 code pairs = 0x3e-bounds-
  check entry windows) + the gate tail 0x141DD89BC (chains FUN_140508b50/14050a090/
  14050a8c0/14050c710; 8,848 .pdata xrefs).
- THE HARNESS TRAP (recorded): the lane's PHASE C = 13 decompile failures - the 2-arg
  decompileFunction(FunctionDB, int) overload does not exist; the correct form =
  decompileFunction(f, 120, mon). The 13 decompiles = OPEN (one re-run on the parked
  clone).
- THE REGISTRY EXTENT: 341 records forward from 0x143652000 (through rec[340]) + 11
  valid preceding records backward = the table extends BOTH ways past the known bounds.
  The 0x141E2FExx cluster = RUNTIME_FUNCTION-shaped triples over the generic emitter
  {0xF2C280, 0xF2C2CC, 0x1E2FE60} (the aligned parses; the lane's 'bare triple' parses =
  misaligned garbage). The 0x141DCxxxx chains = {ptr}+{0x21-node} records (walk
  incomplete).

## 10.35 THE PURCHASE PAIR LANDS (2026-08-16 ~18:5x)

- PURCH-A (claims/inventory-purchase-emitters.md, full read): THE 1200-1399 BAND IS
  EMISSIONLESS - the whole-image LE+BE u16/u32 scans + the COMPLETE 38-site emitter
  census (38/38 resolved, zero band opcodes - including the new second-904 emitter
  FUN_140f2d890 and the post-library trio FUN_140f2d7b0/f2d870/f2d890) + the ZERO
  storefront/eververse string vocabulary all agree: the band = the Season-12+ (Beyond
  Light) Eververse storefront opcode space, ABSENT from the Arrivals build. The fork's
  47 band shapes (16 statusOnly / 31 statusPair - count corrected) = unreachable
  server-side insurance. THIS BUILD'S purchase surface = the Mtx vendor-progression UI
  (FUN_14052f960, local rewards) + the BAP purchased-offers messages (types 21/22) +
  the L1-mapped 403/801/1820/1901/701/702 family. L1's OPEN #5 = CLOSED (the 801
  caller container = FUN_1414b95c9, 545 B). NOTE for the Beyond-Light question: the
  band's absence = concrete client-side evidence of the era split (the storefront
  vocabulary arrived AFTER Arrivals).
- PURCH-B (claims/inventory-purchase-drivers.md, full read): the storefront flow =
  the CUI components -> FUN_1414d7010 (the collections transaction driver) ->
  FUN_1414d7620 (1820, the 3-byte {u8 type=3, u16 id} key) / FUN_1414b95c9 (801/802/804)
  / FUN_1414b9560 (1901) / FUN_140f2d890 (904 = the 'acquire everything unlocked-but-
  unowned in this category' batch verb - its locked/owned predicates FUN_140547800/
  FUN_140555280 READ THE ACQUIREDFLAGS BANK + the collection state = NEW GATE ANCHORS
  for the cuia lane). THE SERVER REQUIREMENTS = implementation-ready: answer each verb
  with its route shape (already correct), mint instances for 1820/1901/904 from the
  extracted item/vendor sale-list data, push the family-4 after-images - the reply
  payloads carry nothing else the client reads (the response census re-confirms L3's
  negative for the whole surface).
- THE CUID-CORRECTION APPLIES HERE: both purchase deliverables' 'D-node = per-verb
  wire-schema' reading = superseded by 10.34 (the D-values = unwind/scope metadata);
  their census DATA stands (the emitter map, the drivers, the requirements table) -
  only the semantic label of the D-column changes.

## 10.36 THE CUID CLOSE-OUT (2026-08-16 ~19:0x)

- THE DEFINITIVE TEST (open item #1 from 10.34, run inline - no lane): the PE's
  ExceptionDirectory = RVA 0x1F08320 in .rdata (0x51C8 B = ~1,744 RUNTIME_FUNCTION
  records) - NOT the catalog. The 'registry' table @0x143652000 sits in the section
  named .pdata (VA 0x1435D0000, 1.37 MB, vsize 0x14D800) which the PE header does NOT
  point at as the exception directory - the VMProtect repack signature (the original
  exception dir moved to .rdata; the 1.37-MB .pdata = VMP's own metadata section).
- THE REFINED VERDICT: the catalog = the VMP-metadata section (the {begin, end, meta}
  records = the VMP function-catalog geometry + the descriptor DAG = the VMP
  scope/cookie/GS-flavored metadata) - the 'wire-schema Rosetta stone' reading = DEAD
  for good, confirmed at the section level. The wire-schema question = FUN_14051dcd0
  (unchanged). THE RESIDUE (confirmatory, NOT campaign-critical): the 13 pair-function
  decompiles (the 3-arg fix, the parked clone), the 0x141E2FExx aligned walk, the
  chain traversal, the true table bounds. VERDICT ON A NEW LANE: NOT needed for the
  campaign - the reconciliation = complete; the residue = rides a future batch if the
  VMP-metadata story ever matters (it doesn't for the commit gate).

## 10.37 CUI-B LANDS: THE DEFINITION RESOLVER (2026-08-16 ~19:1x)

- THE DELIVERABLE (claims/inventory-cui-resolver.md, full read): the 2100 hash = the
  request object's first u32 copied by FUN_140530170 - only the .pdata-catalog VMP
  dispatcher can fill it (zero code callers, zero analyzed producers of a real hash).
- THE 702 SLOTS (the structural answer): the two kNoDefinitionHash slots =
  tag-resolution fields of the account record's +0x2F00 dynamic-state block. The
  complete chain: init = the schema walker FUN_140372a60 (writes the sentinel whenever
  a field's 0x8080-definition tag fails to resolve; the payload-schema records carry
  the target tags - 0x80807781/780/77F... for 2100, 0x80807604/603/602/601/600 +
  0x80802C36/2C35 + 0x808075FF... for 701/702 - and NO stored sentinel) -> refresh =
  FUN_140be41c0's store->local copy (store+0x2F00 -> local+0x13A0 verbatim) -> the
  store values come from THE FAMILY-4 PUSH APPLY (FUN_140e05da0 <- FUN_140e01520, the
  encoding selector).
- THE TWO CAMPAIGN IMPLICATIONS: (1) A NEW SERVER-SIDE LEVER - the family-4 account
  push's +0x2F00 dynamic-state hash fields = server-published data the client copies
  into the 702 slots; the encoder can publish RESOLVED hashes directly (no client
  patch, bypasses the tag resolution). (2) THE TAG SHOPPING LIST - the schema
  descriptor records name the exact 0x8080-tags the payload buffers reference = the
  targets for the definition-catalog route.
- THE REST: the hash-array runs (FUN_140503160/380) = 0x68-stride record APPENDERS
  fed by a ~23-function builder family that is ALSO catalog-dispatched (zero code
  callers; the builders append real FNV hashes from static constants when dispatched);
  the 36-slot socket array's owner chain = COMPLETE (FUN_140e08670 <- FUN_140e080b0 <-
  FUN_140E05DA0 the family-4 diff apply <- FUN_140e01520 the encoding selector - the
  socket resolution runs INSIDE the push decode pipeline); DAT_141FBCC00 = the
  indexed {name-ptr, hash} fallback table with the accessors named (FUN_140e2d570 =
  the hash lookup, -1 -> sentinel; FUN_140e2d5a0/610 = the name lookups, 'NONE').
- THE VERDICT: the resolution machinery exists end-to-end (registrar -> tag records ->
  walker -> pair-table accessors); the dispatch lives in the VMP reader family; the
  slots resolve only with (a) a live definition for the target tag + (b) non-sentinel
  family-4 push fields - structural (definition-catalog/data-side), not a server flag.
  THE OPEN ITEMS (5): the walker's tag->offset grammar; the dispatcher's param_1
  (a live bp on FUN_140f2b690); the 0x140683xxx reader cluster; the loop-table row
  targets (0x141A25B20, live dump); the 11,932-slot runtime table (live RPM).

## 10.38 BOOT-B LANDS: THE TRANSITION CONDITIONS (2026-08-16 ~19:2x)

- THE DELIVERABLE (claims/bootflow-transition-conditions.md, full read): GLM's #6 =
  CLOSED (boot-a + boot-b both landed). 40 states mapped (id 0 = pregame, 28 =
  cleanup - the incident's [28] explained; 1-26 the bootflow family, 29+ the
  setup/activity families). THE DISPATCHER = decompiled end-to-end (request
  FUN_140e1b4d0 / commit FUN_140e1ae70 / pump FUN_140e23530 / validator
  FUN_140e2dbe0 with the source-state bitmasks; the transition notification =
  compiled out - FUN_140b94700 = a stub).
- THE REASON TABLE (boot-a's open #4 answered): all 64 reason records =
  'unavailable' - the build's reason vocabulary = a single entry; the offline path
  requests every transition with reason 0. NOT a server failure - the table IS the
  vocabulary.
- THE SERVER-CONTROLLABLE GATES (exact fields): 16->21 content_check = the manifest
  gate chain FULLY decompiled (9 globals @0x141F916xx, 10 functions: the fetch, the
  decode, the GUID compare, the row filter - the ContentConfig GET the Sunrise server
  answers); 21->22 package_registration = the worker result DAT_14267aa50 + the
  +0x168-hash validator (the -87 chain, the row source = the manifest's field-3 rows);
  22->23 bap_signin = the patchable bootstrap load; 23->24 investment_signin = the
  investment globals-client block. 4 of 26 states read server-supplied fields; the
  rest = client-internal/VMP (the per-state UPDATE bodies = VMProtect - the state
  names reachable only as mov-imms inside VMP code, the L3 pattern again).
- THE SURPRISE-GATE FAMILY = MOSTLY CLOSED: the bootflow's conditions are now named;
  the residue = the VMP per-state handlers with the CHEAP live recipe handed back
  (a 40-line script: *0x14280E1F8 -> wc+0x200+id*8 -> the vtables - all offsets in
  B4). The states 18/19 (insufficient_content_space / illegal_dlc_media) = NO
  registered state object in this build (the ctor fills 37 slots for 39 states) =
  officially unreachable.

## 10.39 CUI-C LANDS: THE COMMIT'S DOWNSTREAM (2026-08-16 ~19:3x)

- THE DELIVERABLE (claims/inventory-cui-commit-downstream.md, full read): the apply
  contract = apply(vtablePtr = *resolved, decodedRecordPtr, decodedSize) with a fresh
  0x7ff6a-cap arena buffer holding the decoded {u16 tag, ptr, len, ptr, len, schemaId}
  record + the schema-driven body - the 5-bit status / 32-bit value = schema FIELDS
  INSIDE the body, never separate args (L3's verdict re-confirmed at the call level).
- THE KEY STRUCTURE: the packed key = {record index << 13 | 13-bit entry index}
  resolved through the holder 0x142439C70's runtime-built 0x40-stride record table
  (entry = base + idx*stride; object = entry - (mask & *(entry+8))) - four
  independent use-sites confirmed the same formula (commit, envelope decoder, msgId
  gate, size getter).
- THE STATIC BLOCKER (named): the per-verb objects/tables = RUNTIME heap allocations
  (the builder chain FUN_1403503e0 -> FUN_14034d2e0 -> FUN_14034c940 ->
  FUN_140350110 decodes the blob DAT_141f44e20 whose qwords = heap addresses) - the
  per-verb applies = live-read territory (the OPEN-1 recipe: the boot-time RPM walk
  base+0x42439C70 -> the holder -> resolve each verb's key -> vtable[0], OR hook the
  apply call in FUN_140e02280, OR replicate the blob decoder - a flash-lane job).
- THE TAG-READ CONSUMER: FUN_140e00df0 = the response processor -> the per-tag
  0x18-stride UI state machine (FUN_1417429c0/9b0/9a0/990 -> 1/3/2/4) + the follow-up
  request cascade (the re-entry into the enqueue pipeline) - the tag's downstream =
  the per-tag UI machinery + follow-up emission.
- THE FAILURE PATH: the store-installed observer's vt[0](self, 1, reply[1]) - the
  install = VMP-hidden (the whole-image raw-byte scan: the ONLY +0x81e0 write = the
  init's NULL); a decode failure completes the message as FAILED (no commit, the
  buffer freed, the store keeps its previous state).
- THE COMPLETION CONTRACT: the codec vt[+0x58] = SIX args (codec, msgId, recordPtr,
  size, extraPtr, extraDword) - the disasm settles it; the no-op FUN_140b94700
  ignores all six; a future non-no-op completion must take the 6-arg form.
- THE CAMPAIGN STATUS: TWO of the three surgical answers now landed (B = the
  resolver + the +0x2F00 lever; C = the commit contract). THE LAST LANE OUT: cuia
  (the gate chain + the tested indices) - when it lands, the assembly begins (the
  tested indices + the all-2 target state from the dump-pair -> the scalpel patch;
  the +0x2F00 encoder lever; the commit contract).

## 10.40 THE LANE DEATH-SAFETY RULE FORMALIZED (2026-08-16 ~19:4x)

- THE RULE (the lane-survival contract, from the death forensics - the cuid TBD-shell
  case + the silent-cap pattern): codified in three places - the lane-brief template's
  DEATH SAFETY section (RE_output/claims/lane-brief-template.md), the AGENTS.md
  Conventions (the LANE DEATH-SAFETY RULE), and the STATE-inventory workstream rules.
  The five: (1) per-phase claims written the moment each phase's evidence lands, never
  end-of-run; (2) near-limit = stop-and-write (the rest = OPEN items + evidence
  pointers); (3) the priority order raw > per-phase claims > log > report-back (the
  report-back = OPTIONAL, the files = not); (4) the collector fallback = assumed (the
  main session completes the deliverable from the raw - the cuid recovery pattern);
  (5) no sleep/poll loops - background the long runs, one spawn, one report, end the
  turn; the main session harvests via the artifact mtimes + the completion notification.

## 10.41 CUI-A LANDS: THE GATE CHAIN + THE TESTED INDICES (2026-08-16 ~19:5x)

- THE DELIVERABLE (claims/inventory-cui-gate-chain.md, final version read end-to-end -
  6 passes; the lane folded passes 4-6 in before its budget death; the artifacts were
  backed up pre-kill to RE_output/content/_scratch/cuia_landing_backup/, hashes
  verified). THE ANSWER A: the gate FUN_140e06070 = VTABLE SLOT 0 of a runtime-built
  CUI transaction object - the instrument's caller @0x140E0880C = the return of the
  INDIRECT call CALL qword ptr [R8] @0x140E08809 inside FUN_140e08760 (the step
  loop). That is why zero xrefs ever existed: Ghidra cannot resolve a vtable call, and
  the vtable has NO static reference to the gate anywhere.
- THE CHAIN (7 levels, every edge a verified direct call): FUN_140be0f20 (the
  account/profile service) -> FUN_140fad240 (the CUI transaction driver, 30-s retry)
  -> FUN_140e0a870 (the per-item state machine, 381 slots) -> FUN_140e0aa40 (the
  transaction manager, FOUR tick families) -> FUN_140e0ac30 (the tick, 500-ms retry)
  -> FUN_140e08760 (the step loop) -> [vtable 0] the gate -> [vtable 8] the bitmap
  apply -> commit. THE INDEX SPACES: 0..255 (the two 0x100-count ticks) and 0..4095
  (the two 0x1000-count ticks) - the four families' scan ranges.
- THE INDICES: NOT static constants - each = the iterator's runtime current-index
  getter. THE STATIC CENSUS = ZERO constant indices. THE VALUES = a LIVE-BOOT item:
  hook the gate (the pass-4 observer = already built + the raw-args form = safe) and
  log (idx, result) during a swap attempt - the recipe (RPM the CUI globals -> the
  vtable -> the iterator getter) = OPEN-1. Also: the second bank FUN_140e06090
  (FUN_140be1cb0()+0x9348, the profile/session flags) = vtable-side too (0 direct
  calls); the tick-2 stack struct's static vtable candidate = DAT_141c25cd8 (live
  check); the catalog field-order correction {D,S,E} + the gate's true descriptor
  = 0x141EAF15C (not 0x141DD89BC) + the digit-shift fix for L3B's addresses.
- THE CAMPAIGN = COMPLETE (all three surgical answers landed): A = the gate chain +
  the iterator-driven indices (the live boot = the closer); B = the resolver + the
  +0x2F00 encoder lever; C = the commit contract. THE ASSEMBLY: (1) ONE instrumented
  boot logs the tested indices + the caller shapes (the pass-4 observers, built +
  safe); (2) the scalpel flag patch writes value 2 to the TESTED indices only (the
  dump-pair validated the all-2 target state); (3) the +0x2F00 encoder lever (cuib)
  for the definition slots; (4) the commit contract (cuic) confirms nothing else is
  read. THE DO-NOT from the ruling still governs: one DLL, one observer, one purpose
  per boot.

## 10.42 PHASE 1 LANDED: THE INSTRUMENTED BOOT (2026-08-16 ~19:5x)

- THE SESSION (the pass-4 observers deployed, ff39d5c9; the user's boot + 2 ability
  attempts + 1 subclass swap): THE GATE = FULL-BANK, ON REPEAT. The flag_test log =
  19,669 lines = one full 12,300-index walk + a second partial (the 500-ms retry
  rescanning); the UNIQUE index set = ALL 12,300 - the transaction machinery tests
  EVERY flag byte (the 4 tick families' 0x100/0x1000 counts tile the full bank).
  The distribution: 4,931 PASS (= exactly the seeded value-2 rows) + 14,738 FAIL
  (the 7,369 zeros x2 walks). THE CONCLUSION: the gate is NOT selective - the game
  wants the ENTIRE bank acquired, the healthy dump's exact state. THE SCALPEL =
  MOOT: there is no subset; the question = the DELIVERY of the all-2 state (the
  10.20 replication rejection), not the values.
- THE ABILITY VERBS = DEAD CONFIRMED LIVE: zero emit_2100 lines during the two
  ability attempts (the L1 finding - no live call site - now proven at runtime).
  The ability select = the cue-then-nothing with NOTHING on the wire: the gate
  upstream never opens.
- THE SUBCLASS SWAP = WIRE-CORRECT: the server captured the 403 (the 0x32 soid)
  + the 701/702 sync pairs - the server half works (the proven loop); the UI
  rollback = the client gate (the full-bank zeros).
- THE 702 HEADER LIVE: the sync_header sampler = {0,0,0,FFFFFFFF x4,0} - the live
  dynamic-state header carries NO kNoDefinitionHash sentinel (the L1 static
  reading's 'two 0x811C9DC5 slots' = the static default; the runtime = 0/0xFFFFFFFF
  markers). The rider's wc read @0x280E1F8 = garbage (not a plain pointer - the
  boot-b global = encoded or the getter = indirect; parked, not chased).
- PHASE 2 = DRAFTED (claims/inventory-phase2-delivery-plan.md): the delivery
  candidates ranked - (2a) the instrumented all-2 re-run (the controlled 10.20
  redo with the full gate picture), (2b) the evaluated-state coupling, (2c) the
  staged delivery, (2d) the +0x2F00 encoder lever (parallel, the definition side).

## 10.43 THE 2A RESULT: THE GATE OPENS, THE ROLLBACK MOVES DOWNSTREAM (2026-08-16 ~19:5x)

- THE 2A BOOT (the clean all-2 seed: 12,300 account rows value 2, RE_scripts/
  phase2a_seed.py, the backup state.db.bak_flags_20260816_172256): THREE results:
  (1) THE JOIN SURVIVED - zero replication-rejection lines (the 10.20 failure did NOT
  recur; the all-2 delivery = now proven possible via the clean DELETE+INSERT seed +
  a fresh boot). (2) THE GATE = FULLY OPEN - 12,312 flag tests, ALL result=1, ZERO
  result=0 (vs the sparse boot's 4,931/14,738). Every one of the 12,300 indices =
  acquired, exactly the healthy dump's target state. (3) THE UI STILL ROLLS BACK -
  the 2 ability attempts + the subclass swap = the same cue-then-nothing. THE
  VERDICT: the acquiredFlags gate = a NECESSARY condition, not the sufficient one;
  the blocker = DOWNSTREAM of the gate.
- THE SIDE-EFFECTS (the flag bank drives real client behavior, first visible proof):
  the boot played the D1 RECAP presentation (the veteran montage - one of the
  all-2 flags = the D1-veteran/legacy state) with the date '12/31/1969' (the epoch-0
  null timestamp in the recap's legacy data). The 'investment derived rebuilt' line
  = the client recomputing from the new flag state.
- THE DOWNSTREAM, NAMED: the 702 live header = {0,0,0,FFFFFFFF x4,0} - the
  dynamic-state hash slots = STILL the unresolved markers (0/0xFFFFFFFF), and the
  wire captures carry the E2E2E2C8/38A8AFEF variants (the sync's dynamic hashes).
  The cuib lever = the named next: the family-4 account encoder must publish the
  +0x2F00 dynamic-state fields with the RESOLVED hashes (the schema tags from cuib's
  list - 0x80807604 family), so the client's 702 slots carry real definitions. The
  alternate suspects (if the encoder lever doesn't commit): the family-5 evaluated-
  state bitmap (the gate's apply = the vtable-8 bitmap writes - now all-set with the
  all-2) and the per-verb applies (cuic's live-read territory).
- THE PHASE-2 PLAN UPDATE: 2a = DONE with the fork taken (the gate = cleared; the
  delivery = solved); the next = 2d (the +0x2F00 encoder) + the data-prep (the
  tag->resolved-hash mapping from the extracted content catalog); 2b/2c = parked
  (the rejection never fired).

## 10.44 THE FINAL WAVE: THE DOMAIN CLOSES (2026-08-16 ~20:1x)

- THE USER'S DIRECTIVE: close the domain - one final parallel wave while the 2dmap
  lane runs. SIX LANES LAUNCHED (7 concurrent with 2dmap): W1 = the schema walker
  grammar (FUN_14051dcd0/FUN_1404c1930 - L3's OPEN #1, the wire grammar's last
  piece, clone cuib); W2 = the blob decoder (FUN_140350110 + the cipher = the static
  verb->key table + the vt[0x28] dump, clone cuic); W3 = the bootflow registrations
  (the 0x141B9F6E0 records -> the complete static transition table + the wc-global
  getter question - why the live *0x14280E1F8 read = garbage, clone bootb); W4 = the
  dump-pair table naming (the 497-MB table's tag-hash matching + the packed strings,
  corpus + dumps, no Ghidra); W5 = the cold-run thunk naming (the ~1,340-1,380
  reachable targets, corpus-only - the parked item revived); W6 = the cuid residue
  (the 13 pair-decompiles with the 3-arg fix + the cluster walk + the chains + the
  true bounds, clone cuid). All briefs carry the DEATH SAFETY section (the
  per-phase claims, the stop-and-write, the priority order).
- THE WAVE'S MEANING: after these land, the static domain = closed - every mapped
  item = either a named live-read (the experiments themselves) or a server-side
  edit. The code phase follows (the 2d encoder + the harness + the test boot).

## 10.45 W1 LANDS: THE WIRE GRAMMAR CLOSES (2026-08-16 ~20:2x)

- THE LANE released its turn after spawning the PyGhidra detached (the
  submit-and-release trap, first real bite); the run COMPLETED (95 KB raw) and the
  main session synthesized the deliverable (claims/schema-walker-grammar.md) per
  the collector fallback. L3's OPEN #1 = CLOSED: the wire grammar = the
  schema-key-resolved recursive tree walk - FUN_14051dcd0 (the size getter +
  memset + the default-fill FUN_140372a60 = the sentinel/tag-resolution writer +
  the walker stub) -> FUN_1404c1930 (the dispatcher, the capacity header
  0xF0003FFE family) -> the walker pair FUN_1404b9200/bf920 (the 0x28-stride
  field rows; type 1 = the sub-tree descent, else the leaf) -> the field-type
  dispatch FUN_14038e400 (the 16-entry reader table @0x141BCFC88 - 7 real
  readers; the 4 modes: plain / flag-set / array / nested) -> the readers run
  the MSB-first bit reader FUN_1403513b0 + the varint FUN_140351530.
- THE OPEN: the 7 reader functions' per-type bit counts (the 5-bit status /
  32-bit value widths live there) = one small pass; the walker-pair encode/decode
  symmetry diff; the header-family call census (43 sites).

## 10.46 W4 LANDS PARTIAL + W1B SPAWNED (2026-08-16 ~20:3x)

- W4 = the silent death (the raw landed, no deliverable/log) - the main session
  synthesized the partial deliverable (claims/dump-table-naming.md): the PHASE-0
  plaintext negatives = confirmed (the 'definition not loaded' name = ZERO
  plaintext hits in either dump - the DAT_141FBCC00 fields = genuinely packed;
  'unavailable' = the bootflow log lines only); the packed strings' RAW BYTES =
  extracted (the S1/S2 32-byte fields). THE UN-REACHED (OPEN): the decode (the
  naive 5-bit unpack = garbage - the real Tiger string format = the
  quicktag/tachyscope decoder's job, one port), the 497-MB table naming (the
  tag-hash match - the brief's core, un-started), the 40 delta-RVA symbol map.
- W1B = SPAWNED (claims/schema-reader-widths-brief.md): the 7 readers' byte-exact
  bit counts - with the ANTI-CRASH CONTRACT hard-coded (foreground PyGhidra only,
  one pass one turn, the deliverable shell written BEFORE the pass, the turn ends
  with the deliverable filled). The W1 lane's submit-and-release death = the
  lesson baked into the brief.

## 10.47 2DMAP LANDS: THE +0x2F00 FIELD = THE CHARACTER RECORD'S MID-REGION (2026-08-16 ~20:4x)

- THE DELIVERABLE (claims/inventory-2dmap-fields.md, full read): cuib's 'account
  record +0x2F00' label = CORRECTED - the block = the family-4 CHARACTER record's
  (key/slot 1 = the wire object id 0xE5E86992 = the fork's kCharacterObjectId)
  dynamic-state region at character-object offset 0x2F00..0x43F0. The write = the
  in-place whole-object wire decode (NO field-specific writer exists - the 0x2F00
  census finds only the two readers: the 702 diff detector + the snapshot copier).
  THE FORK-SIDE FIELD = the character Object's unnamed mid-region (gateSeenPadding
  tail + seenMessages + seenRosterPadding + rosterMirror +
  rosterDestinationPadding + lastOrbitedDestination + newItemFlags +
  instanceProgressWatermarks + progressPreviewPadding + previewMirrors +
  previewProgressionPadding) - published today as zeros/0xFF-seen/watermarks.
  THE BLOCK = SERVER-FED, NOT CLIENT-COMPUTED (the refresh copies the store
  verbatim; the schema-default init runs only when the record is ABSENT). THE 2D
  LEVER = write the resolved values into that region of the fork's character
  encoder. The 701's record (+0x748) = the ACCOUNT record (a different key -
  the VMP thunk FUN_148587a08, not 1).
- THE SLOT POSITIONS: L1's positional evidence + the tag-pair symmetry (the
  0x80802C36/0x80802C35 adjacent pair) -> the two definition-hash slots = the
  header u32[1] + u32[2] (med). THE RESOLVED VALUE = the definition's stored
  default (the registered definition hash u32 from the runtime registry); the
  unresolved = the sentinel. THE OPEN: the exact positions + the tail names
  (a live dump), the 0x10 shift (the live 0xFF run @0x2F0C vs the fork's
  seenMessages @0x2F1C).
- THE IMPLEMENTATION (next): the fork's character encoder writes the resolved
  hashes into the two header slots (0x2F04/0x2F08) + the region's correct bytes;
  the VALUES = the catalog lookup for the 0x80802C36/0x80802C35 tags (or the
  captured E2E2E2C8-family variants); the build + the harness + the deploy + the
  user's boot.

## 10.48 W5 LANDS: THE COLD BAND CLOSES (2026-08-16 ~20:5x)

- THE DELIVERABLE (claims/coldrun-thunk-names.md, 120 KB, claims read + the
  totals spot-verified): ALL 1,304 reachable middle-band thunk targets named -
  49 high-confidence verified roles (the S5/S5b set re-verified), 575 medium
  (126 cluster-window + 442 tight-gap caller + 7 band-internal), 680 low
  (the 9 stub-dense buckets). The bodies = tiny dispatch fragments (the spinlock
  residue + the cold-band tail calls + the unrecovered jumptables - the
  VMProtect-mutation profile). THE PARKED ITEM = CLOSED. The open items = the
  optional upgrades (the xref pass, the 185 >=65B decompiles, the band-internal
  call graph).

## 10.49 W2 LANDS: THE BLOB CIPHER REPLICATED + THE 2D DEPLOY (2026-08-16 ~20:5x)

- W2 (claims/blob-decoder-keys.md, full read): the blob 'cipher' = a XOR-obfuscated
  class name (out[i] = blob[8+i] ^ (i-0xF), the per-class caps) + the 15-weight
  checksum key (SUM s[i]*W[(len+i)%15] mod 2^32, W = the odd run 3..59, the seed =
  the string length) - replicated byte-exact. THE STATIC VERB->KEY TABLE: {Tag
  Handle Array:0x9BDD, datum_array_repository_storage:0x16BD2, Datum Array Table
  Manager:0x107E3, content table patch handles:0x12156, Investment Content Table
  Runtime Data:0x1B2DC}. Cuic's OPEN 3 = settled (the codec vt[0x28] = the no-op
  for every msgType except 0/1/8 = FUN_140e11190, the real tag dispatch) + the
  cuic 'heap-pointer' verdict = CORRECTED (the blob qwords = static .rdata
  vtables). THE OPEN: the schema registry's own blobs + the field-map grammar
  (the 69-triple builder registry @0x1435EC800) = the future lanes' residue.
- THE 2D DEPLOY (the encoder build): the character encoder now publishes the
  dynamic-state header at 0x2F00 ({0, 0x80802C36, 0x80802C35, 0, 0xFFFFFFFF x3,
  0}) - the 2dmap answer's lever, committed (the worktree), the scratch gates
  pass (the identity stamped + the 5 swap-gate matches), the live server =
  restarted (PID 3112, the cache re-stamped, the .bak_pre2d backup). THE NEXT
  = the user's boot (the sync_header slots should show the tags; the swap = the
  discriminator).

## 10.50 W3 LANDS: TWO CORRECTIONS + THE ENCODED WC POINTER (2026-08-16 ~21:0x)

- THE DELIVERABLE (claims/bootflow-registrations.md, full read): (1) THE
  0x141B9F6E0 RECORDS = THE HAVOK REFLECTION/TYPE REGISTRY, not the transition
  registrations - 14 {payload, handler, third} records (the 'hkBaseObject'/
  'hkTransform' vocabulary + the StackTracer init), the handlers = the per-class
  ctors/stream fns writing the shared base vtable 0x141B9F6E8. BOOT-B'S OPEN-3
  PREMISE = CORRECTED (the records complete nothing about transitions; the
  transition table = B1/B4/B8, hardwired in the commit path). (2) THE WC GLOBAL
  = AN ENCODED POINTER: ptr = Decode(raw ^ K), Decode(x) = x ^ (x >> 32) ^
  0x09465C5C267949FF - PROVEN by the emulation (the 64 basis vectors + 200
  randoms; the 18-op VMP loop = the affine map). The phase-1 rider's garbage
  read (0x5C9B6896121425E8) = the RAW ENCODED qword - the global + the address
  were right, the decode = the missing step. THE LIVE FIX = call the getter
  FUN_140e35820 (module_base + 0x140E35820, the null-safe no-arg) via a remote
  thread -> the return = the wc instance -> the B4 offsets (+0x200+id*8,
  +0x390/+0x3a0...) become readable. THE RIDER UPGRADE = the next live_read
  phase1 run can dump the bootflow state vtables properly.

## 10.51 W1B LANDS: THE WIRE WIDTHS, BYTE-EXACT (2026-08-16 ~21:1x)

- THE DELIVERABLE (claims/schema-reader-widths.md, full read): the wire grammar's
  byte-exact close. THE W1 INFERENCE = CORRECTED: NONE of the 7 readers touches
  the bit reader - the readers consume a BYTE CURSOR (the vtable REP-MOVSB + the
  bounds count): c650 = the u-varint -> u8/16/32/64 (LEB128, 7 bits/byte, <=10
  bytes); c710 = the zigzag -> i32/i64; c7a0/c7d0 = the RAW f32/f64 (32/64 bits
  exactly); c800 = the len-varint + bytes; c8b0 = the len-varint + string + NUL;
  c950 = the bounded sub-stream + the recursive walk. THE HARDCODED BIT WIDTHS =
  the image-wide census (466 sites): 1-14,16,18,20,25,30,31,32 (104 sites) for
  the bit reader + 2,6,8,9,16,64 for the varint - including THE 5-BIT STATUS =
  22 MOV-EDX-0x5 sites in the 1416D-1417D REPLICATION-CODEC cluster (the S2
  carrier region, the sobject_message consumers) + THE 32-BIT VALUE = the
  dominant width (104 sites). THE WALKER PAIR (b9200/bf920) = the encode/decode
  = BYTE-IDENTICAL except the callback table (94F38 vs 94DC0). THE OPEN: the 75
  dynamic-width sites + the 4 callback tables' contents + the cursor/bit-window
  handoff.

## 10.52 W6 LANDS: THE DOMAIN WAVE CLOSES (2026-08-16 ~21:2x)

- THE DELIVERABLE (claims/cuid-residue.md, full read): the 13 pair-decompiles
  landed (the 3-arg fix) - the pair-functions = THE INVENTORY-SLOT MACHINERY
  (the 62-slot x 0xA8 pending table + the 0x28B0 bitmap, the 0xA0-stride
  dispatch table, the 0x40-bucket hash64 lookup at *DAT_142439c70, the
  0x40-byte catalogs, the vtable-getter iterators). THE 0x19 NODES = the /GS
  cookie records (the cookie RSP-offsets verified 3/3: 0x30/0x360/0x420). THE
  .PDATA = the whole-block {S,D,E} family: 66,218/66,218 records pass the
  corrected predicate (the block = the directory; the registry = the interior
  record index 24,576; the D-field = the descriptor-node tails). THE CLUSTER =
  the verb-library's metadata stream; THE CHAINS = the linked code-range lists
  (the heads 0x141DC171C/0x141DC2FA8). THE OPEN = only the {len,hi,lo} units +
  the VMP handler map - the domain's last untranslated glyph, and explicitly
  optional. THE WAVE = COMPLETE: W1 (the wire grammar), W1b (the byte-exact
  widths), W2 (the blob keys), W3 (the Havok correction + the encoded wc
  pointer), W4 (the partial naming), W5 (the cold band), W6 (the residue) -
  the static domain = closed. THE ACTIVE THREADS: the marker boot (the user's)
  + the recap-flag lane.

## 10.53 THE MARKER BOOT: A DECISIVE NEGATIVE (2026-08-16 ~21:3x)

- THE 2D MARKER TEST (the encoder's 0x6D6D6D01..09 sweep at the packed 0x2EFC..
  0x2F1C grid) = NOTHING LANDED: the sync_header = still {0,0,0,FFFFFFFF x4,0}
  (the byte-identical header; the 12,312 all-pass flag tests; the 2100 = still
  dead; the swap/ability = the same rollback). THE CONCLUSION: the fork's packed
  offsets do NOT map to the client's store offsets by a simple delta - the
  client's schema walk REORDERS the wire fields into its own store layout, so
  the 2dmap lane's packed-offset arithmetic = the wrong method (the
  landmarks matched by coincidence or the region = misidentified). THE NEXT
  PROBE = the whole-record padding sweep: the offset-encoded markers in EVERY
  padding region (the 0x6D6D0000|offset form) - the one marker that appears in
  the sync_header = the source field, or NONE = the field is ABSENT from the
  fork's record entirely (the live header's 0xFF run = something the fork never
  publishes - the layout's missing-field question).

## 10.54 THE PADDING SWEEP: THE 2D ENCODER ROUTE = DEAD (2026-08-16 ~21:4x)

- THE SECOND DECISIVE NEGATIVE: the padding-wide marker sweep (the 12 offset-
  encoded markers 0x6D6D0000|0x0B00..0x0BB0 across every padding field) = NOTHING
  landed - the sync_header = STILL {0,0,0,FFFFFFFF x4,0} (the byte-identical
  header; the 12,312 all-pass tests; the 2100 = dead; the swap/ability = the
  same rollback). THE KILLER DETAIL: the first marker build's 0x2EFC-grid sweep
  OVERWROTE the fork's own seenMessages 0xFF fill - and the header's 0xFF run
  (@h3..h6) = UNCHANGED. The 0xFFs = the client's OWN default fill, not the
  fork's bytes. NO family-4 field feeds the +0x2F00 block.
- THE VERDICT: the 2dmap lane's 'server-fed, not client-computed' = REFUTED by
  the two measured tests - the +0x2F00 block = the CLIENT-SIDE schema-default
  fill (the {0,0,0,F,F,F,F,0} = the unresolved-tag shape the walker
  FUN_140372a60 writes). THE 2D ENCODER ROUTE = DEAD (no fork field maps to the
  block). THE SLOTS RESOLVE ONLY via the client's own definition catalog
  (cuib's branch (a): a live definition for the target tag) - THE NEXT
  QUESTION = why the 0x80802C36/0x80802C35 tags fail to resolve in the offline
  build (the runtime registry's entries = the client's static content + the
  boot registrar FUN_1418326a0; the candidates: the served content domains'
  completeness vs the client's boot-time registration gates).

## 10.55 THE ESCALATION FILED (2026-08-16 ~18:3x)

- THE PACKAGE: 'STATE-inventory + incident 20260816_182810 +
  claims/escalation-2026-08-16-definition-resolution.md' (the self-contained
  escalation file: the one-paragraph state, the evidence chain, the question =
  the next-move ranking for the definition-resolution failure - the
  tag-resolution lane vs the live registry dump vs the DLL-patch route
  re-opened). THE TRIGGER: the two marker boots refuted the 2d encoder route
  (the +0x2F00 slots = the client's own default fill); the swap/ability = the
  same rollback with the gate fully open; the user's call = escalate.

## 10.56 THE TAG-IDENTITY CHECK: THE REVIEWER'S THEORY = REFUTED (2026-08-16 ~22:0x)

- THE RULING'S ZERO-BOOT STEP RAN: class_scan.py 80802C36,80802C35 over all 508
  package families (2,801,092 entries) = ZERO entries for either class. The
  reviewer's prediction (the classes = the 0x58C investment-globals content
  classes) = REFUTED at the first check: the two tags exist in NO package's
  entry table. THE IMPLICATION: the tags = the client's INTERNAL definition
  indexes (the schema field descriptors' references into the image's own static
  definition data - the descriptor records at 0x142062CC8+ + the boot registrar
  FUN_1418326a0's cells), NOT the package content classes. THE
  'definition-catalog route' via the served content = DEAD (the packages don't
  carry the classes; the definitions = the client's compiled-in data).
- THE NEXT (the ruling's #2, now the primary): THE LIVE REGISTRY DUMP - read the
  runtime cells (0x14322E530+/0x1432367B0+) + the loop-table targets at boot
  (the wc getter-call fix from W3 for the encoded pointer) - which entries the
  registrar filled vs which the walker needs. THE QUESTION SHARPENED: the
  resolution's inputs = the client's own image data - the failure = either the
  boot-time registration's incompleteness (a gate the server can open) or the
  resolver's expectations (the content-table id / the package rows / the
  registration state). The DLL-patch route = the ruling's fallback if the dump
  names an unfixable client-side gap.

## 10.57 THE LIVE REGISTRY DUMP: THE DEFINITIONS RESOLVE (2026-08-16 ~22:2x)

- THE BOOT'S READ (the ruling's #2, executed live): THE REGISTRY CELLS = FULLY
  FILLED. The 701/702 schema's cell array @+0x32367B0 shows the complete tag run
  WITH the resolved pointers: {0x80807601, ptr, ptr, ptr}, {0x80807600, ...},
  THE FAILING TAGS {0x80802C36, ptr, ptr, ptr} + {0x80802C35, ptr, ptr, ptr}
  (three live heap pointers each), then {0x808075FF...}. The 2100 schema's array
  @+0x322E530 = the same shape (0x808079C9/C8/C7/C6/C5 + the pointers, then
  0x80802CA4). THE REVIEWER'S MISSING-DEFINITION THEORY = DEAD AT THE SECOND
  CHECK: the definitions are present + resolved at runtime - the registrar
  FUN_1418326a0 filled everything. THE LOOP TABLE (0x141F9D238) = the mixed
  real-FNV + kNoDefinitionHash rows with the runtime ptrs (the builder's source).
- THE WALL: the remote-thread getter calls = BLOCKED (the WriteProcessMemory =
  denied - the VMP's write protection; the injection route = dead). The store
  globals (DAT_142808A70 / DAT_14280eda0 / DAT_142808860) = the ENCODED pointer
  family (the raw_hi = 0x446D8326 shared = the same K as the wc's) - the W3
  decode applies but the K dwords = still uncalibrated (the getter-call route =
  blocked, so the calibration = must come from a known (raw, ptr) pair).
- THE REFRAME: the resolution = WORKS; the +0x2F00 header's {0,0,0,FFFFFFFF
  x4,0} = the fork's record not carrying the right bytes at the right positions
  (the mapping question, NOT the missing definitions). THE NEXT = the static
  character-record schema walk (the W1 grammar -> the field order -> the store
  offsets -> the fork's packed mapping) OR the K-calibration route.

## 10.58 THE NEXT-STEPS EXECUTION (2026-08-16 ~22:5x)

- THE STATE = REFRESHED (the header + the item 20: the definition wall renamed =
  the RECORD MAPPING). THE DEFINITION LANE = STANDS DOWN (its premise - the
  missing definitions - = refuted at the two checks; the negative = recorded in
  10.56/10.57 + the evidence folded into the mapping lane's brief). THE BOARD:
  mapchar (the schema walk = the primary, in flight), kcal (the K-calibration
  fallback, spawned), updiff (the upstream diff-review, in flight), census031
  (the 0.3.1 socket/plug census - the package_socket_plug_build.cpp = the
  possibly-shortcutting new file, spawned), recap (in flight). FIVE LANES LIVE.

## 10.59 THE RECAP LANE LANDS: THE TRIGGER ISN'T STATICALLY NAMEABLE (2026-08-16 ~23:0x)

- THE DELIVERABLE (claims/recap-flag.md, full read): the complete censuses - the
  two bank getters (11+18 call sites), the gate/apply family, the flag writer
  FUN_140555ec0, the item-service testers - ZERO CMP idx,imm exists anywhere:
  every flag-bank access = runtime-indexed. The recap's gate = the VMP'd
  bootflow/presentation handlers (the 'cinematic_with_roster' state = zero
  references of any form, the VMP pattern) + possibly the content-resolved flag
  hash. THE DISABLE RECIPE: the trigger index in the UNCOVERED set {0..25} u
  {11156..12299} (1,170 indices), the SUPPRESSING VALUE = 0 - resolved by the
  ~11-boot BISECTION or the CONTENT-SIDE route (the cinematic metadata classes
  0x808036F6/0x80808F49 -> the flag-hash -> the index, the tag-hash
  methodology, ZERO boots). The gate-tolerance risk: one 0 row among 12,299 =
  the gate's one fail per walk - unverified whether it reopens the rollback.

## 10.60 THE 0.3.1 CENSUS LANDS: NO SHORTCUT, ONE METHOD GOLD NUGGET (2026-08-16 ~23:1x)

- THE DELIVERABLE (claims/upstream-031-census.md, full read): the upstream's
  socket/plug machinery = the build_data catalog + the account-state staging
  (NO character-record wire fields); the fork's encoder = already emits the
  full socket surface (the byte-identical instance encoder + the
  OrdinarySocketBlock). THE +0x2F00 BAND = byte-identical opaque padding in
  both trees - NO SHORTCUT for the mapping lane exists in 0.3.1. THE
  TRANSFERABLE ARTIFACTS: the upstream's new record fields (the per-row
  mutationSerial +20 / flags +24, the rising nextInventorySerial, the
  profile-row instanceSoid/mutationSerial) = the future port candidates; the
  InventoryChangeList decode at 0x2BFC-0x2CBF = the PROOF that the padding-
  region trial-decodes = the upstream's own method (the same approach as our
  marker probes). THE PORT = a WIDE one (the 0.2->0.3.1 state-model migration
  drags the whole dependency chain - not a cherry-pick).

## 10.61 THE DIFF-REVIEW LANDS: THE CATCH-UP = NATIVE GIT (2026-08-16 ~23:2x)

- THE DELIVERABLE (claims/upstream-diff-review.md, full read): THE TREES SHARE
  ANCESTRY at b12a9da (the fork = the upstream + 14 fork commits; the fork repo
  holds all 68 upstream commits - the catch-up = NATIVE cherry-picks/3-way
  merges, not the file copying). THE CENSUS: 346 upstream files (318 clean
  ports), 28 true conflicts = exactly the fork's server core (the encoders, the
  BAP files, web_service_runtime, the state runtimes). THE PORT PLAN (ranked):
  (1) the small client fixes + the pattern registry + the logging helpers
  (cherry-picks, zero risk); (2) the custom package loading (the trust bypass -
  KEEP the fork's IV fix, they're complementary); (3) the movement/QoL hooks;
  (4) THE INVENTORY/VENDOR STACK = the phased manual port (the settings v6
  chain -> the catalogs -> the codecs -> THE STATE-MODEL DECISION (adopt the
  upstream's character.inventory, replace the fork's storageItems - the sqlite
  migration) -> the runtimes); (5) DEFER the gameplay channel (the in-process
  host vs the standalone server decision); (6) the optional Linux/Wine. THE
  SKIPS: the standalone silver/dismantle/bcrypt fixes (the pieces of (4)). THE
  CONVERGENT-IMPLEMENTATION FINDING: the fork's storageItems + the upstream's
  character.inventory = the SAME family-4 concept, incompatible models.

## 10.62 THE MAPPING LANDS: THE 2D ARC = A PHANTOM, THE FORK = CORRECT (2026-08-16 ~23:4x)

- THE DELIVERABLE (claims/character-record-mapping.md, full read): THE ANSWER =
  NO wire field maps to the store +0x2F00 (the block = the client's own
  schema-default fill - the 0xFFs = the 'all messages seen' default), AND the
  fork's packed record ALREADY publishes the exact expected bytes at the exact
  expected positions: the store 0x2F00..0x2F1F = {0,0,0,FFFFFFFF x4,0} =
  byte-identical to the fork's OWN bytes at the SAME offsets (the gateSeenPadding
  tail zeros + the seenMessages 0xFF @0x2F0C + the seenRosterPadding zeros).
  THE FORK OFFSET(S) TO WRITE = NONE. THE TWO EARLIER SLIPS = corrected:
  seenMessages = 0x2F0C (NOT 0x2F1C - that's its END), acquiredFlags = 0x9348
  (NOT 0x9358) - with the corrections, the fork's layout = the client's at ALL
  the anchors (0x2CC0/0x43F0/0x4C18/0x9348) = the mapping = IDENTITY (no
  reordering, no deltas). THE 2D ARC = A PHANTOM built on the two offset slips.
- THE REAL REFRAME: the +0x2F00 header = the SEEN-MESSAGE BITS + the padding,
  NOT the definition slots - the definition-hash fields = the walker's
  tag-resolution fields = INSIDE the 701/702 payload blocks, not the header.
  THE REMAINING OPEN: the 0x2EB8/0x2EE0 anchors (the summary/gate region =
  possibly a few bytes off) + the tail (the client's 0xB74C anchor vs the
  fork's contentBypass @0xB500 + 591-byte tail = likely wrong) - the two
  candidate positions for the real definition slots.

## 10.63 THE UPSTREAM MERGE: WAVES 1-3 LANDED (2026-08-16 ~23:5x)

- THE MERGE EXECUTED on inventory/snapshot-generalization (the working branch):
  the upstream remote added (stanuwu/Sunrise) + fetched (the 0.2.1/0.3.1 tags);
  the safety tag pre-upstream-031. WAVE 1 = LANDED: the assert-handler setter, the
  language fallback (the 03dd623's kLanguageCapacity dependency = ported into
  steam/definition.h + the field moved to the Settings struct per the upstream),
  the settings cap 1 MiB (the fork's 64 KiB superseded), the anchored pattern
  scans, the CMake file, the boot timing. THE SKIPS (recorded): d4a0cd7 (a merge
  commit), e653bea/fb7b948 (the empty picks - already in the tree), 0633790 (the
  hex-log helper - the 10-file server-core conflict, cost > value). WAVE 2 =
  LANDED: the custom package loading (the trust bypass; the fork's IV fix =
  untouched). WAVE 3 = LANDED: the horizontal noclip, the sword-skate, the
  noclip-collision fix + the fly - all with the both-keep wiring resolutions.
  THE SKIP: e392074 (the inf ammo) = the movement/player UI re-architecture
  conflict (the upstream's post-fold module structure vs our teleport/sword-skate
  pages) - the hook files = banked for a manual wiring later. THE BUILD = IN
  FLIGHT (the background gate).
- THE STATE: the fork's working branch = now the upstream-merged tree; the daily
  deployed stack = UNTOUCHED (no deploy without the user's call). THE NEXT: the
  build gate -> the harness -> the deploy decision (the user's).

## 10.64 THE MERGE STATE + THE FILE-LOCK INCIDENT + THE RECAP VERDICT (2026-08-16 ~23:5x)

- THE MERGE (the state at the pause): the waves 1-3 = COMMITTED on
  inventory/snapshot-generalization (the fixes, the package loading, the
  noclip/sword-skate/fly - FINDINGS 10.63); the e392074 (the inf ammo) = skipped
  (the UI re-architecture conflict). THE BUILD = FAILS on the 7 files whose
  conflict markers = got COMMITTED with the 233c811's --continue (the
  teleport_panel.cpp, the teleport_settings_store.{h,cpp}, the teleport_move.cpp,
  the noclip/runtime.h, the horizontal_noclip.cpp, the sword_skate.cpp). THE
  RESOLVER = RE_build/Sunrise-fork-inventory/strip_markers.py (the both-keep
  marker-stripper, written + ready). THE BLOCKER = the teleport_panel.cpp =
  held by an INVISIBLE process (the PermissionError on every open/rename - no
  matching process name; the mystery-handle class, the same as the earlier DLL
  lock). THE FIX = THE REBOOT (the user's).
- THE RECAP CONTENT-ROUTE = LANDED (the derived-state verdict): the exact index
  = NOT nameable from the content - the recap's surface = the ACCOMPLISHMENTS
  node (the FNV-1 hash 0x2C3700EB) in the character-select presentation graph,
  but no {condition-hash -> bank-index} tables exist in the packages (the
  cinematic metadata = unconditional; the investment-globals tables =
  hash-keyed/type-fielded). THE BISECTION BOOT = the sole route (the
  recap-flag.md recipe stands; the index in {0..25} u {11156..12299}, the
  suppressing value = 0).
- THE POST-REBOOT RESUME: (1) the server restart (the s1_accept); (2) the
  strip_markers.py run + the build gate (the merge's completion); (3) the
  recap deliverable's end-to-end read + the record (the rule); (4) the
  remaining lanes = the recapc landed - the board = the merge completion +
  the recap's bisection decision (the user's).

## 10.65 THE POST-REBOOT MERGE COMPLETION (2026-08-16 ~23:5x)

- THE REBOOT cleared the mystery handle. THE MARKER CLEANUP: the both-keep
  stripper = the wrong policy (the conflicts = the renames, not the additions);
  the final resolution = the REVERT of the 233c811's damaged files (the 7 +
  the wiring + the teleport lifecycle) to the pre-233c811 state - the fly +
  the noclip-collision fix = DEFERRED (the orphaned fly files stay committed;
  the proper manual merge = the later work). THE BUILD = CLEAN (exit 0). THE
  MERGE STATE: the waves 1-2 = landed + working (the fixes, the language
  fallback, the settings cap, the anchored scans, the package loading);
  the wave-3 = partial (the horizontal noclip + the sword-skate = in; the
  fly + the inf ammo = deferred). THE SERVER = restarted (the s1_accept).
- THE RESUME CHECKLIST: (1) the recap deliverable's end-to-end read + the
  record; (2) the remaining deferred merge items = the fly + the inf ammo +
  the inventory stack (the wave-4 manual phase) = the later work.

## 10.66 THE RECAP CONTENT-ROUTE = READ + RECORDED (2026-08-16 ~23:5x)

- THE DELIVERABLE (claims/recap-content-route.md, full read): THE DERIVED-STATE
  VERDICT. The content route delivered the GRAPH (the recap's surface = the
  ACCOMPLISHMENTS node, the FNV-1 hash 0x2C3700EB, in the character-select
  presentation graph - the w64_ui_01e3 screens -> the node tables -> the
  widgets -> the 0x8080496A state machines; the cinematic metadata = 41,753
  rows fully parsed + UNCONDITIONAL) but the DEFINITIVE NEGATIVE: no
  {condition-hash -> bank-index} table exists in the packages (the
  investment-globals classes = hash-keyed/type-fielded, no bank geometry).
  THE INDEX = NOT NAMEABLE FROM THE CONTENT - the bisection boot = the SOLE
  route (the ~11 boots; the index in {0..25} u {11156..12299}, the
  suppressing value = 0). The open items: the type-attribution (the decoded
  corpus = on disk), the live registry dump, the accomplishments node's
  session handler.
