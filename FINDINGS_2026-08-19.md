# FINDINGS — 2026-08-19

## 12.1 THE PANEL-TRIGGER LANE LANDS + THE BOOT-K CENSUS + THE HOT-PATH TRAP
##        (2026-08-19 ~09:37)

- THE LANE (lane_panel_trigger.md, read end to end, the load-bearing claims
  spot-checked against the raw): THE VERDICT = the equip's apply dead-ends
  before any panel trigger, AND the trigger is named. The client has TWO
  STATICALLY DISJOINT delivery paths: path A = the BAP message queue
  (FUN_1416e6ed0's case 0x12 → FUN_1416f1320 "simulation_queue_bap_message_
  queue_response_event_apply" → FUN_140e051b0 the store commit — no walker,
  no notify) and path B = the WorldServer wire (FUN_140e010c0 → FUN_140e0a0b0
  → FUN_140e01960 the wire reader → the walker → the kind dispatchers → the
  stamp chain → the CUI-region panel readers). THE NAMED TRIGGER =
  FUN_140a43300 (the stamp-table dispatcher) + the trio (FUN_140bdd800 the
  record key + FUN_140c93ec0 the marker banks + FUN_140be2cb0(ctx,2) the
  64-entry changed-bitmap notify). THE KIND = the row index (0..5) of the
  per-family 6-row schema table (store + (family+0x251A)*0x60) matched by the
  frame object's definition hash (FUN_140e002d0) — family-4 kind-0 = the
  profile diff-apply (FUN_140e078f0), kind-1 = the stamp compare
  (FUN_140e07ba0). M1 = FUN_140e83510 = the changed-content list builder + the
  per-item vt+0x38 notifier (a registry-driven cycle callback, NOT the
  trigger). M2 = the ui_refresh chain delivers into the DAT_1426edce8 4-slot
  changed-table → the CUI-region readers (FUN_1404cbbc0/FUN_14172ee30/
  FUN_1416b2660-family). THE OPEN ITEMS: the object→kind runtime mapping, the
  boot-A channel question, the two runtime vtables.
- THE BOOT-K (the channel census — two new observers, the ws_wire 0xE01960 +
  the bap_apply 0x16F1320, commit 6037db4): THE ANSWER = the equip's frames
  DO take the WS WIRE — the t=164078 cluster (the equip's client-era) shows
  the ws_wire calls with r9=0x4/0x1/0x3/0x2 (the four families) through the
  wire reader. THE LANE'S BAP-CHANNEL INFERENCE = REFUTED: the equip = on
  the wire; the fix-forward = NOT the delivery channel but the walker's kind
  classification (the equip's objects = not taking the compare kind — the
  next named question).
- THE TRAP (the user's call, confirmed by the evidence): the ws_wire hook =
  a HOT-PATH DETOUR — the per-frame wire reader's entry, the one the join's
  refresh must traverse inside a tight timing window. THE BOOT-K'S
  REGRESSION: the join's repush = MISSING from the server log (the only
  repush = the subscription's t=34586672) → the fix-B arm never fired → the
  normal_z_leg transition never appeared → the tower black-screened (the
  boot-G's shape) → the user's 801 released it (the t=34721109). THE SHIP =
  the intermittent flake family (the boot-G/I pattern), secondary. THE
  LESSON (the trap register's new entry): never hook a per-frame hot path
  with a logging observer — the census's channel question was answered, but
  the hook's timing perturbation re-triggered the tower stall.
- THE PARKED STATE: (1) the ws_wire hook = to be STRIPPED (the batch back to
  13, keeping the bap_apply — the cool path, 51 calls/session) — the BOOT L
  = the tower-load re-observation; (2) the equip's kind question (why the
  wire-path frames don't take the compare kind) = the next lane, with a
  cool-path hook on the walker's kind lookup instead; (3) the opcode session
  = the user's parallel work (the FINDINGS 11.x's opcode/bap vocabulary =
  its starting bank); (4) the veteran hunt = the standing next boot's rider
  (the fold's halving), untouched by all of this.

NOTE (12.2-12.7 added 2026-08-19 ~10:4x, the pen-fix pass): entries 12.2-12.7
were MISSING — STATE cited them but the file carried only 12.1. Reconstructed
from the four lane deliverables (read end to end) + the opencode session DB
(session_v2 time_created + session_message seq/time + the claims-file mtimes).
Newest-timestamp rule: the deliverable file mtimes are the landing times.

## 12.2 LANE B LANDS: THE OPCODE-SWITCH HYPOTHESIS DISPROVEN (2026-08-19 ~00:31)
##        (deliverable = RE_output/claims/opcode-decoder-map.md, mtime ~00:31:36;
##         lane session ses_fe723c257ffeyEbrQ1RZQkrRUz 00:11:22 -> 01:18:31 — the
##         "landed silent" report pattern; parent = ses_fea0881b3ffeXbPpQ9OEFCUOfD)

- THE LANE MISSION: decompile FUN_140DFEC50 (the supposed "only place a u16
  web-service opcode compare can live") and produce the opcode → client-handler
  map. Method: exported-corpus triage (phases 3–13 exist — MORE than the brief's
  3–6) then ONE PyGhidra pass for the gaps. Raw:
  RE_output/content/opcode_decoder/.
- THE VERDICT (claimed + evidence-cited in the deliverable): **FUN_140DFEC50
  contains NO u16 web-service opcode switch/table/compare.** It is the
  queuez-response-envelope processor, entirely vtable-driven: (1) response/
  request-TYPE table lookup FUN_14106F870 (the BAP message-type space 0..307,
  table 0x141FCF6E0) — NOT the web-service opcode space; (2) decoder-key getter
  → the FUN_140E74F80 decoder factory; (3) decoder+0x28 starts the decode;
  (4) handler+0x30 has-decoder + handler+0x20 consume/validate gate; (5) inserts
  a completion record into the queuez pending ring (param_1+0x130, ring indices
  +0x124/+0x128/+0x120), copying the 17-u64 soid block + seq + timestamp;
  (6) failure → FUN_140E07710 fail-pending; (7) FUN_140DFE380 init-once + the
  completion notify. **CONSEQUENCE: client dispatch for web-service verbs =
  (a) BAP-type vtable dispatch (type 11 = c->ws rsp → record R19) plus (b)
  per-request txnId/callback correlation — NOT an opcode switch. The opcode
  semantics live in the per-opcode RESPONSE DECODERS.** Evidence: the body
  verbatim in the corpus (phase7/decompiles.txt ll. 278-281, 419-427, 436-507,
  624, 690), cross-checked against phase6/func_xrefs.txt ll. 21-24 (the two
  body calls target FUN_140E74F80 — type-driven, not opcode-driven). Confidence
  high (the only immediates in the body are log-channel hashes).
- THE OPEN REQUEST (carried into lane C's cross-validation): the CONSUMER of the
  0x80528 response sim-queue message (type-11 record R19's vtable methods,
  0x141C3D2A8 — only qword[0] = 0x140E0DC10 known).
- THE LANDED-SILENT PATTERN (process note): the deliverable file landed ~00:31;
  the lane session ran to ~01:18 before the report — check disk FIRST, not the
  session's silence, for delivery state.

## 12.3 LANE D LANDS: THE VENDOR-VERB INFERENCE — THE 1200-1399 BAND HAS NO EMITTER
##        (2026-08-19 ~01:10-01:13)
##        (deliverable = RE_output/claims/vendor-verb-inference.md, mtime
##         ~01:13:33; report-back ~01:10 per the lane's own header; lane session
##         ses_fe723c255ffejEmCvURXfZj20X 00:11:22 -> 01:17:27)

- THE MISSION: content-side verb inference (zero boots, zero code) — does the
  Arrivals build have a storefront wire surface, and which opcodes carry it?
  Method: pure content analysis; sources cited per row; raw evidence in
  RE_output/content/vendor_verbs/.
- THE VERDICT: **the storefront DOES have a wire surface, and it is NOT the
  1200-1399 band** — that whole band has NO emitter in this build (the 38-emitter
  census, whole-image; zero immediates for 701/1820/1901/904/801-804/1200-1399 in
  the 13-phase corpus; zero result-enum xrefs). The verbs this build CAN emit for
  the storefront = the ALREADY-MAPPED 1820/904/1901/801-804/403/601/701/702 plus
  the BAP purchased-offers (types 21/22).
- THE NEW CONTENT: the 24-entry `_result_` transaction enum at 0x141BDCD70-0x
  141BDD051 (index 0 failure … 1 success … 9 unsellable … 13 selling_inhibited …
  19 buying_inhibited … 23 wrapped_item_already_purchased), the per-vendor
  inhibitBuying/inhibitSelling gates (61/39 open) + returnWithVendorRequest (37
  vendors = the round-trip flag), the 1,576 buyable / 4,102 priced rows across 42
  vendors, the 5,146-item collections reacquire set, the interactions enum
  (QuestAccept/Continue/Complete/RankUpReward/TokenTurnIn …).
- THE RANKED BOOT LIST (the deliverable the next boot consumes): P0-1 Collections
  reacquire (watch svc-10 1820) → P0-2 Collections batch (904) → P1-1 VENDOR OPEN
  (the #1 unknown: does opening a storefront round-trip; watch 1301→1302→1307/
  1309/1310→206; **the fork already answers every shape with StatusResponse{0,
  INT32_MIN} — a POSITIVE result is FREE server-side; a NEGATIVE (no svc-10) =
  the storefront data is 100% local = settles GAP-4 of s1-datagen-spec**) → P1-2
  vendor BUY (deferred — needs currency seeding) → P1-3 SELL → P2-x rank-up/
  quest/records → P3-x roll/umbral (Trials/seasonal gates). Every experiment is
  designed so a NEGATIVE is decisive.

## 12.4 LANE C LANDS: THE 237 EMPTY DECODER SLOTS = STRUCTURALLY IMPOSSIBLE
##        (2026-08-19 ~01:42; the respawn extension ~01:4x)
##        (deliverable = RE_output/claims/decoder-registry-fill-order.md, mtime
##         ~01:42:41; original lane ses_fe723c256ffev7cfJ1CROsLsvT 00:11:22 ->
##         01:25:08 = death-at-report-back; respawn ses_fe6de1946ffeDxD23GUio2VfZN
##         01:27:27 -> 01:45:41, report at ~01:45:41)

- THE MISSION: the feature→decoder fill-order map for DAT_14280E3E0 (308 qwords,
  message types 0..307) — decoder-runbook's "lazy population" premise.
- THE VERDICT (VERIFIED on disk, refs-pass + bijection + 6-dump cross-check): the
  registry's fill order is NOT feature-driven. **FUN_140E75790 = the ONLY writer
  of DAT_14280E3E0 (all 6 image refs: 4 inside it, 2 in the FUN_140E74F80 accessor
  thunk) runs at static-init and writes EXACTLY 71 decoder objects from the static
  71-entry table DAT_141fbef60 (`for i in 0..0x47: registry[table[i]->method0()] =
  table[i]`, body verbatim). The 71 live types == the 71 table entries ==
  byte-identical across all 6 dumps (orbit/destination/logout pre+post/acceptance/
  S1-live, 2 distinct boots). THE 237 EMPTY SLOTS ARE UNREACHABLE IN THIS BUILD —
  no factory entry exists for them, so NO feature state (vendor shelf, collections,
  quests, triumphs, clan, matchmaking, raid) can populate them.** "feature X needs
  decoder Y" is dead as a hypothesis class. The 3 known orderings: registration
  (table order, complete at static init) vs object placement (pool address order
  0x141FBF1C0..0x141FBF500) vs type-numeric.
- THE CROSS-MAP + NEVER-INSTANTIABLE TABLE: every feature maps onto the 71 already
  live at orbit; the never-list {0-5 (0 = deliberate null), 52-99 (activity-
  message sub-enum via DAT_141FBCB60 inside 8/9), 103-249 sparse, 170 (xetrov
  server push — no factory entry), 252-299}. Type 3 (goodbye) and type 170 =
  structurally impossible even with a real server.
- THE RESPAWN EXTENSION (§10-§13): every load-bearing claim RE-VERIFIED
  independently from disk (laneC_verify_derive.py — the 71/237 split, the
  populator body verbatim, the refs pass, the table join, the crossmap — all
  survive unchanged); §11 = three dump-family EXCLUSIONS (live_registry2.json /
  live_read_registry.json / lane_panel_trigger_raw*.txt are NOT decoder-registry
  material — do not re-chase); §12 = the B-lane fold-in (type 11 → decoder
  0x141FBF1C8 → vtable 0x141C2DED8 ↔ R19 0x141C3D2A8, sharing FUN_140E0DC10);
  §13 = the respawn runbook (5 confirmation states, ~45 min, expected NEW = []);
  the static71_vtables.json = the full per-decoder vtable for all 71 WITHOUT a
  live read (the kind lane's static anchor).

## 12.5 LANE E LANDS: THE UPSTREAM CODEC DIFF — 9 WEB-SERVICE OPCODES NAMED, ALL
##        CODEC-PORTABLE (2026-08-19 ~02:03)
##        (deliverable = RE_output/claims/upstream-opcode-codec-diff.md, mtime
##         ~02:03:04; resume session ses_fe6c39a59ffe4nvNCLAaGYQVo4 01:56:23 ->
##         02:03:48; the 3 failed spawns 00:11/00:25/00:47 = the process
##         correction's subject, 12.6)

- THE MISSION: which upstream (Sunrise-master 0.3.1/0.3.2) web-service codecs the
  fork lacks, what they parse, whether our client sends them, port cost. Trees:
  community_forks/Sunrise-master/Sunrise vs our fork (integration branch on disk).
- THE VERDICT: **9 opcodes have upstream codec files we lack — 402 (Dismantle),
  403 (Equip), 404 (Unequip, shared codec), 406 (lock/favorite), 801 (subclass
  socket, VERIFIED ×6 in our boot log), 901 (vendor purchase with the Sunrise
  clock rule), 903 (socket-plug), 1820 (Collections), 1901 (shader apply)** —
  8 file families. All 9 compile against the fork's middleware deps (bit_reader/
  byte_order/envelope signature-identical, parse-only bool+Request) with ZERO code
  changes. Response shapes ALREADY correct (opcode_routes.cpp content-identical;
  MD5 differs only LF↔CRLF). 2/9 VERIFIED sent by OUR client (403 ×6 + 801 ×6,
  payloads byte-exact vs the canonical shapes in merged_20260818_220630.log); the
  other 7 INFERRED (boot-time UI verbs, absent from captures).
- THE MISSING PIECE per opcode = the consume() request-decoding branch + any state
  effect (the actions layer = upstream's prepare_*/Pending* + dormant catalogs,
  NOT portable — the fork lacks the transaction subsystem). Dispatch map recorded:
  504→select, 402→dismantle, 403/404→equip/unequip, 801→subclass, 903→socket-plug,
  1901→shader, 406→item-state, 1820→acquire, 901→refuse. Shared-codec caution: the
  fork's S2 edits live in family5/601/505/503/501/205/206 — never overwrite
  wholesale (3-way merge per file). 701/702 (the top boot opcodes) = unnamed on
  BOTH trees; the 1100/1200 bands + 904/905 = shape-only on both sides.
- HONEST SCOPE (per lane E's own §4): "Compiles with zero changes" = signature-
  level check, not a build; the 7 unobserved verbs = INFERRED not verified; only
  the response-shape tables were content-compared this run.

## 12.6 THE PROCESS CORRECTION: PROVIDER ERRORS ARE NOT A RUN SIGNAL
##        (2026-08-19 ~00:11-01:56, the lane-E spawn saga; recorded 2026-08-19
##         ~10:4x as the pen fix — BINDING)

- THE OBSERVED FAILURE MODE: lane E was spawned 4 times — attempt 1 at ~00:11
  (ses_fe723c254ffe9vD2CsbO29cIbC, 00:11:22 -> 00:22:01), attempt 2 at ~00:25
  (ses_fe716894dffelCpsjDatyC535o, 00:25:48 -> 00:46:00), attempt 3 at ~00:47
  (ses_fe702770effesT4xQyBnDwVD09, 00:47:44 -> 01:32:08), then the RESUME at
  ~01:56 (ses_fe6c39a59ffe4nvNCLAaGYQVo4, 01:56:23 -> 02:03:48 = the landed one).
  The first three = provider errors that looked like run failures — but the 
  deliverable phases (phase1 tree inventory ~10:0x, phase1-inventory ~14:05,
  phase2-port-assessment ~14:20 — all still on disk) and the opencode.db
  session_message rows show the work DID progress across the respawns.
- THE CORRECTION (binding): **provider errors are NOT a reliable run signal — on
  ANY errored lane, check the disk + opencode.db session_message BEFORE
  re-spawning any errored lane.** The deletion/restart churn cost the opcode
  session a real run. Recovery pattern: verify the phase files' mtimes + the
  session_message seq/times first; re-spawn ONLY when both show no progress.

## 12.7 THE NEXT STEPS (the opcode front's forward lane, recorded 2026-08-19
##        ~10:4x): THE CODEC MERGE + THE P1-1 VENDOR-OPEN BOOT

- THE CODEC MERGE (the 2026-08-19-era plan): the 16 files (8 codec families,
  3-way) into the integration worktree + the consume() branches per lane E's
  dispatch map. [CORRECTED 2026-08-19 ~10:5x by the reviewer's audit — see 12.8:
  the "merge" itself is ~90% phantom; what remains = the server vcxproj entries
  + the consume() branches + the 403 reconciliation.]
- THE P1-1 VENDOR-OPEN BOOT (lane D's ranked #1): walk to Rahool or Banshee,
  interact, wait 15 s, watch for 1301/1302/1307/1309/1310/206. A POSITIVE
  (storefront round-trip) settles GAP-4 of s1-datagen-spec and makes the
  transaction-subsystem fold-in (the parked actions layer) mandatory; a NEGATIVE
  (no svc-10) = the storefront is 100% local → the fold-in stays scoped to items.
  P1-2 vendor BUY deferred (currency seeding = another variable).

## 12.8 THE REVIEWER'S AUDIT LANDS: LANE E'S "16-FILE CODEC MERGE" = ~90% PHANTOM
##        (2026-08-19 ~10:4x-10:5x, this session's first recording — the handoff's
##         ruling, spot-verified on disk before recording; the correction file = its
##         own entry + a claims-file amendment below)

- THE CLAIM (the handoff's ruling, verified): **the integration worktree has
  carried ALL 9 codec families since the 8/16 upstream 0.3.1 merge — the park
  deleted the ACTIONS layer (web_service_actions.cpp + its prepare_*/Pending*
  surface), NOT the parse-only middleware codecs.** Lane E compared against
  MASTER (which has none) — a worktree error in that lane's framing (its own §4
  already conceded "only the response-shape tables were content-compared"). The
  "16-file codec merge" = HOURS, not a merge project.
- THE DISK PROOF (spot-verified this session): (i) all 9 families on disk in
  src/middleware/web_service/messages/ — opcode402.{h,cpp}, opcode403.{h,cpp},
  opcode406.{h,cpp}, opcode901/{h,cpp}, opcode903.{h,cpp}, opcode1820.{h,cpp},
  opcode1901.{h,cpp}, opcode801.{h,cpp} + opcode206_codec.cpp; (ii) git
  provenance: b1c366d "refactor(protocol): decode every request in middleware"
  (08-16) + 60c3a44 "feat(protocol): handle inventory web-service actions"
  (08-15) + f8e4a67 (08-16) = the upstream chain that landed 402/903/1820/1901;
  opcode801 = OUR port (84acc9c); (iii) the vcxproj nuance: the DLL project
  (Sunrise.vcxproj) compiles ALL of them (132-138, 680); the external-server
  project (sunrise-server.vcxproj) compiles only opcode801_codec.cpp of the 9 +
  the 5 legacy (205/206/501/503/504/505/601) — so "dead on disk" is imprecise:
  the 8 are compiled-but-unwired in the DLL and uncompiled in the external
  server; (iv) the consume() branches in web_service_runtime.cpp = 403-INLINE
  (the u64 BE item soid + u8 flag parse, boot-proven) + 801 (codec parse +
  prepare_subclass_selection) — the other 7 families have NO consume() branch
  anywhere.
- WHAT ACTUALLY REMAINS (the audit's bounded list): (a) the external-server
  vcxproj entries for the 7 new families; (b) the consume() branches following
  lane E's dispatch map (504→select, 402→dismantle, 403/404→equip/unequip, 801→
  subclass, 903→socket-plug, 1901→shader, 406→item-state, 1820→acquire, 901→
  refuse); (c) the 403 reconciliation — keep the boot-proven inline parse (the
  boot K-era behavior) OR codec-adopt behind a byte-exact harness assert; (d)
  the 403/404 shared-codec question (no opcode404 file on disk — 403's codec =
  the equip descriptor).
- THE CLAIMS-FILE AMENDMENT: the correction is appended to
  RE_output/claims/upstream-opcode-codec-diff.md as a STATUS note (the DELIVERABLE
  RULE — the file must carry its own correction, not just this FINDINGS entry).

## 12.9 THE PEN FIXES LAND: THE RECORD REBUILT (2026-08-19 ~10:5x)

- (a) FINDINGS 12.2-12.7 = RECONSTRUCTED (above) — the four lanes' landings (B
  ~00:31, D ~01:10-01:13, C ~01:42, E ~02:03, exact sources: the claims-file
  mtimes + session_v2/session_message) + the process correction (12.6) + the
  next-steps lane (12.7). STATE's citations of "FINDINGS 12.6/12.7" now resolve.
- (b) STATE-inventory's header paragraph = REWRITTEN (was a full day stale — still
  described the pre-BOOT-A family-4 version-discipline worry; the current truth =
  the parked post-boot-K state: the version discipline landed and boot-validated,
  the swap front narrowed to the equip's kind question).
- (c) THE DEPLOYED-STACK RE-HASH: the live stack at this recording = PID 38612
  (RE_output/s1_accept/sunrise-server.exe, started 08-18 23:51) + dcv build/bin/
  x64/steam_api64.dll (08-19 01:39, SHA-256 B2DB2FC5…, the boot-K build carrying
  the ws_wire hook). The STATE deployed-stack section = re-stamped at the BOOT-L
  deploy (a fresh hash, not the 08-18 archive).
- (d) THE 11.8 ORDERING QUIRK = NOTED (see the 08-18 file's 11.8/11.22 note):
  FINDINGS_2026-08-18.md carries an out-of-order "11.8 THE COMMUNITY S3 ATLAS"
  AFTER 11.22 (line 779) — a known append-order quirk; NOT moved (append-only
  discipline).

## 12.10 THE BOOT-L DEPLOY LANDS: THE WS_WIRE STRIP + THE EQHASH GATE LESSON
##        (2026-08-19 ~11:30-11:45)

- THE DEPLOY (the boot-L preflight, on the user's go): (1) the ws_wire strip =
  committed 7ef2453 (the batch 14 → 13, the bap_apply kept) + both projects built
  clean /m:1 + the built DLL verified (stage=ws_wire GONE, stage=bap_apply present).
  (2) The old server (PID 38612, the boot-K build) stopped; the state.db WAL =
  intact (the 09:30 mutations, replay on open — the 11.10-verified path).
  (3) Backups .bak_bootL (the s1 exe, the s1 cache, the dcv DLL) + the new exe
  (38fa5b12, 6,131,200 B) + the new DLL (6ef4ea03, 5,792,768 B) deployed.
  (4) THE DCV-CACHE IDENTITY = KEYED TO destiny2.exe (ts 0x5F43138B), NOT THE DLL —
  the client-side cache needs NO re-stamp on a DLL swap (verified by header-vs-PE
  compare; the game exe never changes between boots).
- THE EQHASH GATE INCIDENT (the failed first boot + the repair — the 11.10 lesson
  replayed with a new cause): the first boot with the pre-stamped ts/size but the
  OLD header eqHash (0x9EF65EB9BD334F2C) = the content_swap fail (all six domains
  cache=0) → the gate compares the FULL identity {ts, size, eqHash} vs {exe ts/
  size, configured_hash(the persisted DB account)} (cache_file_reader.cpp:148).
  THE CAUSE = the header's eqHash was the STALE 08-18 09:36 value — the 09:30
  commit-restamp had silently failed (a warn, not a stop), and the DB's equipment
  changed across boots B-K. THE REPAIR = the EXACT C++ port (bootL_eqhash_exact.py:
  slot-indexed by the equipment_slot enum 0..15, definitionHash u32-truncated,
  level u32, policy = enum 0/1, plugCount = max(lane)+1, per-lane absent/present +
  plug u32; configured_equipment_identity.cpp:88-108) → configured_hash(DB) =
  0x8653F8C71F8154E6 → the eqHash @20 re-stamped (the identity fields sit outside
  the payload checksum) → THE SECOND BOOT = CLEAN (six domains match, build_data
  cache ok, initialize ok; PID 24840 on 443/8099/30975).
- THE LESSON (recorded for the next deploy): the eqHash gate = LIVE and compares
  the DB-account hash — a pre-stamp must carry the CURRENT configured_hash, not the
  header's legacy value; the bootL_eqhash_exact.py dry-run = the boot-cycle gate's
  eqHash arm now (run it before every deploy; the prior dry-run port was stale on
  the slot-indexing + policy-enum semantics).
- THE ACCEPTANCE STACK (the boot-L hashes, incident.py 11:33): s1 exe 38fa5b1269
  1fdf15, dcv DLL 6ef4ea030638cbda, s1 cache bb954964… → post-repair 8653F8C7-stamp,
  s1 settings 238daf75, dcv cache/settings = unchanged (destiny2.exe-keyed).
- THE HANDOFF (the user's tour, pending at the recording): (i) THE GATE = the tower
  re-observation (the join's repush + the slice-set same-instant with in_world, the
  boot-H shape — with the hot-path hook stripped; a stall WITHOUT the hook = a
  second-cause finding, stop the riders); (ii) THE HALVING = POST /restore first=
  1376 last=3933 (the B-side of the kept fold, 777 flags) after the tower gate —
  recap plays → the veteran ∈ the restored half; gone → ∈ the still-suppressed
  A-side (47..1375, 776 flags); (iii) THE CAPTURES = P0-1 Collections reacquire
  (watch 1820) + P0-2 batch (904) + P1-1 VENDOR OPEN (Rahool/Banshee, wait 15 s,
  watch 1301/1302/1307/1309/1310/206; the ws_capture log = live).

## 12.11 THE BOOT-BRIEF AMENDMENT: THE PURPOSE RECAP AT LAUNCH TIME
##        (2026-08-19 ~11:5x, the user's request — BINDING)

The user's ask: after a long solo-iteration stretch, the boot's exact scope gets
fuzzy by launch time. THE AMENDMENT (appended to the standing boot-brief rule):
every boot brief delivered AT LAUNCH TIME (the moment the user starts the game)
carries a plain-language PURPOSE RECAP — three lines, no jargon: (1) THE MAIN
EVENT = the one question this boot exists to answer and what each outcome means;
(2) THE RIDERS = the standing bisections/observations riding along (one line
each); (3) WHAT IT EXPLICITLY DOES NOT TEST. The pre-boot brief (the go/no-go
package) stays as-is; the launch-time recap = the reminder. The boot-L recap
(the user received at launch) = the template: "the trap closure (tower gate), the
veteran halving, the free captures — NOT the kind question, NOT vendor buy, NOT
new server behavior."

## 12.12 THE BOOT-L HARVEST: THE TRAP CLOSES, THE 1820 = VERIFIED LIVE, THE HALVING
##        LANDS (2026-08-19 ~12:0x, the user's tour + the log harvest)

- THE GATE = PASSED (both sides): the user "loaded into the tower just fine, my
  ship came back". THE SERVER EVIDENCE: the queuez repush FIRED (t=662282,
  bytes=8695 — the fix-B arm that went missing in boot-K's trap) + the healthy
  roster/keepalive flow to city_tower_social_d2. THE CLIENT CENSUS = clean: the
  install count=13 (the strip verified LIVE), NO ws_wire stage, char_test 8015
  (the boot-7 shape — the parse gates exact), bap_apply 63 (the cool path,
  ~51/session at boot-K + the extra swaps), resolve 693, stamp_consume 176,
  marker_poll 92, expr_eval 30, ui_refresh 2, stamp_gate 2, rollback_apply 1
  (the boot-time fire only). THE TRAP = CLOSED: the tower loads clean with the
  13-spec census running — the hot-path hook was the stall cause, the rule
  stands (never hook a per-frame hot path with a logging observer).
- THE CAPTURES — THE 1820 = VERIFIED LIVE (P0-1 ×2): the Ghost Primus pull
  (t=851032) + the Helm of the Ace-Defiant pull (t=861907) BOTH fired svc-10
  opcode 1820 with the canonical 3-byte payloads ("92 09 00" / "80 55 00" — the
  1b-present + 15b-collectibleIndex + 8b-pad shape, opcode1820_codec's format;
  the decode = the codec's job, the file's on disk). THE SERVER answered the
  generic statusPair echo (svc=10 rsp=11 result=ok) and pushed NO family-4 mint
  — the item-appearance outcome = the user's report (pending: did the two pulls
  LAND in the inventory? a YES = the client mints locally on the success status
  — the lane-D negative-proof's local-grant path; a NO = the server mint + push
  = the real gap). NO 904 (the batch control untested — the user pulled two
  singles). NO 1301/1302/1307/1309/1310/206: THE VENDOR OPEN = INFEASIBLE — the
  Tower renders NO vendor NPCs in this build (no entity population yet) → P1-1
  = amended to BLOCKED on entity spawning (the lane-D ranked list correction).
- THE SWAP RE-OBSERVATIONS (the user's): the ability swap + the subclass tree
  swap + the subclass swap all fired their verbs on the wire (2100 ×3, 801 ×2,
  403 ×1 — the payloads canonical); the equip's display = unchanged behavior
  (the kind lane owns it).
- THE VETERAN = the same (no recap, as expected — the halving hadn't fired).
  THE HALVING = LANDED (the user's correction adopted): the live /restore can
  never reach a signed-on client (the flag bank = delivered at sign-on) → the
  halving = the DB edit BEFORE the next boot. POST /restore first=1376 last=
  3933 = ok + journaled (12:06:12); the DB verify: the A-side (47..1375) = 776
  still suppressed, the B-side (1376..3933) = 3,111 restored to 2. THE NEXT
  BOOT = the clean test: recap plays → the veteran ∈ 1376..3933 (the restored
  half); stays gone → ∈ 47..1375 (the still-suppressed half).
- THE NEXT STEPS (the post-boot-L map): (1) the CONSUME BRANCHES = eligible now
  — the 1820 has observed traffic; add the 8 codec families to the server
  vcxproj + the parse/trace/strict-refusal branches per lane E's dispatch map
  (LOG-ONLY for the unobserved verbs, no behavior change until the fold-in
  decision); (2) the KIND LANE = the background flash lane, lands soon → the UI
  decision tree (the community question + the 801-vs-equip census diff); (3)
  the FOLD-IN DECISION = now leans on the 1820 evidence + the user's
  item-appearance report (the P1-1 vendor-open arm = blocked on entity
  spawning); (4) the next boot = the halving observation + the P0-2 batch
  (904) if the user wants the batch control tested.
- THE ITEM-APPEARANCE ANSWER (2026-08-19 ~12:2x, the user): the two pulls DID
  NOT land in the inventory → THE CLIENT DOES NOT MINT LOCALLY on the
  success-status echo (the lane-D local-grant path = REFUTED for 1820) → THE
  COLLECTIONS VERB REQUIRES THE SERVER-SIDE MINT + the family-4 push — the
  generic statusPair echo = NOT sufficient. THE FOLD-IN DECISION = SETTLED for
  the items side (the P1-1 vendor arm stays parked on entity spawning): the
  1820 acquire = the first REAL verb implementation — parse the collectible
  index → resolve the collectible item definition (the upstream
  find_collectible_item_definition_index) → mint the item + deliver the
  family-4 push (the 801/403 frame shape, proven live). The upstream actions
  layer (prepare_item_acquisition + Pending*) = the ADAPT checklist, scoped to
  items. The user arbitrates the implementation scope at the next decision
  point.
- THE ECHO RULE (2026-08-19 ~12:3x, the user's principle — BINDING): the
  generic status echo is NEVER enough — a universal rule, not a per-verb
  special case. Every verb the client sends expects a REAL server-side effect
  (the mint + the family-4/5 push); the echo only carries the status bits. The
  lane-D claim "the fork ALREADY answers every opcode with the right shape —
  a positive is free" = CORRECTED: the SHAPES are right, but the ANSWER is
  never the shape alone — every verb's implementation = parse + effect + push,
  with the echo as the status envelope. (Game-dev corollary: a commercial
  client has no code path that treats a plain echo as a completed verb — the
  client-side grant paths are for the client's own reward flows, not for
  server-verb completion.) This reframes the consume-branch work: the
  parse/trace/log-only branches = the first step only; the real deliverable
  per verb = the mint/push effect.

## 12.13 LANE KIND-CLASSIFICATION LANDS: THE EQUIP'S CHARACTER OBJECT = KIND 1 (the
##        stamp-compare arm) - THE STAMP CHAIN FIRES POST-EQUIP (2026-08-19 ~12:20)
##        (deliverable = RE_output/claims/lane_kind_classification.md, the lane's
##         phase files = RE_output/content/kind_lane/phase1..4_*.md)

- THE QUESTION: which family-4 schema row (kind 0..5) the subclass EQUIP's two
  objects resolve to in the client walker (FUN_140e01520 via FUN_140e002d0), vs the
  boot snapshot. ZERO boots (the lane); everything read-only; the fork DLL/source
  + the live server untouched. The hook work = a DESIGN SPEC only.
- THE ONE-LINE ANSWER (INFERRED, census-pinned): the equip's CHARACTER object
  (first-u32 def 0xE5E86992, the family-4 character slot id) = **KIND 1** — the
  character-object stamp-compare arm (FUN_140e07ba0) — and the stamp chain it feeds
  FIRED post-equip. The brief's candidates (kind 0 = profile / -1 = dropped) are
  REJECTED: kind-0 (FUN_140e078f0) has no FUN_140a43300 and -1 exits the walker
  code-3 without a dispatch — neither can write the character stamp, and the stamp
  WAS written. The runtime row hashes are undumped (INFERRED label; the store row
  table is not on disk) — the mapping rests on the observed stamp + the fork's
  object-id constants (0x32D7B974/0xE5E86992/0x6CFBA3AB = account/character/
  item-instance slots) + the handler semantics.
- THE CENSUS DISCRIMINATION (boot-K, sunrise.log.old — the file I parsed; the
  12:02 rotation moved it to .old): the equip = the family-4 ws_wire at client-t
  164,078 (rdx=0x4; the FINDINGS-12.1 cluster; the server 403 at t=165,562). The
  family-4 frame entered the wire reader; the periodic stamp_consume (result=0
  every ~1031 ms) hit **result=1 at t=164,813 — 735 ms after the equip** — with
  stamp_gate=1 + ui_refresh on the SAME character key 0x234B59FC108 as the sign-on
  fire (t=24,594). The only family-4 stamp writers = the kind-1 arms; FUN_140e05cb0
  (the WS family-4 tail) has NO FUN_140a43300 — so the fire = the walker classified
  the character object kind-1 and dispatched (content differed — the loadout
  changed) → FUN_140e07ba0 → FUN_140a43300.
- THE BOOT-L REPLICATION (the 12:02 rotation = a boot-L run, ws_wire hooks
  stripped per the parked state): the boot-L session REPRODUCED the post-equip fire
  — stamp_gate=1 + ui_refresh at t=241,781 (key 0x1FA1E64C108) with the server's
  equips (801s t=840,157/842,688 + 403 t=844,110, soid 0x...32) — so the fire is
  NOT a hot-path-hook artifact.
- THE M4 RECONCILIATION + THE DOWNSTREAM POINTER: M4's "boot-J fires only at
  init" = a boot-specific variant (OPEN — candidate: boot-J predated the 0xB74C
  stamp-live shape, FINDINGS 11.11). Boot-A + boot-K + boot-L all show the post-
  equip fire — and boot-A's verdict was still "cue-then-revert": the kind
  classification is NOT the panel fix front. The block = DOWNSTREAM (the item node
  reads the ITEM INSTANCE sockets; the increments carry the character after-image —
  the 11.11 divergence, already fixed on the fork side by the boot-L-era
  prepare_subclass_equip item upsert).
- THE DELIVERABLE'S §4-§5 (DONE): the cool-path kind observer spec (FUN_140e002d0,
  RVA 0x140E002D0, ABI RCX/EDX/R8D, `ev=gate_trace stage=kind family=%u def=0x%08X
  kind=%d caller=+0x%llX`, gate_trace batch → 14 → NOT for a pure boot; NEVER hook
  FUN_140e01960) + the lane_census_parser.py `stage=kind` extension (proven
  non-regressive: pristine-vs-current identical counts on the real log + the
  synthetic line test).
- OPEN ITEMS: the item object's kind (2 vs 3); the runtime row hashes (the future
  kind-hook boot pins both); the boot-J discrepancy; a census-before-rotation
  discipline note (both logs rotated at 12:02).

## 12.15 THE 801-VS-EQUIP CENSUS DIFF LANDS: THE EQUIP NEVER RE-EVALUATES THE
##        PERSISTENT SOCKET-UI CONTEXT + ITS STAMP FIRE KILLS THE ~1 s POLL FOR
##        60.2 s (2026-08-19 ~13:33)
##        (deliverable = RE_output/claims/lane_801_vs_equip_diff.md; the lane's
##         phase files = RE_output/content/equipdiff_lane/phase1..3_*.md + the
##         phase1_*/phase2_* raw txt; zero boots, read-only, the live server PID
##         24840 untouched)

- THE LANE: diff the WORKING 801 node-pick window vs the BROKEN 403 equip window
  in the boot-L client census (the same session/parser as the kind lane; the 12.13
  answer left the downstream block open — this lane owns the log evidence, the
  lane_item_apply lane owns the static side).
- THE T-AXIS CORRELATION (VERIFIED, 12 anchors): **client_t + ~602,570 = server_t**,
  ±~50 ms, constant (both clocks real-time-rate; the swap-era anchors agree to ±1:
  the server request t − the client apply t = 602,548 for the 801s 840,157/842,688 →
  bap_apply 237,609/240,141 and the 403 844,110 → 241,562; the delayed arm send
  844,610 → receive 242,047 = 602,563; the 1820s = 602,548-602,563; four 701 applies
  = 602,548; two late 701s drift ~30-50 ms, inside the slop). The four windows:
  (a) baseline 220,000-237,599; (b) the 801 picks 237,600-241,339; (c) the gap
  241,340-241,559 = ZERO events (the whole 240,923-241,561 = empty); (d) the equip
  +2 s tail 241,560-243,560.
- THE SERVER-SIDE DELIVERIES (VERIFIED): the 801 = subclass_select family=4
  objects=1 bytes=188/185 (the ITEM record) + banner_refresh family=0; the 403 =
  subclass_equip family=4 objects=2 bytes=959 (the char after-image + the item
  upsert) + banner + equip_roster family=3 + the +500 ms delayed arm (banner +
  roster + ability_refresh). The client pushes = the byte counts − 28.
- THE DIFF (the one-line verdict): the 403's census window is a strict SUPERSET of
  the 801's (it adds the family-3 roster, the delayed arm, and the stamp
  gate+ui_refresh fire) — what the 801 HAS that the 403 LACKS =
  **(1) the apply-time evaluation of the PERSISTENT socket-UI context pair (expr
  rdx=0xC97B5FB708 r8=0xC97B5FBF08, caller +0x5085EB) — the pair the 801s (and
  the periodic 701 applies) evaluate, which the 403 replaces with a FRESH heap
  pair (0xC97BDFBC68/0xC97BDFC468); the 801#2 repeats the persistent evaluation
  at +781 ms (240,922) while the 403's only repeat = a +3.5 s 12-round wave on
  other fresh pairs; and (2) the stamp poll keeps running after the 801s (result=0
  on-grid), while the 403's window = the poll's fire (241,781, key 0x1FA1E64C108)
  followed by a 60.2 s silence (241,781→301,969).**
- THE EXONERATIONS (shared, byte-for-byte): the bap_apply (1, same rcx
  0x1FA1E0AFD60 per window), the marker_poll 21-idx set (identical incl. the
  doubled 0x000F), the sweep's 10-hash content (identical when it fires), zero
  char_test / sync_header in both. The marker indices, the resolve hash sets, the
  bap count, and the roster presence are NOT the discriminator.
- THE POLL'S HISTORY (the ~1,062 ms stamp_consume poll, BOTH boots): every fire =
  a ~60 s silence after it — boot-L 60,047→120,531 (60.5 s) and 241,781→301,969
  (60.2 s); boot-K 24,594→84,641 (60.0 s). The FAH heartbeats continue through the
  silence (not a freeze). The poll's resume at 301,969 (never re-firing ui_refresh)
  = the user's inventory re-entry, INFERRED.
- THE SYNTHESIS (INFERRED): the 801's ITEM-frame apply walks into the LIVE panel's
  requirement chain (the persistent pair = the open subclass panel's context) → the
  panel re-evaluates in place → the display updates. The 403's item upsert applies
  through an arm that does NOT reach that chain (the kind lane's kind-2/3 item
  question) and its character-object fire parks the stamp machinery in its ~60 s
  settle — the panel's context never re-evaluates, so the display stays stale until
  the re-entry rebuilds it. CONSISTENT WITH 12.13 and REFINING it: the item
  object's apply = the arm that fails to reach the panel's evaluation chain (the
  lane_item_apply lane's runtime fingerprint).
- OPEN: the 60-s silence mechanism (a settle timer vs a screen-session close); the
  persistent pair's identity (a kind-hook boot pins both); the sweep's loose
  trigger (apply-adjacent for the 801#1 + the equip, absent for the 801#2); the
  2100#3's resolve-only signature (callers +0xD0DCC6/+0xF37EF5 appear nowhere
  else).

## 12.14 THE ITEM-SIDE LANE LANDS: THE ITEM'S KIND = 3, ITS APPLY = NOTIFY-SILENT —
##        THE MISSING SURFACE = (c) THE PANEL'S ITEM NODE NEVER RE-READS (2026-08-19 ~13:53)
##        (deliverable = RE_output/claims/lane_item_apply.md; the lane's phase files =
##        RE_output/content/item_lane/phase1_kind_handlers.md, phase1b_helpers.md +
##        phase1b_helpers_raw.txt + phase1c_tail_raw.txt + phase1d_rowsearch_raw.txt,
##        phase2_upsert_content.md, phase3_panel_read.md, phase4_synthesis.md; zero
##        boots, read-only; the live server PID 24840 untouched)

- THE LANE: the item-side apply path — decompile the family-4 kind-2/3/4 handlers
  (FUN_140e08000 / FUN_140e080b0 / FUN_140e08200) + their callees, and name the
  missing surface between the 403's item upsert and the panel's item node. Three
  PyGhidra passes (this lane's own: ~13:30 the helper bodies, ~13:34 the kind-3
  tail chain, ~13:43 the image search) + the fork source (read-only).
- THE RECORD IDENTIFICATION (VERIFIED, exact fork-offset matches): kind 0 =
  the ACCOUNT record (+0x6C3C = the progressions @27,708, +0x742C memcmp 0x300C =
  the 12,300-byte acquired-flag bank); kind 1 = the CHARACTER record (+0x2CC0 =
  equippedInstanceSoids, +0x43F0 = progressions, +0x4C18 = the periodic reset,
  +0x9348 memcmp 0x1000 = the 4,096 flags, +0xB74C/D = the tail stamp); **kind 3 =
  the ITEM INSTANCE record — the +0xB8 RollState and the +0x180..+0x19C tailValues
  match the fork's 416-B instance Object byte-for-byte** (RollState @ 184, tail @
  384) — and the fork's slot numbering agrees (account=0/character=1/item=3 →
  the schema rows). Kind 2 = a 32-entry x 16-byte versioned table at +0x8e4 (this
  lane's disasm corrects the prior lane's stride-8: `ADD RBX 0x10` @ 0x140E0808B) —
  a family-4 slot-2 side record, NOT the item. Kind 4 = a span record.
- THE KIND-3 APPLY (VERIFIED bodies): FUN_140e07510 (the shared entry apply) =
  gated on a POSITIVE version delta (`0 < frame_version - store_version`) and is a
  progression-GRANT machine, not a record writer — the fork encodes roll.progress
  = 0 always ⇒ the gate never opens. FUN_140e08670 (kind-3's tail) = the EQUIPPED
  gate (the store item's soid == the character's equippedInstanceSoids[slot] @
  +0x2CC0) → FUN_1405463d0 = the per-entry SOCKET-STATE DIFF (store roll vs frame
  roll; transition classes 1/2/3, merges 4/5) → FUN_1412f0a70 = the notify gated
  on an ACTIVE socket session (FUN_1412e9540() != -1) + the currently-inspected def
  + class 4/5 only. NO FUN_140a43300 / FUN_140c93ec0 / FUN_140be2cb0 / changed-
  table reach anywhere in the item apply. The +0x180 tail-change notify =
  FUN_140be10f0 → the message-queue event 0x37 + the +0x3061 flag (dead for the
  fork's zero-tail frames).
- WHO WRITES THE STORE (VERIFIED structure): the walker's decode writes the frame
  payload INTO the store record (FUN_140e008a0 = the version-keyed store-record
  lookup → the [frame+0x10] encoding sub-type decode targets the record) BEFORE
  the kind dispatch — the kind handlers = the DIFF/NOTIFY stage, not the writer.
  (INFERRED: the raw-encoding arm copies the full payload — the fork publishes
  Encoding::raw + the u64 version prefix.)
- THE UPSERT CONTENT (VERIFIED, fork): the 403's item object carries the full
  416-B instance WITH the post-pick-reset 36 socketEntryStates + the 12 selectors
  (instance_encoder.cpp ll. 156-168; the persist-before-prepare ordering) —
  candidate (a) "the upsert lacks the sockets" = REFUTED.
- THE (a)/(b)/(c) VERDICT: **(c) — the missing surface = the item-side NOTIFY: the
  store's item record gets the new sockets (the decode), but the panel's item node
  never re-reads them — the item's kind-3 apply fires nothing on the panel's
  refresh surface (the stamp chain / the changed-table DAT_1426edce8), and the
  character-keyed stamp the equip DOES fire drives a CHARACTER-scope rebuild that
  skips the item node's socket picks. Only the manual re-entry (the full store
  re-read) refreshes the node — the boot-evidenced workaround.** The (c) reach
  analysis = VERIFIED (complete handler + callee bodies); the store-write link =
  INFERRED from the decode structure + the re-entry behavior.
- THE 12.15 AGREEMENT (cross-check, the parallel lane's census): the 801's item
  apply evaluates the PERSISTENT socket-UI context pair (the open panel's live
  session — exactly my FUN_1412e9540-gated notify) while the 403's replaces it
  with FRESH heap pairs (no session → no eval) — the two lanes name the SAME
  missing surface from the log side and the static side. REFINING: the item
  object's apply = the arm that fails to reach the panel's evaluation chain.
- THE FIX CANDIDATES (design only): (1) the item-keyed notify trio — the client's
  own kind-1 shape (FUN_140bdd800 + FUN_140c93ec0 + FUN_140a43300 +
  FUN_140be2cb0(ctx,2)) fired on the ITEM record's key; (2) the lighter surface:
  FUN_140be3f80 or a direct changed-table poke under the item's key; (3) the
  delivery-shape alternative (republish the item as a NEW record → walker-A's
  new-record stamp arm) if the runtime kind turns out -1; (4) FIRST the census
  discriminator: the kind lane's §4 cool-path hook on FUN_140e002d0 logs
  `family=4 def=0x6CFBA3AB kind=N` — N=3 ⇒ fixes 1/2 are the whole story, N=-1 ⇒
  fix 3.
- THE RUNTIME-KIND CAVEAT (VERIFIED negative): the three fork object ids have ZERO
  static-image hits (this lane's whole-image search) — the schema row hashes are
  runtime manifest data, so the item's kind = 3 by offset+slot inference, not a
  row dump; kind -1 (the frame dropped pre-decode) remains the residual
  alternative that only the kind-hook census closes.
- OPEN: the runtime kind (3 vs -1); whether the raw decode copies the full payload
  vs a schema-driven subset (decompile FUN_1404c74b0 or a store-dump read);
  FUN_1412e9540's session semantics; FUN_1403d18b0/FUN_1403d0490 (the per-entry
  state getters, bodies unread); kind-2's record identity (not load-bearing).

## 12.16 THE ENTITY SCOPING PROBE LANDS: THE STATIC-ENTITY PATH = A HALF-BUILT,
##        NEVER-ACCEPTED EMISSION, ONE FLAG-FLIP FROM ITS FIRST REAL TEST (2026-08-19 ~14:06)

- THE DELIVERABLE = RE_output/claims/lane_entity_scope.md (the phase files =
  RE_output/content/entity_scope/phase1_prior_work_digest.md,
  phase2_fork_surface_map.md, phase3_gap_and_ranked_items.md). ZERO BOOTS; the
  live server (PID 24840) + the dcv build + the fork = read-only.
- THE PRIOR-WORK DIGEST (the four cited files + the NEWER claims the fork cites):
  the transport grammar (the push wire layout, the 1024-byte lease mask, the
  spawn-set tag classes 0x80809162/64, the 48-byte points, the FNV vocabulary),
  the schema layer (the 38-name property vocabulary, the 0x28-entry walkers, the
  32-primitive reader tables, FUN_1404C74D0), and the apply-table census =
  the standing correction: NONE of the 15 family-4 applies writes an entity and
  the join-burst types {0,1,4,5,54} never reach FUN_140E0F000 client-side → the
  entity path = the queue-event inner sim-event type 7 (sobject_message) under
  the svc-9 type-17 envelope (claims/sobject-carrier.md) — NOT the 15-table
  sub-enum. The s2v0-era "15 handled types" framing = SUPERSEDED.
- THE FORK'S SURFACE TODAY (VERIFIED from source at 7ef2453): the spawn_sets
  domain = 77 stems / 2,090 name hashes / 8,892 points {position, nameHash,
  bubbleMask} (the vendor placement data = fully loaded; Zavala's courtyard
  point = set 0x80ED3B2D p18 [53.69, 65.90, 18.03], nameHash 0x5B9195B4); the
  vendors domain = economy rows only (no entities). The published entity traffic
  = the join burst (4→0→1→54) + the keepalive's PLAYER-state echo (SpawnState/
  TeleportState mirrored from the type-22 deltas). THE EMISSION = BUILT and
  flag-gated OFF: the type-17 queue-event envelope + the 0x89-byte kind-2 record
  (activity_world_population_push.cpp + activity_entity_baseline.{h,cpp}) with
  the refined widths (24-bit identity, 37-bit vector3d, 29-bit forward_and_up)
  and the schemaHash knob default 0x80806AC0 = the schema_capture-hook-VERIFIED
  player-baseline hash.
- THE BOOT HISTORY: 08-16 the emission fired once (no vendor, silent) and the
  next boot black-screened (the 0x7F6000 garbage-row poison — proven); the
  control boot was clean. The refined edits committed (7fdbea3) and sit in the
  deployed source — NO BOOT has ever tested the refined emission.
- THE RANKED WORK ITEMS (the deliverable §4): W1 THE REFINED-EMISSION BOOT
  (settings-only: world_population true + hash 2155899584 — the premise test
  for everything downstream), W2 the receive-cluster observer (one log-only
  hook at FUN_141718510 = the validation harness that separates decode-fail /
  materialize-fail / render-fail), W3 the live ground-truth capture (the schema
  harvest + the player-create capture + the session-index read), W4 the static
  descriptor dumps (delegated), W5 the probe-flip loop, W6 the vendor-archetype
  harvest (settings-only knob flips), W7 the multi-entity loop, W8 static
  enemies (needs the combatant schema hash + the update side).
- THE SCOPING VERDICT: DAYS-scale for the one-entity smoke (the build half is
  done; W1+W2+W3 close the probes in 2-3 sessions); WEEKS-scale if the refined
  boot is silent or black-screens again (the session-index / tail /
  entity-index-global unknowns reopen one boot at a time); static enemies =
  the weeks tail regardless. THE RISKIEST UNKNOWN = the codec-body tree
  alignment (the 153-field F3 census = an inference, and a misaligned walker
  fails silently — the 08-16 signature); the named second = the vendor
  archetype hash (the knob is the PLAYER's baseline — a mannequin may render).
- OPEN (the hand-back): W1's outcome names the next lane; the standing REQUESTs
  (the session-index read, the ring→receive link decompile, the live schema-tree
  dump, the descriptor dumps, the DAT_141F91300 global); the refinepayload
  lane's missing deliverable (claims/refined-payload.md never written — its
  substance lives in the fork tree + lane_refinepayload.log; a future session
  may back-fill it).

## 12.17 THE THREE-LANE CONVERGENCE + THE FIX-A ONE-SHOT (2026-08-19 ~14:2x)

- THE CONVERGENCE (the three lanes, all audit-clean, all spot-verified): (1) the
  kind lane — the equip's character object = kind 1, the stamp chain fires
  post-equip, the block = downstream; (2) the census-diff lane — the 801's apply
  re-evaluates the panel's PERSISTENT socket-UI context pair (expr rdx=0xC97B5FB708,
  caller +0x5085EB; repeated at +0.8 s by the 801#2) while the 403's apply
  evaluates FRESH pairs (0xC97BDFBC68) — the equip never re-evaluates the live
  panel; the marker_poll 21-set, the resolve hashes, the roster, and the bap_apply
  = byte-identical (exonerated); the post-fire ~60 s poll silence = a settle rhythm
  (both boots), not the bug; (3) the item-side lane — the item's kind = 3
  (offset-proven: RollState @0xB8, tailValues @0x180 = the fork's instance Object
  exactly), its apply = notify-silent (no stamp/changed-table reach; its only
  notify = session-gated + class-4/5-only), the store GETS the new sockets (the
  walker's decode writes them; the re-entry proves the content) → THE VERDICT (c):
  the missing surface = the eval context, not the content (a refuted) or the notify.
- THE MECHANISM (INFERRED, the joint reading): the 801's item-only frame applies
  through the item arm while the panel's persistent context stays alive → the panel
  re-evaluates in place → the display updates. The 403's character object (object 0,
  applied first) fires the kind-1 stamp + the character-scope rebuild → the context
  re-allocation (the fresh pairs) → the item's apply finds no live panel context.
- THE FIX-A ONE-SHOT (377ac5c, +58/−119, 6 files): prepare_subclass_equip now
  delegates to the SHARED prepare_item_republish (the opcode-801/2100 builder,
  exposed via internal.h) — the equip publishes the item-only frame (one item
  upsert, flags 0, +1 version) exactly like the working flows. The character
  after-image no longer rides the equip's frame. The banner refresh (family-0), the
  roster refresh (family-3), and the delayed ability refresh STAY (the census
  exonerated them; the ability-bucket deferral = equip-specific). The harness's
  equip assertion → equip_frame_is_item_only (the harness = DB-read-only, so the
  eqHash survives the gate run). The server built clean (/m:1); the DLL = untouched.
- THE NEXT BOOT (the fix-A validation + the riders): the deploy (the harness gate →
  the cache pre-stamp → the restart) + the tour: the equip's display (the main
  event — live update without re-entry?), the halving observation (the recap at
  character select), optionally the P0-2 batch (904).

## 12.18 THE BOOT-M HARVEST: FIX-A = CENSUS-LEVEL PROGRESS, THE UI STILL STALE; THE
##        HALVING NARROWS AGAIN (2026-08-19 ~15:2x)

- THE FIX-A DELIVERY = CONFIRMED ON THE WIRE: the equip's frame = subclass_equip
  family=4 objects=1 bytes=188 (the item-only republish — the 801's exact shape;
  the server's t=612344).
- THE CENSUS MOVED TOWARD THE 801'S SHAPE (all verified in the boot-M client log):
  (1) NO post-equip stamp fire (the only result=1 = the sign-on t=27625 — the
  character-scope rebuild is GONE, exactly the fix-A mechanism's prediction);
  (2) NO apply-time fresh-pair expr rounds (the boot-L 403's fresh-pair eval =
  gone with the character object); (3) the stamp poll keeps running through the
  equip (result=0 — the 801-like behavior); (4) A NEW +5.6 s burst: 8 expr_evals
  on the PERSISTENT socket-UI pair (0x7D95BF8DB8/0x7D95BF90A8 — this boot's
  persistent pair) at t=126250-126266.
- THE UI = STILL STALE (the user: "the subclass UI behavior still remains broken,
  in the same state as previous"). THE IMPLICATION: the persistent-context
  re-evaluation = NOT sufficient for the display update — the missing surface is
  one link deeper than the eval context.
- THE NARROWED DISCRIMINATORS (the boot-M equip vs the boot-L 801, same
  instrumentation): (1) THE MARKER_POLL 21-SET — the 801s poll the 21-index set
  at +203/+312 ms after the apply; the boot-M equip has NO marker_poll after the
  apply (the boot-M's single cluster = t=62125, the panel-open-time, pre-equip);
  (2) the eval TIMING — the 801's persistent-pair evals = AT apply (+ a +0.8 s
  repeat); the equip's = +5.6 s (a periodic round?); (3) the ITEM'S SOCKET
  CONTENT — the 801 = a real socket-entry change; the equip = the pick-reset's
  defaults. THE LEAD CANDIDATE: the kind-3 apply's per-entry socket-diff
  (FUN_1405463d0) → the session-gated class-4/5 notify (the item lane's finding)
  — the 801's pick trips a class the equip's reset never does → no marker
  re-read → the stale panel. A structured boot-M census-diff lane = the next
  analysis (the narrowed space).
- THE VETERAN = still gone → the trigger ∈ the A-side (47..1375). THE NEXT FOLD =
  LANDED: POST /restore 979..1375 (388 flags) — the kept half = 47..953 (388
  still suppressed). The next boot's observation: recap plays → the trigger ∈
  979..1375; gone → ∈ 47..953. ~8 more halvings to the exact index.

CORRECTION (2026-08-19 ~16:2x, the boot-M lane's re-anchor — 12.19): this entry's
"narrowed discriminators" = ANCHOR ARTIFACTS. The boot-M correlation = client_t +
~540,350 = server_t (9 anchors, ±62 ms — NOT the +602,570 I assumed): the equip's
apply = t=71,969 (the 160/895/1127 pushes = the 403's queuez updates byte-for-byte),
NOT 120,641 (that's the 702#4 periodic poll). THE RE-ANCHORED TRUTH: the equip DOES
poll the marker set (+235 ms) and DOES run the apply-time eval rounds — the panel's
live machinery runs on unchanged content. The quiet path = the verdict (see 12.19 +
12.22).

## 12.19 THE BOOT-M CENSUS DIFF: THE RE-ANCHOR + THE QUIET PATH — THE EQUIP'S
##        FRAME IS CONTENT-IDENTICAL, THE MISSING SURFACE = THE CONTENT DELTA
##        (2026-08-19 ~16:26)

- THE CORRELATION RE-ANCHOR (VERIFIED, 9 anchors — CORRECTS 12.18): client_t + ~540,350
  = server_t (spread 540,328-540,390) for the whole boot-M session. THE 12.18 ANCHOR WAS
  WRONG: the boot-M equip = the apply at client t=71,969 (the one carrying the 160/895/1127
  pushes = the 403's subclass_equip 188 + banner 923 + roster 1155 byte-for-byte) — NOT
  t=120,641. The t=120,641 paired apply = the 702#4 periodic socket-UI poll response
  (server 661,000; the sync_headers precede every 701 send — the 702s = the panel's own
  polls). Deliverable: RE_output/claims/lane_bootm_census_diff.md + the phase files in
  RE_output/content/bootm_lane/phase{2,3,4}_claims.md.
- THE 12.18 ITEMS THAT WERE ANCHOR ARTIFACTS (all refuted by the re-anchor): (1) "no
  marker_poll after the equip" — FALSE: the 72,204 cluster = the equip's own poll at +235
  ms (the boot-L 801s' +203/+312, the boot-L 403's +438 — the same mechanism; the prior
  spawn's "both clusters pre-equip" died before the server-log correlation); (2) "no
  apply-time eval rounds" — FALSE: the equip's apply carries the full round-pair incl. the
  C90DFB socket-UI round (rdx 0x7D95FFBBA8); (3) "the +5.6 s burst after the equip" —
  FALSE: the 126,250-126,266 burst = +5,609 ms after the 702#4 (the equip → the burst =
  54.3 s); the burst = 4 alternating round-pairs on two socket-UI contexts, a settle/late
  panel round on the poll's tail, not an equip event. The 12.18 items that SURVIVE: no
  post-equip stamp fire, the poll keeps running, the UI stale (user-observed).
- THE TWO MARKER CLUSTERS (both = the same 26-event set = the boot-L 21-set + 5 extra idx
  0x0089/0x007F/0x0082/0x0080/0x0081): cluster 1 @62,125 = the panel open (the orbit_
  handoff released @62,079 → +46 ms); cluster 2 @72,204 = the equip's apply +235 ms. The
  polls co-occur with the apply-time requirement rounds and NOT with the dispatch (they
  fired with no dispatch possible) ⇒ the marker-poll surface = exonerated, NOT the link.
- THE QUIET-PATH ADJUDICATION (VERIFIED, fork end-to-end): the equip's mutation = a PURE
  SLOT SWAP (equip_subclass_item + two UPDATE items rows; no socket/ability write); the
  416-B instance Object = slot-agnostic (no equipped field — the equipped-ness lives only
  in the character's equippedInstanceSoids); the object version = the CONSTANT instance
  soid; the encoding = Oodle (CORRECTION to the item lane's "raw + version prefix"
  inference); the socket content's only inputs = the definition's readyMask + the 5 ability
  entries (unchanged — boot-M fired ZERO 801s; the DB's instance_state/item_plugs = EMPTY,
  the ability entries = untouched). THE 403's PAYLOAD = 0x600000000000000C = the ALREADY-
  EQUIPPED subclass (a SELF-swap; boot-L's = 0x32, a cross-swap). ⇒ THE EQUIP'S ITEM FRAME
  = CONTENT-IDENTICAL to the store's record → the walker's before/after compare = equal →
  NO dispatch → the kind-3 handler NEVER RUNS → no diff, no class, no notify. THE QUIET
  PATH IS ALIVE — the panel's machinery (the rounds + the polls) re-reads UNCHANGED
  content → no re-render → stale.
- THE 801 CONTRAST (why the picks work): one pick's group-claim flips MULTIPLE
  socketEntryStates ready→active in one frame (resolve_socket_states) → a real content
  diff → the dispatch → a MERGE (class 4/5) → the session-gated notify → the update.
  The equip has no such change to make, by construction.
- THE MISSING SURFACE, ONE LINK DEEPER (high confidence): the CONTENT DELTA — the
  dispatch's trigger. The prior verdicts (12.17's "eval context", the item lane's
  "notify-silent") = true but downstream; the block sits BEFORE both (the dispatch never
  happens). THE RANKED FIX CANDIDATES (DESIGN ONLY): (i) THE QUIET PATH — the equip's
  delivery must carry a socket state that DIFFERS from the store's: the two-frame
  reset→select sequence (frame 1 = the picks cleared, frame 2 = the picks applied — frame
  2's multi-entry ready→active = the merge class). THE BRIEF'S "old-subclass-first" shape
  = NOT viable (a slot swap changes NEITHER record's content — the delta must be
  SYNTHETIC); the release-and-re-add = the class-2 path (no notify, unproven new-record
  trio). (ii) THE CLASS-4/5 GATE — folds into (i): the delta must be a MERGE (single-entry
  = classes 1/2/3 = no notify). (iii) THE SESSION GATE (FUN_1412e9540) — rank 3,
  instrument-don't-fix (the 801s on the same panel prove the session can be live).
- THE NEXT-BOOT RIDER: boot-M's equip = a SELF-swap (its visible delta = minimal even
  working) — the fix's validation boot should equip a DIFFERENT subclass (a cross-swap)
  so the expected update = the panel switching to the new subclass's node state. The fix
  = swap-agnostic.
- OPEN: the walker's exact compare for an identical Oodle frame (one decompile); the
  +5.6 s burst's exact trigger; the FUN_1412e9540 session census; the boot-M svc=250/251
  ~5 s requests (no FAH type=9 lines exist in boot-M — identity unknown, not load-bearing).

## 12.20 THE UPSTREAM CHAIN DIFF LANDS: THE STALE EQUIP PANEL = THE SERIAL MODEL
GAP (2026-08-19 ~16:19)

The upstream-vs-ours equip-chain diff lane (flash) landed. Deliverable:
RE_output/claims/lane_upstream_chain_diff.md (the consolidated sections 1-6) + the per-phase raw
RE_output/content/upstreamdiff_lane/phase{1,2,3,4}_*.md. Read the deliverable end to end;
this entry = the one-paragraph digest.

THE VERDICT (the top candidate, HIGH confidence): the equip panel's live refresh key = the
inventory-row mutationSerial - the client orders a bucket's grid cells by it, and the
upstream's equip re-stamps the moved items' serials (the newest for the mover; the displaced
takes the clicked item's PRIOR serial - the HiddenInvariant 6164a3b / joshrad-dev PR #41
hand-off, whose commit text names the mechanism: "the Client, which orders a bucket's grid by
serial rather than row" + "mutationSerial doubles as the Client's grid ordering token" +
"Matches live and Shadowkeep-era behavior"). OUR fork has NO serial model: the authored Item =
{soid, hash, level, quantity, sockets} (no serial/flags/picks), the CharacterState = the 5
picks + no nextInventorySerial, our character encoder writes constant-zero row serials (we
added our own 0xB74C FNV stamp hack instead), our resolver sets nextInventorySerial =
itemCount (static), and the equip never touches the serial space. The client's grid therefore
sees NO ordering change on our equip -> no live rebuild -> stale until inventory re-entry. This
explains why ALL THREE of our delivery shapes failed (character-only, 2-object, item-only -
none changed the serial space) while the 801/2100 work (their ITEM record's socket-entry bytes
change, which the ability-node panel reads). THE SECONDARY, structural difference: the
upstream's 403 frame = the CHARACTER upsert (the only record that carries the equip state),
while our fix-A = the ITEM-only frame (the 801 shape) - the item Object has no equipped bit/
slot/serial, so the fix-A shape can never refresh the equip panel. Also verified: the
upstream's order = prepare -> reply (version promise) -> stage -> commit-last vs ours =
commit-first (mutate -> persist -> restamp -> frames) - not the panel cause, a divergence to
close. The 5 OPEN PRs = none on the equip surface; the answer is in the master commits (the
two serial fixes + Nyxaraa's 7846599 per-item picks - the snapshot = post-7846599,
pre-6164a3b). PORT CANDIDATES (design only): A = the serial model (the state-side port: the
Item's serial/flags/picks + the CharacterState's counter + the equip re-stamp + the encoder's
row writes) - the falsifiable claim for its boot: the 403's apply flips to the 801's census
signature (the marker-21 poll + the persistent-pair re-eval at apply); B = revert the 403
frame to the character upsert (the b954169 builder was already the upstream's exact shape);
C = the reply version promise re-verification; D = the per-item picks (the later 7846599
sync). THE KEY WEB CITES: github.com/stanuwu/Sunrise/commit/6164a3b3c26c7bedaeadc54ae5c15a8a2add7572
(the serial-grid fix) + /commit/8907e24682c486ca51a7df8efa2d5822ecf38670 (PR #41 base) +
/commit/784659919d575d6ba8826130183618eb46e72138 (the per-item picks).

## 12.21 THE MAIN SESSION'S VERIFICATION + THE DIRECTION FLIP (2026-08-19 ~16:0x)

- THE LANE = AUDIT-CLEAN (all load-bearing claims spot-verified): (1) the
  upstream's 403 frame = ONE CHARACTER object (the character::encode of
  mutation.afterCharacter — family4_selection_move.cpp:245-256 verbatim); (2)
  our state-side Item = {instanceSoid, definitionHash, level, quantity, sockets}
  — NO serial, NO flags (inventory_state.h:69-75 verbatim); the serial FIELDS
  exist in the datagen layouts (the inventory Entry @kMutationSerialOffset + the
  loadout definition) but NOTHING populates them (the only serial-touching code =
  the resolver's nextInventorySerial = itemCount, a STATIC value; the equip
  touches nothing serial — rg-proven zero hits); (3) the commit's own words
  (6164a3b, fetched): "the Client, which orders a bucket's grid by serial rather
  than row, rebuilt it in the first cell" + "mutationSerial doubles as the
  Client's grid ordering token" + "Matches live and Shadowkeep-era behavior".
- THE VERDICT STANDS: the stale equip panel = the SERIAL MODEL GAP. The client
  orders the bucket grid by the row mutationSerial; the equip = a row move whose
  re-ordering the client keys on the serials; our frames carry constant-zero
  serials → the client sees no ordering change → no grid/panel re-render. This
  explains EVERY shape we tried (the character-only, the 2-object, the item-only
  — none changed the serial space) + the working 801/2100 (their item socket
  bytes change, the surface the node panel reads).
- THE DIRECTION FLIP: the fix-A (the item-only frame) = STRUCTURALLY WRONG (the
  item record carries no equipped state; the upstream's 403 = the character
  upsert). THE FIX = the lane's A + B: (A) the serial model port — the
  mutationSerial + flags on the authored Item, the nextInventorySerial counter
  on the CharacterState, the persistence rows, the resolver publishing the real
  counter, the encoder writing the row serials, the equip re-stamping the moved
  items (the newest for the mover, the clicked item's prior serial for the
  displaced — the 6164a3b hand-off); (B) the frame revert to the character
  upsert (the pre-fix-A b954169 builder = the upstream's exact shape).
- THE FALSIFIABLE BOOT CLAIM: the 403's apply flips to the 801's census
  signature (the marker-21 poll + the persistent-pair re-eval at apply) AND the
  panel updates live. The fast path = fetching the upstream's post-6164a3b
  serial-model files (the lane's open item) + adapting them to our server-mode
  tree (the persist layer + the DB schema migration for the serial columns).
  The user arbitrates the port's scope at the next decision point.

## 12.22 THE TWO-LANE CONVERGENCE: THE EQUIP DELIVERS NOTHING THE CLIENT KEYS ON
##        (2026-08-19 ~16:2x)

- THE BOOT-M LANE = AUDIT-CLEAN (spot-verified: the re-anchored correlation, the
  cluster-2 marker poll at +235 ms, the DB's 0-row instance_state/item_plugs, the
  self-swap payload 0xC). ITS CORRECTION STANDS: the equip's apply at 71,969 runs
  the panel's live machinery (the eval rounds + the marker polls) on UNCHANGED
  content → THE QUIET PATH = the blocker: the equip's item frame is
  content-identical to the client's store record (the pure slot swap, the
  slot-agnostic 416-B object, the constant-soid version) → the walker never
  dispatches the kind-3 handler → no socket-diff → no class → no notify → the
  stale panel. The 801 works because a pick's group-claim flips MULTIPLE
  socketEntryStates ready→active in one frame = a real content diff = a MERGE
  (class 4/5) → the notify.
- THE CONVERGENCE (the two lanes = two views of one root): the upstream lane's
  serial-model gap (the client orders the bucket grid by the row mutationSerial;
  our serial space = constant zeros, the equip never changes it) + the boot-M
  lane's quiet path (no content delta → no dispatch) = THE SAME GAP: the equip's
  delivery carries NOTHING the client keys on — no serial re-ordering, no socket
  change. Both lanes' fix candidates therefore compose: (A) THE SERIAL MODEL (the
  upstream's port — the grid re-ordering token the equip must change), (B) THE
  CHARACTER-UPSERT REVERT (the frame that carries the serials + the equipped
  state), and — as the fallback if the serials alone don't trip the panel — (C)
  the synthetic reset→select two-frame delta (the merge-class content change).
  The validation boot = a CROSS-swap equip (boot-M's self-swap = minimal visible
  delta even when working).
- THE PLAN (the user's arbitration at the next decision point): the serial-model
  port lane — fetch the upstream's post-6164a3b serial-model files + adapt them
  to our server-mode tree (the Item's serial/flags, the character's
  nextInventorySerial, the persistence columns, the resolver, the encoder, the
  equip's re-stamp) + the frame revert (B) + the harness. Zero boots until the
  harness gates pass; then the validation boot (the cross-swap equip + the
  halving observation).

## 12.23 THE SERIAL-MODEL PORT LANDS + THE 403 CHARACTER-FRAME REVERT (2026-08-19 ~17:5x)

- THE LANE DELIVERED (six commits on the integration branch, base 377ac5c):
  081c79b (S1: the state fields + the runtime initial stamp), e3fd097 (S2: the
  resolver/encoder), 0dde018 (S3: the persistence + the migration), 78c7bbd
  (S3a: the fresh-seed source = the stamped snapshot), 8df11ab (S4: the equip
  serial hand-off + the 403 character-frame revert), bd5644d (S5: the harness).
  Deliverable: RE_output/claims/lane_serial_port.md; phase files:
  RE_output/content/serialport_lane/phase{1,2,3,4}_*.md. BOTH projects build
  clean (Release x64 v143 /m:1, ZERO errors — the client DLL builds as the
  shared-source gate but is NOT deployed; persistence.cpp is server-only,
  verified).
- THE UPSTREAM RULES (fetched post-6164a3b master, captured with quotes in
  phase 1): the client orders a bucket's grid BY SERIAL, not by row; the equip
  hand-off = the mover takes the freshest generation, the displaced item keeps
  the mover's PRIOR serial (a fresh greatest serial would rebuild it in the
  FIRST grid cell — the 6164a3b commit's own comment); the counter consumes
  exactly two values per swap; the encoder rejects any row serial outside
  [0, nextInventorySerial) and any counter < itemCount or > INT32_MAX.
- THE PORT SHAPE (our tree, adapted not copied): the Item gains
  mutationSerial + flags; the CharacterState gains nextInventorySerial; the
  resolver carries the serials through resolve_item and reads the counter (the
  static itemCount assignment is gone); the encoder writes the row serials and
  enforces the upstream validity; equip_subclass_item stamps the hand-off with
  the INT32_MAX guard; prepare_subclass_equip = the b954169-era character
  upsert again (recovered from git history; the fix-A item-only frame is
  reverted — the item record carries no equipped state); the 801/2100 item
  republish stays.
- THE MIGRATION (VERIFIED against a temp copy of the deployed state.db): the
  deployed items table ALREADY has mutation_serial/flags (61 rows, all zeros —
  the dead space, confirmed live); characters LACKS next_inventory_serial (the
  ALTER adds it). The seed for the existing rows = the ascending-by-row
  assignment (equipment by semantic equipment_slot, storage by item_id;
  counter = count) — the DESIGN DECISION recorded: byte-identical to the
  runtime stamp's assignment, so the first post-migration boot's grid order =
  the order the rows already had. Both the migration and the runtime stamp are
  idempotent (nonzero-counter or nonzero-serial gates), so re-boots never
  reset the equip history.
- THE RUNTIME STAMP (the upstream's seed_inventory_runtime_fields, ported with
  an ALL-ZERO GATE): our DB-backed boot runs state::initialize twice
  (settings pass, then the DB-loaded pass); the gate keeps the DB pass from
  re-stamping, and the fresh settings pass gets ascending serials so the first
  frames satisfy the encoder's invariants. VERIFIED the cache equipment hash
  never mixes serials (definitionHash/level/sockets/abilities only), so the
  stamp cannot perturb the cache identity gate.
- THE HARNESS (bd5644d): the equip assertions now prove the character frame
  (id == characterObjectId, version == equip.characterSoid) + the hand-off
  (freshest mover, prior-serial displaced, counter +2) — all relative to the
  pre-equip snapshot, so they pass on a migrated DB and a fresh zero-serial DB
  alike. NOT run by the lane (the deployed s1_accept stays untouched); the
  harness run = the main session's deploy-time step.
- DEPLOY-TIME STEPS FOR THE MAIN SESSION (recorded in the deliverable §6):
  (1) stop the live server, deploy the new exe, boot — the migration applies
  itself inside persistence::initialize (no manual DB step); (2) the harness
  gate from a throwaway s1_accept copy (EXIT 0 expected); (3) the VALIDATION
  BOOT = a CROSS-swap equip (A→B, then B→A) with the panel refresh watched
  live — if the panel still stalls, the serial model + the character frame
  are now the standing contract for the next census diff (the fallback stays
  the synthetic reset→select delta, 12.22's candidate C).
- OPEN ITEMS: none blocking. Watch-list: any future item-adding mutation must
  stamp its items (the encoder's strict serial < counter rule refuses
  unstamped loadouts); the upstream's finalize re-resolution proof has no
  fork-side equivalent (our equip substitutes the valid gate + the resolver's
  determinism — divergence recorded).

## 12.24 THE SERIAL PORT = HARNESS-VERIFIED + THE IDENTITY-GATE DIAGNOSTIC
##        (2026-08-19 ~17:2x)

- THE PORT (6 commits 081c79b..78c7bbd) = REVIEWED + HARNESS-VERIFIED: the equip
  hand-off code (the mover = the freshest, the displaced = the mover's prior, the
  counter +2, the INT32_MAX guard) + the character-upsert frame revert + the
  harness assertions all spot-checked in the tree; the harness run (the throwaway
  s1_accept copy) = EXIT 0, failures=0 with the equip_serial_* gates (the
  pre/post subclass matches, the mover-takes-freshest, the displaced-keeps-prior,
  the counter-consumes-two) + equip_frame_is_the_character_upsert all ok.
- THE IDENTITY-GATE DIAGNOSTIC (b4cbabf): the gate now logs BOTH identities on a
  mismatch. THE FINDING IT CAUGHT: the harness's expected eqHash = the
  DB-load's configured hash (0xCB7EF89E32245C56 — the python port's value, now
  3-for-3 confirmed as the DB-load's C++ value) while the LIVE server's /restamp
  = the RUNTIME snapshot's hash (0x9EF65EB9BD334F2C) — THE RUNTIME SNAPSHOT ≠
  THE PERSISTED DB after the boot-M session (a real divergence: the /restamp
  stamps the runtime account; the gate compares the DB-load account). THE
  DEPLOY PROCEDURE (recorded): the cache's eqHash = the DB-derived value (the
  bootL_eqhash_exact.py port = the confirmed DB-load truth), NOT the live
  /restamp's value; the diagnostic = the arbiter if they ever disagree again.
- THE DEPLOY SEQUENCE (the main session's next step): stop the live server →
  the backup + the new exe → the cache pre-stamp (ts/size = the new exe; eqHash
  = 0xCB7EF89E32245C56) → the incident re-hash → the start (the migration
  applies itself in persistence::initialize) → the boot verification (the six
  domains + the migration success). THE VALIDATION BOOT = the CROSS-swap equip
  (A→B then B→A — the panel refresh watched live) + the halving observation.

## 12.25 THE BOOT N/O/P SAGA + THE ESCALATION PREP (2026-08-19 ~21:0x)

THE HONEST FAILURE RECORD (the user cut the UI front here — the escalation):

- BOOT N (the serial port + the character-frame revert, 6 commits + b4cbabf):
  the serials = LIVE end-to-end (the DB: 0x31 = serial 18, the counters 20/18/18;
  the encoder writes them; the wire = the 854-B character frame). THE CENSUS: the
  equip's content CHANGED (the serials) → the walker DISPATCHED (the quiet path
  broken) → the character apply ran the full profile machinery (the rollback_apply
  = the kind-0 diff fired — the first post-equip ever; TWO 4,096-index char_test
  walks; the eval rounds; the marker cluster 0x0007-0x018E) — BUT the stamp never
  fired (the 0xB74C content unchanged → no consume → no ui_refresh) and THE PANEL
  = STILL STALE. THE LESSON NAMED: the upstream's serial-model reference = the
  GENERAL EQUIPMENT swap (the weapon bucket grids); the upstream has NO
  subclass-equip flow; the subclass PANEL = a different surface. The serials +
  the character frame = the correct port of the WRONG reference surface.
- BOOT O (216ca08 — the synthetic reset→select two-frame: the item reset to the
  baseline sockets at +1, the item with the picks applied at +2): THE WEASEL =
  THE BOOT-13 TRAP RE-HIT — queuez_frame::append seals with the caller's nonce
  and never advances it, so the two frames sealed with the SAME nonce → the
  client's AEAD failed on the second → the bad-signature kick. FIXED (7e94a2b —
  the append advances the nonce between the frames).
- BOOT P (the nonce fix): NO KICK — the two frames delivered + accepted. THE UI
  = STILL THE SAME (the panel needs the re-entry). THE COMPLETE FAILED LIST: the
  char-only frame, the 2-object frame, the item-only (fix-A), the serials + the
  char frame (the boot N), the reset→select two-frame (the boot P) — every
  delivery shape; the census machinery fires (the polls, the rounds, the
  dispatch, the profile diff) but the panel NEVER re-renders. THE PANEL'S
  LIVE-UPDATE KEY = STILL UNNAMED (the serial + the socket-delta + the notify
  hypotheses = all delivered + all failed at the visible level).
- THE REMAINING DISCRIMINATORS (never run): the KIND-HOOK CENSUS (the kind
  lane's §4 spec — the one instrument that logs the runtime kind + the row
  hashes per object; designed but never built/deployed), the upstream master's
  CURRENT subclass handling (the lane fetched the serial commits but NOT the
  full post-6164a3b tree — a future lane should fetch + diff the whole state
  layer), and the boot-M lane's open items (the walker's exact compare for an
  identical Oodle frame; the FUN_1412e9540 session semantics).
- THE VETERAN: NO recap in boot N or boot P (the user) → the trigger ∈ 47..953
  (the still-suppressed 388 after the 12.18 fold; the next fold = restore the
  second half of 47..953 → ~194 kept). The folds landed: the 47..3933 range has
  narrowed from 3,887 → 388 across four boots.
- THE ESCALATION PACKAGE (the user's go): STATE.md (the updated snapshot below)
  + INCIDENT_20260819_210002 (the fresh one-file catch-up). The escalation
  contract: "STATE + incident 20260819_210002" only.

## 12.26 THE REVIEWER'S RULING LANDS: THE TWO SURFACES ARE EACH GATED AWAY FROM
##        THE EQUIP — THE PLAN = THE KIND HOOK + THE UPSTREAM DIFF, THEN ONE
##        CENSUS-INTENT BOOT (2026-08-19 ~21:5x, recorded ~21:59)

THE RULING (the outside review's facts, ALL FIVE SPOT-VERIFIED ON DISK this
session before recording — the evidence pointers below = the verification):

- FACT 1 — rollback_apply = the kind-0 ACCOUNT handler → boot N's census
  reading is ambiguous. gate_trace_observer.cpp:27-28 (verified): kRollbackRva =
  0xE078F0, the observer's own comment = "the family-4 kind-0 diff-apply (the
  rollback executor)". Boot N's census observed it firing post-equip on a
  character-upsert delivery — either a second (account) object rode the window
  or the kind resolution isn't what the lanes inferred. The kind hook settles it
  in one log line.
- FACT 2 — the notify has a THIRD gate nobody priced in: the changed def must
  equal the currently-inspected def. FUN_1412f0a70's body (verified,
  lane_inv_cuib_raw.txt:2795-2826): calls FUN_1412e9540 (the session), then
  FUN_140b3a4c0 → reads the global DAT_141fb5928 (verified at :2839-2850 — the
  manifest-table resolve of the def the socket UI is inspecting) → walks the
  changed-list for a matching def → only class 4/5 proceeds to the sink
  FUN_1412f3660. An equip-to-a-different-subclass can never fire this notify
  while the panel still shows the old subclass. Structural, not a delivery bug.
- FACT 3 — the notify's sink FUN_1412f3660 = the socket-panel live-update
  machine, dumped 2026-08-17, never analyzed by any panel lane. Body (verified,
  lane_inv_cuib_raw.txt:3058-3211): resets the socket-UI session record
  (+0x33c0=-1, +0x33c4=0), then per socket slot (8 slots, a 0x30 bitmask) zeroes
  state, resolves TWO definition hashes through the manifest per socket entry,
  sets animation floats (0x3f800000), and calls the node setters FUN_14186ab10 /
  FUN_14186a9c0 (:3199-3201). Reachable only through the FACT-2 notify.
- FACT 4 — the character handler contains an equipped-diff surface no lane has
  ever mapped, and boot P removed its input entirely. Kind-1's body
  FUN_140e07ba0 (verified, lane_panel_trigger_raw.txt:1010) calls
  FUN_140e07d30(char+0x2cc0, frame+0x598) = the equippedInstanceSoids diff
  (verified in lane_inv_2dmap_raw.txt:3225+, body "equippedInstanceSoids
  helper"): per-slot soid compare → FUN_140b9e4a0 + FUN_140ba9b10 +
  FUN_140fa8de0(view, slot) (the poke on the DAT_141fcdeb8 view record) → a
  +0xc750 dirty flag via FUN_140ee62d0 → FUN_140ee7a90 (a refresh poke). This
  is the client's equipped-item-changed reaction — the natural panel-repoint
  mechanism, the ONLY one that reacts to the equip state itself. Boot P's 403
  delivers two item frames only — no character object — so this surface had
  nothing to consume. Boot C's "subclass icons disappear" = consistent with this
  surface firing (the tile reacting, badly) while the node panel stayed stale.
- FACT 5 — boot P's equip was a proper cross-swap (verified, the server log:
  ws_capture opcode=403 payload soid 0x6000000000000031) — the validation shape
  was correct; the failure is not test error.

THE ONE-LINE VERDICT: the panel never re-renders because the two candidate
surfaces are each gated away from the equip — the item-side notify (FACT 3's
sink) is dead by the inspected-def gate (FACT 2) for any equip that changes the
subclass, and the character-side equipped-diff chain (FACT 4) — the only
mechanism that reacts to the equip state itself — has never been delivered a
correct cross-swap character frame and mapped to its UI reach. All five failed
shapes now make sense (recorded in 12.25).

THE PLAN (the reviewer's bounded actions, adopted):
- PHASE 1 (zero boots, two flash lanes in parallel): LANE K = the kind observer
  per the kind lane's §4 spec (build only) + the equipped-diff UI-reach map
  (FUN_140fa8de0 / FUN_140ee7a90 / FUN_140ee62d0+0xc750 / FUN_140b9e4a0 /
  FUN_140ba9b10) + DAT_141fb5928's writer (panel open? the equip selector
  preview? does anything repoint it on an equip?) + FUN_14186ab10/FUN_14186a9c0
  (the sink's node setters). LANE U = the upstream master state-layer diff
  (fetch the post-6164a3b stanuwu/Sunrise master — the local
  community_forks/Sunrise-master checkout LACKS 6164a3b, verified — diff the
  whole 403 path + state layer: the objects the upstream's equip reply carries
  today, the ordering, the state effects; name where our boot-N frame diverged).
- PHASE 2 (the synthesis, main session): from K+U, design the fix — the leading
  shape: restore the character upsert in the 403 (the serial-port builder,
  8df11ab has it) with the cross-swap equipped-list change guaranteed to
  dispatch to kind-1, keeping the item frames ONLY if the reach map says the
  node panel also needs the socket content. One variable per boot;
  harness-gated before deploy.
- PHASE 3 (the census-intent boot, single, the user's go): the kind hook
  deployed + the cross-swap equip (A→B then B→A), panel watched live. THE
  FALSIFIABLE CLAIM: family=4 def=0xE5E86992 kind=1 at the equip, the
  equipped-diff chain fires, the panel switches. If the chain fires and the
  panel is still stale → the block is downstream of FUN_140ee7a90 and the hunt
  narrows to one surface. THE RIDERS: the veteran fold (restore 47..953's
  second half → ~194 kept), nothing more.

THE DO-NOT LIST (adopted, binding): (1) no more delivery-shape guessing before
the kind hook runs; (2) don't re-add the 0xB74C FNV stamp hack (the pop-ups
stay dead by design; the stamp chain is exonerated — character-scope rebuild,
proven insufficient in boots A/K/L); (3) don't touch the ws_wire hot path (the
boot-K trap — the kind lookup is cool-path, safe); (4) don't chase the
item-side class-4/5 merge for the equip itself (FACT 2 kills it structurally;
the merge gate only matters for picks on the already-inspected def).

## 12.27 THE TWO PHASE-1 LANES LAND: THE MAP RE-DRAWN — THE PANEL'S ENGINE IS
##        UNREACHABLE FROM THE EQUIP, THE UPSTREAM FLOW EXISTS, AND ALL FIVE
##        BOOTS LACKED THE PROMISED REPLY VERSION (2026-08-19 ~23:2x)

BOTH LANES = AUDIT-CLEAN (every load-bearing claim spot-verified on disk this
session before recording — the deltas below = the corrections). THE HEADLINE:
the reviewer's leading hypothesis (the character-side equipped-diff chain = the
panel switch) is REFUTED as the panel switch — the chain pokes the
character-screen equipment tiles, not the panel — and the upstream DOES have a
subclass-equip flow (the 12.25 "no subclass-equip flow" lesson = refuted: the
nine subclass commits bfa37e7→7846599 postdate the earlier partial fetch).
THE NEW LEAD: D1 = the 403's REPLY VALUE — the promised family-4 version —
which ALL FIVE boots never carried.

LANE U (the upstream state-layer diff; deliverable =
RE_output/claims/lane_upstream_state_diff.md, read end to end; throwaway clone
= RE_output/content/upstreamstate_lane/Sunrise-upstream @ f26ef4e, 36 commits
past 6164a3b — the nine subclass commits verified in 6164a3b..HEAD this
session):
- (a) THE UPSTREAM 403 TODAY: one response body = [0] the ws statusPair with
  value = before.family4Version+1 (THE PROMISED REVISION); [1] the Family-4
  frame @ +1 = ONE oodle character object (equippedInstanceSoids[20] + 350
  rows; the 200-byte change bank EMPTY on an equip); [2] if active, Family-0 @
  +1 (the in-place banner character record); [3] if active, Family-3 @ +1
  (character-then-roster). NO item object ever rides the 403. VERIFIED:
  family4_selection_move.cpp:217-274 = "Builds a single-character Family-4
  upsert" (the character encode, the append_object — no item arm).
- (b) THE STATE EFFECTS: after-image swap into the CLICKED row; every moved row
  re-stamped with a fresh serial; the 6164a3b hand-off (displaced keeps the
  mover's prior serial; net counter +2); commit AFTER the frame fits;
  subclass slot → invalidate_ability_buckets() + investment-worker
  request_slice() + the delayed ability re-derivation.
- (c) THE DECISIVE QUESTION = YES: the upstream's character-only 403 IS its own
  subclass panel-update mechanism — the nine commits (bfa37e7 "Make subclasses
  equippable through the standard inventory swap" → 7846599 per-item picks) are
  ALL in 6164a3b..HEAD (verified via git log this session). Upstream's own
  comments commit to the visible result: "the character screen keeps showing
  the previous resolution" without the invalidation; "stops showing the
  previous subclass's resolution" after the worker slice. TWO additions the
  ruling did not price in: (i) THE PROMISED VERSION ("Stage that revision
  before encoding the reply, or the Client completes against the old store" —
  bap_service_body.cpp:213-234, quoted verbatim this session); (ii) the
  family-0/family-3 refreshes + the ability-bucket rebuild accompany the
  character object.
- (d) THE NINE DIVERGENCES: D1 = THE REPLY VALUE (upstream promises before+1;
  OURS never did — see the correction below); D2 = boot-P's two item frames
  carry no character object (boot-N's 8df11ab builder = structurally identical
  to the upstream's); D3 = our +2 two-frame discipline vs upstream's +1; D4 =
  the displaced item appended at the storage tail vs the clicked row; D5 =
  mutate-at-stage vs commit-after-frame; D6 = the hand-off ported correctly but
  the compaction rows not re-stamped + our ability-entries reset (upstream =
  per-item picks, no reset); D7 = our 403 hardwired subclass-only, no 404; D8 =
  the wire schema IDENTICAL (the 200-byte region reconciles); D9 = the refreshes
  = same families, different staging shapes.
- THE CORRECTION (newest-timestamp rule — the lane's phase-4 text says "OURS:
  value = 0"): the actual wire value = StatusResponse{} default = code 0 +
  value INT32_MIN (web_service_envelope.h:33-37 — value{(numeric_limits<
  int32_t>::min)()}; 403 ∈ kStatusPairOpcodes per opcode_routes.cpp:16-22 →
  write_fields encodes code + value as-is, status_fields.cpp:53-56). The
  mechanism claim SURVIVES unchanged: the value = never the promised +1.

LANE K (the kind observer + the reach map; deliverable =
RE_output/claims/lane_kind_reach.md, read end to end; raw =
RE_output/content/kindreach_lane/):
- T1 (VERIFIED): the kind observer = committed as 649be1a on integration (base
  7e94a2b, worktree clean); the pure-observer body = gate_trace_observer.cpp
  ll. 502-525 (spec-exact line, the module-range guard, the batch 13→14); the
  DLL builds clean (/m:1, 0 errors) = 5,794,304 B, SHA-256
  7F9C96F24523ABA4... — NOT deployed (the deployed DLL = 6ef4ea030638cbda
  untouched, mtime 10:54).
- T2 (VERIFIED): the chain's five bodies — FUN_140fa8de0 = the per-slot DIRTY
  BITMAP (svc + (slot>>5)*4 + 0x60 |= 1<<(slot&0x1f), t2_raw:357-358) on a
  mode-2/3 view service; FUN_140ee7a90 = FUN_140ee0510(session+0x5a8) = a
  SNAPSHOT RELOAD; FUN_140ee62d0 = returns &DAT_141fbfff0 (the global session);
  FUN_140ba9b10 = if the changed slot == the currently-viewed slot →
  FUN_140ba9a40(itemSvc, 0x1d) = the inspected-item event-29 refresh. THE
  REACH = the character-screen EQUIPMENT TILES + the item-inspection popup —
  NOT the subclass panel, NOT the loadout grid. The +0xc750 dirty = a
  WRITE-ONLY latch (2 writers — the chain + the event dispatcher
  FUN_140bea9d0 param_2==9; ZERO readers among the session's 36 xrefs; boot-I
  corroborated statically).
- T3 (VERIFIED): DAT_141fb5928 = 33 read xrefs (all the socket-panel family) +
  EXACTLY 2 WRITES — FUN_140b3aea0 (gated param_2 ∈ {4,6}, a registered
  callback on the panel's event id *DAT_141fb5d40, registered in the
  screen-init FUN_140b395d0) and FUN_140b3bed0 (a screen-init) — BOTH mirror a
  state service; NOTHING on the equip path writes it. The reviewer's FACT-2
  gate-death = confirmed at the writer level.
- T4 (VERIFIED): the sink's node setters = per-definition handler dispatches
  that render THE SUBCLASS PANEL'S SOCKET NODES (the "m_slot_number" widget
  property, t34_raw:759/1010) — the panel's only known live-update engine,
  reachable ONLY through the notify gate.
- K's OPEN ITEMS: (iii) the sink FUN_1412f3660's full xref set (who else calls
  the engine besides the gate); the writer-1 event's concrete trigger (which
  UI action raises *DAT_141fb5d40 with mode 4/6 — candidates: the panel's
  preview/select events); the +0xd8/+0x438 node tables (runtime-resolved).
- MINOR: (a) the deliverable's t2_raw line-cites are drifted (the claims
  verified by content/header anchors this session — the raw's regions differ
  from the cited lines; future readers: trust the "=== DECOMPILE ... ==="
  headers); (b) a mojibake byte sits in the new comment at
  gate_trace_observer.cpp:~500 ("THE COOL PATH �?") — cosmetic, clean on the
  next touch; (c) K's tooling note: inline bash commit messages truncate
  mid-string (the first two commit attempts left "The") — write commits via a
  .sh script in C:\Users\rasla\AppData\Local\Temp\opencode\.

THE SYNTHESIS (INFERRED, the re-drawn map + the revised theory):
- THE PANEL'S LIVE-UPDATE ENGINE = the socket-node sink, gated by the
  changed-def == inspected-def compare, and nothing on the equip path ever
  repoints the inspected def (K's writer-level proof). The character-side chain
  pokes the tiles, not the panel. BOTH candidate surfaces = mapped dead for the
  equip — the reviewer's two-surface verdict holds, and the "chain = the panel
  switch" extension is now refuted.
- THE REVISED THEORY (the new lead): the upstream's equip works because its
  delivery = a COMPLETE transaction — the promised reply version (D1: the
  client's completion token — "or the Client completes against the old store")
  + the character frame + the refreshes + the ability-bucket rebuild. The
  client, completing the optimistic equip against the right revision, runs its
  own post-equip reactions — plausibly including the panel re-bind through the
  panel's OWN lifecycle event (K's writer-1 mode-4/6, the preview/select
  event), which would repoint the inspected def and open the notify gate.
  ALL FIVE boots violated D1 — the census watched the frame apply and never
  the transaction completion. THE PANEL'S LIVE-UPDATE KEY, candidate-named:
  the optimistic transaction's completion, whose token = the promised version.
- THE AMENDED PHASE-3 (per K's amendment + U's named shape): the fix = THE
  UPSTREAM-EXACT 403 — (i) statusPair.value = the staged family4Version (+1,
  promised before the frame); (ii) the character upsert restored (8df11ab's
  builder = the right shape); (iii) the +1 single-frame discipline (drop the
  synthetic +2 two-frame); (iv) the displaced item placed in the CLICKED row
  (D4); (v) the ability-bucket invalidation + worker wake verified on our
  equip. ONE variable = the delivery contract. THE KIND HOOK rides (the
  per-object census; the item's kind 2-vs-3 for free). THE FALSIFIABLE CLAIM:
  family=4 def=0xE5E86992 kind=1 at the equip, the transaction completes (no
  kick/rollback), and the panel shows the new subclass live. THE NEGATIVE
  (still decisive): if the panel stays stale with the completed transaction →
  the block = the panel re-bind surface (K's open items: the sink's other
  callers + the mode-4/6 event trigger) — a named one-surface hunt.
- THE PENDING ARBITRATION (the user): the fix scope (the upstream-exact 403 =
  recommended) + whether the two cheap zero-boot lanes (the sink-xref pass +
  the mode-4/6 trigger hunt) ride in parallel with the implementation.
