# TOOLS - the reusable instrument registry

STATUS: live (2026-08-26). Census-driven: every general-purpose tool in
RE_scripts, what it is FOR, and its self-test where one exists.

RULE: before any lane forks a scanner/dumper/parser, check this table first.
Generalize an existing tool instead of adding probe_NN.py - the forks that
motivated xref_scan.py (lane_svc43_xref_scan.py, hashtable_xref_scan.py,
iv_writer_hunt3+) are the cautionary record. A new CORE entry should carry a
self-test/oracle per the project's "give every reader an oracle it can fail
against" rule.

## Meta / workflow layer (this branch)

| Tool | For | Check |
|---|---|---|
| RE_scripts/db_hygiene.sh | opencode DB report/vacuum guard | refuses while a session is live; dry-run default [caps: db-maintenance, vacuum-guard] |
| RE_scripts/build_index.py --root X | regenerate indexes for any checkout (claim refcount corpus = root docs + claims + docs/handoffs + docs/boots + findings/, 2026-09-05; FINDINGS deduped across locations by newest mtime; --root rebinds the corpus too) | entry count == grep '^## ' totals; synthetic-tree test: same FINDINGS at root+findings/ parses once, handoff/boot citations un-orphan a claim |
| RE_scripts/logindex.py | index ANY sunrise.log-shaped logs -> SQLite (ev/stage/kv per line) | --selftest; event count == parsed-line count [caps: log-indexing] |
| RE_scripts/logq.py | query a logindex: --ev/--stage/--fn (parsed fn field - the mtrace field order lies to raw greps, T3.3)/--grep/--source/--range/--tail/--aligned; --aligned LOUDLY refuses silence when the .drift.json is missing (the hint names merge_timeline); --bare prints t+message without the path prefix (T3.2) and is the DEFAULT under --grep (--full restores citation); --tail prints a TRUNCATED marker (empty-mask #9); --help works AFTER the positional (3.1) | exit 1 = clean no-hits (a result, not silence); TRUNCATED line names N of M [caps: log-query, cross-machine-align] |
| RE_scripts/loggrep.sh | zero-infra locate (full-width, live-file warn, file:line cites); PINNED to /usr/bin/grep (T1.1: the interactive-shell grep can be a ugrep wrapper blind to RE_build//RE_output) | warns when target modified <60s [caps: log-locate] |
| RE_scripts/sgrep.sh <pat> [path...] | shadow-proof repo search: execs /usr/bin/grep with -r -n -a (binary-as-text), so it CANNOT inherit the ugrep/.gitignore blindness (T1.1 - two published false claims). Default paths RE_build RE_output | blind-grep simulation must flip bootstrap_check's proof to LOUD (self-test 2026-09-05) [caps: repo-search, shadow-safe] |
| RE_scripts/bootstrap_check.sh | session-start instrument: budgets, STATUS lines, SEARCH-TOOL PROOF (bare grep vs /usr/bin/grep count on the newest archive - LOUD on blindness), SPECIFIED INSTRUMENTS grep (empty-mask #8), index liveness | always prints BOOTSTRAP OK last [caps: session-bootstrap, doc-budgets, search-proof, instrument-specs] |
| RE_scripts/gate_boot.py <brief> | mechanical pre-boot gate: the original 6 sections PLUS the v3 brief-tie fields (PRIOR ART with q.sh queries; STATE READERS rejecting 'inferred from'; EFFECT CLAIM; ABANDON OUTCOME; FIX SURFACE server/client with the U18 SERVER-SIDE GAP requirement) and, when INSTRUMENTS are declared, READOUT TRIGGER (prior-log cite or NEVER-OBSERVED), OBSERVER BUDGET, CALL FREQUENCY, HOOK COUNT (== verify_hook_rvas' TARGETS TABLE verified count - the true hooks-installed number), INSTRUMENT LIVENESS in brief-named SOURCE files | --literals fails loud on missing literal in deployed binary; third-branch streak >= 2 on the brief's FRONT refuses without a MODEL REVIEW section [caps: boot-gate, brief-tie, outcome-ledger] |
| RE_scripts/boot_outcome.py | outcome-ledger recorder: one JSONL record per boot (boot_id/front/outcome_class) to RE_output/map/boot_outcomes.jsonl; consumer is gate_boot's third-branch counter (empty-mask #6) | duplicate boot_id refused without --force; prints the front's trailing third-branch streak after recording [caps: outcome-ledger] |
| RE_scripts/registry_audit.py | registry rows vs tool reality BOTH DIRECTIONS: TOOLS.md row -> tool (caps exact-match) AND tool's # REGISTRY line -> TOOLS.md row (reverse drift); plus the router-file check (AGENTS.md pathed references must exist - the dump_search class - and be git-tracked) | names drift with file:line; exit 1 on any [caps: registry-audit, tree-direction, router-check] |
| RE_scripts/verify_hook_rvas.py | resolves EVERY client-hook RVA constant against .pdata and fails if it is not a function START. MANDATORY before any boot shipping a new or changed hook address. Born from p2(112): profile_harvest shipped 0x1A6040 for 0x1417A6040 (dropped digit); the bad value still sat inside the module so the observer's `module_range` check PASSED and the detour attached to an unrelated function - `install result=ok`, zero fires, through a Tower dwell + subclass swap + full character switch. A range check proves an address is IN the image, never that it is the RIGHT one. 2026-09-05 (T1.4): ALSO checks the INSTALL TABLES (shared parser hook_targets.py) - declared size vs initializer count, null/rva-0 entries; the 09-05 FAILURE 2 build (value-initialised {nullptr,0} tail detouring RVA 0) PASSED the constants scan and is now caught on paper. Declared-but-unused constants surface as an advisory. `RE_HOOKS_DIR` env overrides the tree for testing | prints per-RVA verdicts + `TARGETS TABLE: n/n entries verified, N bad` (the gate's HOOK COUNT ties to THIS line); exit 1 on any FRAGMENT/MID-FUNCTION/UNRESOLVED or table problem. Addresses past their nearest .pdata entry are reported NOT-CODE (data constants) and listed for review, not failed [caps: hook-rva-verify, install-table-check] |
| RE_scripts/hook_targets.py | parse the client's INSTALL TARGET TABLES (the source of truth for what actually gets detoured): declared size vs initializer count, per-entry name/rva resolution, commented-out entries correctly excluded (the retired image_set row), declared-but-unused constants. Shared by verify_hook_rvas.py + preflight.py | standalone run prints tables + problems; the 47/47 real table parses clean, and a temp tree with kTargetsSize=48 (the historical bug) FAILS [caps: hook-targets-parse] |
| RE_scripts/probe_audit.py | THE INSTRUMENT-SOURCE GATE (the 09-06 BLIND-GUARD conversion - instrument changes need the same gates as shipped code). Scans the client hook sources for the four shipped defect classes mechanically: (A/R1) an alignment guard with no citation of the SUBJECT field's decoded alignment - the guard that refused the gate's own +0x57C/+0x94E blobs for five boots; (B/R4) a novelty signature that is a plain XOR of two fields - it collides to zero exactly on MATCHES, the event the probe exists to see; (C/R6) a lookup-class install row with no LEAVE-side coverage - a lookup's outcome is on its return; (D) DUPLICATE RVAs in the install tables - dual detours on one function start (the 09-06 rig freeze), waivable in source with `DUAL-OK: <reason>`. Writes RE_output/map/probe_audit.json, the freshness token deploy_client_dll.sh refuses a client without | `--selftest` runs synthetic hook trees for every arm, the negative arms built from the SHAPES OF THE SHIPPED DEFECTS; prints PROBE_AUDIT SELFTEST: n/n PASS. exit 1 on any finding [caps: probe-audit, instrument-gate, deploy-refuse] |
| RE_scripts/replay_trigger.py <log-or-dir> --pred <fixture.py> | THE TRIGGER-REPLAY HARNESS (TOOLING_AUDIT M1): replay a probe's trigger over a RECORDED log before deploying it - a recorded run is a fixture. Stateful (`class Trigger.feed(ctx)`) or stateless (`def check(ctx)`) fixtures over parsed k=v lines with `t=` extracted. Answers "how many times would this have fired, and when" | exit 1 on ZERO fires (a trigger that never fires on recorded data is a boot-killer - p2-160); runaway guard aborts at --window; ACCEPTANCE PROVEN: the re-arm fixture (RE_output/map/fixtures/rearm_ack_trigger.py) replayed over p2-175 + p2-176-runA shows EVERY withdrawal window closed with acks=1, never 8 - the N=8 threshold was unreachable by construction, on data that predated the build [caps: trigger-replay, fixture-replay] |
| RE_scripts/preflight.py | THE WORLD GATE (enforcement-layer #2): machine checks, separate from the document gate - deployed==built hashes (client+server), client DLL EXPORT COUNT vs manifest (C5), settings+backups, server process + port listeners, hook-table arithmetic + detour FOOTPRINT via .pdata (08-30 C2), --brief INSTRUMENTS literals in the DEPLOYED binaries, rig hash via ssh certutil (best-effort WARN). `--record` (re)writes RE_output/map/instruments.json - run after every intentional deploy; check mode FAILS on hash drift, export drift, or a binary NEWER than the manifest | live run 2026-09-05: PASS 13 ok/0 fail (rig == mac == built 3cc844c4); negative arms: tampered manifest rc=1, stale manifest FAIL, absent literal FAIL, missing settings FAIL [caps: preflight, env-gate, instrument-manifest] |
| RE_scripts/incident.py | incident digest: DB events + logs + procs + hashes | prints LIVENESS counts; exit 1 if root-bound sections all empty [caps: incident-digest] |
| RE_scripts/q.sh <term...> | one-name recall over indexes + raw archive | falls back to raw grep if index missing |

## Static-analysis core (destiny2_unpacked_full.exe)

| Tool | For | Oracle/self-test |
|---|---|---|
| RE_scripts/pe_reader.py | PE section map VA->file offset; imported by 8+ scripts | basis of every oracle below passing [caps: pe-parse, section-map, pdata-bounds, static-read] |
| RE_scripts/struct_view.py | THE STRUCT FIELD ANNOTATOR (tool-brief-struct-view.md): dump + VA -> annotated field walk joining field_xref (post-T1.3), the reconcile function map, and beacons.json per displacement, with pointer classification (module/other-mod/heap/small-int/ascii) and bounded pointer follow (the 20.276 base-alias caveat printed). COVERAGE HONESTY: the forbidden-phrase rule - only NOT-SCANNED / UNCOVERED / SCAN-COMPLETE-n-hits, never "no writers"; every hit a CANDIDATE; disp8 hits UNVALIDATED by default (validate=True costs ~44s/disp - measured) with opt-in --validate-disp; RENDER-CAP rule (--max-rows 40, TRUNCATED marker, counts exact). Static or runtime VA input; static<->runtime via the dump's destiny2 module | --selftest 12/12 (17s: field_xref-oracle join equality, ff/2 passthrough, coverage-honesty arms, owner+naming joins, classification unit); real-data: registry-global known-value oracle exact in BOTH dumps + cross-dump structure consistency; refusal arm rc=1 [caps: struct-annotate, dump-field-view, field-xref-join] |
| RE_scripts/xref_scan.py lo hi | **TRAP (20.291 R9.3): its `[unknown]` hits are UNJUDGED CANDIDATES, not references** - `mov qword [rcx+0xC00],0` was reported as a ref into the dispatch table because the immediate parsed as a rip-relative displacement; the tool says "judge by context" and that must actually be done. GENERAL xref scanner: rip-relative code refs (BOTH .text sections) + --ptrs data-pointer mode (vtables/callback tables) | `--self-test`: getter 0x1404DC070 -> table 0x141F91AD0 (re-verified PASS 08-31 post-fix); pen's hand-read ref 0x140B47D22 now found |
| RE_scripts/disasm_fn.py va | disassemble one pdata-backed function to its exact end. **TRAP (20.291 R6): ANCHOR FIRST.** Passing an address that is not a function start produces a plausible-looking but FALSE instruction stream - the participant-image format spec was retracted because 0x1404f4740 is +0x40 inside 0x1404F4700 and the invented decode contained a `mov ebx,0x6c38` that is nowhere in the binary. Run pdata_bounds.py on every address before disassembling it. 2026-09-05 (T3.4): a .pdata GAP is a REFUSAL (rc=1) with the linear-disassembler handoff (lane_svc43_disasm_range.py), not a warning - the 0x1404DD470-class leaf getters used to print an invented stream | gap VA refuses rc=1 (0x1404DD470 tested); pdata-backed VA disassembles rc=0 [caps: function-disasm, gap-refuse] |
| RE_scripts/lane_svc43_disasm_range.py a b | LINEAR disassembly of a VA range, no early stop | n/a - use for raw ranges |
| RE_scripts/needle_scan.py | byte-needle search: minidump (`--dump`), static PE (`--pe`, default RE_output/destiny2_unpacked_full.exe), raw file (`--any`), and the STATIC CONSTANT SITES scan (`--const` - "who references constant X in .text"; the 0x2AC0 stride-scan class, M2). Parameterized 2026-09-05 (T1.2: was hardcoded to a nonexistent Windows path while registered as general) | `--selftest`: u32/raw parse + live stride 0x2AC0 (>=1 site) + a VERIFIED-absent constant (0 hits, loud) [caps: needle-search, dump-sweep, static-const-scan] |
| RE_scripts/hash_table_dump.py / hashtable_xref_scan.py | hash->ptr table dump + refs into it (the generalized form is xref_scan.py) | known-entry check |
| RE_scripts/field_xref.py <hex-disp> | STRUCT-FIELD xref: every .text access to `[reg + disp32]` AND `[reg + disp8]` (mod=01, added 08-31), classified read/WRITE/RMW. Answers "who touches field +N" — the complete x86-64 displacement coverage: disp32 (mod=10), disp8 (mod=01), --sib-scan (mod=00 SIB no-disp), and xref_scan (rip-relative). Disp8 hits are boundary-validated by linear disassembly of the enclosing function (pdata bounds) — the 0x1416F43F6 false-positive class is eliminated. 2026-09-05 (T1.3): the ff/fe group is split by ModRM.reg — `ff /2` is a VTABLE CALL, labeled CALL-INDIRECT, NEVER "write/RMW" (the old column nearly produced "nothing writes +0x38" from a scan that could not have seen a writer); a small displacement ALWAYS prints the DISP8-COVERAGE-REQUIRED line, not only when disp32 came up empty. TRAP (20.276): scan the field under ALL its base aliases — a record field read via a sub-object base (+0xA8) spells a different displacement | `--selftest`: 3 known disp32 accesses + 3 known disp8 confirmed hits (0x1416E9CF0/0x1416EBD51/0x14171812A) + the 0x1416F43F6 false positive correctly rejected + negative displacement yielding 0 writes + synthetic ff/2=CALL-INDIRECT, ff/0=RMW, fe/1=RMW, 89=WRITE arms (PASS 2026-09-05) |
| RE_scripts/callers.py <va> | DIRECT-caller enumeration: every E8/E9 rel32 in both .text sections targeting the VA, each site resolved to its owning function (fragment chains followed). Generalizes the ad-hoc caller scans of 20.271/20.273/20.276. Blind spots (stated in --help): indirect calls (vtables, function pointers — pair with xref_scan --ptrs), VMP regions | `--selftest`: must find the documented edge site 0x141769295 -> 0x1417692E0 owned by 0x141769230..2D4 (FINDINGS 20.271 R2) and classify it 'call' (PASS 2026-09-03) [caps: e8-callers, callgraph-enum] |
| RE_scripts/codec_spec.py | THE EXECUTABLE CODEC SPEC VALIDATOR (tool-brief-codec-specs.md): declarative specs generating BOTH decode and encode, validated per spec: T3-P2 recorded fixtures, T3-P1 seeded round-trip property (1000 rounds default), T3-F1 single-bit-flip fault localization (declared padding + beyond-consumed bits excepted - a flip there decoding identically is the truth about padding), T3-F2 declared truncation semantics, T3-F3 const/nonzero rejection AT ENCODE, T3-F4 naive-variant arms (deliberately wrong implementations must FAIL - the guard-fails-pre-fix rule applied to the tests), T3-P3 femu cross-check slot (via femu_decode; specs carry cross_check_note substitutes until the VA+sig is wired). Three shapes: byte-layout (u8/u16/u32/u64 + absolute-bit optional fields), bit_stream (the ent-wire MSB-first grammar with decode params), native (WRAPS name_codec - never a fork). Specs: RE_scripts/codec_specs/<name>.py. NOTE: package named codec_specs (stdlib codecs collision) | --validate all 4 specs: name_cipher 6/6, join_request 6/6, anchor_codec 6/6, payload_kind2 5/5 (2026-09-06); --decode/--encode CLI; naive-variant arms prove the fixtures can fail [caps: codec-spec, round-trip-property, femu-crosscheck] |
| RE_scripts/slice_back.py <site-va> | THE BOUNDED BACKWARD SLICE (tool-brief-slice-back.md): "where does this field's value come from" - pdata-gated (gap = refuse + linear handoff; mid-instruction anchor = REFUSED), disassembles from the FUNCTION START (never the site), walks backward over the finite instruction list, classifying every value source: BOUNDARY:PARAM / MEM (+disp, joins field_xref/struct_view) / STACK / GLOBAL / CALL-RETURN (indirect calls named "(indirect)", no guessed callee; --follow-calls v1 names only, descent is v2) / CONSTANT / BOUNDARY:LOOP-CARRIED (only edges whose loop body writes a traced reg; capped to the 4 nearest the site with a TRUNCATED marker). Budget: --max-insn with a loud SLICE TRUNCATED marker (T3.1). RENDER-CAP rule applied to loop marks | --selftest 8/8 (synthetic boundary-class completeness, no-silent-descent, budget truncation, real pdata gap refusal); real run: the 0x140E2B405 write site slices to CONSTANT 1 + PARAM rcx + capped loop-carried marks (2026-09-06) [caps: backward-slice, provenance-boundary] |
| RE_scripts/callgraph.py | THE CALLGRAPH DB (tool-brief-callgraph-db.md): build + query over RE_output/map/callgraph.db (gitignored cache). direct edges = the callers.py scan loop + owner_of (imported); vtable/callback slots = xref_scan.scan_ptrs (the dual-encoding engine, 20.209 R2) with targets NORMALIZED to static VAs at build (a target-keyed query finds both encodings; the encoding string keeps provenance). Direct edges by construction only record E8/E9 sites (the immediate-parse false-edge class cannot become an edge). QUERIES CAPPED at 40 rows with TRUNCATED markers and exact counts (the RENDER-CAP rule; an undiscriminated list is the decoration failure). BUILD TRIGGER from the brief was overridden by direct user instruction - recorded in the claim | --selftest 5/5: the documented callers edge; THE ENT FIXTURE (4 blocks x slot 10 = exactly the 4 ENT handlers, via RUNTIME_BASE-relocated-pointer normalization); planted-false-edge detection; both-encoding counts. DB: 184,680 direct edges + 136,457 slots (108 absolute vs 136,349 relocated - dual encoding was load-bearing) [caps: callgraph-db, vtable-edges, reachability] |
| RE_scripts/name_codec.py | THE PROFILE-BLOCK NAME CIPHER, BOTH DIRECTIONS (20.179 R1 / 20.195). Reader: plain[i] = key16(i) ^ ((wire[i]*0x7b4f)&0xFFFF); writer inverts the multiply with 0xBBAF. `--encode` builds wire words for a name, `--decode` reads them back, `--try-both` prints a captured buffer BOTH ways so a dump of unknown form (wire or stored) is told apart by which reading is legible. It lives outside the game so a name can be checked without a boot - p2(123)'s expected bytes were computed here and registered in the boot brief BEFORE booting, then matched exactly. TRAP: key16(0)=0 is a SPECIAL CASE, not a rotation result - at i=31 the rotation is by ZERO and key16(31)=0xB0C4. Names 31+ words long break if that is collapsed | `--selftest` 10/10: inverse is a true modular inverse, the index-0 special case is distinguished from the i=31 zero-rotation, 64-word round trip, empty-name-stores-zero, terminator-stores-key16(L), string round trip, and encoding is not the identity [caps: name-codec, encode-decode] |

## Crash / runtime triage

| Tool | For |
|---|---|
| RE_scripts/log_archive.sh [--label ID] | BOOT-LOG CAPTURE + BACKUP: archives mac/rig/server logs into RE_output/logs/<ts>/ with sha256 + manifest (the fix for losing all-but-the-current boot's client logs). Rig fetch best-effort via ssh (loud on unreachable). Wired into boot_verdict + both deploy scripts as triggers | dedupe skips identical logs; LIVENESS line; exit 1 on degenerate [caps: log-archive, rig-fetch, dedupe] |
| RE_scripts/minidump_parse.py | CRASH TRIAGE ONLY - names the faulting module from a minidump. Does NOT read dump memory: for that use minidump_reader.py (or femu --graft-dump) [caps: minidump-parse] |
| RE_scripts/negative_audit.py | SCAN-NEGATIVE HYGIENE, two-tier (2026-09-05): TIER 1 stale-premise (predates an invalidating encoding fact - actionable, exit 1); TIER 2 undeclared-ENCODEDS (migration backlog - visible, exit 0); retraction/correction-QUOTING text auto-waived visibly (the flag used to hit the retraction of the claim it kills). Pointer-class matching is word-boundary based (bare "ref" matched "prefer", backlog 3.5). Root HANDOFF*.md in the corpus. The 20.209 R2 class - a tool's coverage limit promoted to a world-fact | 13 waived / 0 active on the real corpus (2026-09-05); synthetic tier1/tier2/quoting/glob arms [caps: scan-negative-audit, invalidation-scan] |
| RE_scripts/minidump_reader.py | THE DUMP-MEMORY WORKHORSE: lazy seek-based reader for full dumps (6GB+ fine) - read_va/module/ranges, CLI: summary, `--read VA COUNT`, `--rva RVA COUNT` (RVA = static - 0x140000000). The engine under femu --graft-dump. Verified on dump_p2146.dmp (6.49GB, loads in 0.0s) [caps: dump-read, module-lookup, range-list] |
| RE_scripts/dump_search.py | SEARCH a full dump's memory for u64 values / byte patterns / ASCII across every committed range; hits reported as base-relative RVAs with optional hexdump context. Uses minidump_reader.py as its engine - it adds ONLY the scan, format handling stays there. THE ANSWER TO RUNTIME-BUILT STRUCTURES: found the single pointer to ent_recv in 6.49 GB (20.222), which proved 20.209 R2's "the entity chain is statically unreachable" WRONG. TRAP: destiny2_unpacked_full.exe stores RUNTIME-relocated pointers (base 0x7FF6AF7F0000), so a qword search for a static VA finds nothing - search 0x7FF6AF7F0000 + rva. Scenario-scoping applies: a dump only answers about states the process actually entered (20.221 R5). --selftest: must find "player_broadcast" AND land it at base+0x1CA1518, matching 20.209 R3's independently recorded static VA (PASS 2026-08-31) [caps: dump-search] |
| RE_scripts/diff_minidumps.py / diff_captured_tails.py | before/after artifact diffs |
| RE_scripts/merge_logs.py / merge_timeline.py | unified timeline across client+server logs; N-source (2026-08-27: typeless-client grammar fix, server-pivot default, size-window pairing w/ distinct-size guard). 2026-09-06 (two-client era): DUAL-ORIENTATION anchor attempt, unmatched-tail tolerance, size-window SIGN fix (the delta was used with a flipped sign - every size-window-anchored client's unified times were mirrored), min_distinct_sizes 5->3, and PER-LEN CHAINS: cluster per body-length with a drift-rate bound (a client stall moves the offset as a step; rare types' wild deltas shred global clusters - the per-len chain locked mac/rig at sub-second offsets where all global attempts mislocked). --selftest: 20 checks incl. typeless-fixture phase, re-run PASS after each change |
| RE_scripts/live_console.py / retail_view.py | Layer-1 triage windows on live log streams |
| RE_scripts/rig_dll_helper.py | REMOTE-SIDE DEPLOY HELPER (run ON the rig): backup/verify a deployed DLL - SHA256 assert + literal presence. The remote half of deploy_client_dll.sh. NOT a memory reader - rig live-memory reads = run the live_read_*.py / dump_sunrise_memory.py scripts on the rig via ssh | backup makes .bak_<stamp>; verify asserts hash + literals, exits nonzero on failure [caps: deploy-backup, deploy-verify] |
| `tcpdump -i en0 -s0 -U -w X 'udp port 3097 or 3074 or 3075 or 30976'` | THE PEER-CHANNEL INSTRUMENT (20.144). Client-to-client traffic never reaches the server and the client log records only counts, so a pcap is the ONLY way to see it. No sudo: this user is in `access_bpf`. Capture ALL FOUR ports and CLASSIFY BY FLOW PAIR - the peer channel's mac-side port is NOT fixed (20.167: mac:30976<->rig:3097 carried all 2386 channel packets; a 3097-and-3097 filter matched ZERO). Steady state is DTLS-encrypted; read it by SIZE and CADENCE, not contents. LIVENESS (earned 20.166, hard): the "received by filter" counter is BOGUS on this mac (counts noise on empty nets) - prove an empty pcap is a real null by sending a probe packet (e.g. python socket to 127.0.0.1:3097) and confirming capture; and check `route -n get 192.168.1.136` for the rig-traffic egress interface FIRST (08-29: en0, not en13) |
| RE_scripts/capture_bap30975.sh <seconds> | THE BAP-WIRE INSTRUMENT (20.145). Captures tcp/30975 on BOTH en0 (rig client<->server) and lo0 (mac client<->server, loopback) into RE_output/captures/bap30975_<ts>/. No sudo. For a CLIENT-HOSTED fireteam, have the MAC client host (a rig-hosted session's client<->client stream never crosses the mac). Bodies may be session-sealed; framing/sizes still readable, and the fork's bap_listener framing is the decode reference |
| RE_scripts/bapdecode.py | pcap -> BAP-frame -> type-12 membership decode (tshark reassembly, svc25/26 key recovery, AES-GCM, region walk). Generalized from the GAH-REGION-DECODE lane's scratch decoders. Interpreter: miniconda python3 (needs `cryptography`) | --selftest (12 checks: planted-session positives + HMAC/tag/size negatives that must fail); --verify against the 224-body ground truth (RE_output/scratch/gah_en0) - 224/224 + full pcap run 2026-08-28 [caps: pcap-decode, bap-frame-decode, key-recovery, membership-decode] |
| RE_scripts/femu.py | FUNCTION EMULATION RIG v2 - THE BOOT-SAVER: execute one client function (or sequences via the Rig API) against the real binary + a dump of real client memory, WITHOUT a boot. Dump-backed demand paging, .data rebase + read-taint, CRT whitelist, mapping sanity, fault diagnostics (RIP + trace). Interpreter: miniconda python3 (`unicorn` installed 08-29). 2026-09-06: call() gained OPTIONAL stack_args (5th+ MS-x64 args, ent-cluster convention) - None is byte-identical to the old behavior | --selftest 19 checks (no-dump + dump modes); ACCEPTANCE: the type24 decode loop ran the client's svc22 apply decoder against dump state with zero scaffolding (RE_output/scratch/femu_v2_acceptance.py) - its recorded 0x10130 fault expectation is STALE post-d561a40 (it was the shifted-pull bug's symptom; the fixed engine returns rax=1; see the femu_decode row) [caps: function-emulation, demand-paging, data-rebase, crt-whitelist, purity-classify, fault-diagnostics] |
| RE_scripts/femu_batch.py | batch purity/behavior classification over the 91k-function spine; verdict enum + DirtyGuard (image writes restored from file bytes) | --selftest 6/6; real run: 100 fns in 0.0s (9 pure / 10 import / 79 state-dependent / 2 exceptions) [caps: batch-classification, dirty-guard] |
| RE_scripts/femu_decode.py | THE DECODE SCAFFOLD (tool-brief-femu-decode.md): one command for "what does the client's own code decode this blob to" - pdata gate (GAP + non-start REFUSED, --force names the offset), --sig arg marshalling (buf/len/out placeholders), --stack-args (5th+ MS-x64 args, the ent-cluster convention), --reader-init (the ent bit-reader preset), faithful provenance report (holes -> UNTRUSTED rc=2; rebase_reads; imports; pages_pulled). INTERPRETER: miniconda python3 (unicorn), fails LOUD under /usr/bin/python3. Exit codes: 0 ok / 1 refused / 2 untrusted / 3 not-returned (the reason IS the finding) / 4 selftest | --selftest 12/12 (synthetic xor decode oracle, determinism, gap+non-start refusal, import abort, crash diagnostics, hole/rebase classification, stack-args frame, reader-init); CLI equivalence vs direct Rig on the svc22 scenario byte-identical (2026-09-06). NOTE: femu_v2_acceptance.py's recorded 0x10130 fault is STALE - it encoded the pre-d561a40 shifted-pull bug's symptom; under the fixed engine the svc22 decode returns rax=1 (4 pages, 2 rebase reads) and tool==engine exactly [caps: femu-scaffold, decode-run, abi-args] |
| RE_scripts/reconcile.py | corpus <-> spine join -> RE_output/map/function_map.db: every hex/FUN_ citation in FINDINGS/claims/STATE mapped to its ENCLOSING function, names only from explicit patterns; incremental by file hash. Interpreter: /usr/bin/python3 | --selftest 9/9 (mid-func mapping, data classification, dedupe, negatives); real: 9,748 citations -> 1,671 functions, 488 names [caps: corpus-reconcile, function-map, incremental-scan] |
| RE_scripts/funcq.py | query the function map: what does the project know about function X (names/citations/neighbors); --coverage scoreboard; --corpus search. Interpreter: /usr/bin/python3 | cited baseline 1,671/91,445 (1.83%); big-tier 12%, mid 3.1%, small 1.4% [caps: function-query, coverage-report] |
| RE_scripts/beacons.py | string-beacon census: 14,041 ASCII/UTF-16 strings extracted, .text swept for rip-relative refs (10,406 refs -> 3,669 functions with naming evidence). Interpreter: /usr/bin/python3 | --selftest: POSITIVE client strings (privacy-mode, peer-creating, tried-to-join-self, managed-session-start) present once; NEGATIVE fork-side tags (ev=steamnet) absent; disasm spot-check 39/40 [caps: string-extraction, xref-sweep, naming-evidence] |
| RE_scripts/night_pull.py | pop N items from RE_output/map/queue.json -> night-lane brief (723 seeded: 123 dark heavyweights, 400 beacon-rich mids, 200 smalls) | morning --done marks; brief carries status-mark + quarantine rules [caps: queue-pull, brief-generation] |

### WHEN TO EMULATE VS BOOT (the femu accuracy contract - read before
### spending an hour on a paired boot for a question femu can answer)

femu executes the binary's REAL instructions, so fidelity depends only on
whether the function's WORLD is available:

- **EXACT (boot-grade - use femu, not a boot):** pure-compute functions.
  Hashes, codecs, bit-unpackers, crypto primitives, framing, enum lookups.
  Deterministic and identical to a boot; repeatable with no state pollution.
- **FAITHFUL-TO-DUMP (use femu, note the scope):** functions over captured
  runtime state. Execution is exact against the dump's snapshot; the answer
  is "against THIS state at that moment". Check the result flags:
  `rebase_reads` (answer depends on the rebase heuristic), `holes` (dump did
  not capture the memory), `pages_pulled` (provenance).
- **NOT EMULATABLE (fails loud, classified):** imports beyond the CRT
  whitelist (D3D/Steam/OS), threads/sync/exceptions, VMP-residual regions,
  state the dump did not capture. The abort reason IS the classification.
- **BOOT REQUIRED:** emergent behavior - state machines across systems,
  timing, live multi-client interaction, anything past the snapshot moment.

Rule of thumb: "what does this code COMPUTE" -> femu first (seconds).
"does the CLIENT do X in a live session" -> that is a boot.
Uncertain which tier? `femu_batch.py` classifies candidate functions cheaply
before you commit to either.

## Deploy / boot pipeline (canonical - do not fork deploys either)

| Tool | Gates inside |
|---|---|
| RE_scripts/deploy_p2d6_gameplay.sh | drops clients; stages from build; asserts deployed==built [caps: server-deploy] |
| RE_scripts/deploy_client_dll.sh <mac\|rig> "<literals>" | hash assert + literal grep IN deployed file |
| RE_scripts/restamp_build_data.py | identity restamp after server rebuild [caps: identity-restamp] |
| RE_scripts/boot_record.py / boot_diff.py | boot artifact capture + cross-boot diff |
| logged_empty.h (steam/interfaces/tables) | THE CENSUS INSTRUMENT: per-slot logging stub + argument registers, over a table sized PAST the interface. Any boot then returns the real ABI and its argument shapes for free. See LESSONS 18 |
| retail_log_enqueue_observer.cpp | CALLER CAPTURE: _ReturnAddress in the retail log funnel, reported as a module-relative RVA. Turns any log line into a code address (LESSONS 18c) |
| RE_scripts/pdata_bounds.py | resolves any address (RVA or static VA) to its OWNING FUNCTION, FOLLOWING UNW_FLAG_CHAININFO (a fragment entry's begin is NOT a function start - two of p2(74)'s six RVAs were fragments, 20.112) from .pdata RUNTIME_FUNCTION entries - the exact bounds source in a stripped image. The other half of caller capture: an RVA means nothing until it is resolved (LESSONS 18c) [caps: pdata-bounds, function-resolve] |
| RE_scripts/boot_verdict.sh | reads BOTH client logs + the server's and prints the lobby-lane verdict; compares Adding-player xuids MECHANICALLY (own vs peer) because the bare line fires every boot and proves nothing (20.101). v2 `--sigtable <solo-archive> <paired-archive> "sig"...`: the PER-ARM FAILURE-SIGNATURE TABLE (empty-mask #2) - a signature present in BOTH arms is NOT peer-attributable, printed side by side; all greps pinned to /usr/bin/grep | synthetic 4-arm test (BOTH / only-A / only-B / absent) + real-archive smoke (withdrawals+acks fire in BOTH arms = not peer-attributable, 2026-09-05) [caps: boot-verdict, per-arm-discriminator] |
| RE_scripts/reset_lobby_claims.sh | clears the IN-MEMORY lobby-claim table (restarts the server) and re-verifies listeners/nat; run between ANY two runs from p2(67) on, or a stale pairing silently invalidates the test. 2026-09-05: readiness compares the PORT SET (each required port bound?), never a count (DEFECT 7: 30975 binds twice so a healthy server "failed" every run); IDEMPOTENT - server up + claims=0 + set complete = NO-OP that says so (the 08-30 A3 class), --force overrides; --check = read-only preview | live --check against the running server 2026-09-05; port-set grep proven present+absent [caps: server-reset, lobby-claims, idempotent] |
| RE_scripts/decision_log.py | pre-register STATE-CHANGING actions with hypothesis + expected outcome BEFORE taking them (enforcement-layer #4; the 08-30 A3 class - an action with no registered expectation cannot be judged afterwards); `complete` closes the loop, `show` makes OPEN decisions visible | register/complete/show round-trip; unknown id rc=2 [caps: decision-log, action-pre-registration] |
| RE_scripts/project_dashboard.py --serve [--lan] | the operator's second monitor (READ-ONLY), v2: boot SEGMENTS (a client process log spans many boot cycles - re-entered character_select starts a new one; server = pid change), bootflow lanes per client with dwell, DECLARED contract counters (RE_output/map/dashboard_contract.json - the session updates it when the front moves; stale-nag vs STATE.md), wire-type census per source (KNOWN_TYPES map + NOVEL flags), instrument census, warn stream, per-source log blocks (NEVER a fake merged timeline - three different t= epochs), health chips. Degrades gracefully | live 2026-09-06: both lanes surfaced the region hang; segment counters monotonic (the sliding-window 4->7->3 bug dead); server-restart boundary detected before the feed (the wipe-after-feed bug) [caps: project-dashboard, live-tail, boot-segments] |
| RE_scripts/obf_fold.py | constant-folding helper for obfuscated code (self-test passed) [caps: obf-fold, chain-analysis] |

## One-off probes (~172 files under RE_output/claims/*.py)
Deliberately NOT registered. Grouped automatically as PROBE FAMILIES by
build_index.py (INDEX_claims.md bottom); long families = U7 escalation fired
late. Archive candidates after refcount review.

### state_hash_oracle.py (2026-08-30, PARTIAL - do not fork)
`RE_scripts/state_hash_oracle.py` - offline Python model of `lookup3::hash_bytes` and
`build_session_state`, for reproducing or searching a session-state hash without a boot.
The hash and state-model halves are transcribed from source and are correct by
construction. NOT YET USABLE END TO END: its self-test against a logged arm A hash fails
because a real body cannot be rebuilt from logs - `publish_snapshot` echoes each peer's
own 86-byte NetAddr blob byte-exact and the logs carry join descriptors instead.
Generalise this file (feed it a server-dumped replica or body) rather than writing a new
hash script. See RE_output/claims/session-state-profile-image.md OPEN (b).
