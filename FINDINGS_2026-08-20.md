# FINDINGS — 2026-08-20

## 13.1 LANE V LANDS: THE SINK'S SECOND DOOR — THE PANEL'S DEF-CHANGE REPOINT,
##        ROOTED IN THE PLAYER-CLASS RESOLVE (2026-08-20 ~00:3x, recorded ~00:37)

LANE V (the sink-xref hunt, K's open item iii) = LANDED + AUDIT-CLEAN (the
load-bearing claims spot-verified on disk this session before recording — the
pointers below). Deliverable = RE_output/claims/lane_sink_xrefs.md (read end to
end); raw = RE_output/content/sinkxref_lane/ (lane_sinkxref_p{2,3,4,5}_raw.txt
+ the PyGhidra scripts, all generated read-only this lane).

THE ANSWER: the socket-panel sink FUN_1412f3660 has EXACTLY 2 code callers
(verified, lane_sinkxref_p2_raw.txt ll. 3-8 — the xref list: 3 DATA + the 2
calls):
- CALLER A = FUN_1412f0a70 @0x1412F0AE8 (the notify gate) — GATED (session +
  changed-list contains the inspected def + class 4/5). The equip's own apply
  chain = the gate's producer by construction (FUN_140e0a0b0 → FUN_140e01960 →
  FUN_140e01520 → FUN_140e05da0 → the kind-3 FUN_140e080b0 → FUN_140e08670 →
  the gate — each link's only code caller, verified) — the kill = the
  inspected-def compare, FACT 2 stands.
- CALLER B = FUN_140f40e80 @0x140F40EB3 (NEW — the def-change repoint) — NOT
  gated by the changed-list / DAT_141fb5928 / class-4-5. Its body (verified,
  p2_raw): fires the sink when the panel's OWN inspected def (session slot
  +0x1ea4; the sentinel -0x7ee3623b = 0x811C9DC5 = kNoDefinitionHash) differs
  from the incoming record's def (*(param_2 + 4)), then commits the node-state
  record to session +0x1ea0. = THE PANEL'S OWN "the inspected def changed →
  re-render" surface.

THE REPOINT'S ROOTS (all verified in the p3/p4/p5 raws):
- FUN_140dc3960 = THE CHARACTER SPAWN / PLAYER-CLASS RESOLVE callback (the
  strings, verified at p5_raw ll. 70-76: "can't spawn because the
  player_globals doesn't have a default character class. player_globals tag:
  %s" + "can't spawn because the character sheet's player class is invalid...")
  → FUN_1412f1a40 (the socket-state rebuild+commit → the repoint) +
  FUN_1412f3f50 (the inspected-def setter). NO direct code callers (the
  .rdata-registered callback form). THE NATURAL "class changed → the panel
  re-binds + re-renders" SURFACE — the named candidate for the completion's
  UI reaction.
- FUN_1412ed6d0 / FUN_1412f3ed0 = the +0x800 pending-change flag drain/set pair
  (both ← FUN_140fd13b0, the flag driver), each firing FUN_1412f1a40.
- FUN_1412f39c0 = the socket-state tables update + rebuild; FUN_1412f3f80 = THE
  INSPECTED-DEF SETTER (writes the session's +0x840 — the repoint's gate slot;
  DISTINCT from DAT_141fb5928); FUN_140bc1cf0/1cd0 = the refresh-commits;
  FUN_140ca7490/7e90 = the panel mode/refresh handlers.
- NONE of these roots receives a call from the family-4 apply chain (the xref
  sets = disjoint, verified) — statically, the equip reaches only the dead gate.

THE SESSION'S SIDE (the boot-M lane's open item iii, CLOSED at the static
level): FUN_1412e9540's session gate = "the socket-UI service is up" — armed by
the lifecycle callback FUN_1412e9940 → FUN_140414b70(id 0xffffec70, cat 0x20,
size 0x35f0) → &DAT_142f41500 + the record's +0x31 live-flag byte (verified,
p3_raw ll. 360+, p5_raw ll. 1271-1274); release = FUN_1412e81f0 — NOT a
per-equip latch. The gate is therefore live whenever the panel's service runs;
the equip's problem = never the session.

THE VERDICT + THE IMPLICATION:
- The static map now covers BOTH doors of the panel's engine. The second door
  (the repoint) is the panel's own "my inspected def changed" reaction — and its
  deepest root = the PLAYER-CLASS RESOLVE. THE REVISED THEORY, now with a named
  client-side chain: the equip's optimistic completion (D1, the promised
  version) → the player-class re-resolve (FUN_140dc3960, the spawn callback) →
  FUN_1412f1a40 (the rebuild) → the repoint (FUN_140f40e80) → the sink → the
  re-render. INFERRED — the completion → spawn-callback link is exactly what
  the static map cannot settle.
- THE PHASE-3 BOOT = unchanged in design, now sharper: its positive case names
  the chain; its negative case (the stale panel despite the completed
  transaction) = TWO NAMED HOOK SURFACES — instrument FUN_140dc3960 (the spawn
  callback) and FUN_140fd13b0 (the +0x800 flag driver), a one-hook census each,
  no guessing.
- OPEN micro-items (for later lanes): the sink's .rdata address-takes
  (0x141E69A68/0x141E69AA0 — which pointer table, any indirect caller?); the
  +0x1ea0/+0x1ea4 slot vs DAT_141fb5928 (TWO inspected-def slots with different
  writers — which one the node renderers actually consume).
- LANES X (the implementation) + W (the event trigger) = still running.

## 13.2 LANE W LANDS: THE PANEL'S RE-BIND EVENT = A USER-SELECTION NOTIFY —
##        THE EQUIP CANNOT RAISE IT; THE COMPLETION-TOKEN THEORY IS REFUTED
##        THROUGH THAT EVENT (2026-08-20 ~01:2x)

LANE W (the event-trigger hunt) = LANDED + AUDIT-CLEAN (the load-bearing claims
spot-verified on disk this session — the pointers below). Deliverable =
RE_output/claims/lane_event_trigger.md (read end to end); raw =
RE_output/content/eventtrigger_lane/eventtrigger_phase{1..7}_raw.txt.

THE FINDINGS:
- THE EVENT = a selection-changed notify keyed by the inspected-def property
  hash. VERIFIED: *DAT_141fb5d40 (the panel's event ptr) == *DAT_14206da80
  (the state-service key writer-1 reads) == the u32 at the .data slot
  0x1432513B0 (phase1_raw S1 vs S6 — identical pointer values, verified this
  session). The panel registers a listener ON the state property and, on
  fire, re-reads it into DAT_141fb5928 (writer-1's body, verified:
  `(param_2 - 4U & 0xfffffffd) == 0` → FUN_1404292c0(service, buf,
  *DAT_14206da80) → `DAT_141fb5928 = *buf`).
- THE REGISTRATION LIST = EXACTLY ONE callback (writer-1), registered by the
  panel's init FUN_140b395d0 (FUN_140304bb0(uVar4, local_res10, 1,
  FUN_140b3aea0) — the once flag) + unregistered by the teardown
  FUN_140b37bf0; the key global = exactly 3 xrefs, all the panel's own
  lifecycle.
- THE MODES: 4 = select/set, 6 = deselect/clear (writer-1 re-binds on BOTH;
  the sibling item-inspection callback gates ONLY mode 6 → clears to
  -0x7ee3623b = 0x811C9DC5 = kNoDefinitionHash — verified).
- THE RAISING ACTION = the panel UI's own selection/preview change — NOT the
  equip. The inspected-def pair store = written by the pair-SET
  FUN_140427380 (18 xrefs, the census in phase7_raw:1138+) whose callers =
  the panel's select logic (FUN_140a55cb0/FUN_1404fe180, kNoDefinitionHash-
  guarded), the panel's flow state machine FUN_140b47250 (with analytics
  emits), and the activity-transition UI (FUN_140e24a20/FUN_140e25270) —
  NONE = the 403-response apply. Xref-level: nothing on the ws/equip path
  references the event key or the pair store. THE FIRE DISPATCHER ITSELF =
  VMP-HIDDEN (a verified negative — the listener tables' only users = the
  register/lookup wrappers; the invocation = VMP-obfuscated or a runtime
  vtable, the inventory lane's known shape).
- THE SECONDARY (the 403 consumer): the statusPair code/value = schema fields
  decoded into the body, consumed by the per-verb applies = runtime-resolved
  objects built from the encoded blob DAT_141f44e20 — the client-side
  version-compare, if it exists, = live-read territory (NOT statically
  findable; the inventory lane's known open item).

THE COMBINED SYNTHESIS (V + W — the map is now complete at the static level):
- The panel's re-bind event (DAT_141fb5928's writer) fires ONLY on the
  panel's own selection/preview change — the equip's completion cannot raise
  it (W's xref proof). THE D1 COMPLETION-TOKEN THEORY = REFUTED AS AN
  EXPLANATION THROUGH THIS EVENT — but D1 remains the upstream contract
  component (its client-side consumer = live-read territory).
- The panel's re-render after an equip, if it exists at all, must flow
  through: (a) DOOR 2 — the def-change repoint (V's finding), whose deepest
  root = the player-class resolve callback FUN_140dc3960 (the runtime
  question: does the completed equip's character frame raise it?); or (b) the
  panel's flow state machine FUN_140b47250 (W's open item — a possible
  "panel re-select on equip" surface, one decompile pass away); or (c) the
  user's own next interaction (the only proven trigger to date).
- THE BOOT = unchanged in design; its discriminator is now precisely "does
  the client's OWN reaction fire at runtime". THE NEGATIVE CASE'S NAMED
  SURFACES (if the panel is still stale): FUN_140dc3960 + FUN_140fd13b0 +
  (new) FUN_140b47250's callers — a one-hook census each.
- THE CONTINGENCY NAMED (for the first time, explicitly): if the client's
  reaction still doesn't fire after the upstream-exact delivery, the fix =
  CLIENT-SIDE — the Sunrise DLL (the census DLL we already ship) CAN drive
  the panel's event/repoint from inside the process (the in-process mod's
  advantage). That would be a new front (client-side UI driving), not a
  delivery shape — recorded now so the boot's negative has a pre-mapped
  next step.
- W's handed-back open items: FUN_140b47250's callers (one decompile pass);
  a live hook on FUN_14187b8d0's return (log key+callback — names the fire
  at runtime); the event-name hash at 0x1432513B0 (a live read + a
  manifest-hash reverse lookup).
- LANE X (the implementation) = still running.

## 13.3 LANE X LANDS: THE UPSTREAM-EXACT 403 = IMPLEMENTED, HARNESS-GREEN,
##        BUILD-VERIFIED (2026-08-20 ~01:5x)

LANE X (the implementation) = LANDED + AUDIT-CLEAN (the commit chain, the
stage-before-reply code, the frame shape, the clicked-row swap, the harness
evidence, and the build hashes ALL spot-verified on disk this session).
Deliverable = RE_output/claims/lane_equip_contract.md (read end to end); the
per-phase claims = RE_output/content/equipcontract_lane/phase{1,2,3,4}_claims.md.

THE COMMITS (integration, base 649be1a, worktree clean — verified):
576edd7 (i) the promised reply · 5afca25 (ii)+(iii) the character upsert + the
+1 discipline · 61f1b62 (iv)+(v) the clicked row + the reset removal · 8922e0d
(vi) the harness · 2f94b39 (vii) the mojibake.

THE COMPONENT VERDICTS (verified):
- (i) THE PROMISED REPLY: bap_service_body.cpp:129-151 (verified verbatim) —
  the 403 branch stages the equip FIRST (stage_subclass_equip), then encodes
  the statusPair with status.value = outcome.subclassEquip.after.family4Version
  (the upstream contract comment quoted in-code); a refused equip = the plain
  StatusResponse{} pair (the 505 failure contract).
- (ii)+(iii) THE FRAME + THE DISCIPLINE: the single character-upsert builder
  restored (queuez_subclass_equip.cpp: "ONE character-upsert frame at the
  staged...", verified); the boot-P reset→select two-frame = gone; +1 stage;
  the nonce audit = clean (reply/frame/banner/roster nonces, no double-advance
  path); the family-0 banner + family-3 roster refreshes = untouched.
- (iv) THE CLICKED ROW: state_account_runtime.cpp:143-153 (verified) — the
  displaced item takes the PICKED row (the upstream std::swap shape), not the
  storage tail; the serial hand-off = unchanged (already correct).
- (v) THE ABILITY ARMS: the delayed-refresh arm (05111eb) = present and kept;
  the character-level reset = REMOVED (the D6 alignment); the 637bddc
  invalidation + worker wake = NOT PORTED, with evidence (OPEN-3): the fork's
  ability domain = the full 486-row enumeration (an equip makes nothing stale)
  + the external server has no in-session rebuild path — a blind port would
  break every subsequent encode. The right call; recorded as OPEN.
- (vi) THE HARNESS (phase4 §4.2, verified): EXIT 0 both — the selection-
  version-test = 81/81 incl. the new gates (equip_version_exactly_plus_one,
  the clicked-row proofs, equip_frame_is_the_character_upsert, the reply
  round-trip incl. the plain-pair INT32_MIN contrast); the equip-diff = EXIT 0
  with the byte-diff proof (the serials 0xE18/0xE0C, the equipped soid 0x2CC0,
  the 0xB74C stamp toggle). DB-READ-ONLY proven (no write calls on the test
  paths; the cache header byte-stable; the scratch DB's logical content
  unchanged). The scratch env = C:\Users\rasla\AppData\Local\Temp\opencode\
  equipcontract_harness (the live stack untouched).
- (vii) THE MOJIBAKE = fixed (the U+2014 in gate_trace_observer.cpp:501). TWO
  MORE mojibake bytes remain in X's OWN new comments (bap_service_body.cpp
  ~136/~156 — the same U+2014 corruption) — cosmetic, clean on the next touch.

THE BUILDS (verified this session — the hashes match the deliverable):
- sunrise-server.exe = 6,150,144 B, SHA-256 1CA70C623627D647... (the boot-P
  exe was 6,143,488 B).
- steam_api64.dll = 5,795,840 B, SHA-256 3DA28DFD62C594F9... — NOT the
  649be1a hash (expected: the DLL project compiles the shared server sources,
  so the contract changes land in it; the client-side delta = the kind
  observer only, which IS in this build — 649be1a = its base).

THE DEPLOY NOTES (the main session's steps — X recorded, verified):
1. The live cache header's eqHash (0x9414749E56399E10) ≠ the live DB's
   computed hash (0x034A3E9E4451BC5A) as of X's copy — the boot-P session's
   swaps moved the DB past the last restamp. THE DEPLOY-TIME RESTAMP = the
   THEN-CURRENT DB hash (the bootL_eqhash_exact.py port), NOT the /restamp
   runtime value (the 12.24 lesson); the b4cbabf diagnostic = the arbiter.
2. THE DLL CHOICE: deploy X's build (3DA28DFD — the kind hook included, one
   tree) or rebuild at 649be1a — the main session's call at deploy time. The
   deployed 6ef4ea030638cbda (boot-L, count=13) has NO kind hook and must NOT
   ride the census boot.
3. OPEN items: OPEN-1 = the full D5 reorder (commit-after-frame — out of
   scope; the reply-value staging = the load-bearing half); OPEN-2 = the
   per-item picks migration (7846599); OPEN-3 = the invalidation (above).
   Optional cleanup: prepare_item_republish's clearedSockets (no true-caller).

THE NEXT STEP = the deploy + THE CENSUS-INTENT BOOT, on the user's go (the
boot brief below in the main session's report; the falsifiable claim:
family=4 def=0xE5E86992 kind=1 at the equip, the transaction completes, the
panel switches; the negative = the three named hook surfaces).

## 13.4 THE BOOT-Q DEPLOY LANDS: THE GATE IS CLEAN, THE t=0 IDENTITY WARN =
##        THE PERMANENT EMPTY-ACCOUNT ARTIFACT (2026-08-20 ~09:3x)

THE DEPLOY (the user's GO): the full sequence ran via RE_scripts/deploy_boot_q.py
(the gated deploy script, recorded for reuse): the game-closed gate -> the server
stop -> the .bak_bootP chain (the exe 6,143,488 B + the cache + the deployed DLL
6ef4ea) -> the hash-verified copies (the server exe 1ca70c62... + the DLL
3da28dfd... - the kind hook included) -> the cache pre-stamp (ts 0x6A86B4BA / size
0x27BA000 = the new exe's identity + eqHash 0x034A3E9E4451BC5A = the bootL_eqhash
exact port over the THEN-CURRENT live DB) -> the server start (PID 41732 on
443/8099/30975). The incident re-hash = INCIDENT_20260820_093644 (the acceptance
stack: the new pair + the untouched settings/caches + the DB's WAL-fold mtime).

THE GATE READING (the one anomaly, explained end to end - the boot verification
is CLEAN): the log's `ev=build_data stage=identity result=mismatch` at t=0 with
expected_eq=0xAF63BD4C8601B... = THE SETTINGS-PASS EMPTY-ACCOUNT HASH - computed
this session: the configured_hash over characterCount=0 = 0xAF63BD4C8601B7DF
(the 13-hex-prefix match vs the log's mangled value; the mangled tail = the
diagnostic's shared line buffer race, a logging artifact, not a hash bug). The
t=0 pass hashes ZERO characters (the settings pass precedes the DB load - the
log's own `ev=account stage=identity ... characters=0` at t=0 vs characters=3 at
t=31) -> it can NEVER match the cache -> the warn = permanent and harmless at
every boot (the boot-P-era "clean" readings carried it unnoticed). THE REAL GATE
= the DB pass (t=31+): NO mismatch line (a match = silent per the b4cbabf
diagnostic) + the six domains swapped result=match (the socketEntryBuckets
domain = json 14 / cache 0 -> rebuilt -> match) + `build_data stage=cache
result=ok` + `initialize ok`. THE DEPLOY = CORRECT; the stamped eqHash = the
DB-load truth. FUTURE BOOT-VERIFICATION NOTE: read the t=0 identity line as the
expected empty-account warn; the DB pass's silence + the domain swaps = the
real gate.

THE NEXT = the game launch (the user) with the launch-time recap; the tour =
the cross-swap equip A->B/B->A + the veteran fold observation + the 801 sanity.

## 13.5 BOOT Q = THE WIN: THE PANEL SWITCHES LIVE (2026-08-20 ~10:0x)

THE USER'S VERDICT: "oh my god we actually did it, it works." THE SUBCLASS
EQUIP'S PANEL UPDATES LIVE — the front is CLOSED after five failed delivery
shapes. THE BOOT-Q DELIVERY = the upstream-exact 403 (the promised reply
version + the single character upsert @ +1 + the clicked-row placement). The
census (the kind hook's FIRST live run) confirms every designed claim, all
verified from the client log this session:

- THE FALSIFIABLE CLAIM = CONFIRMED IN THE EXACT FORM: `ev=gate_trace
  stage=kind family=4 def=0xE5E86992 kind=1` at t=119000 (the first equip,
  A→B), t=216719 + t=219359 (the second, B→A), and t=231984 + t=232844 — the
  character object = KIND 1 at every equip, the walker caller (+0xE015D4).
- THE ITEM'S KIND = SETTLED (the kind lane's open item #1): `family=4
  def=0x6CFBA3AB kind=3` — the item object = kind 3 (not 2), confirmed at the
  801s (t=222781/224844/226594) and the frame-era deliveries.
- THE WIRE = THE CONTRACT: the server log shows TWO 403s — the payloads
  0x600000000000000C (the equip to 0xC) then 0x6000000000000031 (the return to
  0x31) — each answered with `stage=subclass_equip result=ok family=4
  objects=1 bytes=854` = ONE 854-B character upsert (the boot-N builder's
  exact shape, now with the promised reply). The family-3 roster rides
  (2 objects). No kick, no rollback.
- THE STAMP CHAIN = BACK (the boot-N silence reversed): stamp_gate result=1 +
  ui_refresh + stamp_consume result=1 at t=217719 (right after the equip era)
  and t=277890 — the chain fires with the character frame.
- THE FAMILY MAP (the kind hook's bonus): family-0 banner = def=0xD859602C
  kind=1 + def=0xC6040EB0 kind=0; family-3 roster = def=0x63E0C862 kind=1 +
  def=0x1C35451D kind=0; family-4 account = def=0x32D7B974 kind=0.
- THE MECHANISM VERDICT (now VERIFIED live, closing the 13.1-13.2 theory):
  the complete transaction (the promised version + the character frame)
  triggers the client's own reaction at runtime — the panel re-binds and
  re-renders. The static map's residual question (which reaction fires) =
  answerable later if attribution is wanted; the delivery contract = the fix.
- THE ATTRIBUTION CAVEAT (the one-variable trade-off, recorded honestly): the
  boot composed the five components; WHICH was load-bearing = unproven. An
  optional attribution boot (revert the reply value alone, keep the rest) =
  the cheap discriminator if we ever want it. Not needed for the front.
- THE VETERAN: NO recap again (the user) → the trigger ∈ the 388
  still-suppressed in 47..953. THE NEXT FOLD (the next boot's rider): restore
  the second half → ~194 kept; the recap's return/absence = the direction.
- THE REMAINING (post-win): the front's OPENs (lane X's OPEN-1 = the D5
  commit-after-frame reorder; OPEN-2 = the per-item picks 7846599; OPEN-3 =
  the 637bddc invalidation, structurally N/A); the veteran fold; the parked
  fronts (the verb layer, the entity front, the P0/P1 batches). The deployed
  stack = the boot-Q build (the server 1ca70c62... + the DLL 3da28dfd... with
  the kind hook, batch 14).

## 13.6 THE UNIVERSAL LESSONS = BAKED INTO AGENTS.md (2026-08-20 ~10:1x)

The user's standing demand: never re-run this front's failure loop. THE TEN
LESSONS (the full text = AGENTS.md's new "THE UNIVERSAL LESSONS (2026-08-20,
binding)" section, added this session): (1) THE REFERENCE IS THE ORACLE — the
fresh fetch + the comment-quoting discipline (the "no subclass flow" lesson =
refuted; the fix's key = an upstream comment); (2) INSTRUMENT BEFORE
INTERVENTION — the discriminator instrument before any fix; (3) COMPLETE THE
MAP BEFORE MOVING — the full body + the xref census (the dumped-unanalyzed
sink, the un-priced gate); (4) THE COMPLETION VIEW — the reply envelope = part
of the contract ("the apply ran" ≠ "the transaction completed"); (5) LOG
ROUTING, NOT ACTIVITY — the decision-point hooks (the kind), not the
activity hooks (the polls); (6) PRE-NAME THE NEGATIVE — the falsifiable claim
+ the pre-mapped next step, the attribution gap recorded; (7) ESCALATE AFTER N
FAILURES — the honest failure list + the outside review; (8) THE DISK IS THE
TRUTH — the deliverable rule's three catches (the INT32_MIN, the drifted
cites, the t=0 false alarm); (9) DOCUMENT EVERY GATE'S EXPECTED VALUE — the
verification-without-an-expectation hazard (13.4); (10) THE CONTRACT IS THE
VARIABLE — one delivery contract per boot with the census as the arbiter.
Each rule = a named failure mode from this front, with its evidence pointer in
the 12.25-13.5 record.

## 13.7 LANE S LANDS: THE PURPLE DIAMONDS = THE FORK'S DELETED 'acquired' STATE
##        (2026-08-20 ~10:3x)

LANE S (the diamond-state dig) = LANDED + AUDIT-CLEAN (the load-bearing claims
spot-verified on disk this session — the pointers below). Deliverable =
RE_output/claims/lane_diamond_state.md (read end to end); raw =
RE_output/content/diamondstate_lane/.

THE VERDICT:
- THE STATE CHAIN (verified): the diamond's color = the subclass ITEM
  INSTANCE's 36-lane `socketEntryStates` bytes (object offset 0xF7), where
  ready=0x10 renders PURPLE (the client's own availability classifier
  FUN_14054da80: state 0x10 → code 9 "ready to unlock" — the ready-click path
  FUN_14053fef0) and acquired=0x11 / active=0x12 render COLORLESS. The bytes
  flow: the lane readers FUN_1403d0490/3d18b0 → the guard family
  FUN_140a55d30/c10/c80/cb0 → the color composers FUN_1405012e0/FUN_140a262a0
  → the node widgets FUN_14100a6e0.
- THE DIVERGENCE (verified in the tree): OUR fork's resolve_socket_states
  (subclass_socket_selection.cpp ll. 57-78, read this session) baselines every
  readyMask entry as `ready` (0x10) — the fork's enum (instance_encoder.h,
  read this session) = {absent=0, ready=16, active=18} with `acquired = 17`
  DELETED (and the validator rejects it) — while UPSTREAM
  (subclass_socket_selection.cpp ll. 41-50, read this session) baselines
  `(acquiredSubclassAbilityMask & bit) ? acquired : ready`, with the mask
  defaulting ALL-SET (`std::uint64_t ln{~std::uint64_t{0}}` in
  account_state.h — "defaulted all-set, assuming the Client only allows
  clicking an already-acquired node"), so upstream ships {acquired, active}
  only = the colorless fully-unlocked tree. Git-verified: the upstream's
  baseline exists since 8660090; OUR degraded body arrived WITH the fork's
  first commit — lost in the port, never a deliberate edit.
- THE NON-CAUSES (verified negatives): the account flag bank, item_plugs, and
  the ability-bucket mirror = NOT in the diamond render chain (the render
  reads only the item's socketEntryStates).
- THE FIX SHAPE (one variable, design only — the user arbitrates): restore the
  upstream resolver contract in the fork — (1) re-add `acquired = 17` to the
  enum + the validator; (2) baseline readyMask entries → acquired (re-add the
  all-set mask parameter); (3) re-add the Attunement forcedActive block. No DB
  migration (the states = recomputed per frame). THE EXPECTED VALUE (lesson 9):
  every delivered item frame's lanes ∈ {0x11, 0x12}; any 0x10 = a regression.
  THE NEGATIVE'S INSTRUMENT (lesson 6): a census hook logging the 36 state
  bytes at encode time + the client's FUN_14054da80 return per node.
- OPEN micro-items: FUN_1414b5300 (one budget-skipped xref caller — not
  load-bearing); the literal purple RGBA (runtime manifest data — not needed
  for the fix); keep resolve_socket_states_baseline's ready output (the
  synthetic-reset arm's merge-class source — untouched by this fix).

LANE T (the 3-swap stall dig) = still running.

## 13.8 LANE T LANDS (VIA THE RESUME): THE 3-SWAP STALL = A TRANSACTION-
##        COMPLETION LEAK — THE DELIVERY ORDER, NOT THE CONTENT (2026-08-20
##        ~11:4x)

LANE T died at report-back (the landed-silent pattern); the RESUME synthesized
from the original raws + one sanctioned Ghidra dump. BOTH = AUDIT-CLEAN (the
load-bearing claims spot-verified on disk this session). Deliverable =
RE_output/claims/lane_swap_stall.md (read end to end); the phase claims =
RE_output/content/swapstall_lane/phase{3,4}_claims.md + the original lane's
raws (the 137-function writer census session_writers_p2_raw.txt, the upstream
survey).

THE VERDICT:
- THE WINNING CANDIDATE = a client-side TRANSACTION-COMPLETION LEAK: each swap
  arms an optimistic transaction the fork's delivery never retires — the 403
  promises the family-4 revision BEFORE the after-image frame is proven to fit,
  and a failed push still ships the promised reply (`return true` at
  bap_service_body.cpp:190, verified this session — where the upstream
  REFUSES). The client's pending-transaction budget (~3, observed; VMP-hidden)
  exhausts → the selector stops responding until the menu-exit teardown
  (FUN_1412ef220 + FUN_1412ed9c0) clears the context.
- THE UPSTREAM'S THREE INVARIANTS (all verified quoted on disk this session —
  the fork breaks all three): (i) "Stage that revision before encoding the
  reply, or the Client completes against the old store" (bap_service_body.cpp
  ~l. 217 — DEPLOYED at boot Q); (ii) "The State transaction must not commit
  when the Client cannot receive its after-image" + "staging a second revision
  would make the response and pushed Family-4 ladder disagree" →
  return false (queuez_outcome_staging.cpp:62/73); (iii) `framedSize <=
  response.size() && transactions::commit(...)` (the commit-after-frame).
  The fork mutates+persists BEFORE the push + ships the promised reply on any
  stage success — lane_equip_contract.md OPEN-1 recorded the D5 reorder as
  un-ported. THE PROMISED VALUE (i) = deployed; THE DELIVERY ORDER = not.
- THE REFUTED CANDIDATES (field by field, the census): (a) the session-record
  growth = REFUTED — zero increments across all 20 accumulation offsets (the
  +0x4c count = a frame word replace, the +0x1ea0 repoint = a fixed in-place
  record copy FUN_140f3af80 with no count field (verified, the new
  phase3_repoint_raw), the rest = latch/bounded/reset patterns; (b) the
  version-mirror cap = REFUTED as a counter (the compare slots +0x1ea4/+0x840);
  (c) the serial space = REFUTED (clear-only + countdown timers); (d) the tile
  bitmap = REFUTED (the bitwise-OR = idempotent; +0xc750 = write-only); (e) the
  flow machine = REFUTED (the bounded enum {0,1,2,6}); (f) the handle slots =
  REFUTED (once-per-visit allocation); (g) the 637bddc/7846599 stale-resolution
  = the WRONG SYMPTOM CLASS (those fix the stale-picks display, not the input
  stall — they remain the named fixes for OPEN-2/OPEN-3).
- THE FIX SHAPE (one variable = the delivery order): port the D5
  commit-after-frame invariant — stage the after-image frames FIRST, refuse
  the 403 (plain pair, no commit) on any push failure, commit only when
  `framedSize <= response.size() && commit` — so a promised revision can never
  ship without its authoritative frame.
- THE RESIDUAL RUNTIME QUESTION (if the stall persists): which side blocks —
  settled by ONE hook (the P4.4 spec: FUN_1412f3f80 per-click log +
  FUN_1412ef940 frame-apply log), discriminating three live: the clicks stop
  reaching the dispatcher (transaction exhaustion confirmed) vs the def stuck
  at the +0x840 gate (the completion never landed) vs the frames stop arriving
  (a server-side drop).

THE CLEANUP ARBITRATION (the user): both fixes (13.7's acquired-state restore
+ 13.8's delivery-order port) = two independent upstream-contract restores with
INDEPENDENT observable outcomes (the diamonds' color vs the swap count) — one
boot can validate both without confound; or split per the strict one-variable
discipline. The veteran fold = the standing rider either way.

## 13.9 THE LANE CONSISTENCY RULES = CODIFIED (2026-08-20 ~12:0x)

The user's meta-question (the lanes tunnel-vision like the main session — same
model family, less context) → THE ANSWER = the observed failure modes are
NAMED + countermeasured, codified as AGENTS.md's new "LANE CONSISTENCY RULES"
section (7 rules, each = a real failure from this project's lane history):
(1) THE PREMISE AUDIT — the lane's phase-0 act = verify the brief's premise
against disk (≤30 min); a refuted premise → STOP + write the refutation as the
deliverable (a refutation = a successful lane) — kills the "faithfully answers
the wrong question" mode (lane E's wrong-tree comparison, the boot-M anchor
artifacts); (2) THE REFUTATION IS A DELIVERABLE — every brief names what would
refute the expected answer; the lane's incentive = the truth, not the brief's
confirmation; (3) THE 3-LINE REPORT-BACK — the fixed pointer template (≤3
lines, ≤10% of the budget) — kills the death-at-report-back mode (lanes C and
T died at exactly this step; the budget belongs to the files); (4) PHASE-
CONSISTENCY — each phase re-reads the previous phase's claims before writing
its own; the per-phase claims = the required minimum; the consolidated file =
optional (the collector/resume pattern that recovered lane T); (5) THE
LOAD-BEARING MARK — the brief marks the load-bearing claims; the lane writes
those with full quotes + exact offsets (the main session's spot-verification
reads them first); (6) TWO DEAD-ENDS = A REWRITTEN BRIEF — never re-run a
dead-ended question verbatim; (7) THE COLLECTOR IS ALWAYS ARMED — the main
session completes a dead lane's deliverable from the raw or spawns a
synthesis-only resume; check disk + opencode.db before any respawn (the 12.6
correction).
THE ARCHITECTURE UNDERNEATH (the honest framing): the lane consistency =
THREE layers — (a) the brief's structure (the premise audit + the falsifiable
question + the load-bearing mark = the anti-tunnel-vision layer); (b) the
budget discipline (the 3-line report-back + the optional consolidation = the
anti-death layer); (c) the main session's verification + collector arms (the
spot-verification + the resume = the anti-silent-failure layer). No layer
alone fixes the model-family drift; the three together = the system's
consistency contract, now binding.

CORRECTION (2026-08-20 ~12:1x, the user's call): the premise audit = REMOVED
(the brief already carries the verified facts - a 30-minute disk re-check =
wasted lane time; the verification arm is the main session's job, not the
lane's). The section = REWRITTEN as AGENTS.md's "THE ANTI-RABBIT-HOLE RULES"
(the execution contract for lanes AND the main session) with the user's actual
target leading: rule 1 = STOP -> SEE IT END TO END -> VERIFY -> ONE SURGICAL
ACTION (the canonical near-miss = the t=0 scare, where the end-to-end read
never happened first) and rule 2 = A FIX MUST NOT MAKE NEW BUGS (one variable,
the contract-preserving shape, the harness gate, the regression check). The
anti-death + anti-silent layers (the 3-line report-back, the phase-
consistency, the load-bearing mark, the rewritten-brief, the collector) =
kept as rules 3-8.

## 13.10 THE NO-SYNTAX-TAX RULES = CODIFIED (2026-08-20 ~12:2x)
The user's second meta-point (the model family's simple syntax errors compound
across a session) -> AGENTS.md's new "THE NO-SYNTAX-TAX RULES" section (6
rules, each = an observed time-waster this project has paid for): (1)
PARSE-CHECK BEFORE RUN — `python -m py_compile` (or `bash -n`) before any
script's first real run; (2) SMOKE-RUN FIRST — the trivial-input run must exit
0 before the real run (the harness-scratch pattern, generalized to ALL
scripts); (3) ONE GHIDRA RUN = ONE SCRIPT — the parse + smoke pass BEFORE the
~77 s project run (the observed 3-6 debug-run iterations per lane script = the
tax this kills); (4) VERIFY THE BYTES BEFORE CONCLUDING — the mangled console
output (the missing "stage=", the stray NULs) = re-read the raw bytes before
any conclusion (the t=0 near-miss's second face); (5) NEVER GUESS THE TARGET
TEXT — the edit tool's oldString = copied from a fresh read; a failed match =
re-read, never re-guess (two failed-edit rounds this session); (6) ASCII-ONLY
COMMENTS — the U+2014 mojibake corrupted three comments.

## 13.11 THE CLEANUP DEPLOY LANDS + THE DEPLOY-SCRIPT HANG = DIAGNOSED + FIXED
##        (2026-08-20 ~13:5x)

THE CLEANUP DEPLOY (the user's arbitration: both fixes, one boot): the lane-Y
builds deployed via the boot-Q procedure (the deploy script, the hashes
updated to the new builds) — the server exe 4329D95E... (6,165,504 B) + the
DLL A17DB6DD... + the cache pre-stamped to the then-current DB hash + the
.bak_bootQ chain; the server = live (PID 13272 on 443/8099/30975). THE BOOT
VERIFICATION = CLEAN (the DB-pass identity silent-match, the six domains
match, all listeners up, cache ok, initialize ok — the t=0 empty-account warn
= the expected settings-pass artifact per 13.4). The incident re-hash =
INCIDENT_20260820_135025.

THE DEPLOY-SCRIPT HANG (the user's diagnosis, then the fix): the script
"keeps running after the deploy is done" = the server was spawned as the
script's CHILD with INHERITED stdout/stderr handles; the long-lived server
holds the pipes open forever, so the harness wrapper never sees EOF — the
script itself exits fine (its own logs proved the deploy done), the wrapper
hangs. THE FIX (deploy_boot_q.py S5, landed + parse-checked): the server now
starts DETACHED — stdin/stdout/stderr = DEVNULL + close_fds +
DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP. (The user's kill last time =
exactly this; recorded so the next deploy never hangs.)

THE NEXT = the game launch (the user) with the launch-time recap: the
cleanup-validation boot — (1) the diamonds colorless; (2) 4+ swaps without the
stall; (3) THE REGRESSION CHECK = the equip panel still switching; (4) the
veteran fold observation (the rider). The stall fix's OPEN item (the full
stage-then-commit) = the named follow-up if the refusal alone doesn't clear
the stall.

## 13.12 THE CLEANUP BOOT HARVEST: THE DIAMONDS CLOSED, THE STALL SOFTENED BUT
##        RESIDUAL ON THE 801 ARM, THE VETERAN STILL GONE (2026-08-20 ~14:1x)

THE USER'S VERDICT: (1) THE PURPLE DIAMONDS = GONE (fix 1 validated live); (2)
THE TREE SWAPPING = "definitely better and smoother than before, but it does
still snag every now and then - I'll have to try clicking on a different
diamond in the other tree I wanna select or what have you for it to work" (fix
2's refusal arm = helped; the stall = softened, not closed); (3) THE VETERAN
RECAP = still absent.

THE CENSUS (verified from the logs this session): the snag era = a burst of
801s (the diamond clicks - the payloads 0x6000000000000032 + the socket entry
bytes 95/8D/97/90/96/8E/92) with the client applying the item frames (192
family-4 kind lines, def=0x6CFBA3AB kind=3 per click) - and ZERO 403s in the
era. THE RESIDUAL'S READ: the tree-swap arm (the 403) carries the refusal fix
(10edee5); the 801/2100 arms (the ability-change + the subclass-selection
branches) were LEFT UNTOUCHED per the mission (lane Y's OPEN-2) and carry the
SAME persist-before-push leak shape - the snag on the diamond clicks =
consistent with the un-fixed arm. THE NEXT FIX (one variable = the 801/2100
delivery order): port the same reorder (stage first, persist last, refuse +
revert on push failure, the plain-pair refusal) to the 801/2100 arms. THE
PRE-NAMED NEGATIVE (if the snag persists after it): the full stage-then-commit
rework (lane Y's OPEN-1 - the frame builders read live state). The veteran:
no recap again - the trigger remains in the 388 suppressed (the next fold =
restore the second half -> ~194 kept, the next boot's rider).
