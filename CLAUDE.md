@AGENTS.md

This project's full instruction set lives in AGENTS.md (imported above).
Claude Code tier mapping for the SPEND MAP: mechanical -> haiku; judgment ->
sonnet default, opus only on explicit user request. The delegate-don't-act
principle carries over unchanged. Project knowledge lives ONLY in this repo's
plain files (STATE.md, FINDINGS_*.md, AGENTS.md) - never in Claude-private
memory stores.
