# FINDINGS — 2026-08-15 (setup session, post-handoff)

## 9.1 FOUR SETUP ITEMS COMPLETE (tooling debt from 8.32)

### (1) TagDecode.cs oodle-fail bug FIXED
`RE_tools/TagDecode.cs` line 64: on double oodle failure (f=0 then f=1) it now THROWS
`InvalidDataException("Oodle decompress failed for block {b} ... — refusing to append raw
bytes")` instead of silently appending raw decrypted block bytes. `dotnet build` clean
(only pre-existing CS0162 unreachable-code warning at the legacy path after `return;`).
All future decodes are fail-loud.

### (2) Rust + tiger-pkg native decode (RE_tools/tigercli) — WORKS, byte-exact
- `winget install Rustlang.Rustup` → cargo 1.97.1, rustc stable-x86_64-pc-windows-msvc.
- `cargo install tiger-pkg` FAILS BY DESIGN: tiger-pkg 0.21.0 (crates.io, cohaereo, MIT,
  github.com/v4nguard/tiger-pkg) is a LIBRARY crate (has_lib, bin_names=[]) — the HANDOFF's
  command cannot work. Built a thin CLI wrapper instead: `RE_tools/tigercli` (src/main.rs,
  ~90 lines) using `PackageManager::new(dir, GameVersion::Destiny(DestinyVersion::
  Destiny2Shadowkeep), None)` + `read_tag(TagHash(v))` where v = raw tag u32 (TagHash stores
  the tag value directly; pkg_id() = (v−0x80800000)>>13 — SAME arithmetic as our C# tool).
- VERIFIED byte-exact against ground truth:
  - 0x80EC3F60 → 120,040 B, head u32@0 = 0x1D4E8 (size), u32@4 = 0x39AB (count=14,763) ✓
    (matches claim 3.1 exactly)
  - 0x8133719C → 362 B ✓
- tiger-pkg crypto notes (src/crypto.rs): built-in per-version keys (aes_key_0/1, nonce
  base); nonce shift identical to ours (nonce[0]^=pid>>8, nonce[1]=0xF9 for d2_sk,
  nonce[11]^=pid&0xFF); REDACTED blocks need optional `keys.txt` (group:key:iv) — not
  needed for us (all flags 0x3, 8.26). Oodle auto-loads oo2core_3_win64.dll from
  `<packages>/../bin/x64/` — matches our layout exactly.
- First-run builds a package cache (scans all 2,202 .pkg files; wrote cache files) —
  subsequent runs fast. Usage:
  `RE_tools\tigercli\target\release\tigercli.exe "<pkgs dir>" <tag_hex>... [--out dir]`

### (3) PyGhidra ENABLED + VERIFIED (Ghidra 12.1.2, C:\Users\rasla\tools\ghidra_12.1.2_PUBLIC)
- Ghidra 12.x ships PyGhidra as a CORE feature module
  (Ghidra\Features\PyGhidra, PyGhidra.jar + bundled jpype/pyghidra wheels in pypkg\dist).
  Jython is the removed default (WhatsNew: Jython = extension now).
- Plain `analyzeHeadless.bat` does NOT start PyGhidra ("Ghidra was not started with
  PyGhidra. Python is not available") — use `support\pyghidraRun.bat` (GUI) or
  `support\pyghidraRun.bat -H <analyzeHeadless args...>` (headless).
- First run: installed pyghidra into a new Ghidra venv
  `%APPDATA%\ghidra\ghidra_12.1.2_PUBLIC\venv` (python 3.13.15; offline pip from the
  bundled pypkg\dist). Subsequent runs are prompt-free.
- VERIFIED: headless run with `-postScript smoke.py` printed PYGHIDRA_OK — CPython
  GhidraScripts now execute. Python scripts now usable for the Phase13+ style passes
  (the Java-script compile-fix tax is dead; keep .java where already written).

### (4) AES-GCM STANDARDIZED ON tiger-pkg (python ctypes wrapper superseded)
Decision per HANDOFF option B: tiger-pkg/tigercli is now the standard decode path; the C#
TagDecode.cs tool remains the cross-check reference. The python ctypes BCrypt wrapper
(RE_scripts/extract_spawn_payloads.py lines 29-70) is NOT fixed and is superseded; its
suspected marshaling bugs: ChainingMode property written with
sizeof(c_wchar*17)=34 bytes for a 15-char+1NUL string (should be 16 wchars = 32 B) and
no output-buffer resizing on STATUS_AUTH_TAG_MISMATCH. Do not use it for new work.

## 9.2 VENDOR-WALK PRE-RESUME EVIDENCE (tigercli verification side-effects — NEW, alters the plan)

### (a) 0x81A2926E (candidate #3) DECODES CLEANLY via tiger-pkg — block-1172 class CLEARED
- C# R16: "oodle decompress fail at block 1172" → no blob. 8.27's 1,304 B was raw-fallback
  corruption (claim 12.5).
- tigercli: pkg 0914/4718, size 0x518, reference 0x80805BB1, decoded 1,304 B, NO error.
  Head: u32@0 = 0x518 (SELF-SIZE = 1,304 — {size,count} header convention, count@4 = 0),
  then 8-byte-stride tag-like values from @0x10: 0x81327D22, 0x81327D28, 0x81327D2E...
  (pid 0x593 entries 7458/7464/7470, +6 stride). child0 @+0x10 = 0x81327D22 = TAG-LIKE —
  the corrupted 8.27 blob showed garbage there. Saved:
  RE_output/content/tigercli_test/81A2926E.bin (plus 80B754B4.bin, 8133719C.bin,
  80EC3F60.bin).
- The gate on this clean blob is now directly testable (child0 = 0x81327D22 → decode →
  root ≥ 1576 B → slot48 elementClass 0x80807BE8). R20 (block-1172 enumeration) may be
  OBSOLETE — its purpose was to fix the decode; tiger-pkg's patch/block selection already
  did. (Need to check pkg 0x593 exists — 0x81327D22's family.)

### (b) 0x80B754B4 (candidate #1) entry reference = 0x80B754B3 — SELF-TAG FINGERPRINT CONFIRMED
- tigercli: pkg 01ba/5300, size 0x155000 (1,396,736), reference = 0x80B754B3, type 40
  subtype 1. The entry-table u32@0 column for pkg 0x1BA row 5300 = the SELF-TAG OF ROW
  5299 — exactly wave-5 R19's branch (a) prediction ("u32@0 of row 5300 = 0x80B754B3
  (self-tag of row 5299) → table shifted / header row 0 → DECODE IDX 5301"). Both tools
  agree (C# ClassOf gave the same 0x80B754B3 in R16).
- Decoded blob (1,396,736 B) matches entry file_size EXACTLY (0x155000), but the blob
  head u32@0 = 0x494A494A (NOT 0x155000) and +0x10 = 0x60408000 (not tag-like) — body is
  0x494A494A/0x31A631A6 filler. This blob is arithmetically NOT the container
  (claim 12.4) whether or not the row is shifted. The R19/R21 question stands: is the
  TRUE 0x80B754B4 blob at row 5301 (shift) — and is THAT container-shaped?
- Note: tiger-pkg read row 5300 by index and returned a self-consistent entry (size
  matches decode) — the "shift" is a row-SEMANTICS question (whose tag does row 5300
  describe), not a tool bug. R19's raw row dump decides.

### (c) 0x8133719C unchanged: 362 B, fails child0 (0x120 @+0x10) — hash-table reading stands.

## 9.3 NEXT-ACTION STATE (unchanged execution order, updated with 9.2)
R19 (entry-table row dumps: 0x1BA rows 5288-5312, 0x59B rows 4496-4520, 0x914 rows
4706-4730 + header rows + full-table 0x80805BB1 scan) → R21 (three-way decode idx
5299/5300/5301 + structural scan of the 1.4MB blob) → R20 (block-1172 cross-test — only
if the tiger-pkg clean decode of 0x81A2926E somehow doesn't gate) → re-run the R16 gate
on the corrected row, with tigercli as the decode engine.

## 9.5 CONTAINER FOUND + GATE VERIFIED (vendor lane R19/R21/gate wave)

VERIFIED by main session (tigercli decodes + python struct checks, all numbers
reproduced):
- **0x81A2926E IS the investment_globals CONTAINER.** Clean 1,304 B decode
  (self-size header u32@0 = 0x518), 81/81 children tag-like, child0 @+0x10 =
  0x81327D22. The client's gate passes end-to-end: root 0x81327D22 = 1,912 B
  (>= 1576), slot48 @776 = 0x81327CCB, slot48 blob = 370,224 B,
  find_array_at(8): count u64@8 = 15,424, relative = 16, header @32,
  u64@32 == 15,424 ✓, marker @28 = 0x80809FBD (>>16 = 0x8080) ✓, elementClass
  @40 = **0x80807BE8** (kItemIndexTableClass) ✓ — ACCEPTS.
- **Item index table = 0x81327CCB** (pkg 0x593 entry 7371, 15,424 rows — matches
  the cache itemCount exactly). 8B rows after the 32-byte find_array_at header.
- **0x80B754B4 is DEAD**: R19 dump verified — row 5300 blockInfo sz = 0x155000 =
  1,396,736 = decoded size exactly (NOT shifted); row 5301 sz = 48 B (a float
  config table, not a container); row 5299 = 40 B fails child0. The 0x1BA
  entry-table reference column is a PAIRED-SELF-TAG layout (rows 5299↔5300 swap
  self-tags) — the "class 0x80B754B3" R16 reading was that pair's left half.
- 0x8133719C remains a dead candidate (362 B hash table, child0 = 0x120).
- Root slot table (verified reads): slot 11 @184 = 0x81327CE4 (constants),
  slot 17 @280 = 0x81327CE0 (bucket, 4,720 B), slot 48 = 0x81327CCB (item index),
  slot 68 @1096 = 0x8131933C (progression), slot 97 @1560 = 0x8131932C (socket
  list, 384 B). Root = 8 + 119×16 → ~119 slots to census.
- R20 (block-1172) OBSOLETE — tiger-pkg's patch/block selection fixed the decode
  for free. Vendor-walk claims 13.1-13.7 in
  RE_output/claims/vendor-walk-R19R21-gate.md (all gate-relevant numbers
  re-derived by hand above and matching).

NEXT WAVE (vendor walk §7 final form, now unblocked): full root slot census
(slot, tag, class) — classes from tigercli get_entry reference for the 0x593
family (0x81327D22 → 0x80807D84, 0x81327CCB → 0x80807BE4, both class-like —
pkg 0x593 uses normal class references, unlike 0x1BA's paired-tag layout) →
find the odd-class slot = VENDOR table → decode rows {u32 definitionHash @0,
u32 targetTag @16, stride 24} → vendor definition blobs. Plus: enumerate the
15,424 item-definition tags from 0x81327CCB (the item universe, a direct
S1 datagen input).

## 9.6 DECODER POST-READ EXECUTED — DEFINITIVE NEGATIVE (NEW []) + bubble/schema lanes verified

SPEC 2B ran live on 2026-08-15 (same boot, PID 17540): PRE banked at orbit
(71/71, type 3 zero, stable double-read), POST after user's Change Character at
character-select (5 s drain, stability assert passed): **NEW slots: []** —
zero-yield. Per live-reads-spec acceptance #2/#6 this is a DEFINITIVE NEGATIVE:
the Change Character logout path closes the BAP socket BEFORE sending the
"c->b goodbye" (type 3) — the only client-sent message type never populated.
The decoder factory table is now bounded (71 types max observed; type 3 never
instantiates on a clean menu logout). Next decompile target: the
socket-close-before-send shutdown path. Files: RE_output/content/
registry_logout_pre.json + registry_logout_post.json; runner script
RE_scripts/spec2b_inline.py (banked for reuse). This closes HANDOFF in-flight
item 2 (decoder post-read) — the queuez/decoder map era is fully closed.

Bubble lane VERIFIED (main session re-derived the anchor): cache hashName
section 171/171 == FNV-1-32 (FNV-1a 0/171); fnv1("default") = 0x2EA8FB98;
coverage 381/381 sets, 8,779/8,779 points. Corrects FINDINGS 8.30's FNV-1a-64
assumption for the spawn/bubble vocabulary. Deliverable:
RE_output/content/spawn_bubble_labels.json.

Schema oracle lane: HONEST NEGATIVE (accepted) — no field-name oracle exists;
names stripped by build design (u32@12 slot carries 0x811C9DC5 "unnamed"
sentinel en masse; 742 non-sentinel values banked, 0 joins vs ~18k strings).
Image-class aux targets dumped live at orbit (RE_output/content/
schema_aux_dumps.bin, 6 targets x 0xA0 x 16 B — three look like code-pointer
arrays, three data tables). Remaining schema items need a destination session
(heap aux array 0x22AC2EE1020 is stale post-relaunch) or static passes; none
block S0.

## 9.7 VENDOR SLOT FOUND — slot 116 VERIFIED (vendor walk CLOSED)

Main-session verification (all numbers re-derived):
- Root 0x81327D22 slot 116 @1864 = 0x8131931D (pkg 0x58C idx 4893) ✓.
- Blob 12,312 B = 48 + 511×24 EXACT. find_array_at(8): count u64@8 = 0x1FF = 511,
  relative 16, header @32, u64@32 == 511 ✓, marker @28 = 0x80809FBD ✓,
  elementClass @40 = 0x8080784E. Rows: 511/511 targetTags tag-like + unique,
  0 zero-hashes, all pkg 0x58C; row grammar {u32 definitionHash @0, u32
  targetTag @16} ✓.
- Census: 0x80807850 → 511 (== row count EXACTLY — the element class);
  0x8080784A → 1 (the table's own entry reference). Unnamed in every corpus
  (Sunrise definition_index_table.h has NO slot-116 constant — only 11/17/48/
  68/97; Sunrise has no vendor code at all).
- Honest framing: STRUCTURALLY identified as the investment table of the odd
  root-slot class (the vendor-table hypothesis per the walk's exclusion
  method); the "vendor" semantic label remains hypothesis-grade until a
  definition-blob probe shows vendor-shaped content (display strings,
  inventory tables, currency references).
- Item index table (slot 48, 0x81327CCB): 24B rows confirmed (370,224 = 48 +
  15,424×24); 15,424 tag-like unique targetTags spread over pkgs 0x58F (8192),
  0x58C (3241), 0x593 (2144), 0x590 (1811), 0x592 (25), 0x594 (11).
- VENDOR WALK CLOSED: container 0x81A2926E → root 0x81327D22 → slot table
  (119 slots) → vendor slot 116 (0x8131931D, 511 definitions) + item universe
  (15,424 rows) + socket/progression/bucket/constants slots all mapped.
  Claims in RE_output/claims/vendor-walk-R22.md.

## 9.8 R23 VERIFIED — vendor hypothesis WEAKENED; ITEM UNIVERSE 100% EXTRACTED

Main-session verification (all numbers re-derived):
- Item universe: 15,424/15,424 decoded (JSONL lines == rows), 0 failures; sizes
  min 384 / median 930 / max 3716 B; reference 0x80807BEA for 100%; entry size
  == decoded size across the board. RE_output/content/item_universe_index.jsonl
  + item_blobs/ (15,424 files, 18.7 MB). This is the S1 datagen backbone,
  fully materialized.
- Vendor table (slot 116, 0x8131931D): 511 rows, all unique in both columns.
  OVERLAP VERIFIED: 277/511 (54.2%) of the table's definitionHash keys also key
  an item definition. Probe blobs: self-identifying {u64 size @0, self-hash
  u64@8 (+@12), 2-5 embedded 0x80809FBD-marked arrays}; zero display strings,
  zero item references beyond the self-hash.
- HONEST VERDICT (accepted): the "vendor" label is WEAKENED. A vendor-inventory
  table should reference sellable items; none do. Better reading: hash→
  definition index of an unknown item-adjacent class (0x80807850/4A/4E,
  in-blob-only id space). Going forward: "slot-116 table / class 0x80807850",
  "vendor" as working name only. Two levers filed by the lane (R23.R1 nested
  array grammar, R23.R2 one xref on 0x80807850 to settle vendor vs
  item-adjacent).
- NEXT: per-item field decode on item_blobs/ (Sunrise constants: socket count
  u64@8=240, bucket id @184, socket block @104) → content/item_definitions.json;
  then the manifest-ingestion lane (R24) to name the fields from the public
  Bungie.net manifest.

## 9.9 FRONTIER-MODEL REVIEW INCORPORATED + REASONING HEURISTICS (plan amendment)

User supplied a stronger-model cross-review of the session; verified its claims
against disk ("every claim I spot-checked reproduced"). Amendments adopted:

1. S0 STARTED IN PARALLEL (done: fork cloned RE_build/Sunrise-fork @ b12a9da;
   inventory lane complete + verified). The inventory's headline: **the in-process
   BAP server is ALREADY an external server** — relay publishes 127.0.0.1:bapPort
   (state_runtime.cpp:105-107, verified verbatim), listener always inits, the
   transport talks a real loopback socket in both modes. S0's real work = HTTP(S)
   SignOn/config surfaces + content handoff (build_data.bin + 16B bootstrap token
   from game memory — bootstrap_token_publish.cpp:16 verified) + Oodle borrow
   (oodle_installed.cpp under middleware/compression/oodle — lane's paths had
   cosmetic omissions, substance exact). First commit spec + v0 scope +
   3-risk list in RE_output/claims/s0-server-inventory.md. PR #9 NOT at HEAD
   (b12a9da has PR #13 only).
2. SPAWN FAILURES 5/5 CLOSED (frontier point 2): one tigercli pass decoded all 5
   previously-failed sets (0x80B42145, 0x80C250C4, 0x80C754AE, 0x8154874E,
   0x8155FE4E — all class 0x80809162 confirmed). Spawn inventory = 386/386.
3. R23.R2 XREF EXECUTED BEFORE GRAMMAR WORK (frontier point 3 — the decisive
   lever): Ghidra immediate-scan of d2_full (SearchImmediate.java, verified
   compile) — 0x80807850 = ONE hit (data @0x141F927B8, no function, 0 xrefs);
   vendor-table tag 0x8131931D = ZERO hits; sibling classes 4A/4E/60/61 = one
   data hit each (content-class registry region). VERDICT: the client has NO
   code consuming slot 116 — the class is data-registry-only. Vendor storefront
   semantics (if they exist) are server-side-only or manifest-defined. R23.R1
   grammar work is demoted; the manifest lane's DestinyVendorDefinition join
   settles the data-side question.
4. Cold-run batches 2-6: PARKED EXPLICITLY (frontier point 4) — owned by the
   main session's Ghidra passes; low urgency; queued behind S0-first-commit.

REASONING HEURISTICS APPENDED (why the model concluded what it did — now standing
decision logic for this project):
- H1 PARALLELIZE BY RESOURCE CLASS: RE-static and build-engineering don't share
  resources — never serialize them. Start the longest pole (S0) immediately.
  Building is an oracle too: runtime ground truth beats static inference for
  semantics questions.
- H2 EVIDENCE HIERARCHY: one direct xref (the consumer) > any amount of
  structural grammar inference. Order decisive-cheap evidence first; R23.R2
  answered in one pass what R23.R1 would only gesture at. (Validated: the xref
  settled it; grammar work demoted.)
- H3 LEDGER HYGIENE: filed-but-unowned items die (the 5 spawn failures and the
  cold-run batches had no owner). Every open item gets an owner or an explicit
  parking lot.
- H4 CHEAP-CLOSURE INSTINCT: when new tooling lands, immediately retry
  previously-blocked one-pass items (tiger-pkg cleared the 5 for free).
- H5 PERMISSION x FAILURE-MODE: bash-enabled lanes dying mid-command can leave
  partial side effects — write-early + crash-safe incremental writes stay
  sacred and are now baked into every lane brief.
- H6 THE GATE IS SYMMETRIC AND LOAD-BEARING: the model's first act was
  spot-checking our claims against disk; ours is the same against lanes
  (2 cosmetic path slips caught on the S0 inventory, substance exact).

## 9.10 THREE-LAYER ONTOLOGY ADOPTED (second frontier-model review) + conflict check

The model's three-layer reframe is adopted as the standing ontology. CONFLICT
CHECK vs in-flight work: NONE — verified per-file; every running lane owns a
disjoint file set (item lane = item_blobs/*; manifest lane = content/manifest/*;
build lane = the fork tree; HTTPS-spec lane = claims only; layer2-map lane =
content/root_slots/* + new JSONs).

- LAYER 1 — wire protocol: DONE (our five specs). Role: the contract S0 must
  honor; the S0 validation oracle.
- LAYER 2 — content data: ~half done, FINISHABLE (mechanical, no unknowns).
  Extracted deep: items (100%), spawns (386/386), investment chain, named tags,
  bubbles. Remaining: bulk activity definitions (decodable, never run at scale),
  remaining ~45 investment-root slot types, encounter/director data, census
  long tail (~60 real classes; ~10 deep). ACTION TAKEN: layer2-map lane launched
  (root slots + census tail classification → the ranked completion todo list).
- LAYER 3 — server decision logic: AUTHORING, NOT DECODING. By provenance it
  cannot be decoded: authority never shipped in destiny2.exe; the client holds
  only consumers (render/effects) + schemas. The ~8.7K middle-band/cold-run
  code = the CLIENT'S half (prediction, effects, animation) — bounded value,
  not server secrets. Consequence: S3+ = game development informed by community
  docs + D1 priors + live capture (if ever) + guesswork calibrated against what
  the client accepts. Exact parity remains impossible by construction.
- S0 AS THE PIVOT: the moment the project stops reading the game and starts
  writing its successor. Everything decodeable feeds S0; S3+ is BUILT ON it.
  Consistent with H1 (build is an oracle) — now elevated to project identity.
- Cold-run batches 2-6 + middle-band naming: RE-SCOPED to "client code depth"
  (the client's half) — parked, non-blocking, demoted below all Layer-2
  mechanical work.

REASONING APPENDED (why this model framed it this way):
- R1 PROVENANCE ARGUMENT: what can a file possibly contain? The client binary
  can contain what it executed — consumers and schemas — never the authority
  that ran elsewhere. Layer classification by data provenance, not effort.
- R2 NOUNS-VS-VERBS: content = nouns (bounded, enumerable, extractable);
  logic = verbs (unbounded, authored). The census IS the Layer-2 work
  breakdown — completion is measured in class coverage.
- R3 IDENTITY SHIFT: archaeology → construction. The same act (building the
  standalone server) is simultaneously the last Layer-1 milestone and the
  first Layer-3 artifact.

## 9.11 VENDOR QUESTION CLOSED — three independent lines of evidence (manifest lane VERIFIED)

Main-session verification (independent re-join, all numbers exact):
- Manifest DBs on disk: Arrivals-era (zip 11,880,819 B, inner sqlite
  111,097,856 B, sha256 09A322805F0FD982...) via Wayback (keyless API now
  requires a key — ErrorCode 2102, recorded) + current-era CDN DB (36,953,298 B).
- Join (signed-int32 id conversion): item universe 15,343/15,424 (99.47%) in
  DestinyInventoryItemDefinition (table has 15,425 rows — near set equality);
  slot-116 507/511 in DestinyVendorDefinition (table has exactly 507 rows in
  era — THE ENTIRE VENDOR TABLE). Named samples verified: Anarchy 0x8DA63B0E
  (item), Commander Zavala 0x04243655 (vendor).

THE VENDOR VERDICT — slot 116 IS the DestinyVendorDefinition hash space, proven
three independent ways:
1. STRUCTURE (R22): odd-class root slot, 511 rows, 24B index grammar.
2. CLIENT (R23.R2 xref): ZERO code consumers of class 0x80807850 or the table
   tag — the client never reads slot 116; vendors are served server-side
   (storefront content rides family-5 web-service messages, not this table).
3. MANIFEST (R24): 507/511 hashes == the era's entire vendor table (set
   equality). The 4 outliers: 3 exist as item+vendor in the 2026 manifest
   (added/re-hashed later), 1 (0xE31B110B) matches nothing anywhere —
   client-side-only hash.
R23's "weakened" label and R24's "confirmed" are BOTH correct readings of their
own evidence — the table is vendor definitions WITHOUT item references because
sale lists are encoded as small-index pairs into sibling tables (the embedded
arrays), not item hashes. Resolution recorded for the grammar work.
- Field-schema oracle live: manifest_item_schema.json (57 top-level item keys +
  nested key sets). Item names now available for all 15,343 matched items —
  feeds the item-definitions lane's structural decode.
- NEXT (queued): full 507-vendor name harvest; classify the 81 unmatched items
  + 4 vendor outliers vs the 2026 manifest; vendor name index for S2's
  storefront work.

## 9.12 S0 HTTPS-SURFACE SPEC VERIFIED (22 claims; 3 assumption corrections)

Main-session verification (grep, all key lines exact):
- setopt_guard.cpp:87 forces VERIFYPEER/VERIFYHOST off — ANY self-signed cert
  works; no pinned-key bypass needed (no pin in corpus).
- external_server_route.cpp rewrite_host: scheme prefix preserved, whole
  host:port replaced by client.externalServer.host → BOTH SignOn POST and
  config GET land on PORT 443 (not configurable); egress connect() rewrite is
  a surviving gate (IPv4 only, forced destination).
- config_getter_answers.cpp: kTokenSize = 37 static_assert; the served
  manifest's ContentConfig protobuf FIELD 5 must equal the client's
  config_guid byte-for-byte; SignOn response field 6 must stay "d2legacy".
- Lane path slips (cosmetic, corrected): config_getter_answers.cpp lives under
  client/hooks/config_getter/, not client/content/.
- The v1-standalone milestone scope, 9-risk table, and 5-step implementation
  order accepted (RE_output/claims/s0-https-surface.md). Acceptance runbook
  patched with the 443/TLS/GUID corrections.

## 9.13 ITEM-DEFINITION GRAMMAR VERIFIED + FULL DB CAMPAIGN LAUNCHED

Main-session verification (40/40 on all three core checks, re-derived):
- u64@0 == decoded size (40/40); u64@8 == 240 CONSTANT (40/40 — NOT socket
  count; correction to prior context, real socket counts 6-11 live in the
  socket block, eclass 0x808077C4); u32@0xA0 == own definitionHash (40/40).
- Grammar accepted: every array = descriptor-resolved with 0x80809FBD marker
  (268/268 in sample); stat block = RelativeArray variant (marker 0x808077B9,
  rows stride 40 {u32 row@+16, i32 value@+20}); five block-relative fields
  @0x10/0x68/0x70/0x80/0x88 (equipment/ordinary-sockets/stats/socket-list/art);
  bucket field @0xB8; row-stride table for 9+ element classes; 0/40 outliers,
  0 ascii runs (names fully hashed in-blob).
- Full batch launched (item-db-full lane): 15,424 blobs × verified grammar +
  manifest displayProperties.name join (15,343 names available) →
  RE_output/content/item_definitions_full.json — the NAMED item database,
  the S1 datagen backbone.

## 9.14 S0 FIRST COMMIT VERIFIED — sunrise-server.exe LISTENS STANDALONE (BAP external, zero wire changes)

Main-session verification (independent rebuild + smoke, not the lane's):
- Working tree vs b12a9da: EXACTLY 4 files — server_runtime.cpp (+19, the
  SUNRISE_STANDALONE_SERVER guard; verified minimal: skips client consumer
  registry + UI, calls transport::initialize directly), server_main.cpp,
  sunrise-server.vcxproj (277 ClCompile items), sunrise_server.rc. No other
  touches; not committed (user's call).
- MY rebuild: sunrise-server.exe builds clean (v143 override, /W4-as-error).
- MY smoke test (settings.json no-BOM, port 30975): real log
  (Sunrise\logs\sunrise.log next to exe — console stays empty by design):
  `ev=initialize result=ok`, `ev=transport stage=listen result=ok port=30975`,
  python TCP connect → `ev=transport stage=accept result=ok conn=1`,
  expected `content_manifest skipped reason=no_packages`. THE BAP SERVER RUNS
  IN ITS OWN PROCESS WITH ZERO WIRE CHANGES — the largest single chunk of S0.
- Deviations accepted: content_manifest skip-on-missing-packages (mirrors the
  DLL's notApplicable short-circuit); port 30975 (game owned 30974; SO_REUSEADDR
  double-bind observed — DLL/server port separation needed before client
  switch-on); core/ui/busy added to compile set (two state files include it);
  new minimal .rc (the DLL's references detours/imgui licenses).
- Known: bootstrap token value = ASCII "2MFioXto7iAUN4Qj" (16 B, from 8.12's
  verified crypto derivation; caveat stands — confirm against the live publish
  at client-acceptance time). build_data.bin handoff = copy from
  "dcv build\bin\x64\Sunrise\cache\" into the server's Sunrise\cache\.
- NEXT MILESTONE LAUNCHED: HTTP/SignOn surface (spec s0-https-surface.md +
  corrected acceptance runbook): listener shell on 443 → /SignOn via
  http::consume → config GET via manifest::encode with field 5 == config_guid →
  data handoff (token + cache) → end-to-end client acceptance (tier 2, needs
  the user's game session).

## 9.15 LAYER-2 MAP VERIFIED + TIER-0 CAMPAIGNS LAUNCHED

Main-session verification (read-only re-derivation):
- Root 0x81327D22: 119 slots — 5 unknown EXACTLY match the lane's list
  (slot 2 = 9,552 B, slot 36 = 1,000,104 B mega-object, 47 = 380 B,
  87 = 320 B, 117 = 7,424 B), 14 direct, 13 empty, 1 non-tag, 86
  array/known (recorded shape '?' in the JSON schema — cosmetic).
- Census long tail: top-40 exact (0x80809C36 → 241,586, 0x808071E8 →
  237,115, ...), 40 class_scan requests filed, all unnamed-in-corpus.
- Key structural discovery accepted: census space (entry-table references)
  is DISJOINT from the investment elementClass space (all 84 root
  elementClasses absent from census).
- The activity-definition cluster is the big unextracted category: 0x80809994
  (468), 0x80808AAE (468), 0x80809187 (85, wraps NEW elementClass 0x80808AAD),
  0x808091DE (85). Confirmed decodable (0x80B4A06A proof), never run
  at scale. Verified cluster = 1,106 entry rows (468+468+85+85 — activity
  campaign; 0x8080918B contributes 0 entry rows, census-absent per 9.16;
  its 109 named-tag records are a separate population).
- TIER-0 LAUNCHED: (a) activity-campaign lane (class_scan re-run + bulk
  tigercli decode of the 1,106-tag cluster → activity_defs catalog);
  (b) root-residuals lane (classify the 5 unknown blobs + element-row grammar
  of the 80 arrays, all offline on saved blobs). P5-P9 parked with owners.

## 9.16 ACTIVITY-CAMPAIGN QUOTING BUG — LANE SELF-RECOVERED; 0x8080918B CORRECTION

Review-flagged bug CONFIRMED then already-handled by the lane itself (write-early
discipline working): unquoted PowerShell comma-joined hex args wrap to negative
Int32 → class_scan silently returned 0 entries with EXIT=0. The lane diagnosed
it (CLAIM AD-001, reproduced with a probe), re-ran QUOTED, and the entries files
now carry real content (0x80809994 → 74.5 KB, 0x80808AAE → 74.5 KB, 0x80809187
→ 13.6 KB, 0x808091DE → 11.9 KB). Lane still running — NO replacement spawned.
HYGIENE FIX applied at the correct layer: domain brief TRAP #10 (quote
comma-joined hex args) — the bug was the shell, not the JSON.
CORRECTION (two independent sources): 0x8080918B is ABSENT from the census
(class_reference_census.json has no row; class_scan returns []) — the
layer2-map lane's "0x8080918B companion, ~55 tags" claim is RETRACTED; its
dangling-block observations may belong to 0x80809187's family under a
different id. 0x80809187/0x808091DE both = 85 rows (census).
REVIEW ITEMS 3+4 WERE ALREADY DONE (see §9.9): R23.R2 xref executed (one data
hit, zero code consumers of 0x80807850; tag 0x8131931D zero hits); the 5 spawn
failures decoded 5/5 via tigercli (386/386 sets).

## 9.17 REVIEW ROUND 3 INCORPORATED — gating rule + batched acceptance + audit/S1 lanes

1. SLOT-116: ALREADY CLOSED PERMANENTLY (§9.11) — the manifest join (507/511 =
   the era's entire DestinyVendorDefinition table, independently verified) +
   the zero-consumer xref IS the verdict, and the review's reframe (zero code
   consumers STRENGTHENS the vendor reading: exactly what server-side
   shipped-on-disk content looks like) matches the recorded synthesis.
2. LONG-TAIL TRIAGE RULE ADOPTED (standing policy): no more blind census scans.
   Extraction acceptance criteria: reachable from the investment root, the
   activity cluster, or explicitly required by S1/S2 datagen — otherwise skip.
   The 40 filed census scans are PARKED, not queued. Layer 2 is "done enough"
   the moment the S1/S2 specs stop asking it for things.
3. RECONCILIATION AUDIT LANE LAUNCHED (read-only): corpus sweep for consumers
   of retracted/superseded claims (0x8080918B, 12,699 artifact, FNV-1a-64
   spawn/bubble confusion, R23 vendor weakening, 8.27 container-fit void,
   8.25 third-key retraction, pre-tigercli paths, the 5 spawn "failures")
   → correction ledger for the main session to apply.
4. S1 DATAGEN SPEC LANE LAUNCHED: hardcoded datagen map (family 3/4) → the
   extracted-content replacement map → sqlite schema v1 → startup-state
   pipeline → gap list. The Layer 2→3 bridge, written while the activity
   campaign + item DB finish.
5. ONE-SESSION ACCEPTANCE SCRIPTED into the runbook (s0-acceptance-runbook.md
   §ONE-SESSION EXECUTION SCRIPT): pre-session prep (elevated 443, no-BOM
   settings, build_data.bin copy, token/GUID), in-session launch + harvest
   (server log, decoder 71-set read), verdict table. ONE game session
   validates the entire HTTP/SignOn milestone end-to-end — no partial burns.

## 9.18 FULL ITEM DB VERIFIED — S1 BACKBONE MATERIALIZED (15,424/15,424)

Main-session verification (streamed re-check): 15,424 records; sizeOk
15,424/15,424; u64@8==240 15,424/15,424 (constant held at scale); non-empty
names 14,116 (+1,227 empty-string names = 15,343 coverage, 99.475%, matching
the lane's nuance); 5,386 records carry divergence flags (A4 3,192 + B0 2,539
+ A8 7 + hash 81, minus overlaps — consistent). 111,417 array headers, all
0x80809FBD, 0 unresolved (lane).
- Scale discoveries accepted: 0x808077AE stride 88→8 correction ({u32,u32}
  pairs); 1,340 zero-array items (tier placeholders, currencies, mods);
  u32@0xA4/0xB0 NOT constants at scale (B0 = 73% other manifest row ids →
  reference field); sandbox-perks split (0x808077BC at statH+16 / 0x808077B4
  at art+16); second equipment-block layout (sentinel-start); 7 new element
  classes. Flag-and-keep policy held.
- S1 unlock (lane, accepted): item_definitions_full.json = the backbone table
  — definitionHash → fixed fields, five blocks, arrays, names — enough to emit
  family-4 inventory rows + socket application + investment stats for every
  Arrivals definition WITHOUT runtime manifest lookups.
- Open: A4/B0 field semantics; the 7 new element-class names; sentinel-start
  equipment layout fields (REQUEST filed in item-db-full.md).

## 9.19 ACTIVITY CAMPAIGN VERIFIED (1,106/1,106) + CHILD-EXTRACTION LAUNCHED

Main-session verification: activity_index.jsonl = 1,106 records (0 failures);
catalog = 468 defs with children, 6,111 out-of-cluster child tags, 0 in-cluster.
Confirmed the campaign's key structural discovery: 3 of the 6 resolved child
classes (0x8080925B/0x80809A88/0x8080925E) ARE census top-40 giants — the
"mystery long-tail classes" are activity-component classes.
- Grammar accepted (spot-verified against the lane's claims): self-size header;
  u32@8 = FNV-1 (NOT FNV-1a) of the activity's ambient name (the activity name
  space uses the other FNV variant — consistent with the bubble-lane's
  FNV-1 finding for spawn names); nested 0x80809FBD marker containers; tail =
  ASCII activity-name strings. AAE→def 1:1 bijection (468/468).
- CHILD-EXTRACTION LANE LAUNCHED: class_scan + bulk decode of the 6-8 child
  classes + the 6,103 out-of-cluster children → full activity-component
  grammar + child-tag resolution. (0x8080925B = the most-referenced content
  object in the cluster — x6,041 refs — identified this wave.)
- Remaining activity items: 0x8080918B per-pkg block resolution (re-filed);
  AD-R001 nested-descriptor wiring; AD-R002 element-hash identity.

## 9.20 ROOT RESIDUALS VERIFIED — INVESTMENT ROOT 119/119 STRUCTURALLY MAPPED

Main-session verification (blob re-reads): slot 87 = all-zero stub (size@0=320,
79 zero-u32s); slot 117 = descriptor @0x18 {115,16} → 115×64 = 7,360 byte-exact;
slot 47 = 380 B with "vendor_challenge" string; slot 36 = 1,000,104 B, u32@8 =
0x43B15180 (namehash), u32@16 = 0x81320673 (tag ref) — all matching the lane.
- 119/119 slots carry structural verdicts: 6 known + 80 arrays (73 with stride
  evidence, 39 byte-exact) + 14 direct + 5 newly-classified containers (2/36/
  47/87/117) + 13 empty + 1 non-tag. Slot 36 = the investment root's big
  definition tree (7,385 nested tables; NOT the vendor table — vendor-hash
  hits at random level). L2-R001 CLOSED.
- Left (re-filed): slot-36 table A grammar (7,763 rows), 7 unknown element
  classes, slots 4/21 nested payload trees, slot-47 role pin (class_scan
  0x8080294D), optional bulk row extraction (109,253 rows).
- FINAL LAYER-2 GRAMMAR LANE LAUNCHED for those (offline, bounded).

## 9.21 S1 DATAGEN SPEC VERIFIED — STOREFRONT DATA LAYER COMPLETE (manifest solves sale lists)

Main-session verification (own re-join with corrected signed↔u32 conversion):
vendor sale lists: 507 rows, 498 nonempty itemList, 15,126 sale items, 6,135
unique hashes, **6,109/6,135 resolving in the item universe** (exact lane
numbers), 4,102 priced, 497/507 named. §9.11's limitation (sale lists absent
from the 511 package blobs) is RESOLVED by the manifest: the storefront DATA
layer is complete; only the purchase opcodes (701/1200-1399) remain unmapped
on the wire — a separate RE task, deferred.
- Static inventory: 97/97 authored hashes resolve in item_definitions_full.json
  (100%); ~20% of the ~1,878 hardcoded datagen values are data-sourced today;
  the rest = runtime state (flags/objectives/entitlements) with seed-and-keep
  as the cheapest resolution.
- sqlite schema v1 (12 tables) + S1 step order S1-0..S1-7 with acceptance
  gates (96,280-B account-object memcmp parity; second-boot frame equality).
- Correction accepted: family5_flag_overrides = 39 rows (not 40).

## 9.22 S0 HTTP MILESTONE VERIFIED (my own smoke) + STRAGGLERS + SPAWN MERGE

- MY OWN five-point verification passed: SignOn POST https://127.0.0.1:443 →
  200/372 B; config GET → 200/105,232 B; BAP accept; boot log lines all
  present (https listen ok port=443, GUID logged 09d54b23-..., token ok,
  oodle ok, build_data cache ok, per-request statuses). TWO real bugs were
  caught by the lane's own testing (SChannel ASC_REQ_ALLOCATE_MEMORY leak;
  body-drain record bytes) and fixed. The standalone server now speaks the
  client's full contract: HTTPS SignOn + config + BAP. S0's only remaining
  step = the tier-2 client acceptance (ONE user game session, scripted in
  the runbook; the manifest cache is already byte-identical to the game's).
- Stragglers lane verified-by-evidence: 0x808047B7 = client-side UI screen
  container (measured header grammar: self-size, magic 0x80D5FFBF, tag slots,
  32-B element rows; NOT server-relevant); 0x80809BAB = loading/spaceflight
  environment asset (52-B wrappers → 0x80809C0F placement lists; structured,
  NOT filler; client-visual). Triage rule applied — no bulk extractions.
  NOTE: the lane's "still open" list is stale (P1-P5/P8 closed by the activity
  campaign + root residuals AFTER its brief was written) — reconciliation
  catch, noted.
- SPAWN MERGE DONE (audit NEW #1 resolved): the 5 decoded sets merged into
  spawn_sets_full.json — now 386/386 sets, 8,892 points (was 381/8,779;
  0x80C754AE is a genuine 0-point set). All "386/386" claims are now
  data-backed; the "8,779 points" figures should read 8,892 going forward.

## 9.23 HOST RESTART — LANE RECOVERY + RESUMPTION

opencode server killed and restarted (user): 4 in-flight lanes went stale.
Artifact triage (main session) established exact recovery points:
- vendor-table-full: 511 blobs decoded + manifest join DONE; parse/JSONL/claims
  never ran → resumed at the parse step.
- activity-children: bulk decode COMPLETE (61,531 indexed entries, 61,508
  blobs); shape census/catalog/claims never ran → resumed at census step.
- census-triage-v2: scan + 186 sample decodes DONE; classification/JSON/FINAL
  never ran → resumed at classification step.
- root-leaf-grammar-v2: no outputs; blob analysis redone from spec → resumed.
All four resumption lanes launched with the observed state in their briefs.
Lesson logged: resume-by-artifact works (all four had partial, valid,
resumable state on disk — the write-early/crash-safe discipline is what made
the recovery possible).

## 9.24 CENSUS TRIAGE v2 COMPLETE + VERIFIED — LAYER-2 TRIAGE CLOSED

Main-session verification: 40/40 records; role histogram exactly {16 logic-
content, 16 table, 8 asset, 0 unknown}. 173/173 samples decoded, entries==
census ratio 1.000.
- The census space is DISJOINT from both element-class universes (40 ∩ item27
  = ∅, ∩ root84 = ∅; item27 ∩ root84 = ∅) — the only bridge: 0x80800070 (the
  0x80809A88 activity container's element class == the item DB's definition-
  container class).
- Classification accepted: 3 activity-component classes (0x8080925B/925E/
  9A88) IN FLIGHT elsewhere (activity-children lane); 13 logic-content
  classes queued for a future extraction layer with row formats documented;
  8 assets stop at verdicts with evidence (audio/cinematics/placement/
  geometry + the 0x80806EF4→0x8080966D pure-alias discovery); 16 tables stop
  at verdicts.
- Two corrections by the lane (0x80807140 heads-only identity; 0x80807194
  standard container) accepted.
- LAYER-2 TRIAGE: CLOSED per the triage rule — every long-tail class now has
  an evidence-backed verdict; nothing blind-extracted.

## 9.25 ACTIVITY-CHILDREN VERIFIED — FULL ACTIVITY CLUSTER DECODED (S4 content)

Main-session verification: index per-class counts EXACT (5,941/4,489/4,630/
20/85/14/46,235/90/27); 0x8080925B grammar `len == 40+nameLen` holds 300/300
sampled; companionTag@20 → 0x8080925E links 500/500 sampled (int-normalized;
two earlier check formats were MY bugs, caught and corrected — gate
symmetric).
- 0x8080925B verdict accepted: flat per-activity-NAME companion descriptor
  (5,941/5,941, size+nameLen self-consistent; names like 'edz', 'pandora',
  'city_tower_d2', 'mercury_trials_lighthouse') linking every def to its
  0x8080925E data companion — the most-referenced object in the cluster.
- The complete def → component graph is now enumerable: def (468) → name
  companions (5,941) → data companions (→ 0x80809462) + audio sets
  (0x80809A88 → 0x80809A8A) + mode bindings (0x80809780 → mixed) + parameter
  objects (0x80809440) + family aggregates (0x80809962, 85) + AAE companions
  (0x80809BA3 → 0x80809BAB) + anonymous content (0x80809C0F, 46,235).
  237 MB of decoded activity content on disk; out-of-cluster children 100%
  resolved (27 no-entry = pkg-absent edge, logged).
- Remaining (re-filed): 0x8080918B block resolution; next-layer classes
  (0x80809A8A/0x80809462/0x80809BAB/0x80808495); AD-R001/AD-R002.

## 9.26 VENDOR TABLE FULL VERIFIED — SLOT-116 FULLY RESOLVED (R23 question CLOSED)

Main-session verification: 511 records; sizeOk 511/511; selfKey 487/511
(u32@12 variant, 24 flagged — matches); 0x80807860 arrays = 507, 0x80807861 =
507 (one each in 507 blobs; the 4 outliers are client-side-only defs); 13
element classes match the lane's histogram.
- VERDICT ACCEPTED: the 511 blobs ARE runtime-serialized
  DestinyVendorDefinition records. 0x80807860 = CATEGORIES (24-B elements
  carrying the manifest categoryHash verbatim), 0x80807861 = ITEMLIST
  (variable-size, data-relative pointers), counts matching the manifest
  (490/507 + 488/507 exact; deltas = manifest-vs-client content divergence,
  Classified/Tess/Recaster/planet vendors). R23's "vendor unconfirmed" is now
  RESOLVED — four independent lines: structure, client xref, manifest join,
  and now full-population grammar.
- SALE-LIST VERDICT: 0 item references, 0 item-hash hits, 0 currency hits —
  the blobs hold INDEXES + category hashes; items resolve through the
  manifest itemList. A private server can serialize manifest vendor JSON into
  this exact blob grammar for the family-5 investment path.
- S1/S2 unlock: the first complete NAMED vendor table for Arrivals (507 named
  vendors, full displayProperties, per-vendor storefront structure known).
- Open (filed): 61-element internals, count deltas, D31/D2F semantics,
  identifier hash tables, client consumer of 0x80807850 (R1).

## 9.27 LAYER-2 DISCOVERY CAMPAIGN COMPLETE — final lane verified

Main-session verification: slot-36 Table A = 7,763 contiguous 40-B rows
byte-exact (310,520 B); key column strictly increasing over ALL rows
(0x20858..0xffffafd4); trailing 689,456 B = package-4 tag tree (not rows).
root_leaf_grammar_v2.json exists with all four sections.
- 7/7 former-unknown element classes RESOLVED: all direct containers of
  nested 0x80809FBD tables (5..15,983 tables each, header-validated) with
  {value,0}-pair rows; slot 40 fully decoded (5x 0x808078AE tables, {hash,idx}
  rows); recurring nested eclasses tie them to slots 2/47.
- Slots 4/21: slot 4 regular at 3 levels (1,170-row {hash,0,relOffset,0}
  index -> 14,805/14,805 validated nested tables); slot 21 = 59-entry hash
  list + 1,267-table tree. Record bodies at level 4 = the only byte-level
  gaps, needing a live decoder for field SEMANTICS (not structure).
- LAYER-2 FINAL STATE: 100% of the 80 array slots carry row-level models;
  the whole investment root (119/119 slots), the item universe (15,424),
  vendors (511), spawns (386/386), activities (468 defs + full component
  graph), bubbles, and the census long tail (40/40 classified) are decoded
  and interlinked. Discovery is DONE — remaining work is semantic
  interpretation (live-decoder reads) or S1/S2 consumption.

## 9.28 HYGIENE WAVE (review point 1) — domain brief + FINDINGS fixed; ledger lane running

Main-session edits applied: domain_brief.md cold-run line (12,699 → ~8,692 real
+ purge note), content line (386/386 sets, 8,892 points), FNV convention split
into the TWO VOCABULARIES rule (tag-name = FNV-1a-32; spawn/bubble/activity-name
= FNV-1-32); FINDINGS_2026-08-14.md:1037 tiger-pkg supersession note.
- Ledger lane running: applies the 8 claims-file fixes + closes the ~1,100
  arithmetic + the 109-record provenance question.
- SWEEP COUNT CLOSED (Ghidra pass, CountRunFunctions.java): d2_full captured-run
  ranges hold exactly 1 function — thunk_EXT_FUN_081d0801 @0x145D53000 (run1),
  0 in run2. 8.23's "one survivor left in place" is EXACT; the survivor is a
  legitimate external thunk, not sweep noise — no re-poisoning risk for future
  count-based passes.
- SEQUENCE (per review): S1 implementation + S2 spec lanes launch AFTER the
  hygiene lane lands (S1 consumes numbers from those files).

## 9.29 S0 TIER-2 ACCEPTANCE PASSED — GAME BOOTED AGAINST THE STANDALONE SERVER

THE ACCEPTANCE BAR IS CLEARED (2026-08-14 late night). Evidence chain (all
captured this session):
1. The game (PID 39456) booted with client.external_server enabled, speaking
   to the ISOLATED standalone server (RE_output/s0_accept/). Server log shows
   the game's own traffic: SignOn POST → 200 (372 B), config GET → 200
   (105,232 B, field 5 = fingerprint GUID 09d54b23-...), BAP accept on 30975,
   and `ev=queuez stage=poll result=ok` — the family-0 subscription.
2. DECODER REGISTRY = 71/71 EXACT — RE_output/content/registry_acceptance.json
   populated the identical 71-type set as the in-process boot (0 missing,
   0 extra). The client instantiated every decoder type from the standalone
   wire — the strongest possible wire-parity proof.
3. The game reached character select on the standalone server's datagen.

ROOT CAUSE OF THE WHOLE ACCEPTANCE SAGA (final, evidence-complete):
- The settings-file EDITS were the killer, not the feature, not the DLL
  version, not logging. Every failed armed boot died at DLL settings-parse/
  init (zero logs, zero network) because the settings file had been
  RESERIALIZED (python json.dump) during arming. The winning protocol:
  byte-level edit of the backup — exactly two values changed (enabled
  false→true; config_guid → fingerprint), no reserialization, no BOM, same
  line endings. That file boots. (The "0.2.0 predates the feature" theory was
  REFUTED by strings-scan: both DLLs carry the feature strings; the backup
  itself carried the disabled external_server block all along.)
- The isolated-server strategy held: the acceptance server (RE_output/s0_accept)
  was immune to the S1 lane's fork rebuilds, and the tier-1 verification
  (SignOn/config/BAP curl) preceded every boot.

POST-ACCEPTANCE (per plan): DLL = original 0.2.0 already in place
(steam_api64_021_fork.bak parked); settings restored from .bak_s0accept after
this session's boots so the daily in-process game stays untouched. The
standalone acceptance stack (isolated exe + caches + settings) remains in
RE_output/s0_accept/ for reuse.
- OPEN (decided separately, not improvised): the fork-DLL (0.2.1) external
  boot was never retested after the minimal-delta fix — the acceptance above
  used the ORIGINAL 0.2.0 DLL. If the fork DLL is wanted for the game side,
  its own two-boot sequence applies later.

## 9.30 VERIFICATION GATES 1-2 PASSED + S1-3 PARALLEL-PATH LAUNCHED (user directive 2026-08-15)

GATE 1 — S1 lane claims S1I-1..12 mechanically re-derived (verify_s1_s2_gates.py,
this session): ALL PASS. 15,424 bijection (index lines == blob files == 15,424;
report booleans true; item_vs_cache mismatch 0; buckets 0..49); detail_vs_cache
93/93 checked 0 mismatch; authored 97/97; names 15,343/15,424; memcmp pair both
96,280 B sha equal (1221DCF6..., case-matched); DB counts EXACT (meta 3, accounts 1,
characters 3, items 55, flags 5,255, objectives 990, family5 47, entitlements 19);
boot-from-DB two-identity-lines verified in the live log.
- FINDING: state.db (25,047,040 B) carries UNCLAIMED progress beyond the claims file:
  vendors 507, vendor_sale_items 15,126, item_plugs 294 (instance_state/progression
  empty). Claims stop at S1I-12; the resume lane's first task = claim S1I-13 for the
  as-is state before new code.
- S1I-8 mechanism accepted (cache re-stamp); s1_cache_domains.json counts
  cross-check against independent findings (items 15,424, scenarios 466, hashNames
  171, spawnNameHashes 2,090 all consistent).

GATE 2 — S2 spec verified (spawn_sets_full.json + labels + fork-source anchors):
386/386 sets, 8,892/8,892 points (labels: 381 labeled + the 5 known unlabeled, exact
match to the spec's failure list); set 0x80C250C4 = 6 points 3×C9DECE4A+3×5B9195B4
confirmed; city_tower_d2 = 5 labeled sets/55 pts + the 6-pt unlabeled set = 61 in the
JSON (internal consistency holds). B3 premise VERIFIED in fork source
(activity_message_route.cpp:164 prepare_join passes entity_slots::kSlotCount = full
grant); push log line + kMaximumMessageType=58 + spawn_reader.h stride 48/rotation@0/
position@16/nameHash@32 all verified. ONE cosmetic catch: the spec cites
spawn_reader.h under client/content/spawn_sets/ — real path is
middleware/content/packages/tables/spawn_reader.h (substance exact, same tree the
spec's source list names). B1/B2 rest on prior verified negatives (FINDINGS 8.28/9.6).
S2-0..S2-4 order internally consistent: S2-0 gates on R1/R2 + S0 only; S2-3 depends
on S1-4 (whose data already sits in the DB).

GATE 3 — SETTINGS TRAP RECORDED: domain_brief.md TRAP #11 (byte-level two-value edit
from a known-good backup; NEVER reserialize; the DLL parser dies pre-log/pre-network).
Six boots bought it; it now costs zero.

S1-3 DECISION (parallel path, per directive): two background flash lanes launched
2026-08-15:
- S1 implementation resume (utility; ses_ffbc6b590ffeVNzE8Bl8UF35tM): Option A
  continues — S1-4→S1-7 per spec; reconcile unclaimed DB state first (S1I-13);
  brief RE_output/claims/s1-lane-resume-brief.md.
- Root-slots five-domain decode (content-extraction; ses_ffbc6b58effeiYJ2WshJD1AYvd):
  inventoryBuckets 49 / socketEntryLists 14 / socketEntryTables 9 / abilityBuckets 3 /
  progressions 88 from investment_root_full.json + root_slots/ via the proven
  tigercli pipeline; brief RE_output/claims/s1-rootslots-lane-brief.md. When it
  lands + verifies: swap the oracle (Option A cache → Option B decoded domains) and
  re-run the affected S1 gates.
- Fork-DLL (0.2.1) external-boot retest stays PARKED (0.2.0 proven).

## 9.31 ROOT-SLOTS LANE LANDED + VERIFIED — FIVE DOMAINS DECODED (Option B unblocked)

Lane (content-extraction, ses_ffbc6b58effeiYJ2WshJD1AYvd) completed: all five S1-3
Option-B domains decoded, cache-MATCH, delivered (s1_domains_*.json).
MAIN-SESSION VERIFICATION (independent tigercli decodes + struct reads, this
session): ALL PASS.
- inventoryBuckets 49/49: decoded 0x81327CE0 (4,720 B) myself; count@140 = 49;
  all 49 descriptors {bucketId, firstSlot, slotCount, arraySelector} field-equal
  to the JSON rows (0 mismatches).
- socketEntryLists 14/14: decoded 0x8131932C (384 B); count@8 = 14; the 14
  definitionHashes match the lane's list in EXACT order; 13 distinct target tags,
  rows 0+13 both → 0x81318000.
- socketEntryTables 9/9: spot-verified target 0x81318088 (2,444 B): count@16 = 24,
  rel@24 = 56, marker@76, eclass@88 = 0x80807A86.
- abilityBuckets 3/3: JSON rows 3; sample row (list 2, selection [6,7,10,11,2],
  bucket kinds {30,4,8,3,12,...}) matches the claim. NOTE (lane's honest negative,
  accepted): domain is account-config-DEPENDENT — rebuild via s1_domains_step3.py +
  final if the authored characters change.
- progressions 88/88: decoded 0x8131933C (190,664 B); count@8 = 88; scope-byte
  histogram EXACT {0:33, 1:24, 2:1, 4:1, 5:4, 6:2, 7:1, 8:22}; JSON rows 88.
- S1RD-06 VERIFIED AT SOURCE: definition_index_table.cpp resolve_descriptor —
  base = offset + kDescriptorStride(8); header = base + relative (R22's 16+rel
  was the descriptor@8 coincidence); marker@header-4, eclass@header+8 confirmed
  on three blobs.
- S1RD-08 VERIFIED AT SOURCE: cache_domain_validation.cpp canonicalize() sorts
  named/items/details/buckets/socket-lists/socket-tables/ability with the named
  _less comparators.
- GATE SYMMETRY LOG: my own two initial check-misses (hex zero-padding in the
  expected list; misreading the header convention) — the lane's grammar was
  precise and the blobs confirmed it. Lane: 2 real self-catches mid-run
  (S1RD-06/07), 0 false claims.

ORACLE SWAP (per directive, queued): the five JSONs are the Option-B content
source. The swap (server-side content loader replacing cache::load) is FORK code
— serialized behind the running S1 lane (owns RE_build/Sunrise-fork/**). On S1
lane completion: launch the swap lane, then re-run the affected S1-3 gate
(domain counts == cache counts).

## 9.32 S1 LANE LANDED + VERIFIED (S1-4..S1-7 all gates green; one preservation-label catch)

S1 lane (utility, ses_ffbc6b590ffeVNzE8Bl8UF35tM) completed through S1-7. MAIN-SESSION
VERIFICATION (this session, my own runs):
- S1I-13/14: DB counts exact (vendors 507 / sale items 15,126 / vendor_index: 0 rows
  -1, 507 distinct, 1 legit index-0 row — ETL hex-prefix fix verified in
  s1_vendor_etl.py:45); the item_plugs 294→0 transition is explicitly claimed
  (S1I-19 step 4 nativeDefaults) and confirmed. 6,109/6,135 + 4,102 priced + 497
  named match §9.21's independent measurement.
- S1I-16/17: MY OWN --s1-test runs (3×): exit 0; family5 published_vs_db equal
  163 B (db_flags=39/db_values=8); character resolve_all ×3 items=16 (all 48
  picked resolve); encode 46,928 B; persist write_back + second_boot equal ×3 +
  total 143,371 — reproduced twice, idempotent. Flip test: my flip run confirmed
  published_vs_db equal by construction (both sides read the DB); the claimed
  "differ" line is db_vs_settings and belongs to the settings era — NOT reproduced
  this session: the era reconstruction (authored DB + 93-detail cache + full
  settings) fails to boot against the CURRENT exe (encode_db fail; logging config
  in the full settings hides boot lines). Environment artifact, claim transcripts
  stand; lane's FINAL note 3 is not reproducible as written — recorded.
- S1I-18: 13-domain size identity arithmetic reproduced EXACTLY (949,662 B).
- S1I-19: current cache parsed myself: 68 details, eqHash 0x9718FEE6734313F9 EXACT,
  internal-consistent 942,462 B. CATCH (minor): the "pre-pick cache" backup
  (build_data.bin.bak_s1i6) is an intermediate 54-detail rebuild, NOT the 93-detail
  pre-pick cache — the 93-detail content survives in build_data.bin.client_identity
  (client identity). Preservation-label inaccuracy, no data loss.
- S1I-20/21: 48/48 resolve + second-boot 143,371 B equality reproduced; post-write-
  back counts all exact (incl. item_plugs 0); backups exist.
- MY OWN GATE CATCH (symmetric): I reserialized the full settings with json.dump
  (106 KB > 64-KB cap → boot fail) — trap 11's reserialize rule applies to the
  server-side docs too (size cap, settings_runtime.cpp:22). Fixed byte-wise
  (46,310 B), then the era boot failed on a different, deeper mismatch — recorded
  above.
- Deployed state confirmed restored + re-verified after all probes (RUN FINAL
  green; cache 68 details / eqHash exact / policy settings 5,121 B).

ORACLE SWAP LAUNCHED (per directive, S1 lane complete → fork free): utility lane
(brief RE_output/claims/s1-swap-lane-brief.md) — server-side content loader
consuming the five s1_domains_*.json, runtime swap after cache::load, per-domain
MATCH log lines vs the cache (the S1-3 validation), --s1-test green as the gate.

## 9.33 MARIONBERRY SAGA ROOT CAUSE FOUND — MISSING bdNet DISCOVERY LISTENER (2026-08-15)

S1 in-game acceptance (game → standalone server) failed with marionberry on BOTH the
S1 and the proven s0 binary; the failure signature (client log, first captured
client-side trace of the project): "The platform transport method is not ready to
send secure data" → prerequisite ENUM(8) → cleanup; in some boots the game's SignOn
never even fired (signon readiness native=0), in others it fired + the BAP connected
but the session stalled (no shello).

ROOT CAUSE (VERIFIED, source-quoted): egress_discovery_responder.cpp:158-161 —
"An external server runs its own bdNet listener, so the probe must reach it
unanswered. if (client.externalServer.enabled) return {};" — in external mode the
DLL DISABLES its in-process discovery responder (the sendto-hook that answers the
game's NAT-probe/STUN on UDP 3074/3075). The standalone server (S0 extraction =
BAP + HTTPS only) has NO bdNet/discovery listener → the probes hit nothing
(client log: recvfrom 10054 x5, "Unable to contact STUN server 127.0.0.1:3074",
NAT type 2 fallback) → the game's network state degrades → signon readiness
native=0 (marionberry at title) or a stalled BAP session.

DIAGNOSTIC GOLD (keep): the healthy in-process boot's log = the reference —
STUN answered (NAT type 1, "Success sending STUN request type [2]", no RSTs),
readiness native=1, signon route, transport accept, "Endpoint traffic enabled",
char select. The external boots differ EXACTLY at the discovery probe. First
client-side log harvest (45 KB, all levels debug) = dcv build\bin\x64\Sunrise\logs\
sunrise.log(.old); the healthy baseline = sunrise.log (in-process, native=1).

WHY LAST NIGHT PASSED (INFERRED, recorded): the zombie PID 25224 (in-process-mode
boot from 8/14 18:06) still held a working discovery responder on 3074 at
acceptance time; its socket is gone now (tonight = 10054 RSTs). The game also
TOLERATES the degraded NAT in some boots (the fire/no-fire split), which masked
the gap for one session.

THE FIX (next): add the bdNet/discovery listener (UDP 3074 + 3075) to the
standalone server — a direct port of build_reply() in the same file (NatProbe:
echo 4 request bytes + clientIp^0x76C3F6BC BE + clientPort^0xF6BC BE; IpDiscovery:
type byte + version 2 LE + clientIp BE + clientPort LE) — the reply formats are
fully specified in the responder source. Acceptance = the probes answered + the
game boots to character select against the standalone server.

FIX LANE LAUNCHED: utility (brief RE_output/claims/s1-discovery-lane-brief.md):
standalone server UDP 3074/3075 discovery listener + python probe verification +
(if the user is willing) one in-game boot.

## 9.4 AGENT CONFIG CHANGE (user directive)
All 14 subagent files in .opencode/agent/*.md: `bash: deny` → `bash: allow` (user:
"enable bash for the subagents, let's let them do stuff, you will still be the
verification arm"). Main session remains the verification arm (mechanical checks of
agent claims) per standing rules. Registry restart was done by the user.

## 15:00-15:20 — THE PHR BREAKTHROUGH (the -87 case advances decisively)

VERIFIED (the full chain, all on disk):
- The ruling's step-4 in-process control: the package table = BYTE-FOR-BYTE
  IDENTICAL across the external + in-process modes (2,199/2,199 rows; the
  diff_modes_package_table.py). The table is mode-independent + innocent.
- The full tail diff: the +0x110 tails == the config field-4 buildSignatures =
  2,199/2,199 EXACT (the partial capture's 1-bit row-0 diff = a mid-write race).
- THE POISON: cache_phr_0000f7ea.dat (the game's Patch Hash Resource, created
  13:42 by the first external boot's FAILED registration). Its presence makes
  the in-process boot enter the registration (state 3) + black-screen; removed,
  the in-process = REACHES THE CHARACTER SELECT (the default Sunrise RESTORED).
  The server/discovery = EXONERATED for the in-process black (the dead server +
  the free UDP = still black with the PHR present).
- THE PHR FORMAT (parse_phr.py, phr_structure.py): the ASCII fingerprint GUID
  09d54b23-9aec-88c0-8d21-69e89d62197a + NUL + the records = {the .pkg header
  16B (version 0x26, u16 2, packageId, u16 1, sig8) + the h2 u32 + zeros} +
  the variable-length bodies (the gaps 3792/4424). The h2 varies per package
  (id 0x199 -> 0x596767B4; id 0x1C1 -> 0x59676902) = no common hash matched.
- THE VALIDATOR (phase_registration3.txt:1971): the expected = *(int*)(rec+0x168)
  (4 bytes!) vs the computed = thunk_FUN_14480a10f(kind byte @+0x16c, id u16 @+4)
  -> 0xffffffa9 (-87) on mismatch; the zero-skip = @+0x168==0; the passes = the
  +6==0 or the +0x168==0. The thunk = the cold-region chain (14480a10f ->
  14689840b -> 1453de4c4 -> 147ffd25d, the packed region, args invisible).
- THE REGENERATION: the working in-process session RE-WROTE the PHR (15:12:31,
  the same size). The fresh vs the poisoned = 517 byte-diffs in exactly two
  places: (1) @0x25 the type byte (fresh 0x01, poisoned 0x02); (2) the trailing
  ~11KB (0x269986+, fresh = the 0x05/0x06 fillers, poisoned = zeros). THE
  POISONED PHR = AN ABORTED WRITE (the -87 registration died mid-persist).
- TRAP 18 (domain_brief): the json.dump settings reformatting kills the launch
  with Bungie's "problem reading game content" (the parser is format-sensitive);
  the byte-exact restore fixed it. TRAP 19: never leave the aborted PHR in place
  (the delete = the fix for the in-process black).

OPEN:
- Does the NEXT in-process boot with the FRESH (type-1) PHR present = reach the
  character select (the fresh = benign, only the aborted type-2 = the poison) or
  the black (any PHR = the poison)? ONE BOOT.
- The external -87: the expected-hash provenance = the game's own DB; the h2 =
  the 4-byte expected candidate; the thunk = the cold-region computed. The
  WinDbg breakpoint (destiny2+0x381DD0) = the live read of both sides.
- The user's hypothesis: a "properly formatted PHR" (the type-1 complete write)
  = maybe = the external fix — test = the external boot after a healthy
  in-process session (the fresh PHR present).


## 9.34 THE POST-RULING EXECUTION (16:1x-17:0x) — cold-page diff DONE, WinDbg DEAD

- ACTION 1 (cold-page diff): the 4KB spans around the chain addresses (14480a10f /
  14689840b / 1453de4c4 / 147ffd25d) = DECRYPTED in BOTH dumps (both != the disk's
  encrypted bytes). THE COLD-ENCRYPTION THEORY IS DEAD. Residue: two pages carry tiny
  mode-diffs (a nulled absolute pointer in FUN_1453de4c4's page; one 4-byte value in
  FUN_14689840b's page) — later proven inert (see 9.36, the zero references).
- ACTION 2 (WinDbg) BLOCKED — TRAP 19 (domain_brief): the attach = VMProtect's
  anti-debug (GetContextState 0x8007001F); a later retry ARMED the breakpoint
  correctly (destiny2+0x381dd0 confirmed by bl) then the game KILLED the debugger's
  context reads and CRASHED ITSELF after login. The debugger attach is IMPOSSIBLE on
  this build. ReadProcessMemory = the only live-read avenue.

## 9.35 ESCALATION SESSION (20:54-21:26, reviewer + lanes) — the hash chain disassembled

Deliverables (all on disk, all later verified):
- verdict_harness.py + verdict_candidates.json: 67 failing-candidate rows, 20 ids
  (bootstrap 0x385/0x1CE + investment-globals-client 0x58C-0x59B/0x708/0x709/0x913/0x914),
  from the package TABLE (kind @+0x10C = 1 uniform there — a different field than the
  validator's kind @+0x16C; the candidates alone do NOT answer the validator-kind question).
- Lane 2 (PHR h2 comparison): the poisoned vs the fresh PHR records = byte-identical
  for the failing packages (the failing ids have no records in either) -> THE PHR IS
  EXONERATED as the expected source (it is the persist layer only).
- Lane 1 (asm reconstruction, RE_output/content/lane1_asm/): the validator's thunk =
  a relay chain into a 2-chunk 96-bit ARX mix. The kind byte selects the 16-byte IV:
  kind==0 -> the buffer @image RVA 0x1F44CE0 (runtime 0x7FF643B34CE0); kind!=0 ->
  @image 0x1BCCAC8. The id u16 goes to DEAD frame slots (never read back). Seeds
  A=B=C=0xDEADBEFF (NOT 0xDEADBEEF).
- THE SMOKING GUN CANDIDATE: the kind0 IV is MODE-DIVERGENT in dwords [4] and [8]:
  healthy C51BC00C/867BDB35 vs failing 8D16DCF5/867BDB3F (note: dword8 differs only
  in low bits; dword4 is wholly different -> a DIFFERENT 16-byte value was loaded,
  not bit-flip corruption). The kindN IV = identical in both dumps.
- Computed outputs: kind0-healthy-IV = 0x281141FD; kind0-failing-IV = 0x3DB8F835;
  kindN = 0xFB438DF4 (both modes).
- Lane 1 (mode-diff read check): the two cold-page mode-diff locations (the nulled
  pointer, the 4-byte constant) = ZERO references from the chain's executed flow ->
  inert noise. The anti-tamper cookie (global @0x20A9A88 xor rsp) = self-referential
  and passes in both dumps.
- Watcher boots: 21:12 (PID 3608) = a WinDbg retry (same trap-19 GetContextState
  failures; game died; 64 records captured). 21:17 (PID 18400) = clean EXTERNAL boot
  (server log conn 7/8: signon + config GET @21:17:47) -> 2,199 task records captured
  to task_watch_records.bin.
- THE WATCHER GAP (trap for the record): the captured records = 32-byte SCHEDULER
  NODES (type 0x0F @+0x10, u16 0xFFFF @+0x12, pointer @+0x18 -> 320-byte objects at
  0x140 stride) — NOT the validator records. The kind byte was never captured live;
  verdict_candidates.json does not answer the validator-kind question either. The
  answer came from the dumps instead (see 9.36) — dump-scans for known constants
  beat targeted capture harnesses.

## 9.36 THE MECHANISM CLOSED END-TO-END (GLM 5.2 outside review + main-session verification)

GLM 5.2's scan (verified independently by me, all numbers reproduced):
- Scanned BOTH dumps for the three computed hash values as raw LE bytes:
  failing dump: 0x281141FD = 2,199 hits (2,198 consecutive at 632-byte stride + 1
  stray); 0x3DB8F835 = ZERO hits; 0xFB438DF4 = 3 hits (stride 0x288, trailing ASCII
  "pack" in the neighborhood). healthy dump: 0x281141FD = 3,546 hits (a 2,199-entry
  array at 648-byte stride + ~1,343 further hits at 632 stride); 0x3DB8F835 = 1 hit
  (surrounded by non-record bytes — coincidence-grade); 0xFB438DF4 = 3 hits.
- MY STRUCTURAL CONFIRMATION (the confirmatory pass the review deferred):
  - The failing dump's registration-record array = 2,198 records x 632 B at
    0x19731e51580: header {u16 0x26, u16 2, u16 packageId @+4, u16 1, sig8 @+8}
    (matches the PHR record grammar); kind byte @+0x16C = 0x00 for ALL 2,198;
    expected u32 @+0x168 = 0x281141FD for ALL 2,198. u16@+6 = 1 for 2,196 records.
  - All 67 records for the 20 failing-candidate ids: kind=0, expected=0x281141FD.
  - THE SAME RECORDS IN THE HEALTHY DUMP (found by header signature): id=0x385 rec1
    = expected 0x281141FD, kind 0, sig 9340040e94bc994b, @0x104=0xaf0d6ac7,
    @0x164=0x3000 — BYTE-IDENTICAL to the failing dump's record (same for 0x58C).
    THE EXPECTED SIDE IS MODE-INDEPENDENT.
- THE ANSWER TO THE KIND QUESTION: the failing records carry kind==0. The -87 =
  ARITHMETIC: the failing boot's kind0 IV is diverged -> computed 0x3DB8F835 !=
  expected 0x281141FD. The healthy boot's IV -> computed 0x281141FD == expected.
  0x3DB8F835 appears NOWHERE in the failing dump (computed, compared, mismatched,
  aborted, never stored).
- Every prior observation fits: the in-process passes by construction; the PHR/fork/
  config/manifest were innocent (none touch the IV); the dump-pair's only meaningful
  difference = the result register.
- The healthy dump's extra 0x281141FD copies (648-stride array vs the failing dump's
  632-stride array) are consistent with on-pass per-record result storage growing the
  record by 16 bytes; the 632-stride run in the healthy dump = another results table.
  Not load-bearing; recorded for completeness.

## 9.37 THE FIX PATH (GLM's plan adopted; tools built)

- iv_patcher.py (built + syntax-checked): latches destiny2.exe, verifies the module
  base == 0x7FF641BF0000 (abort on mismatch — never write to a wrong address), then
  WRITES the 16 healthy IV bytes into base+0x1F44CE0 and RE-ASSERTS them every 5 ms
  through the registration window (the game may write the buffer at any time; the
  per-boot vs per-registration write question is undetermined). Also logs the result
  register (DAT_14267aa50: 1 = PASS, 0xFFFFFFA9 = -87) and the registration state
  live. ONE BOOT, ONE VARIABLE: if it renders -> the IV divergence was the cause,
  CONFIRMED LIVE, and external mode boots; if not -> the divergence was a symptom
  and the patcher retires.
- Ghidra writer-hunt lane LAUNCHED (background, utility): xrefs/writes to 0x141F44CE0
  + the owning struct 0x141F44C00 + blob scans for both IV byte strings -> the writer
  function + call chain + in-process-only vs shared classification. Deliverable:
  RE_output/claims/iv-writer-hunt.md. Prime suspect: the synchronous in-thread
  content-config completion path (in-process only). The proper fix = port that write
  trigger into the fork's external mode (server-side or DLL-side per the hunt's
  result) — the runtime patcher retires the day that lands.
- AFTER A RENDER: the S1 tier-2 acceptance runs immediately (fully staged: boot
  against the sqlite DB, character + inventory from persistence) -> then the S2 seam
  (orbit/world load) with the dump-pair instrument aimed at it.


## 9.38 THE FIRST PATCH-BOOT ATTEMPT — the t+0 write CRASHES the game (control launch = clean)

Sequence (22:05-22:2x):
- The user accidentally launched the LIVE 2026 game, then the Arrivals build FOUR
  times while the live game ran -> 5 destiny2.exe processes, ALL silently waiting
  pre-window (single-instance guard queue; the first Arrivals zombie held the lock
  and itself waited on the live game's session lock). "Game won't open" = the
  processes existed but no window. FIX: kill all + launch once.
- The iv_patcher's first arm died on the loader-lock (find_module_base error 299 —
  the old watcher saga) -> resolve_base() retry + pattern-gate fallback + multi-pid
  selection added (find_pid_all, newest/not-seen first, alive-check loop exit).
- THE T+0 WRITE = FATAL: the re-armed patcher latched PID 33000 and wrote the
  healthy IV at t+0..1s -> the game crashed ~9s in. WER (destiny2.exe.33000.dmp):
  c0000005, faultingRIP = 0x180092440, in NO mapped module, BEX64. NOT the trap-19
  self-destruct (that signature = 0x7FF681E641F9, identical in both WinDbg-era
  dumps 20320/16936 — verified). The 0x180092440 crash = a different failure mode:
  a jump into heap-range memory.
- INTERPRETATION: the kind0 IV buffer (0x141F44CE0) sits inside an async-task pool
  struct (the one with the task-string pointers at 0x141F44C00). Writing it at t+0
  corrupts the pool's early phase (freelist); the game only tolerates the buffer's
  content changing AFTER its own init writes it. THE CONTROL LAUNCH (no patcher)
  = the old -87 black screen, game healthy -> the patcher's write = the crash cause,
  CONFIRMED.
- REFINED PATCHER (built): pattern-gated — read-only watch from latch; the write
  fires ONLY when the game itself has written the FAILING IV (D62AB2C1F5DC168D
  3FDB7B8655C7DC3B) into the buffer (proof the buffer is in its IV phase), then
  re-asserts every 5 ms until the result register reads 1 (PASS) or 0xFFFFFFA9
  (-87), then stops writing. If the buffer never shows the failing pattern before
  the verdict -> no write (the symptom hypothesis wins). Buffer transitions are
  logged per change. One boot, one variable.
- Side note: the stale live_task_watcher.py (PID 18384, armed 21:08) survived its
  45-min deadline (stuck in a latch loop) and was killed 22:2x. Harmless (RPM only).


## 9.39 THE PATTERN-GATED BOOT (22:32) — THE +29s TIMELINE REFRAMES THE MECHANISM

Live capture (iv_patch_out.txt, pid 26524, external boot, patcher watch-only):
- +8s: IV buffer = pool-phase garbage (2 transitions: 517ecb81... -> 4aa4729d...)
- +11s: result register = 1 (THE DEFAULT — the register STARTS at 1; the old
  "result=1" readings = default, not necessarily a validator pass)
- +29s: the game itself writes the FAILING IV (d62ab2c1f5dc168d...) AND the result
  register flips to 0xFFFFFFA9 IN THE SAME 5ms POLL -> the -87.
- +49s: state 3 (registration completed, failed).

THREE CORRECTIONS TO THE MODEL:
1. REFUTED: "external mode never performs the healthy write" (GLM's framing) —
   the external path DOES write the IV, at +29s, as part of the registration path
   itself, microseconds before the validator reads it. The divergence = the VALUE
   written, not the presence of a write. (The healthy mode writes the healthy value
   at the analogous moment.)
2. THE T+0 CRASH EXPLAINED: the buffer = async-task pool memory until +29s (the
   garbage transitions prove it). Writing it at t+0 corrupts the pool -> the 9s
   crash (WER 33000). Writing it after +29s = too late (the validator already ran).
   There is NO safe write window for the IV from outside — the game's own write
   and the validator's read are the same function's microseconds.
3. THE EXPECTED SIDE IS THE STABLE TARGET: the 2,198-record array (632-byte
   stride, expected @+0x168 = 0x281141FD) is written early and NOT rewritten
   during the failing registration (the failing dump carries it post-failure).

IV STRUCTURE CLUE: healthy D62AB2C1 C51BC00C 867BDB35 55C7DC3B vs failing
D62AB2C1 8D16DCF5 867BDB3F 55C7DC3B — dwords 0/3 = shared constants, dword 4 =
the mode-dependent value, dword 8 = differs only in the low nibble. The IV =
derived data ({const, f(mode), const-ish, const}), not a key. GUID-hash candidates
(FNV-1a/FNV-1/CRC32/djb2/sdbm/MD5/SHA prefixes of both GUIDs) = NO match (zero-boot
scan, negative).

NEXT TOOL BUILT: iv_patcher2.py — the EXPECTED-side patcher. Scans the live
game's committed memory for the 0x281141FD pattern, validates each hit via the
record header {u16 0x26, u16 2, u16 id != 0}, and rewrites every expected to
0x3DB8F835 (the value the game will compute from its own failing IV at +29s) ->
expected == computed -> the registration passes. Re-asserts every 5s until the
verdict. The result register's default-1 trap is handled (1 counts as PASS only
once the state >= 3; -87 aborts the write window).


## 9.40 THE HEAP IV-ARRAY DISCOVERY — the hash resource itself is mode-divergent

- THE EXPECTED-ARRAY PATCH FAILED (boot 22:42): with the fixed region walk, the
  scan genuinely finds ZERO 0x281141FD hits in the live game before the -87 ->
  the 632-byte record array does NOT exist pre-registration; it is built at/
  after the validation (the persisted DB, not the validator's live records).
  The expected-side has no live window either. (iv_patcher2 retired.)
- WHILE HUNTING THE IV'S SOURCE: scanning both dumps for the healthy/failing IV
  byte patterns outside the .data buffer:
  * the config/manifest/build_data files: NEITHER pattern (the config-source
    theory REFUTED in raw form).
  * the healthy dump: 4 copies of the healthy IV on the heap, 960-byte stride
    (0x18cdba4b000 + k*0x3C0); the failing dump: ZERO copies of the failing IV.
- THE STRUCTURE (4 x 960 B): {256 B random blob, image pointers @+0x210
  (0x7FF641C42620/40, 0x7FF641C3D000), heap pointer @+0x230 (to base-0x48 =
  a pool header), hash-chain-constant records @+0x238/+0x248/+0x26C (variants
  of the chain's 0x20FAABACC011DF84/0x99261133: 85 f9 11 c0 ac ab fa 20 |
  33 11 26 XX), count dword @+0x258, the IV dwords 4..C @+0x25C}.
- THE MODE DIVERGENCE (the key): the healthy dump's 4 structures carry the
  KIND0 IV (count 0xC1B22AD6 + dwords 0CC01BC5 35DB7B86 55C7DC3B); the FAILING
  dump's 4 structures carry the KINDN IV (count 0x365D4A3A + dwords 73A66058
  7E63E676 E40892B5). THE GAME'S HASH-IV STORAGE ARRAY ITSELF IS
  MODE-DIVERGENT — the .data buffer is downstream of it.
- The failing .data buffer = a HYBRID of the healthy constants: {healthy d0
  D62AB2C1, mode-dependent d4 F5DC168D, healthy d8 867BDB35 + 0x0A000000 flag,
  healthy dC 55C7DC3B}. dword4 = no simple derivation from either IV set
  (zero-boot scan of adds/xors/rotations: negative).
- Writer-hunt lane: pass 3 found a SECOND warm-region mix copy reading
  _DAT_141f44ce0 directly (the same ARX ops, unrecovered jumptable @0x147ad3d5f);
  pass 4 = launched 22:42, no log growth since (lane possibly stuck — will
  reconcile). Still NO writer identified statically.
- NEXT TOOL BUILT: iv_patcher3.py — finds the heap array live via the
  85f911c0acabfa20 signature, rewrites each struct's count+IV field to the
  healthy kind0 IV, and WATCHES the .data buffer's transitions to see whether
  the +29s writer derives from the array. If the buffer then gets the healthy
  value -> char select + the array = the confirmed root. If it stays hybrid ->
  the writer is array-independent and the static hunt wins.


## 9.41 THE ARRAY-PATCH NEGATIVE + THE FORK-HOOK TURN (23:0x)

- The heap-array patch (iv_patcher3, boot 22:54): the scan + neighbor probe worked
  (4/4 struct fields rewritten to the healthy kind0 IV at +20.5s, verified in the
  log), and the .data buffer STILL transitioned to the hybrid at +28s -> the +29s
  writer does NOT read the heap array. The array's kind0/kindN divergence = a
  symptom, not the cause. (The experiment = a clean negative; iv_patcher3 retired.)
- The hybrid's provenance (zero-boot sweeps, all negative): not a constant in the
  image or either dump (the healthy IV IS an image constant @ file 32,780,512 =
  the .data default; the hybrid is not); no simple derivation from either IV set
  or the mix's state words (healthy mix state A/B/C/D = 281141FD/D784C36E/
  2BE2A769/3FA28384; hybrid = 3DB8F835/8B42BC2E/479D4C84/60BD9583; neither set
  contains the hybrid's parts); the XOR deltas 0x480D6CF9/0x0A000000 not in the
  image. The hybrid = computed by the external registration path from an
  unidentified input.
- The GUID-gate hypothesis REVISITED AND DROPPED: the healthy dump's 632-byte
  record array proves the in-process boot RAN the registration (and passed) —
  the d2legacy GUID does NOT skip it; the hybrid experiment (in-process +
  fingerprint -> result 1) shows the pass/fail = MODE-driven, not GUID-driven.
  (The settings swap was made + reverted byte-exactly; the server's
  validate_served_guid() rejects non-UUID overrides anyway — server_main.cpp:265.)
- External interposition = conclusively impossible: the write->validate gap =
  microseconds (same 5ms poll in every capture); t+0 writes crash the pool phase.
- THE FORK-HOOK TURN (the fix): the validator FUN_140381dd0 (destiny2+0x381DD0,
  a WARM function, decompiled in phase_registration3.txt) reads the kind0 IV
  buffer at entry. The Sunrise DLL = loaded in-process with Detours hooks.
  THE FIX = a new fork hook (external mode only): at the validator's entry,
  write the 16 healthy IV bytes into base+0x1F44CE0, then call the original.
  The hook runs at the exact moment the buffer is in its IV phase (post-pool),
  there is no race (the validator reads AFTER the hook), and the write is an
  in-process .data write (no external WPM, no anti-tamper exposure).
  If the validator is called per record, the re-assert at every entry keeps the
  buffer healthy throughout the registration.
- The fork = RE_build/Sunrise-fork (the 0.2.1 line; the fork-DLL external retest
  = parked since 9.29 — this hook folds it in). The installed DLL = the original
  0.2.0 (hash 9aff2e4e597bb471); installing the fork DLL = its own two-boot
  sequence per the parked protocol.


## 9.42 THE -87 IS DEAD — CHARACTER SELECT REACHED ON THE STANDALONE SERVER (23:1x, the fork-hook boot)

THE MILESTONE: the fork-hook DLL (iv-validator-hook) booted the game in external
mode PAST the black screen to the character select. THE PROJECT'S ONLY OPEN
BLOCKER IS CLOSED — the validator-entry IV re-assertion works live.

The boot (user-observed): signon + config fetch -> the registration PASSED ->
character select (EMPTY ROSTER) -> the class-select screen -> Hunter chosen ->
finish -> the WHITE world-loading screen (infinite, expected).

Evidence:
- The server log (live): the game's character-creation traffic — ws opcode 701
  (the unmapped family-5 opcode class), the queuez subscribe warnings
  (unrecorded/unselected steps), the bap family activity. The game = fully past
  the registration and INTO the character flow.
- The client log = buffered (the game was still at the white screen; the file
  still shows the old 11:51 -87 boot). The hook's install line + the
  registration pass = to be flushed at shutdown.
- The hook = verified at the source level before the boot (9.41): the exact
  16 bytes, the 0x381DD0/0x1F44CE0 RVAs, the 7-param ABI, the external-mode
  gate, the module-range bounds check.

THREE FOLLOW-UPS (the new frontier):
1. THE EMPTY ROSTER (S1 acceptance item): the state.db holds 3 characters but
   the select screen showed none -> the external signon's account identity does
   not bind to the datagen account (or the character publish path = the
   in-process-only part). The S1 acceptance's next step = the identity/character
   binding fix, then the inventory render check.
2. THE WHITE SCREEN (the S2 seam): the game = loading the world after the
   character creation -> the server serves no world/activity data yet. This =
   the S2 milestone (the spawn/entity pipeline — the S2 spec exists, 386/386
   spawn sets decoded and ready).
3. The client log harvest at shutdown: the hook's install/write lines + the
   registration PASS for the record.

THE FIX'S STATUS: the runtime evidence now matches the static verification. The
fork-hook DLL (sha 1abdf9bb7d3e4d7d) = INSTALLED in dcv build\bin\x64\ (the
original 0.2.0 = backed up as steam_api64_020_original.bak). The hook = a no-op
in the in-process mode (the gate), so the daily in-process game = unaffected.
The writer-hunt lane (the root-cause research) = demoted to background
research — the hybrid's provenance (0x480D6CF9) no longer blocks anything.


## 9.43 POST-SHUTDOWN HARVEST (23:2x, the boot's residue)

- The client-side log = NOT written by this boot (the external-arm settings carry
  the logging-off config; the sunrise.log still = the 11:51 in-process boot).
  The hook's own log lines therefore have no client-log trace — the evidence for
  the fix = the user's observed character select + the server log's
  character-flow traffic + the static verification (9.41). Accepted as complete.
- The PHR = UNCHANGED (still the 15:12 fresh type-1): the registration passed
  without a PHR rewrite (the rewrite = tied to the in-process/graceful-exit path
  or a later stage than this boot reached). No poison to clean up.
- No WER crashes: the whole boot = clean (the fork DLL + the hook = stable).
- The server log's final traffic = the character-flow loop (ws opcode 701, the
  bap family activity, the queuez subscribe warnings, the svc-250 keepalives
  until the game closed). The white screen = the game waiting for the world the
  server does not serve yet (the S2 seam — see 9.42's follow-up list).
- CLOSED FOR THE NIGHT: the -87 saga (the project's only blocker) = solved and
  the fix = installed (the fork-hook DLL, sha 1abdf9bb7d3e4d7d; the original
  0.2.0 = steam_api64_020_original.bak). Next session's first items: (1) the
  empty-roster identity binding (S1 acceptance), (2) the world-load handoff
  (S2), (3) the stuck writer-hunt lane's reconciliation (its pass-3 log = the
  only unharvested artifact).


## 9.44 S1 TIER-2 ACCEPTANCE — PASSED (23:4x, the roster-fix boot)

THE BAR IS CLEARED. The roster fix (9.43's data-only bundle: 3 missing ability-bucket
rows for the picked subclasses' socket lists 1/5/11) deployed + the server's boot gate
passed ("swap domain=abilityBuckets json=6 cache=6 result=match"). The boot's evidence
chain (the server log, verbatim):
- queuez snapshot family=3 objects=4 (1 roster list + 3 character records — the fix)
- family=4 companion 49 objects (account + 48 item instances) + the select move
  (family=4 objects=2) — the character selection WORKED.
- THE JOIN BURST (the S2 spec's exact order): type 4 join_result -> type 0 entity slots
  (the full 1024-B lease mask) -> type 1 global state (146 B) -> type 54 bubble host
  state -> type 5 replicate_membership (591 B) — then the roster pushes:
  "dest=city_tower_social_d2 groups=1 objects=34 state=3 slice=48 spawn=0x811C9DC5
  player=0x9EAA300100100103" — THE TOWER SESSION IS LIVE.
- The keepalives: membership=1, spawn_state/teleport_state polling — the session
  healthy. THE USER = IN ORBIT, NAVIGATING THE DIRECTOR (destinations working).

S1 STATUS: the tier-2 acceptance = PASSED (the boot against the sqlite DB, the
character render + selection, the inventory companion, the activity session join).
THE ONE WRINKLE (cosmetic, recorded as the next S1 polish item): the equipped armor
does not apply its appearance — the character renders with the "space look" (the
class-inspection placeholder). The family-4 items ARE published (48 instances); the
appearance gap = the socket/plug application on the armor items or the armor detail
rows' appearance fields — a family-4 encoder investigation, NOT a blocker.
- The next frontier: the destinations -> the world (the Tower) = the S2 seam (the
  join path = fully live; the entity pushes for the world's static spawns = S2 v0).


## 9.45 THE WORLD LAYER — ALIVE (23:5x, the user walking the Tower, then Mercury)

Beyond the acceptance bar, same boot: the user ran around THE TOWER (the world
loaded with the existing character — the earlier white screen = the new-character
intro path only), then flew to MERCURY. The server log: a NEW activity session
(soid ...002) with dest=strike_bond (the Mercury strike), state=3, region=16,
slice=0, the membership + roster pushes flowing. THE MULTI-DESTINATION WORLD
LOAD = PROVEN (the client renders the world geometry from its own .pkg content;
the server's activity state keeps the session). The worlds are EMPTY of entities
(the static spawns = S2 v0 — the last piece).


## 9.46 SESSION CLOSEOUT (23:5x-0:0x) — documentation + cleanup after the milestone

- DOMAIN BRIEF updated: TRAP 19 (the WinDbg = permanently dead, the self-destruct
  signature 0x7FF681E641F9), TRAP 20 (the poisoned PHR), TRAP 21 (the IV buffer =
  pool memory until +29s + the result register's default-1 + the loader-lock 299
  retry), TRAP 22 (the single-instance guard), TRAP 23 (dump-scans beat capture
  harnesses; task nodes != validator records), TRAP 24 (the server's non-UUID
  config_guid gate). The corpus map's Sunrise line = updated to the fork (the active
  codebase; Sunrise-021 = prior context).
- CLEANUP DONE: the stray live_task_watcher (PID 21940) killed; the redundant
  .bak_guidtest settings backups removed (the originals in place + hash-verified:
  eac79932c4889aad / e41f51259f2cc748); the roster-fix backups (.bak_rosterfix) KEPT
  as the rollback artifacts; the retired patcher scripts + logs KEPT (they document
  the dead ends the FINDINGS reference).
- RUNNING STATE AT CLOSEOUT: the fixed s1_accept server (PID 31280, the abilityBuckets
  6-row build) up on 443/30975/3074/3075; the fork-hook DLL installed in dcv build
  (sha 1abdf9bb7d3e4d7d, the original 0.2.0 = steam_api64_020_original.bak); no game
  process; the fresh benign PHR in place (15:12, untouched).
- THE NIGHT'S ARITHMETIC: the -87 (30+ hours) dead via the validator-entry fork hook;
  S1 tier-2 acceptance PASSED (the roster fix: 3 missing ability-bucket rows); the
  world layer alive (the Tower walk + Mercury, empty of entities); S2 v0 (the static
  spawn pushes) = the next lane; the armor appearance + the hybrid's provenance =
  the two research items.
