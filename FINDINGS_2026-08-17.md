# FINDINGS 2026-08-17 — the consolidation night

Archive of the consolidation run (2026-08-16 night → 2026-08-17 morning): the git
unification (one integration branch), the true upstream 0.3.1 + 0.3.2 merges, the
build integration, and the census. Continuation of FINDINGS_2026-08-16.md (10.66).

## 10.67 THE CONSOLIDATION LANDS: ONE FORK, TWO TRUE MERGES, GREEN BUILD (2026-08-17 ~00:5x)

THE DIRECTIVE (the stronger model's high-note path, executed end-to-end):

- STEP 1 — the 5 dirty S2 files committed on master: commit 7fdbea3 (the refined-payload
  edits: schema hash 0x80806AC0, carrier 7, width corrections). Master tree = clean.
  The files were content-identical to the fold in 710f471 — no divergence introduced.
- STEP 2 — the integration branch: created at eb03736 (the wave work) in the
  Sunrise-fork-inventory worktree; merged 710f471 CLEANLY (commit 0661f7c). Discovery:
  the equip-verb work was already in eb03736's ancestry (merge-base b954169) — the
  folded branch's only unique commit = the 5-file S2 fold. Build #1 = clean (both
  projects, MSBuild VS2022 + /p:PlatformToolset=v143). Tag: integration-port-0661f7c.
- STEP 3 — the true 0.3.1 merge: commit 4cca48d with BOTH parents (0661f7c + 8188762).
  39 conflicts resolved: 26 keep-ours (the boot-proven inventory core: the encoder, the
  queuez, the account state, the deferred fly/teleport cluster), 5 take-theirs (noclip/
  sword-skate/package-trust/CMake ports), 6 manual unions (settings parsers, vcxproj,
  default_settings.json, sunrise.rc). teleport_panel.h restored (upstream deleted it).
- THE DISCOVERY: upstream 0.3.1 = a full PARALLEL inventory subsystem (the std::variant
  ServiceOutcome transaction design, the state_account_* runtimes, the socket-plug/vendor/
  collectible catalogs, a NEW cache format) + a gameplay/DTLS subsystem. The census
  (69 commits) drove the policy: our boot-critical path stays the base; upstream's
  additive APIs fold in; the variant transaction subsystem + the new cache format = PARKED
  (the wave-4 decision, with boots, = the next session's; the full map = the census).
- THE BUILD INTEGRATION (fe11228): 1,999 cascade errors → 7 seams → 0. The seams:
  the kDismantleRewardPolicyCapacity runtime.h cascade; the inventoryEquipmentPadding
  layout rename; ResolvedItem.mutationSerial/flags; the transaction structs; the
  upstream-vs-our cache format (REVERTED to ours end-to-end: the content build, the
  cache codec/format/domains, the build_data runtime); the teleport→movement renames;
  the fly deferral (fly.cpp = excluded from the build — the pre-existing decision);
  window_focus + the spawn content files = OURS (wrongly deleted in the first park pass,
  restored); upstream's new catalog .cpp files were never in OUR server vcxproj (upstream
  has no server project) — the linker error source, fixed by entry additions + later
  dormancy after the cache-format revert. THE GAMEPLAY/DTLS SUBSYSTEM = KEPT (compiled
  + linked).
- STEP 3b — the 0.3.2 merge (the user's "another update, much smaller, debug statements"):
  commit 27eb33e with BOTH parents (fe11228 + 4aebb14). 15 conflicts, the same policy.
  FOLDED: the HUD/debug overlay release (logo/session/status overlays, the hud module,
  ui_texture_slots, graphics_texture_upload + windowscodecs.lib) and THE SPAWN-POINT
  DOMAIN (state spawn_sets::Point + kPointCapacity, nearest_point/valid_points/
  snapshot_points, the cache SpawnPointRecord + spawnPoints domain, the format version
  bump 23→24, find_nearest_spawn_point, the content spawn_set_build + catalog_builder,
  the status overlay's nearest-spawn readout) — 33 per-line fold edits via
  RE_scripts-adjacent Python (Temp\opencode\fold_spawn_032.py). PARKED: 0.3.2's vendor
  domain additions + the movement/player cluster (player_position KEPT — the overlay
  needs it — with the read/write_position + velocity bodies folded into our
  teleport_move.cpp). Commit 7fe8116. Tag: integration-upstream-031-032.
- THE END STATE (verified): both projects BUILD CLEAN on integration (the server exe +
  the DLL, v143). NOT DEPLOYED — the daily stack (s1_accept + the dcv build) = untouched;
  boots expect pre-merge behavior until a deploy is scheduled.

THE BASH FINDING (recorded in AGENTS.md): `bash` on the PATH = the WSL stub, WSL has no
distro installed. Git Bash (`C:\Program Files\Git\bin\bash.exe`) = the working bash;
inline complex commands intermittently truncate the tool output — the reliable form =
a .sh script file + Out-File capture.

THE MOVE-2 READ (package_socket_plug_build.cpp, 253 lines, read end-to-end): a
CONTENT-layer extractor — the socket-plug rule/pool/member catalog for the cache
(category expansion seeds, tracker-socket synthesis, FNV-1a pool interning). NO
character-record bytes — the anchor shortcut lives in upstream's character_encoder.cpp
+ layout.h + family4_inventory_updates.cpp instead (the next session's diff input).

THE DELIVERABLES: RE_output/claims/inventory-upstream-census-031-032.md (the park/fold
maps + the ranked next-session fold-in list); STATE-inventory.md (distilled ~70 lines);
STATE.md (rewritten); AGENTS.md (the bash invocation rule).

## OPEN LANES AT CLOSE (2026-08-17 ~00:5x)

1. THE ANCHOR CLOSE-OUT (the swap blocker): the 0x2EB8/0x2EE0 summary/gate anchors +
   the 0xB74C tail — the upstream encoder diff = the first input (census §5).
2. THE RECAP BISECTION: ~11 boots, index in {0..25} u {11156..12299}, suppressing value 0.
   Fold each anchor-test boot into a bisection step.
3. THE UPSTREAM FOLD-IN QUEUE (census §6): the transaction subsystem adoption, then the
   cache-format decision (vendors/socket-plugs/collectibles), then the movement/player
   client cluster — each = a decision validated by boots, not tonight's work.
4. THE DEPLOY QUESTION: when to deploy the integration build (the identity re-stamp +
   both STATE files, per the cross-session rule).

## 10.68 THE ANCHOR CLOSE-OUT, STEPS 0-1 (2026-08-17 ~01:5x, in flight)

THE OBJECTIVE: close the subclass-swap blocker via the post-consolidation anchor
close-out (steps 0-4; zero boots until step 2). WHAT LANDED SO FAR (no boots):

- STEP 0 (the upstream encoder diff) = DONE, and THE DIFF COMES UP EMPTY for the two
  blocker regions (claims/anchor-closeout-upstream-encoder-diff.md): upstream's
  layout.h + character_encoder.cpp publish THE SAME bytes we publish at
  0x2EB8/0x2EE0/0xB74C (identical constants — zeros in the paddings, the same gate
  fields, contentBypass @0xB500 + the 591-B tail in both trees). Upstream's ONLY real
  structural difference near the verified anchors = the InventoryChangeList
  @0x2BF8..0x2CC0 (the client's "16 x 12-B pending-mutation table @+0x2BFC" —
  upstream names it: writeSlot/nextSequence + 16 {sequence, mutationSerial, kind,
  flags} records) + the row mutationSerial/flags fills + the REMOVAL of our 0x6D6D
  marker sweep (probe residue the integration tree still carries — must go).
  family4_inventory_updates.cpp read end-to-end: the change bank = TRANSIENT
  acquisition-feedback descriptors, "the canonical encoder leaves the bank empty on
  every later snapshot" — NOT a swap-acceptance gate. Verdict: the upstream shortcut
  = exhausted; per the objective, the fallback = the dump + the W1 grammar.
- STEP 1 (the byte comparison) = the dump side LANDED (claims/anchor-closeout-
  region-derivation.md): (a) THE HEALTHY DUMP HOLDS NO CHARACTER RECORD — the store's
  family-4 account record was found (0x18D97DDEDC0; the real 4,931 x 0x02 bank) and
  its selectedCharacterSoid = 0: the dump = captured before character selection, so
  the character record = never applied. Negative finding, explained. (b) THE ACCOUNT
  IDENTITY PROOF (the big positive): the dump-side account record = byte-identical to
  the deployed wire account record except 45 roster-live bytes — the store = the wire
  layout, confirmed end-to-end. (c) THE FRESH CHARACTER SLOT (the store's family-4
  block after the account) = ALL ZEROS at every region — the client's fresh baseline =
  zeros, so our wire's zeros at 0x2EB8..0x2EE0 + 0xB74C/0xB74D = quiescent (the
  588-byte hypothesis = REFUTED as a misplacement; the tail gate = a compare-only
  2-byte field at the tail's END, and 0x00/0x00 = matches the fresh store).
- THE HANDLER SEMANTICS (the W1-grammar side, from the phase4/5 corpus): THE GATE
  HANDLER READS LAND EXACTLY ON OUR FIELDS — FUN_140e07dd0(dst+0x2EE0, wire+0x2EE0)
  reads u16 @+8 = 0x2EE8 (OUR inventoryGateDefinitionIndex) + u8 @+0x18 = 0x2EF8
  (OUR inventoryGateState) — family7-closeout's "8 early" = RETRACTED (the +8 is
  inside the struct base 0x2EE0). The summary sub-block @0x2EB8..0x2EE0 = 40 B =
  20 x u16 (the compact per-slot def-index list) — OUR zeros there = the candidate
  wrong bytes; the fork's equipmentSummary holds the 20 character-slot biased def
  indices (0x04A4/0x239B/...) = the candidate expected bytes. The tail handler =
  compare-only + a flag dispatch (FUN_140a43300).
- THE IN-FLIGHT GHIDRA READ (lane_anchor_decomp.py, background): FUN_140513540 (the
  summary differ's exact stride), FUN_140a43300/c93ec0 (the tail-flag dispatch),
  FUN_140e06380 (the gate-load), FUN_140bdd800 (the investment apply) — the final
  patch bytes = decided from those. THE NEXT = the encoder patch (remove the
  markers; publish the summary list bytes) + the phase-A harness PASS + then THE
  BOOT GATES = the user's call (steps 2-4).

## 10.69 THE ANCHOR CLOSE-OUT: THE PATCH LANDS, THE HARNESS PASSES (2026-08-17 ~09:4x)

THE GHIDRA READ LANDED (lane_anchor_decomp.py, PyGhidra, -noanalysis — the
pyghidraRun.bat -H invocation; a plain analyzeHeadless run = "Python is not
available" + a no-flag run triggered a FULL auto-analysis = killed): the handler
cluster's verbatim C. THE VERDICTS (claims/anchor-closeout-region-derivation.md
PHASE 4): (a) THE GATE = EXONERATED — the open handler reads u16s @0x2EE0/0x2EE4
(the content-definition pair, only consumed when the open FIRES) and the change
handler reads u16 @0x2EE8 + u8 @0x2EF8 = OUR FIELDS EXACTLY (family7-closeout's
"8 early" = RETRACTED); (b) THE SUMMARY SUB-BLOCK @0x2EB8 = 2 x 16-B {u64 serial,
u8 state, u16 defIndex} entries with a recency-window check — our zeros SKIP (the
differ's wire!=0 gate) = inert, and the real fill = the PARKED mutation-serial
machinery (census item 1, not this lane); (c) THE TAIL = THE MECHANISM: 0xB74C/
0xB74D = the client's CONTENT-CHANGE STAMP — on difference the client writes its
changed-marker banks (0xFF via 0xB74C, 0 via 0xB74D; FUN_140c93ec0) + dispatches
(FUN_140a43300 over DAT_1427BE750). OUR STATIC ZEROS = the client never observes
a content change on ANY push = the swap's refresh never fires. (Labeled
INFERENCE for the commit outcome — the decompiles prove the mechanism, the boot
proves the outcome.)

- THE PATCH (commit 40bf3f4 on integration, on top of 7fe8116): (1) the 0x6D6D
  marker sweep REMOVED (12 padding markers — the probe residue upstream deleted);
  (2) the content stamp @0xB74C/0xB74D = FNV-1a-32 over the 20 equipped SOIDs,
  truncated u16 (lo @0xB74C, hi @0xB74D) — loadout-stable, changes exactly on
  equipment changes. Both projects BUILD CLEAN (v143; the server exe + the DLL
  steam_api64.dll).
- THE HARNESS (the phase-A --s1-test pattern, the inventory_build_test_anchor
  scratch env): BLOCKED FIRST by the v24 cache gate (the objective's step-2
  warning, exactly as predicted: the harness's v23-era cache = refused, cache=0).
  THE FIX (rebuild_v24_harness.py, landed in RE_output/content/inventory_cache_
  rebuild/): the v23→v24 conversion = the header + 4 B (spawnPointCount u32 = 0)
  + the version 24 + the exe's PE identity re-stamp (TimeDateStamp @ e_lfanew+8,
  SizeOfImage @ opt+56 — my first stamp read the wrong offset, caught by the
  cache=0 repeat) + the SAME payload + the SAME checksum (verified: FNV-1a-64
  over constants + the domain bytes round-trips the stored value). THE SECOND
  RUN = HARNESS PASS EXIT 0: the loader domains match (49/14/9/6/88), 3 x 18
  items resolve, the character encodes (46,928 B), the persistence second-boot
  round-trip = byte-equal (account 96,280 + family5 163 + character 46,928).
- THE REGRESSION (the decisive zero-regression proof): the patched record vs the
  pre-patch baseline artifact = EXACTLY 2 differing bytes: 0xB74C (00→55) +
  0xB74D (00→F9) = the stamp 0xF955 = the independently computed FNV over the
  equipped SOIDs, byte-exact. EVERY other anchor = byte-identical.
- THE NEXT = the boot gates (the user's call): STEP 2 = the integration deploy
  VALIDATION boot (7fe8116, NO anchor patch — one variable = the consolidation
  deploy; the v24 cache rebuild + the identity re-stamp + incident.py + the hash
  re-verify first, per the objective); STEP 3 = the anchor-patch boot (40bf3f4 on
  top — the money boot; retest the subclass swap); STEP 4 = the subclass
  inventory script + the ability-lock observation. THE RECAP BISECTION = folds
  into the step 2/3 boots (index {0..25} u {11156..12299}, value 0).

## 10.70 THE STEP-2 DEPLOY: LIVE + THE BOOT = PENDING (2026-08-17 ~10:1x)

THE MACHINE INCIDENT FIRST (recorded for the log): at 09:59 the PC hard-reset mid-
session (Event 41 Kernel-Power + 6008; BugcheckCode 0, no WHEA, no minidump, no
power-button). The same 41+6008 pair = on disk for 7/12 and 7/26 — a PRE-EXISTING
intermittent hard-reset pattern (~2-week cadence), NOT caused by our tooling (user-
mode software cannot produce a kernel-level reset); the trigger = likely the
sustained all-core load of the back-to-back /m LTCG builds on a marginal PSU/thermal
setup. Everything survived (all four staged binaries finished at 09:57, the git tree
clean, all the recordings on disk). POLICY GOING FORWARD: any further build runs
/m:1 (single-threaded, low load).

- THE DEPLOY (step 2, executed): the integration build WITHOUT the anchor patch =
  LIVE — the server exe (7fe8116, 4,649,472 B) → RE_output/s1_accept/sunrise-server.exe
  (backup .bak_anchor_step2) + the DLL (7fe8116, 5,287,936 B) → the dcv build's
  bin\x64\steam_api64.dll (backup .bak_anchor_step2). THE CACHE = v24: the deployed
  96-detail v23 cache converted (rebuild_v24_deploy.py — the +4-B header expansion,
  spawnPointCount 0, the same payload + checksum) + re-stamped to the deploy exe's
  PE identity: ts 0x6A833D2B / size 0x17D5000 / eqHash 0x6FC29EB4EE4D7275 (the DB
  fingerprint, kept). The settings + the DB = untouched (byte-exact). The acceptance-
  stack SHA-256s: server exe 70DF0B90…F06, DLL C4D5ADFC…1A1, cache 7A6533FA…753,
  settings C11BBE77…493, state.db 076271E9…E4F.
- THE RECAP BISECTION STEP 1 = applied (state.db.bak_anchor_step2 first): account
  flag index 0 → 0 (the first {0..25} u {11156..12299} candidate, value 0). The
  observation = does the montage still play at character select. The revert =
  UPDATE value=2 or the DB backup.
- THE BOOT (the user's): acceptance = identical to pre-merge (the roster renders,
  the world loads, no new asserts) + the recap observation. ONE VARIABLE = the
  deploy. incident.py = run (INCIDENT_20260817_101124.md). THE NEXT = step 3 (the
  anchor-patch boot, 40bf3f4) once step 2 validates.

## 10.71 THE STEP-2 BOOT: PASS + THE DECAPITATION (2026-08-17 ~10:2x)

- THE BOOT (the user, ~10:1x-10:2x): the step-2 acceptance = PASS — the hunter
  selected, ORBIT reached, the world loaded, no new asserts; the server log = the
  normal 701/702 syncs + the keepalives + the 250/251 polls, no server-side errors.
  The recap montage = STILL PLAYED → the bisection step 1 = NEGATIVE: account flag
  index 0 ≠ the recap trigger (and the derived "ALL uncovered = 2" hypothesis =
  REFUTED — the recap plays with index 0 = 0). The ability/subclass swap = still
  the rollback (EXPECTED — the pre-patch build carries no anchor fix; that's step 3).
- THE NEW DIVERGENCE: the inventory-screen character = DECAPITATED (the head model =
  gone). The pre-existing context: the armor appearance gap (FINDINGS 9.44: "the
  equipped armor does not apply its appearance — the character renders with the
  'space look'") = the helmet-quirk's family (the helmet never showed, the face =
  visible). The head's TOTAL absence = NEW with this boot. THE SUSPECTS, narrowed:
  (1) THE FLAG EDIT (index 0 → 0) — the recipe's gate-side risk ("one 0 row = one
  gate fail"); index 0 ∈ the uncovered set (NOT one of the 26..11155 ability rows),
  so a cosmetic/appearance gate = plausible; (2) THE DLL (the 0.3.2 HUD/texture-
  upload folds — the client-side render hooks). EXONERATED: the cache (the payload =
  byte-identical, the checksum unchanged), the appearance code (the character_record
  cluster = byte-identical pre/post consolidation — the 0661f7c..7fe8116 diff =
  empty), the DB (untouched). THE DISCRIMINATOR = the flag revert boot (index 0 →
  2, the protocol's revert anyway): the head returns → the flag edit = the
  decapitator (a valuable map entry: index 0 = a cosmetic gate); the head stays
  gone → the DLL = the next suspect (the DLL-revert test).
- THE SHIP = "back to being invisible" (the user): the pre-existing appearance-family
  issue (the space-look gap), not a step-2 regression — recorded.
- THE NEXT BOOTS (the user's arbitration): boot 2 = the flag revert (index 0 → 2,
  the same deploy) + the head/recap observation — one variable = the revert; boot 3
  = the step-3 anchor-patch boot (40bf3f4) + the bisection step 2 (the first half of
  {1..25} u {11156..12299} → 0). The bisection protocol = extended: record EVERY
  visible change per candidate (the decapitation = exactly such a signal), not just
  the recap.

## 10.72 THE UPSTREAM FLAG DIFF: OUR SPARSE SEED = THE UPSTREAM'S, VERBATIM (2026-08-17 ~10:4x)

- THE DISCOVERY: the upstream's default settings (Sunrise/resources/default_settings.json
  @ upstream/master) carry an AUTHORED flag seed — the unlocks.character_flag_runs +
  unlocks.account_flag_runs = [start, count] pairs. THE ACCOUNT RUNS EXPAND TO EXACTLY
  4,931 INDICES, RANGE 26..11155, ALL VALUE 2 = THE IDENTICAL SET to our sparse seed
  (state.db.bak_flags_20260816_172256) — set-equality VERIFIED in Python (upstream 4,931
  == sparse 4,931, identical=True). OUR SPARSE SEED = THE UPSTREAM'S AUTHORED SEED,
  VERBATIM (the previous sessions adopted it — the phase2a lineage). The upstream's
  last run = [11145, 11] (11145..11155); the first = [26, 21].
- THE CONSEQUENCE: the upstream boots with EXACTLY the sparse seed → the recap NEVER
  plays (the user's "native boots normally" = fully explained: no all-2, no veteran
  forgery). THE RECAP TRIGGER ∈ {0..25} ∪ {11156..12299} — the indices NO ONE authors:
  upstream seeds ZERO of them (verified: the overlap = empty). The uncovered set = the
  unmapped territory; the trigger = the one live index among them (the real server's
  veteran flag). THE BISECTION = the resolver, unchanged — the diff confirms the
  candidate set without shrinking it.
- THE SECOND FIND (the potential ability relevance): the upstream's CHARACTER seed =
  177 authored rows (indices 61..2269, the character_flag_runs) vs OUR character scope
  = 5 rows (16..19, 82) — the character banks DIVERGE. The upstream's 177 = their
  curated CHARACTER-level gates (the per-character flags the client reads). OUR 5 =
  a fraction — the character-gated features = mostly unset for us. THE SUBCLASS/
  ABILITY GATE = possibly among the upstream's 177 (the ability-lock open item 4 =
  a candidate home). The upstream's account run structure ([390,95], [487,72],
  [1832,172], [4245,81]...) = the semantic clusters = a flag MAP for 26..11155
  (the run boundaries = the feature boundaries; the naming route = the content's
  flag-hash tables crossed with the run starts).
- THE EMPIRICAL-DISCOVERY ANSWER (the user's second question): four routes, ranked:
  (1) the suppression-observation protocol (the boots — every candidate → the visible
  deltas, the extended protocol); (2) the upstream's authored seed = the dictionary
  (the run clusters = the feature blocks; their 177 character flags = the first
  suspect list for the ability gate); (3) the content-side flag-hash → index mapping
  (the presentation graphs); (4) the dump-state diffs (the healthy/failing client
  banks). The upstream's character seed adoption = a NEW candidate experiment for a
  later boot (the user's arbitration).

## 10.73 BOOT 2: THE FLAG = EXONERATED, THE DLL = THE DECAPITATOR SUSPECT (2026-08-17 ~11:1x)

- THE BOOT (the user, ~11:0x): the flag-revert boot (index 0 → 2, the all-2 baseline;
  the same deploy otherwise). RESULTS: (1) STILL HEADLESS — the decapitation PERSISTS
  with the flag reverted → account flag index 0 = EXONERATED as the decapitator (no
  cosmetic gate at index 0); the DLL (the 0.3.2 HUD/texture-upload folds) = now the
  prime suspect, the exe = a weak second, a transient client-side model-streaming
  fluke = possible. (2) THE SHIP = back, but the user's memory of boot 1 = uncertain
  (weak datapoint; the ship = the known intermittent appearance-family issue —
  recorded, not weighted). (3) The recap = still plays (the step-1 negative confirmed
  as not a fluke — the bisection baseline = clean for boot 3). (4) The general health
  = fine (the world + the orbit). The server log = normal.
- THE STEP-1 CLOSE: index 0 = reverted + verified (12,300 x 2) — the bisection state =
  the all-2 baseline, ready for the step-2 half-set suppression (folded into the next
  boot).
- THE NEXT BOOT = the user's arbitration: (a) THE DLL-REVERT TEST (recommended): the
  pre-merge DLL (steam_api64.dll.bak_anchor_step2) + the same pre-patch exe — ONE
  variable = the DLL; the head returns → the integration DLL's HUD/texture folds = the
  decapitator (and the step-3 deploy = should ride the OLD DLL); the head stays gone →
  the DLL = exonerated too (the exe or a fluke), and the step-3 = proceeds unmodified.
  (b) STRAIGHT TO STEP 3 (the anchor patch, 40bf3f4 — the money boot) with the head
  triage riding along as a co-observation (the confound = noted). The boot-brief rule
  (AGENTS.md) = applies to every future boot.

## 10.74 BOOT 3a = DEAD ON ARRIVAL: THE OLD DLL = NO LONGER VIABLE (2026-08-17 ~11:2x)

- THE BOOT 3a (the DLL-revert test, the user's pick) FAILED AT LAUNCH: the game =
  "problem reading game content" (the trap-18 family — the game's format-sensitive
  content validation). THE TRIAGE: the PHR = UNTOUCHED (the fresh 8/15 15:12 file, no
  new write — no trap-19 poison); the packages/settings = unchanged by us; the server =
  healthy (the content validation = a client-side early-launch stage, the server = not
  yet involved). THE LIKELIEST MECHANISM: the OLD DLL = the pre-0.3.1 build whose
  package-trust bypass = narrower than the integration's (the 0.3.1 fold = the custom
  package loading / trust bypass) — the current client content state requires the
  broader bypass, so the old DLL = permanently not viable. THE VERDICT: THE DLL-REVERT
  TEST = DEAD — the old-DLL A/B = impossible. THE DLL = restored (the integration
  build, hash C4D5ADFC ✓ = the boot-1/boot-2 state).
- THE CONSEQUENCE: the decapitation's cause = must come from the SOURCE analysis (the
  DLL-diff lane), not the old-DLL A/B. The lane = died after its setup read (the raw =
  824 B: the census summary only, no claims — the collector fallback = re-spawn with a
  tighter brief or the main session does it). THE STEP 3 = proceeds with the
  INTEGRATION DLL (the only bootable client state) — the decapitation = an accepted
  cosmetic divergence for the money boot, with the surgical fix = the later lane
  deliverable.

## 10.75 THE FLAG-MAP LANE LANDS: THE MANIFEST NAMES 12 RUNS + A MATERIAL CORRECTION (2026-08-17 ~11:3x)

- THE LANE (flags-flagmap, flash): the deliverable = claims/anchor-flags-flagmap.md,
  read end-to-end. THE BIG THREE: (1) THE INDEX-KEYED TABLE EXISTS — the Arrivals-era
  manifest's DestinyUnlockDefinition = 21,569 rows, one per index, and the official
  schema says `index` = "the index of the entity as it was found in the investment
  tables" = THE FLAG INDEX (the same space the bank gates read). The caveat: the index
  = a PER-PATCH compile ordinal — the current-era manifest keeps 12,396 hashes but ZERO
  of the same indices → the Arrivals DB = the only valid name source for THIS client.
  (2) 12/332 runs NAMED: [241,1] = Hunter Class; [561,72] = the platform block
  (Steam/Stadia/purchase gates); [683,5]/[717,2]/[728,8] = the level blocks; [2213,45]/
  [2569,62] = the full-game/level-10 gates; [3524,15]/[3540,27] = the vanilla exotic
  trio; [4641,244] = Warmind; [5327,41]/[5383,26] = the objective-completion blocks —
  plus 73 named gap indices (the class/weapon/level blocks). Run boundaries = feature
  boundaries, CONFIRMED on the named sample. (3) THE VETERAN VERDICT: the manifest
  CANNOT name the uncovered set (0 names, 0 xrefs, both eras) — but the full
  1,170-index →hash table = extracted, and the head-block lead: indices 0-2 =
  the 0x904E7433/7430/7431 trio (the only 0x904E rows in the table, ABSENT from the
  current era = removed pre-content) = the strongest veteran candidates; the lane's
  bisection priority = 0..25 first (~5 boots).
- THE MATERIAL CORRECTION (the lane's OPEN-1, VERIFIED by me against the DB backups):
  the sparse seed's 4,931 indices = NOT the full 26..11155 span — the span = 11,130
  indices with 6,199 INTERIOR GAPS the upstream also never set. THE TRUE RECAP
  CANDIDATE SET = {0..25} ∪ {the 6,199 interior gaps} ∪ {11156..12299} = 7,369 INDICES,
  NOT 1,170 (the recap-flag lane's uncovered-set arithmetic = wrong). The boot-1
  negative (index 0) = still valid (index 0 ∈ the corrected set); the derived-ALL
  refutation = still valid. THE BISECTION PLAN = corrected: the 0..25 head block FIRST
  (the veteran trio lead — the step-3 boot's piggyback = suppress 1..13, the first
  half of the remaining head), then the interior gaps, then the tail. The boot count =
  ~13 halvings worst-case.
- THE OPEN ITEMS (handed back): (1) the bank↔manifest correspondence = unverified
  (the bank's 12,300 vs the table's 21,569 — a scope-filter question; one live
  instrument run would settle it); (2) the 5CE1 corpus-order → bank-index map = one
  type-field identification away; (3) the veteran hash shortlist (0x904E7433/30/31) =
  the first check against any future condition-hash source; (4) the web mirrors
  (destinysets/sickontuesdays) = the per-patch name-list route if needed.

## 10.76 THE CHARMAP LANE LANDS: THE 177 = ALREADY DEPLOYED + THE DELTA PROBE LADDER (2026-08-17 ~11:5x)

- THE LANE (flags-charmap, flash): claims/anchor-flags-charmap.md, read end-to-end.
  THE HEADLINE (CLAIM 3, the premise correction): THE UPSTREAM'S 177-ROW CHARACTER
  SEED = ALREADY DEPLOYED — the live DB scope character_object = 177 rows value 2
  (since Aug-16 00:41), and the S1 wire dump's bank @0x9348 = exactly the 177. The
  "we seed only 5 vs their 177 / the missing 172 rows" framing = RETIRED — those are
  TWO DIFFERENT banks: the 177 → the CHARACTER OBJECT's bank @0x9348 (the gate
  FUN_140e06090's bank; ours = the full 177), and our 5 rows → the ACCOUNT's 4
  per-character blocks @+0x10518. Nothing is missing; the adoption = moot.
- THE POST-STEP-3 EXPERIMENT = THE DELTA PROBE LADDER (CLAIM 5), ranked: (1) THE
  CHARACTER BANK ALL-2 (the 4,096 rows → 2) — the top candidate: the 10.42 instrument
  proved the gate family walks ALL 0..4095, and the character bank's 3,919 zeros = the
  next candidate for the full-walk failure (the account's all-2 fixed the account-level
  gates in 10.19; the character-level ones = never got that treatment). (2) The profile
  bank all-2 (512). (3) The per-char blocks all-2 (4 x 256). (4) The character-only 67
  → 0 (the selective-gate hypothesis — contradicts 10.42, last resort). ONE VARIABLE
  PER BOOT, the boot-brief rule applies. THE CAVEAT: the upstream's own reference boots
  with the same 177 — but upstream = exploration-only, never proven on a subclass
  swap, so the "healthy reference" does not constrain the swap gate's index set.
- THE CLASSIFICATION (CLAIM 4): the 177 = 79 account-shared / 32 profile-shared (the
  whole [144,25] block = profile-mirrored, NOT the subclass block) / 67 character-only
  (the 1131-1264 stretch with the two 3-runs, the stride-2 even table 1514-1524). The
  semantic cross = a SETTLED NEGATIVE (CLAIM 6): no local source names any index —
  the classification = structural only; the per-index names (if any) = in the VMP'd
  evaluators or the live derived state.
- THE PLAN IMPACT: the step-3 = UNCHANGED (the stamp = the money variable). The
  ladder's item 1 (the character bank all-2) = the FIRST post-step-3 probe if the
  stamp does not commit the swap — it is now the best-evidenced second candidate. The
  step-3's deploy prep = add the --s1-test harness smoke run on the patched exe (the
  lane's OPEN-2: re-verify the character record emits end-to-end on the deployed
  integration build).
- THE RELAUNCH STATUS (the freeze test): the game = killed after the freeze at the
  investment-hook install stage (the log stopped at the token; the family5_commit =
  never logged). The relaunch = the user's — if it freezes again at the same spot,
  the next step = the DLL rebuild with per-stage logging (/m:1).

## 10.77 THE DLL-DECAPI LANE v2 LANDS: THE FOLDS = EXONERATED, THE SUSPECT = THE RECORD BYTES (2026-08-17 ~12:0x)

- THE LANE (claims/anchor-dll-decapi-suspects.md, read end-to-end): the 0.3.2 render
  folds = EXONERATED (the logo-sheet upload = D3D-legal + one-shot + publishes only
  into Sunrise's own ImGui texture-ID array — no game slot collision; the HUD = pure
  ImGui with UNWIRED game-state readers (poll_world_step/observe have NO callers);
  no new game hooks anywhere in 0661f7c..7fe8116). The 0.3.1 tier = clean too (the
  appearance cluster + state/account = net-zero parked; the mutationSerial/flags =
  dead fields). The in-range behavioral deltas = the family-0 seed rewrite policy
  (LOW) + the activity-region slot 1→2 (irrelevant). The collateral: the assert-
  handler fold makes category-6 graphics asserts FATAL (a halt, not a headless) +
  the HUD = half-wired.
- THE LANE'S OUT-OF-RANGE SUSPECT = the anchor stamp (40bf3f4) — but the boots 1/2
  (the decapitation's debut) ran the PRE-PATCH build (7fe8116) = NO stamp. THE
  SYNTHESIS (the main session's): the lane = didn't know the deploy = the pre-patch.
  THE ACTUAL remaining suspect for the boots 1/2 = THE 0x6D6D MARKER SWEEP — the
  7fe8116 encoder = carries the 12 markers (the integration lineage, since 997d67b),
  and the pre-deploy daily build (the master-lineage refined exe) = has NONE. The
  deploy = put the markers into the daily wire for the first time. THE KEY MARKER:
  the resetFlagsPadding marker @0x4C18 = EXACTLY the client's 64x200 record-bank
  anchor (FUN_140E07E60) — a corrupted bank head = a plausible content/appearance
  model break. THE TEST = ALREADY RUNNING: the step-3 boot = the PATCHED exe
  (40bf3f4) = the markers REMOVED (the patch deleted the sweep) + the stamp added —
  IF the markers were the decapitator, the step-3 boot's head = BACK. The user's
  head observation on the current boot = the discriminator.
- THE STEP-3 BOOT = now tests FOUR observations at once: (1) THE MONEY = the swap
  commit (the stamp); (2) THE HEAD = the markers' removal (the decapitation
  discriminator); (3) the recap = the bisection step 2 (already reported: STILL
  PLAYS → the veteran ∉ 1..13, the 0x904E7431/7430 trio = cleared); (4) the
  ability swap.

## 10.78 THE STEP-3 BOOT: THE MARKERS = THE DECAPITATOR (CONFIRMED), THE STAMP = NOT ENOUGH, THE FLAGS TALK BACK (2026-08-17 ~12:1x)

- THE HEAD + THE SHIP = BOTH BACK (the user, this boot) → THE 0x6D6D MARKER SWEEP =
  CONFIRMED as the decapitator AND the ship-invisibility culprit (the markers = the
  only wire-byte delta removed between the boot-2 and the step-3 builds; the
  resetFlagsPadding marker @0x4C18 = the client's record-bank anchor = the
  corruption point). The pre-deploy daily wire = marker-free = the head/ship =
  fine; the boots 1/2 = the markers = the headless + the ship flake. THE PATCH
  (40bf3f4) = FIXED BOTH as a side effect of the marker removal. The DLL-decapi
  lane's suspect = the record bytes (the right family, the wrong commit — the
  lane's OPEN-1 = resolved by this boot).
- THE MONEY = NEGATIVE: the subclass swap AND the ability swap = BOTH STILL FAIL
  (the cue-then-rollback persists WITH the stamp live). THE VERDICT: the content
  stamp = NOT sufficient for the swap commit (it may be necessary — the marker
  banks now fire on the swap — but the commit = needs more). THE NEXT VARIABLE =
  the charmap ladder item 1: the CHARACTER BANK ALL-2 (the 4,096 rows → 2; the
  3,919 zeros = the full-walk gate failure candidate — one edit, one boot).
- THE FLAGS TALK BACK (the user's popups — the empirical discovery in action):
  (1) "Project:CB.NAV — You've found a fragment of a Bray Navigational Disk" =
  a quest-item acquisition notification; (2) "CRUCIBLE INFRACTION — Infraction
  Level: 3 — stripped of your Rank and permanently restricted from Ranked play" =
  the client's PENALTY popup. BOTH = NEW with the step-3 boot = the flag state =
  the all-2 MINUS indices 0 + 1..13 (the boots 1/2 = the all-2 with only index 0
  suppressed = NO popups). INFERENCE: the indices 1..13 = the CRUCIBLE-RANK/
  INFRACTION block (the 0 = the stripped/penalty state → the popup) + the Bray
  quest flag — the head block's semantics = the first empirical names for the
  uncovered set. THE OBSERVATION PROTOCOL = extended: every candidate block's
  suppression = record EVERY popup/notification delta (not just the recap).
- THE BISECTION STATE: the veteran ∉ {0..13}; the remaining = {14..25} ∪ the
  6,199 interior gaps ∪ {11156..12299}. THE NEXT FOLD (the next boot's piggyback)
  = suppress 14..25 (the rest of the head block).
- THE NEXT BOOT (the user's arbitration, the boot-brief rule applies): the
  character bank all-2 (the ladder item 1 — the money retest) + the bisection
  fold 14..25 (the piggyback). The popups = expected to persist (the account's
  1..13 = still 0); their absence = the signal to record.

## 10.79 BOOT 4: THE LADDER ITEM 1 FAILS, THE HEAD BLOCK CLEARS, THE FLAGS NAME THE ORBIT GATE (2026-08-17 ~12:2x)

- THE MONEY = STILL NEGATIVE: the subclass + ability swaps = unchanged (the rollback
  persists with the character bank all-2 + the stamp live). THE LADDER = moves to
  item 2 (the PROFILE bank all-2, 512) then item 3 (the per-char blocks, 4 x 256).
- THE BISECTION = THE HEAD BLOCK CLEARED: the recap = still plays with the ENTIRE
  0..25 suppressed → the veteran ∉ 0..25 (the 0x904E trio + the whole head =
  exonerated). The remaining = the 6,199 interior gaps (26..11155) + the tail
  (11156..12299). THE NEXT FOLD = the gaps' first half (the gap list = on disk,
  lane_flags_geometry.json). The 14..25 suppression = NO LONGER NEEDED → the next
  boot reverts it (the orbit + the popups settle).
- THE FLAG TALK-BACK, ROUND 2 (the user's observations, this boot — the 14..25
  block's semantics): (1) THE GAME LOADED DIRECTLY INTO THE TOWER from the
  character select (skipping the orbit); (2) THE ORBIT = LOCKED — the ghost (Tab)
  shows "Quit Game" instead of "Go to Orbit"; (3) more info + main popups. THE
  INFERENCE: the indices 14..25 = the ORBIT/TRAVEL/GUIDED-STATE gate block (the
  value 0 = the "cannot return to orbit" state — the real game's tutorial/forced-
  mission state). This = the second empirical block name (after the 1..13 =
  the Crucible-rank/Bray block). THE DISCOVERY VALUE = high — the flags = a real,
  client-visible state machine, and the suppression protocol = reading it block
  by block.
- THE NEXT BOOT (the user's arbitration): (1) THE LADDER ITEM 2 = the profile
  bank all-2 (the swap probe); (2) THE REVERT = account 14..25 → 2 (the orbit
  returns + the popups settle — the head's cleared, the suppression's job = done);
  (3) THE BISECTION FOLD = the gaps' first half → 0 (the veteran hunt moves to
  the interior). The orbit-lock = a one-boot cost for the discovery (the user =
  still in the Tower this session; the quit = the normal exit).

## 10.80 BOOT 5: THE RECAP DIES — THE VETERAN ∈ THE GAPS' FIRST HALF (2026-08-17 ~12:3x)

- THE BISECTION HIT: the recap = GONE this boot → THE VETERAN TRIGGER ∈ the
  suppressed gap-half (3,099 indices, the first half of the 6,199 interior gaps,
  47..8027 in the gap-run order). THE NEXT FOLD = halve that half: keep the first
  ~1,549 suppressed + revert the second ~1,550 → 2; the recap plays → the trigger
  ∈ the kept half, gone → ∈ the reverted half. ~11 halvings to the exact index.
- THE MONEY = STILL NEGATIVE: the subclass + ability swaps = unchanged with the
  PROFILE bank all-2 (the ladder item 2 = FAILED). THE LADDER = item 3: the
  PER-CHAR BLOCKS all-2 (4 x 256 @+0x10518).
- THE ORBIT-LOCK RE-ANALYSIS (the 14..25 revert did NOT restore the orbit): the
  boot-4's Tower-direct + no-orbit = NOT the 14..25 block after all — the boot-5 =
  the 14..25 = 2 + the lock = persists. THE CORRECTED ATTRIBUTION: the boot-4's
  OTHER variable = THE CHARACTER BANK ALL-2 = the cause (the character bank's
  guided-state/tutorial flag = the value 2 = "cannot return to orbit" — the real
  game's forced-mission state, at the CHARACTER level). The boot-3 (the character
  bank = the 177 seed) = the orbit = normal → consistent. THE DISCOVERY: the
  character bank = carries the guided-state gate (the 3,919 new 2s = one of them).
  THE FIX = the character bank revert (the probe = done + negative → the revert =
  the natural next step, and the orbit returns).
- THE NEXT BOOT (the user's arbitration): (1) THE CHARACTER BANK REVERT (the 177
  seed restore — the orbit returns); (2) THE LADDER ITEM 3 = the per-char blocks
  all-2 (the swap probe); (3) THE BISECTION FOLD = the gap-half's half (above).
  The boot-brief rule applies.

## 10.81 THE FLAG-RESEARCH SWARM: ROUND 1 LANDS, N1 = THE CANONICAL SPINE (2026-08-17 ~13:4x)

- THE SWARM (the user's two-wave flag-research plan, the post-escalation alignment):
  Round 1 = the NAMED flags (N1 = the canonical table, N2 = the consumer walk on
  the d2_full_cuic Ghidra copy, N3 = the package strings); Round 2 = the UNNAMED
  (U2 = the graph walk, U3 = the liveness census). DROPPED (the overlap with the
  new session's lanes): the 5CE1 corpus-order map (theirs). SKIPPED (the user's
  call): the D1 archive + the community route. The new session = owns the swap
  front (the consumer-trace lane + the boot-6 + the hash re-record).
- N1 = LANDED (claims/flags-r1-n1-canonical.md, read end-to-end + spot-verified):
  (1) THE NAMED SET = COMPLETE — 108 names = exactly 35 in the 12 named runs + 73
  in the gaps (0 missed, 0 spurious); every named row = a BARE GATE (unlockType 0,
  scope 0, no description — the name = a UI-test label, not a behavior). (2) THE
  ERA-CROSS: 105/108 survive with IDENTICAL names (the names = era-stable on the
  hash); the indices shift in clean per-block offsets (the class/race head +1836,
  the weapon ladder +1836/+1837 with the Glaives insertion, the platform +3650,
  the levels +3656, the exotics +9513, Warmind +9147, the objectives +9009/+9024/
  +9039); the current era adds 3 names (Glaives, Epic Games Store, Requires
  Account Validation). (3) THE ORPHANS: the faction-rally trio (14987-14989, the
  Dead Orbit/New Monarchy/Future War Cult victory gates) = gone from the modern
  era ENTIRELY (the all-tables census) — purged wholesale. (4) THE MASTER TABLE:
  flags_canonical_table.json = the first machine-readable full 21,569-entry
  index→hash array + the 108 named rows with the era-crosses (spot-verified: the
  veteran trio 0x904E7433/7430/7431 @ 0/1/2, the Sweet Business 0x843462C8 @ 3534
  = the live-instrument anchor, Warmind 0x586D997D @ 4719).
- THE N1 OPEN ITEMS: (1) the bank↔manifest correspondence = still the one live
  instrument run away (the Sweet Business gate hash → the client's resolved
  index); (2) the duplicate names ("Hunter Class" ×7, "Level 15" ×2, the ×15
  objectives) = the name = a FAMILY label — the hash = the identity, the index =
  the slot; (3) the other-table era-crosses = the r24 pattern reuse.
- THE WAITING: N2 (the consumer walk) + N3 (the package strings) = in flight; the
  integration = the master dictionary once both land.

## 10.82 N2 LANDS: THE FIRST STATIC HASH→INDEX RESOLUTION SITE (2026-08-17 ~14:1x)

- N2 = LANDED (claims/flags-r1-n2-consumers.md, read end-to-end; the raw's scan
  lines spot-verified: the platform pair's record @0x141F93168 + the 2 refs + the
  consumer FUN_140548e00). THE VERDICT: the client statically references EXACTLY
  TWO of the 651 named hashes — the platform pair 579/581 ("a different platform")
  in the compiled requirement-expression table @0x141F93168. EVERYTHING ELSE
  (the class/level/exotic/objective gates, Sweet Business, the veteran trio) =
  ZERO static presence — the runtime-resolved registry (the boot-built path),
  now proven hash-side for the whole named set (the recap lane's runtime-indexed
  verdict = extended from the code side to the hash side).
- THE ONE REAL FIND (CLAIM 3, the decompile verbatim): FUN_140548e00 = THE
  REQUIREMENT-EXPRESSION EVALUATOR — its type-3 node reads the two platform
  hashes and resolves each through the item-service FUN_14050a210 vtable+0x6F8
  (hash in → index out, -1 = miss) + merges the evaluated-state byte at the
  resolved index. THIS = THE FIRST STATIC HASH→INDEX RESOLUTION SITE EVER FOUND
  in the client — the compiled condition-table path = the gate machinery's
  template. THE CALLER FUN_1405084c0 = the CHARACTER-LEVEL REQUIREMENT SESSION —
  it calls FUN_140548e00 + touches the +0x2CC0 equippedInstanceSoids (the
  equipped-items gate evaluation) = THE SWAP'S NEIGHBORHOOD (the equipment
  requirement = the same family the swap's gate belongs to).
- THE SWEET BUSINESS CONTROL: 0x843462C8 = ZERO static presence (the full
  image + the corpus, both forms) → the correspondence check CANNOT use it; the
  PLATFORM PAIR = the cheapest instrument instead (the vtable+0x6F8's answer for
  0x0FCFFEF9/0x42889986 = the resolved indices = the bank↔manifest answer in ONE
  boot, no server change). NOTE: the node's "breakpoint" suggestion = the trap-19
  violation — the instrument = the HOOK form (the gate-read logger's family),
  not a debugger.
- THE TYPE-TAG BRIDGE (CLAIM 4): the corpus's 42 type-tags = the exe's static
  dispatch tables (the type vocabulary = exe-side real — the persistence/stat
  machinery); the corpus's per-record hashes = content-side only. The corpus→
  bank-index map = therefore NOT statically derivable from the exe (the new
  session's 5CE1 lane = unaffected — the runtime registry dump = the remaining
  route).
- THE PREDICTED-BEHAVIOR MODEL (the general template): every named flag's gate =
  the runtime registry resolve → the evaluated byte == 2 → the silent pass/fail
  (no error surface). The flip's only visible effect = the downstream content
  gate (the exotic kiosk, the platform check) unlocking.
- THE N2 OPEN ITEMS: (1) the vtable+0x6F8 resolver's body (the actual hash→index
  walk) = the next decompile target; (2) FUN_140540320 = the expression VM's
  generic driver (undecompiled); (3) the selector list @0x141BF3E60's writer =
  the gate-configuration layer (unmapped); (4) the 0x141F93180 config record's
  field meaning = open.
- THE WAITING: N3 (the package strings) = in flight; then Round 2 (U2 + U3).

## 10.88 THE ESCALATION RULING + THE STATE CORRECTIONS (2026-08-17 ~12:5x)

- THE ESCALATION ("STATE + incident 20260817_123841", the user's call) = RESOLVED:
  the reviewer audited against disk and returned the ruling. THE RULING (binding):
  (1) LADDER ITEM 4 (the selective 67 → 0) = RULED OUT — it contradicts the 10.42
  instrumented full-walk; not without the consumer trace naming those indices first.
  (2) NO NEW all-2 rungs after item 3 (the family = exhausted by construction).
  (3) The transaction-subsystem fold = parked until the consumer trace lands on its
  gates. (4) The dead arcs = stay closed (definition-catalog, 2d route, kcal
  calibration, 2100 emitter). (5) The boot-brief rule = every boot, no exceptions.
  THE THREE BOUNDED ACTIONS: A = the consumer-trace lane (zero boots, the priority-
  one analysis — the stamp is live and the marker banks fire, yet the rollback
  persists; the failure now lives DOWNSTREAM of the dispatch, never traced);
  B = BOOT 6 (the user's go; incident.py + the hash re-verify first, AFTER the
  STATE hash correction); C = the 5CE1 corpus-order lane (after A, or interleaved
  only if the user lifts a second lane).
- THE STATE CORRECTIONS (applied, the reviewer's — I own the pen): (1) THE
  CACHE/SETTINGS HASHES = STALE — they move on the server's stamp-on-persist; the
  live re-hash (~12:57) recorded the current values (s1 cache EE1CAD28…, s1 settings
  238DAF75…, dcv cache 05776C99…, dcv settings 1D9ED901…, both manifests 4522597B…)
  + the RULE (re-hash before every boot's hash check or it false-fails). (2) The
  suppression set = indices 1..13 (NOT 0..13 — index 0 = 2 since the boot-2 revert;
  DB-verified ~12:5x: zeros = {1..13} ∪ 3,099 gap-half). (3) The dll-decapi open
  items recorded: the HUD = half-wired (position::reset() every frame, permanent
  "out of world" — wire-or-rip pending); the assert category-6 fold makes graphics
  asserts FATAL (a freeze ≠ a headless when triaging).
- THE DB VERIFICATION (read-only SQL, ~12:5x): account 12,300 (9,188 x 2 + 3,112 x
  0), character_object 4,096 x 2, profile 512 x 2, character 5 x 2, characters = 3,
  family5_overrides = 47 — the boot-5 truth, exactly per the reviewer. The live
  stack = still up (the game + the server from the boot-5 session; the DB locked =
  expected). The boot-6 cycle = the user's quit + the server stop first.
- THE NEXT: the action-A spawn (the consumer-trace lane brief, written from the
  ruling's mission list + the L3/CUI-C residue), the boot-6 prep script (written,
  not run — the DB = locked until the boot cycle), then the boot-6 brief = the
  user's go.

## 10.89 THE BOOT-6 PREP + THE ACTION-A SPAWN (2026-08-17 ~12:5x)

- THE PREP SCRIPT (RE_output/content/recapc_flag/_boot6_prep.py, written, NOT run —
  the live server holds the DB; it runs at the boot cycle): (1) the DB backup
  .bak_anchor_boot6; (2) THE CHARACTER_OBJECT REVERT — DELETE the boot-4's inserted
  rows (the 4,096 minus the 177-seed set) → the 177 seed restore (the orbit
  returns); (3) THE LADDER ITEM 3 (the LAST rung) — the scope='character' bank
  all-2 (insert 0..255 missing → all 256 value 2; the encoder replicates the bank
  into the account's 4 per-char blocks @+0x10518+0x500*c); (4) THE BISECTION
  HALVING — the gap-half's first 1,549 stay suppressed (0), the second 1,550 →
  value 2.
- THE ACTION-A SPAWN (the consumer-trace lane, flash, one lane): the brief =
  RE_output/claims/anchor-consumer-trace-brief.md. The three missions: (a) xref the
  READERS of the four DAT_1427BE750 flag bytes (FUN_140a43300's targets); (b) xref
  the readers of the two changed-marker contexts (FUN_140be16d0 @+0x400..0x430,
  FUN_140be1700 @+0xC/+0x14); (c) complete FUN_140e051b0 (the L1 commit-point open)
  — L3's pass-A already named it the per-family store router → FUN_140e02990 →
  FUN_140e02280 (the commit gating ONLY on the decode result); the lane VERIFIES
  the load-bearing L3 claims against the raw corpus and completes the chain.
  DELIVERABLE: the chain "content-state changed" → "UI commit/rollback", or the
  proof it dead-ends into the family-5/evaluated-state or the 701/702 dynamic
  state. Inputs on disk: the pass-A-D decompiles (lane_inv_delta_raw{1..4}.txt +
  lane_inv_cuic_raw.txt), the 47 family5_overrides rows, the 163-B family-5 record,
  the d2_full_cuib project. The lane = read-only, no boots, no fork edits.

## 10.90 BOOT 6: THE LAST RUNG FAILS, THE LADDER CLOSES — AND THE SWAP EMITS VERBS
##        (2026-08-17 ~13:4x, the harvest)

- THE BOOT CYCLE (executed, ~13:2x-13:3x): the server stopped (the old pid 18352) →
  _boot6_prep.py landed EXACTLY per the plan (the character_object 4,096 → 177 ✓, the
  character scope 5 → 256 all-2 ✓, the account zeros = 1,562 = {1..13} ∪ the kept
  1,549 ✓, the backup .bak_anchor_boot6 ✓) → the server restarted (pid 25008, the
  fresh log window 13:24:08, the domains match 49/14/9/9/88, the cache ok) →
  incident.py (INCIDENT_20260817_132809.md: the acceptance-stack hashes all verified
  — exe EEA46843…, DLL C4D5ADFC…, settings 238DAF75…, cache EE1CAD28…).
- THE MONEY = STILL NEGATIVE (the user): the subclass + ability swaps = the same
  cue-then-rollback WITH the per-char blocks all-2 + the stamp live. THE LADDER
  ITEM 3 = FAILED → THE ALL-2 FAMILY = EXHAUSTED BY CONSTRUCTION (the account, the
  character_object, the profile, the per-char = ALL tested, ALL failed). Per the
  ruling: no new all-2 rungs; item 4 stays ruled out; the swap front pivots 100% to
  the action-A consumer-trace output + the new server-side fork below.
- THE ZERO-CODE SWAP-LOG OBSERVATION = THE DECISIVE WIN (the server log window, read
  end-to-end): the client EMITS the verbs during the swap — two opcode-801 requests
  (the evaluate verbs, payloads …0C8900 / …0C8800, t=491766/493625) then THE 403
  EQUIP (payload 600000000000003100, t=495047). THE SERVER'S ANSWER = queuez
  stage=subclass_equip result=ok family=4 objects=1 bytes=824 recorded=1 + the
  banner_refresh, then svc 10/11 ok. VERDICT PER THE RULING'S FORK: the gate is NOT
  client-side pre-commit — the server's answer IS the commit input, and THE FORK'S
  SUBCLASS-SLOT HANDLER (the queuez subclass_equip path) = THE SUSPECT SURFACE.
  Supporting evidence: the 702 dynamic-state dump CHANGED after the equip (the
  t=472829 payload vs the t=502844 payload now carrying the soid 9EAA300100100100 +
  new flags) — the client-side dynamic state DID register the change, yet the UI
  rolls back (consistent with L3: the rollback lives in the family-4 after-image +
  the ability gate).
- THE BISECTION HALVING (the direction CORRECTION — the ruling's parenthetical was
  INVERTED): the recap = GONE with the kept half (47..3933) still suppressed and the
  reverted half (3934..8027) restored → THE VETERAN ∈ THE KEPT HALF 47..3933 (the
  trigger must be 2 to play; the reverted half IS 2 → a veteran there would have
  played). The empirical direction = settled by the established 0=suppress
  semantics; the next fold = halve 47..3933 (~775 kept / ~774 reverted).
- THE FLAGS TALK BACK, ROUND 3: the popups = unchanged (expected — the 1..13 stay
  suppressed); THE PRIMARY + SPECIAL WEAPONS = DISABLED (the user: "same as before,
  the flags messing with stuff again") — a NEW EMPIRICAL MAP ENTRY: a
  weapon-availability gate ∈ the currently suppressed sets ({1..13} ∪ {47..3933},
  present since at least the boot-5). The user = not bothered; recorded for the map.
- THE NEXT: (1) the orbit question = pending the user's answer (the character
  revert's confirmation — the boot-5 attribution); (2) the action-A consumer-trace
  deliverable = the next decision input (the lane is running); (3) THE NEW FORK =
  the fork's subclass_equip handler + the pushed 824-B family-4 record (what the
  server's answer contains) — the zero-boot inspection, the suspect surface named
  by the ruling's fork.

## 10.91 THE LANE DEATH + THE COLLECTOR FALLBACK + THE RESUMPTION (2026-08-17 ~14:3x)- THE ACTION-A LANE = DIED mid-synthesis (the silent-death pattern — no final text;
  the files intact). WHAT IT LANDED before dying (PASS 1 of mission a — all VERIFIED
  with verbatim evidence, read end-to-end from lane_consumer_trace_raw.txt + .log):
  (1) DAT_1427BE750 = a POINTER SLOT to a heap object of 4 records {u64 match
  @+8+9i, u8 flag @+0x10+9i} + a u64 header — the closeout's "4-entry static table"
  reading = CORRECTED; the slot = zero at rest, runtime-initialized; the healthy
  dump's heap record = the vtable-ish ptr + match rec[0] 0x264134C78499717C, the
  failing dump's match = 0. (2) FUN_140a43300 = the 4-iteration match loop setting
  flag=1; its callers = 3 — the tail-stamp compare FUN_140e07ba0 + FUN_140dfd740 +
  the NEW second dispatch site FUN_140e05eb0. (3) The four flag bytes = ZERO static
  refs → the readers must deref the slot at runtime (the readers = the slot's
  readers; named: FUN_140a42e60/a42ed0/a42fd0/a44340/a42eb0 + 5 no-func hits).
  (4) FUN_140e168d0 = a thunk into the VMP stub region (FUN_148587a08) — the flag
  word = the VMP record key.
- THE COLLECTOR FALLBACK (per the lane death-safety rule): the deliverable =
  completed by me from the raw → claims/anchor-consumer-trace.md (the landed part,
  with the OPEN list = pass 2 + missions b/c). THE RESUMPTION: the completion lane =
  re-spawned (~14:3x, background) with the FULL resume context (the collected
  deliverable + the raw + the log + the original brief = its zero point — it does
  pass 2, mission b, mission c from there; its deliverable =
  claims/anchor-consumer-trace-completion.md, merged at landing). The brief =
  claims/anchor-consumer-trace-completion-brief.md.
- THE STATE DISTILL: STATE.md + STATE-inventory.md = rewritten (the boot-6 outcome:
  the ladder closed, the swap-log fork → the fork's subclass-slot handler = the
  suspect surface, the bisection direction corrected → the veteran ∈ 47..3933, the
  weapon-gate map entry, the lane state). THE NEXT: the lane's landing (the
  notification, no polling) + the user's orbit report + the fork's subclass-slot
  handler inspection = the next lane candidate (the user's lift).

## 10.92 THE ORBIT = BACK — THE CHARACTER-BANK ORBIT GATE = CONFIRMED (2026-08-17 ~14:4x)

- THE USER'S REPORT (~14:4x): the orbit = RESTORED this boot ("yes, orbit came
  back") → THE BOOT-5 ATTRIBUTION = CONFIRMED EMPIRICALLY: the character bank (the
  character_object scope) carries the GUIDED-STATE/ORBIT gate, and the value 2 =
  "cannot return to orbit" (the tutorial/forced-mission state). The boot-6 revert
  (the 4,096-all-2 → the 177 seed) = the fix, and the lock's culprit index ∈ the
  3,919 indices the boot-4 probe added beyond the 177 seed (the 177-seed boots 1-3
  = orbit normal; the boot-4 all-2 = locked; the boot-6 revert = restored).
- THE SWAP CONTEXT: the swap still failed in the boot-6 WITH the 177 seed restored
  — consistent with the boots 1-3 (the 177 baseline = a failing state), so the
  character bank @ the 177 seed = NOT the swap blocker; the ladder item 3
  attribution = stands unchanged.
- THE STANDING STATE: the completion lane = running (the notification = the next
  event, no polling); the fork's subclass-slot handler inspection = the next lane
  candidate (the user's lift); no next boot scheduled (the bisection fold 47..3933
  = the standing piggyback).

## 10.93 THE STANDING DIRECTIVE: NO NEW LANES, CONSOLIDATE INTO THE ESCALATION (2026-08-17 ~14:5x)

- THE USER'S DIRECTIVE (~14:5x, binding until lifted): (1) when the consumer-trace
  COMPLETION LANE lands, HOLD OFF on spawning any further subagents — no new lanes
  without the user's explicit go; (2) the lane's deliverable = KEY to consolidating
  the findings of a DEEP RESEARCH SWARM the user ran in another session; (3) on the
  lane's landing: update the STATE files FOR ESCALATION (the "STATE + incident <ts>"
  package pattern — the reviewer's next audit base); (4) THE GOAL = MAP EVERY FLAG
  IN THE GAME (the full flag map: the suppression protocol's empirical blocks + the
  manifest name table + the swarm's findings + the consumer-trace's client-side
  reader map = the consolidation). The fork's subclass-slot handler inspection +
  the 5CE1 corpus-order lane (action C) = both FROZEN behind this directive.
- THE CONNECTION (why the lane is the consolidation key): the consumer-trace's
  DAT_1427BE750 slot-reader cluster + the flag-byte readers = the client-side
  consumers of flag-state change — the anchor that turns the swarm's flag findings
  into named client behavior. The landed part-1 deliverable already carries the
  cluster names (FUN_140a42e60/a42ed0/a42fd0/a44340/a42eb0 + the no-func hits) +
  the zero-static-refs finding.
- THE NEXT EVENT = the lane's landing notification (no polling, per the rules). On
  landing: read the deliverable end-to-end → verify the load-bearing claims against
  disk → update STATE.md + STATE-inventory.md for the escalation → present the
  package to the user (the user arbitrates the escalation's question).

## 10.94 THE CONSUMER-TRACE LANDS: THE CHAIN IS CLOSED, THE ROLLBACK POINT = NAMED,
##        THE FLAG BANK = IN THE DIFF (2026-08-17 ~17:1x)

- THE LANE LANDED (~17:0x) with the VERDICT: CHAIN CLOSED — a REFRESH chain, not a
  commit gate. The deliverable = claims/anchor-consumer-trace-completion.md (read
  END TO END) + the raw (lane_consumer_trace2_raw.txt, 8 passes ~5,600 lines). MY
  SPOT-VERIFICATION (the deliverable rule, the load-bearing bits): FUN_140e078f0's
  memcmp(+0x742c, 0x300c) + the equip-state machine @+0x1249c/0x124a0 (state 4 →
  FUN_140bb5280, 3 → FUN_140bb5b00) + the per-item +0x124a4 diff loop = VERIFIED
  verbatim in the raw (lines 3772-3833); FUN_140fa8fd0 (the acquire-flag writer,
  the UI's equipped/featured match → param_1[0x10]=1) = VERIFIED (lines 4765-4811);
  the record vtable 0x141C2F240 (PREF 0x7FF6AF7F0000, [1]=the gate FUN_140e82ab0,
  [6]=the UI refresh FUN_140e80fc0, [2..4]/[7]=the no-op) = VERIFIED (lines
  3878-3888; the pass-6 guess = retracted in-raw).
- THE NAMED CHAIN: WorldServer wire → FUN_140e010c0 → e0a0b0 → e01960 → FUN_140e01520
  (the store after-image memcmp walker) → FUN_140e05da0 → [family-4 kind-1]
  FUN_140e07ba0 (the stamp compare) → FUN_140c93ec0 (the marker banks — CORRECTED:
  the writer sets/clears BOTH banks together) + FUN_140a43300 (the stamp table) →
  FUN_140a42fd0 (the edge-triggered flag consume: gate FUN_140e82ab0 → UI refresh
  FUN_140e80fc0 → FUN_1404cc440 → the 4-slot changed table DAT_1426edce8 →
  FUN_140a48f60 → DAT_1427c3750 vt+0x18). In parallel: [family-4 kind-0]
  FUN_140e078f0 = THE ROLLBACK DECISION POINT (the desired-vs-store diff-apply:
  the content-dirty flag +0xded11, the 127-record acquired diff FUN_140e07af0, the
  acquire-flag writer FUN_140fa8fd0, the equip-state machine). THE COMMIT =
  FUN_140e02280 (decode-only) — L3's verdict RE-VERIFIED.
- THE FLAG-MAP BREAKTHROUGH (the swarm's consolidation key): the rollback's diff
  memcmps THE ENTIRE ACCOUNT FLAG BANK — 12,300 bytes @+0x742C, length 0x300C =
  EXACTLY the 12,300-index bank — and sets the content-dirty flag on ANY change.
  THE FLAGS' CLIENT-SIDE CONSUMER = NAMED: the flag bank is part of the
  desired-vs-store diff the UI applies on every push. The stamp-table consume chain
  = the second consumer (a UI list-model refresh driven by flag-state changes).
- THE OPEN ITEMS (the lane's, handed back with evidence pointers): (1)
  FUN_140bb5280/bb5b00 tails (the equip-state handlers' decoded action); (2)
  FUN_140e83510's 276-line body (the changed-content list builder); (3) the WS
  dispatcher tails (which opcodes drive the walker); (4) the two tail VMP anchors
  DAT_1427c3750/DAT_1427f1858 (a live dump read = the cheapest next input); (5) the
  bit-pollers' consumers FUN_140ba3e70/be9170; (6) L3's unchanged opens (the
  family-5 caller, the 2100 resolution, the CUI region).
- THE DIRECTIVE = EXECUTED: NO new subagents (the standing directive); the STATE
  files = updated FOR ESCALATION; the escalation package = "STATE + incident <ts>"
  (the fresh incident = generated); the goal = MAP EVERY FLAG IN THE GAME — now
  with the client-side consumer named (the flag bank's diff-apply) as the
  consolidation anchor for the deep-research swarm's findings.

## 10.95 THE CROSS-SESSION CONSOLIDATION: THE SWARM'S N1+N2 EVALUATION CONFIRMED
##        (2026-08-17 ~17:2x)

- THE OTHER SESSION'S EVALUATION = FOUND + READ (the FINDINGS entries "10.81 THE
  FLAG-RESEARCH SWARM: ROUND 1 LANDS, N1 = THE CANONICAL SPINE" ~13:4x + "10.82
  N2 LANDS: THE FIRST STATIC HASH→INDEX RESOLUTION SITE" ~14:1x — the other
  session evaluated N1 + N2 while my session ran the swap front; both sessions
  appended to the same FINDINGS file). NOTE: the two sessions' entry numbers
  COLLIDED (two 10.81s + two 10.82s); MY entries = renumbered to 10.88-10.94 (the
  append-order fix; the other session's 10.81/10.82 = the swarm entries, mine =
  the escalation/boot-saga entries; the timestamps disambiguate).
- MY CONFIRMATION OF THE OTHER SESSION'S SPOT CHECKS (the user's ask): (1) N1's
  master table = VERIFIED against disk (flags_canonical_table.json: 21,569
  index_hash entries, 108 named rows, the veteran trio 0x904E7433/7430/7431 @
  0/1/2, the Sweet Business 0x843462C8 @ 3534 + its era-cross to the current
  13047 with the identical name, the 3 orphans = the faction trio, the 3
  current-only names — all verbatim per the claim); (2) N2's platform-pair record
  @0x141F93168 + the consumer FUN_140548e00 = the other session spot-verified
  against r1n2b_raw; my independent read of both deliverables end-to-end = agrees
  with its distillation.
- THE TWO ADDITIONS THE OTHER SESSION MADE THAT I CARRY FORWARD: (1) THE TRAP-19
  CORRECTION: N2's live-instrument suggestion ("a breakpoint on the item-service
  vtable+0x6F8") = a debugger = trap #19 — the instrument = THE HOOK FORM (the
  fork's own gate-read logger family), not a breakpoint. (2) THE SWAP-NEIGHBORHOOD
  CONNECTION: N2's FUN_1405084c0 (the character-level requirement session, the
  evaluator's caller) touches the +0x2CC0 equippedInstanceSoids = the equipped-
  items gate evaluation — THE SAME FAMILY the swap's gate belongs to. The swarm's
  requirement-expression VM (FUN_140548e00) + my consumer-trace's rollback
  executor (FUN_140e078f0) = the two halves of the swap's gate machinery, now both
  named.
- THE SWARM PLAN (the other session's, recorded): Round 1 = N1 (the canonical
  table, LANDED) + N2 (the consumer walk, LANDED) + N3 (the package strings, IN
  FLIGHT — the claims file flags-r1-n3-strings.md = being written; read it when
  the user says it's done); Round 2 = U2 (the graph walk) + U3 (the liveness
  census) for the unnamed. DROPPED (the overlap): the 5CE1 corpus-order map (my
  session's action C = the frozen duplicate). SKIPPED (the user's call): the D1
  archive + the community route.
- THE CONSOLIDATED FLAG-MAP SHAPE (the three layers, all landed): (1) THE NAME
  TABLE (N1): the index → hash → name (108 named; the names = era-stable on the
  hash; the indices = per-patch slots). (2) THE GATE MACHINERY (N2): the runtime
  registry resolves the hash → index; the requirement-expression VM reads the
  evaluated byte == 2 (the silent pass/fail); ONE static resolution site exists
  (the platform pair). (3) THE APPLY MACHINERY (mine): the raw bank (12,300 B) =
  the diff-apply's input on every push (FUN_140e078f0) + the stamp chain = the UI
  refresh. THE ONE BRIDGE QUESTION = the bank↔manifest correspondence (N1 OPEN-1/
  N2 OPEN-1): settled by the platform-pair instrument in ONE boot (the hook form,
  no server change) — the cheapest decisive step for the whole map.
- THE NEXT: N3's landing (the user's signal) + the correspondence instrument =
  the next boot candidate (the user's arbitration; the bisection fold rides
  along) + the STATE files = integrated below (the swarm round-1 state).

## 10.96 THE ROUND-2 LAUNCH + THE BOOT-6 TRACE HARVEST (2026-08-17 ~17:5x)

- THE ROUND-2 WAVE = SPAWNED (this session, the user's go — the no-new-lanes
  directive lifted for the batch): U2 (the consumer-context walk, anchored on the
  named chain), U3 (the corpus-coverage census + the dispatch join), U4 (the
  string→index bridge, the calibration problem). The briefs =
  claims/flags-r2-u2-consumer-context-brief.md / -u3-coverage-brief.md /
  -u4-string-bridge-brief.md; the hook design doc =
  claims/anchor-hook-gateread-design.md. (The first spawn wave died with the
  opencode kill — zero residue, re-spawned.)
- THE BOOT-6 TRACE HARVEST (the main session's readers lane — ZERO boots): the
  boot-6 client log (dcv build\bin\x64\Sunrise\logs\sunrise.log, 1.6 MB) carries
  13,874 flag_test lines from the EXISTING ability_gate observer
  (FUN_140E06070, the account-bank test — deployed since the 10.42 era). THE
  PARSE (gate_trace_reader.py → gate_trace_boot6_parse.json): ALL 13,874 lines =
  ONE bank base (rcx 0x9CACDFF2E0) + ONE caller (+0xE0880C) = the account bank
  walked FULLY (all 12,300 unique indices) in the first 30 s of the client boot
  — the transaction machinery's full-bank test, confirmed live. The result
  split: 10,750 pass ≈ the 10,738 seeded value-2 rows ✓. THE SWAP WINDOW =
  EMPTY: zero flag_test lines during the swap attempt → the swap's gate does NOT
  go through FUN_140E06070; the swap-time readers = the rollback-executor side.
  THE CENSUS ANSWER (the account scope): the client reads ALL 12,300 account
  indices every boot (the alive/dead split = "all alive" for the account bank).
- THE HOOK IMPLEMENTATION (the main session's lane): the new gate_trace observer
  module = LANDED + BUILT + DEPLOYED — three log-only detours in the fork's
  ability_gate pattern (the pass-2 no-deref lesson honored): (1)
  FUN_140E06090 (0xE06090, the character-object bank test `FUN_140be1cb0()+0x9348
  +idx==2` — the sibling gate's census); (2) FUN_140E078F0 (0xE078F0, the
  rollback executor entry — the diff-apply marker per push); (3) FUN_140FA8FD0
  (0xFA8FD0, the acquire-flag writer — the item-index + value + caller = the
  swap's gate-check footprint). Files: Sunrise/src/client/hooks/gate_trace/
  gate_trace_observer.{h,cpp} + the activation + the vcxproj entries (the
  worktree, uncommitted until the boot validates). The build = /m:1 clean (13
  new functions); the deploy = dcv build\bin\x64\steam_api64.dll 8614C028…
  (5,289,984 B; the backup .bak_gate_trace_pre = the C4D5ADFC 7fe8116 DLL). The
  server = untouched; the game = closed at the deploy (the clean copy).
- THE CORRESPONDENCE = the zero-boot route (the next main-session task): the
  item-service vtable+0x6F8 resolver's body decompile → the runtime table walk →
  the -87 dump-side read of the platform pair's resolved indices (the dumps =
  boot-built registries already on disk; RELOC_DUMP 0x7FF641BF0000).
- THE NEXT BOOT = the gate-trace boot (the user's arbitration): the tour + ONE
  swap attempt; the parse after = the character-scope census + the swap's
  acquire_check footprint + the rollback_apply markers + the caller RVAs. NO DB
  changes (the pure observation — the bisection fold = the NEXT boot's piggyback,
  kept out so the census observes the current state).

## 10.97 THE BOOT-7 HARVEST + THE SUBCLASS-SLOT INSPECTION (2026-08-17 ~19:5x)

- THE BOOT-7 PARSE (gate_trace_boot7_reader.py → gate_trace_boot7_parse.json):
  (1) THE CHARACTER-BANK CENSUS = char_test 8,015 calls, ONE caller (+0xE0880C =
  the SAME transaction walker as the account bank), the FULL 0..4095 walk in the
  first 30 s, pass=177/FAIL=7,838 — the pass count = EXACTLY the 177-seed
  character_object set → the wire→store→gate-test pipeline = confirmed
  end-to-end (the boot-6 revert = live in the client's store). (2) THE ACCOUNT
  WALK = flag_test 13,874, all 12,300, pass=10,750 (≈ the 10,738 seeded). (3)
  THE DECISIVE ABSENCE: rollback_apply = 0 + acquire_check = 0 ACROSS THE WHOLE
  SESSION — the diff-apply machinery never fired on ANY push (including the
  normal queuez pushes + all three swaps). The swap's cue-then-rollback = a
  UI-side revert with NO store diff and NO gate-test traffic through any hooked
  reader. (The caveat: the walker chain = the WorldServer wire path; the queuez
  family-4 increments = a different client apply, unobserved by these hooks —
  and the client's decode log shows only the FAH/PRIMARY channels.)
- THE SUBCLASS-SLOT HANDLER INSPECTION (the fork source, read end-to-end): the
  403 → web_service_runtime (the policy check subclass_equip_request_valid → the
  outcome) → queuez_outcome_staging (equip_subclass_item — the picked item →
  equipment.slots[subclass], the displaced → storage, the publish; persist_
  subclass_equip — the items rows in_equipment/slot 11; the cache restamp) →
  prepare_subclass_equip → the loadout resolve (FRESH from the mutated
  equipment.slots) → character::encode → the push. THE CHAIN = CORRECT BY CODE
  READING: the mutated loadout feeds the encoder, the equippedInstanceSoids
  [subclass] + the content stamp change. THE NAMED DIVERGENCES (ranked, the
  remaining suspects): (1) THE ENCODER EMITS NO SOCKET/PLUG STATE AT ALL — zero
  socket/plug writes in the character encoder (the client's subclass display
  model = possibly the socket array's plug, which the record never carries — the
  kind-3 socket machinery + the 36-slot array FUN_140e08670 = the client-side
  home); (2) the frame's VERSION arithmetic ("one above the peer's current
  version") — a client-side stale-frame drop would leave the store unchanged
  with zero diffs (exactly the observed shape); (3) the client's queuez-side
  family-4 apply = unobserved (the walker hooks don't cover it). THE NEXT STEP
  (zero boots): the harness comparison — encode the character PRE vs POST equip
  → the byte diff → which regions actually change → which of the three the
  client's model reads.
- THE LANES: U4b (the Record-space bridge continuation) + the correspondence
  lane (the zero-boot platform-pair resolution) = both spawned (the briefs =
  claims/flags-r2-u4b-record-bridge-brief.md + correspondence-lane-brief.md).

## 10.98 THE SWAP'S ROOT CAUSE = NAMED: THE 2100 = UNHANDLED (2026-08-17 ~19:6x)

- THE HARNESS (--equip-diff, built + run in the scratch env): the character
  record PRE vs POST the subclass equip = 11 DIFFERING BYTES in exactly the right
  regions — the subclass slot's equipped SOID 0x32→0x0C @0x2CC0, the two
  inventory rows swapped (the picked 0x0C in, the displaced 0x32 out), the
  equipment summary's definition index, the content stamp 0xEB44→0x55F9 — and
  ZERO flag bytes touched. VERDICT: the server-side equip chain = EXONERATED at
  the record level (the mutation → the loadout → the encoder → the push = all
  correct; the pushed record DOES change).
- THE BOOT-7 2100 EVIDENCE (the ability_gate observers, the client log): THREE
  emit_2100 lines with REAL resolved hashes — 0x841B4EF7 ×2 (the two ability
  swaps) + 0xAB28899E (the subclass swap's ability change) — the caller
  +0xC8B906. L3's "the payload defaults to kNoDefinitionHash, the UI's resolver
  has nothing to resolve TO" = REFUTED for the live swap: the UI resolved the
  real targets and emitted the verbs. The server received all three (the
  ws_capture payloads = {u32 hash, u8 flag}: 841B4EF700 ×2 + AB28899E00).
- THE SMOKING GUN (the fork source): opcode_routes.cpp's kStatusOnlyOpcodes =
  {..., 2100, ...} — the server answers the 2100 with the status-only echo and
  has NO 2100 HANDLER anywhere (no mutation, no push). THE ROLLBACK MECHANISM
  (now fully explainable): the swap = the 403 (the equip — HANDLED, the record
  changes) + the 2100 (the ability/definition change — UNHANDLED, the status
  echo only); the client's optimistic UI shows the new subclass, the store's
  after-image arrives WITHOUT the ability/definition change, the requirement-
  expression gate fails, the UI reverts. THE CONVERGENCE: the fork's missing
  2100 handler + the encoder's missing socket/plug emission = the SAME gap —
  the ability/definition side of the swap = entirely unhandled server-side.
- THE FIX (the next implementation, the integration worktree): the 2100 handler
  — parse the {u32 definitionHash, u8 flag} payload → resolve the ability
  definition through the build data → write the ability state into the character
  model → the encoder emits it (the socket/plug region) → the push carries it.
  The fork's state model = needs the ability-selection field (the parked
  ability-lock open item 4 = now the named work).




## 10.96 N3 LANDS: THE STRING HARVEST + THE RECAP'S FACE = THE D1-ACCOMPLISHMENT FAMILY (2026-08-17 ~15:4x)

- N3 = LANDED (claims/flags-r1-n3-strings.md, read end-to-end + the artifacts
  spot-verified on disk). THE FOUR YIELDS: (1) THE PREMISE CORRECTED + THE
  DECODER BUILT: the _en_ packages = VOICE AUDIO (Wwise banks), not strings; the
  real string banks = block-encrypted in the main packages — so the lane built a
  COMPLETE PYTHON PACKAGE DECODER (lane_flags_r1n3_decoder.py: the AES-128-GCM
  tiger keys + Oodle via ctypes, validated 6/6 byte-identical vs tigercli; it
  decodes ANY entry in ANY patch file — tigercli = final-patch only). The
  gotchas recorded: the house's pkg_reader.py has an off-by-2 pkg_id bug; oo2core
  returns 0 on success; the GCM tag = appended. (2) THE STRING CIPHER CRACKED:
  the containers = class 0x80809A8A (the 48-byte record slots) + the
  subtract-0x1F per-byte cipher (the round-trip proven: "Requires Level 20").
  (3) THE HARVEST: 223,244 unique strings install-wide (strings9a8a_v5.json) —
  the 8/14 class list = grossly incomplete (the investment family alone =
  119,156 entries vs 0 listed). (4) THE CORRELATION: the manifest names =
  CONFIRMED in-package (27/72 exact + 45 substring + 0 absent) + the core unlock
  bank container 0x8132F896 with the NEW vocabulary: "Requires Exo Guardian" (the
  THIRD race gate — the manifest only names Awoken/Human), the "Requires Level
  4/11/18/28" family, the Warmind fireteam gate, and ~30 "Requires *" strings
  beyond the manifest's 108 = the unnamed flags' candidate names.
- THE RECAP'S FACE (the same-session follow-up, the grep over the string index):
  the D1-ACCOMPLISHMENT FAMILY = 9 conditions, all languages — "Completed all
  Destiny 1 campaigns.", "Reached a Grimoire score of 5,000 or higher in
  Destiny 1.", the Black Garden/Dark Below/Crota's End/House of Wolves/Taken
  King/Rise of Iron completions, "Collected Thorn, the Last Word, and Gjallarhorn
  in Destiny 1." = THE RECAP MONTAGE'S DISPLAYED CHECKS (the recap lane's
  accomplishments node = the surface; these = its conditions). THE VETERAN'S
  EVENTUAL INDEX = one of these (or the derived "any D1 accomplishment" state) —
  the bisection's final answer = LABELABLE. The two routes to the exact index:
  the bisection (in flight, 47..3933) or the targeted container re-decode (the
  D1 family's container record order × N1's index table = the string→index
  bridge).
- THE ROUND-1 COMPLETE + THE ROUND-2 REVISED (the plan change, per the N2/N3
  lessons): U2 = the CONSUMER-CONTEXT walk (from the gate primitives up — NOT
  the hash walk; the hashes = runtime-resolved), U3 = the corpus-coverage census
  + the dispatch-table join (the liveness defers to the runtime), U4 = THE
  STRING→INDEX BRIDGE (the N3 container order × N1's index table — the unnamed
  flags named wholesale if it lands), ALL resting on the GATE-READ HOOK (the
  DLL-side logger on the two gate primitives = the runtime foundation + the
  correspondence instrument + the swap's read footprint). The STATE files = the
  integrated post-Round-1 state (this entry's companions).







## 10.99 THE PER-INDEX FLAG DICTIONARY LANDS: THE SHIFT MAP, THE RECORD BRIDGE, THE ELITE-4 (2026-08-17 ~22:4x)

- THE SESSION (a parallel worker of the main session): assemble the per-index flag
  dictionary — semantic identity per bank-slot 0..12299 — via three flash lanes
  (read-only: zero boots, zero DB writes, zero fork edits). THE DELIVERABLE =
  RE_output/claims/flag-dictionary.md + the machine table
  RE_output/content/lane_dict_assembly/flag-dictionary.json. The lane briefs +
  deliverables: claims/lane_dict-{shift-sweep,record-bridge,string-index}-brief.md +
  claims/lane_dict_{shift-map-sweep,record-bridge,string-index}.md.
- LANE 1 — THE FULL SHIFT-MAP SWEEP (claims/lane_dict_shift-map-sweep.md): all
  21,569 Arrivals unlock hashes run through the healthy dump's runtime registry
  (DAT_142743318, count 21,613). THE CORRESPONDENCE VERDICT'S ASSUMPTION REFINED:
  the registry = Arrivals MINUS 57 PLUS 101 (net +44, NOT 44 inserts). The shift = a
  30-knot step function (26 clean knots m 0..20522, then a 4-knot churn where the 57
  removed rows sit); the anchors reproduce exactly (trio 0/1/2, platform 581/583,
  Level 10 689, Sweet Business 3538, 0x42889986 = miss). THE BANK = registry
  0..12,299 EXACTLY saturated = manifest 0..12,245 + 54 inserted rows; m 12,246
  crosses out (registry 12,300); for m > 12,245 there is NO bank byte. THE MoT CROSS
  = 0/25 records, 0/25 objectives, 0/4 nodes → THE RECORD-STATE IS NOT THE UNLOCK
  REGISTRY (the veteran records' SEPARATE space confirmed with certainty; the recap
  trigger = a regular unlock byte, not a record byte).
- THE ELITE-4 (this session's synthesis): exactly 4 of the 101 client-inserted
  (post-2020-snapshot) unlock rows have bank bytes INSIDE the veteran's bisection
  range 47..3933: bank/reg 340 (0x074D3FB6), 341 (0x684821C7), 951 (0x4A05DDB5),
  952 (0x516F384A). 340/341 = the only inserted rows still shipped in the 2026 era
  (current idx 2255/2256, still unnamed) — the prime 'real-server-set-on-D1-import
  flag that the 2020 snapshot never named' candidates. Cross: none equals a MoT
  record/objective/node hash.
- LANE 2 — THE RECORD-STATE BRIDGE (claims/lane_dict_record-bridge.md): THE RECORD-
  STATE'S WIRE HOME = the account objectiveValues bank (+0xA438, 6,200 i32), keyed
  by the manifest DestinyObjectiveDefinition.index, proven on four independent
  sources (settings seed → DB objectives → wire encoder → healthy dump bank). Record
  190's objective 0x5DAEBC4C = objective index 1682, seeded 2000 ≥ cv 1 = COMPLETE
  (records 191/192 at 1683=1000/1684=50). The server never mutates the bank. THE
  CLIENT READERS NAMED (VMP-free decompiles): FUN_141582830 + the 127-record diff
  FUN_140e07af0 → FUN_140e07510 → FUN_140fa8fd0 (the 'acquire-flag writer' = actually
  the RECORD-STATE writer). THE BRIDGE: the recap trigger = a FLAG byte (2) in-band
  47..3933 (a flag read empirically, not an objective read); flag[1682]=2 = the
  statically-nameable single-byte candidate (unlock-run byte in-band whose index
  coincidentally equals the seeded-complete objective slot) — but the flag↔objective
  spaces are INDEPENDENT (mirror 55%), so the coincidence is not a design. THE NEXT
  BOOT (the main session's): test flag[1682] alone (bank 1682 = manifest 1678, hash
  0x34321F7D), then the elite-4, then halve the ~3,886 in-band set flags.
- LANE 3 — THE STRING→INDEX EXTENSION (claims/lane_dict_string-index.md): the ORDER
  RULER = REFUTED for the unlock containers too (0/830 index-monotone; N3's
  'alphabetical' dump = an extractor sort, NOT the container order — position
  carries no index info). THE IDENTITY JOIN = 122/164 strings land on an index: 72
  HIGH (the manifest's named strings → unlock rows), 50 MEDIUM (the 25 D1 records'
  name+desc → records 190-214, each gating an OBJECTIVE index 1682-1712), 41 NONE
  (package-only in BOTH eras — the runtime-registry vocabulary: season gates,
  fireteam gates, Shadowkeep currencies, race/level orphans). THE UNLOCK↔OBJECTIVE
  LINK = DEAD (0/8,549 objectives carry an unlockValueHash).
- THIS SESSION'S CORRECTION (the deliverable rule in action): lane 2 claimed "10 of
  25" MoT objectives resolve — an UNSIGNED-int64 id-query undershoot (the 15
  'missing' rows exist; their row ids are negative signed int64s; 0x8951E1C4 =
  -1991122492). Lane 3's 25/25 map = authoritative, re-verified signed-robust this
  session: 190→1682, 191→1683, 192→1684, 193→1685, 194→1686, 195→1687, 196→1688,
  197→1689, 198→1691, 199→1690, 200→1692, 201→1693, 202→1701, 203→1694, 204→1695,
  205→1696, 206→1697, 207→1698, 208→1699, 209→1702, 210→1703, 211→1709, 212→1710,
  213→1712, 214→1711. RULE: query DestinyObjectiveDefinition by json.hash (or the
  signed int64 id), NOT the unsigned id.
- THE DICTIONARY'S COVERAGE (flag-dictionary.json, shift-applied): 12,300 bank rows;
  105 named + 548 family-labeled; the unnamed interior inside the veteran range =
  ~3,580 rows; 21 rows (26..46) fully silent; the 3 faction-trio + 47 inserted rows
  sit above the bank (registry ≥ 12,300 = no account byte). RANKS: the recap-veteran
  chunk (NEXT BOOT ORDER = flag[1682] → the elite-4 → halve the fold) + the legacy
  objective lanes (1700/1704-1708) + the 41 package-only strings.
- THE STATUS: all three lanes landed clean (the shift sweep first, then the record
  bridge, then the string index); no lane died; the resume protocol was prepared but
  not needed (content/lane_dict_assembly/resume-template.md). THE NEXT: the main
  session's boot brief (flag[1682] first) + the record-table merge at the fork's
  discretion + the 41-string live-instrument lane.

## 10.99b RECOVERY INCIDENT — FINDINGS OVERWRITE + TRANSCRIPT-BASED RESTORE (2026-08-17 ~23:1x)

- THE INCIDENT: this worker session accidentally OVERWROTE this file (~75 KB, 1041
  lines) with a 5,639-byte entry via a write-tool misuse (write = full replace, not
  append). The file was untracked in git (no git recovery). The overwrite was caught
  the same minute.
- THE RECOVERY (FULL, verified): the opencode session store (opencode.db
  session_message) holds EVERY read tool result verbatim. Two independent worker
  sessions (this one + ses_fed3d7a63) had read this file at 19:47-19:48, capturing
  BOTH halves (lines 1-695 and 696-1041) with the display's "N: " line prefixes. The
  prefixes were stripped with a strict sequential validator (0 real mismatches), both
  halves cross-validated byte-identical between the two sessions, and the resulting
  file matches the pre-overwrite trim's exact code-point length 75,077 (diff 0).
  VERIFIED: 1041 lines; every entry 10.66-10.98 present; the 695/696 join exact; the
  6 trailing blank lines preserved.
- THE LESSON (recorded for every session): (1) NEVER use the write tool on an
  append-only archive — appends = a shell/python append or the edit tool anchored on
  the file's tail; (2) opencode.db session_message = a lossless transcript fallback
  for exact file recovery (the read result is stored verbatim with prefixes); (3)
  the recovery recipe = recover_rebuild.py/recover_verify2.py/recover_restore.py in
  C:\Users\rasla\AppData\Local\Temp\opencode\ (copied to RE_output/content/lane_dict_assembly/recovery/ for reuse).
