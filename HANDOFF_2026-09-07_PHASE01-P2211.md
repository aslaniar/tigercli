# HANDOFF 2026-09-07 (EVENING) — THE CHAIN IS LIVE, STAGE B IS CHURN, AND THE p2-211 BOOT IS STAGED BUT NEVER RAN

STATUS: live (2026-09-07 ~19:00, written by the opencode banking session after
the Claude Code session hit its limit mid-staging). Read STATE.md's header
first, then this. Companions: FRONT_multiplayer-chain.md (the live mechanism
chart, updated through 20.338); FRONT_peer-render-chain.md (the pinned chart);
FINDINGS 20.336-20.338 (full text, committed on main at 0bf42d6).

## WHAT HAPPENED AT THE CUT-OFF (read this first)

The Claude session finished the RESEARCH and banked it as three commits on
branch wt-phase01-alloc (20.336/20.337/20.338), then began staging the next
boot and died at the session limit right after WRITING the staging script but
BEFORE RUNNING it. The user said "bank all our info, update all our docs."

What the banking session (this one) did:
1. Verified the live tree was untouched by the interrupted session, then merged
   wt-phase01-alloc into main (fast-forward, main is now 0bf42d6 = 20.338).
   The pre-merge uncommitted state is preserved as stash@{0} ("pre-merge
   snapshot... 2026-09-07") — every file in it was verified byte-identical or
   superseded by the branch before the merge; drop it whenever you like.
2. Finished the doc layer the interrupted session never reached: STATE headline
   + NEXT + DEPLOYED refresh, FRONT_multiplayer-chain.md Stage B/D rows,
   FRONT_peer-render-chain.md row-8 refinement, TOOLS.md row for chain_status.py.
3. Verified the staging state on disk (details below) and recorded it UNRUN.

## THE THREE BANKED FINDINGS (one paragraph each)

**20.336 — the archive audit.** The server logs for 3 of the last 4 boots are
STARTUP-ONLY (24/41/24 lines); p2-207 lost the mac entirely. p2-207's ledger
record ("EFFECT NULL ON BOTH MACHINES") is VOIDED — no mac behind it, and it
cites lines that occur zero times in the archived log; the p2(42) question is
OPEN. 20.335 R4's "the trailing pair is the only variable" does not survive the
archive either. And the row-8 census needle was wrong (searched handler
addresses, which an object never stores — only its vtable base); fixed and
re-run: the verdict stands and is now actually tested.

**20.337 — C2 built as a patch; Stage B withdrawn as an instrument error.** The
cross-member allocation map exists as 0001-c2-cross-member-allocation.patch
against RE_build/Sunrise-fork-inventory — NOT deployed, NOT applied. C1
(refill cadence) withdrawn: the 17 pushes are spread across the whole boot.
Stage B's four contradictory statements are recorded in place; the cause was a
regex requiring `reason=` (only refusals carry it), which inverted the verdict:
the measured truth is 482 of 545 snapshots DID carry a peer. chain_status.py is
the mechanism so it cannot recur.

**20.338 — the vtable read + the Stage B correlation.** The receive-block
construction chain's root (0x140B5ECD0) is entry 0 of a 14-entry handler table
registered by one READABLE call hiding in the VMP code — and the registration
RAN: in the p2-206 dump the descriptor object is live on the heap holding the
exact registration arguments, and the root's address sits in NINE byte-identical
heap records; the ent ctor 0x1416BB1E0 has zero heap refs; the control needle
returns 0. So "architecturally unreachable" is NOT what the evidence says —
the subsystem is registered and live. What is measured is ONLY that the
constructor never ran. Whether the ROOT executes-and-bails or is never
dispatched is NOT settled and must not be asserted either way. Stage B: the 48
same_account refusals are a contiguous JOIN TRANSIENT on one session (guards
are fine); the 65:3 asymmetry is SESSION LIFETIME — the rig's session is
long-lived (456 snapshots), the mac's churn at 6-9 then replaced. WHY the mac's
sessions churn is the open question, and it is server-side.

## THE STAGING STATE (verified on disk, 2026-09-07 ~19:00)

- `.claude/worktrees/fork-p2211/` — a worktree of the NESTED fork repo
  (RE_build/Sunrise-fork-inventory) at HEAD 3578d98, carrying the 13 uncommitted
  files that ARE the deployed server state. Contains:
  - `apply_p2211.py` — the boot's changes, WRITTEN AND NEVER RUN (verified:
    0 hits for `kRecvRootRva` in the observer and 0 hits for
    `stage=session_release` in activity_session_lookup.cpp). It adds:
    (a) CLIENT: a read-only call counter on the chain root (RVA 0xB5ECD0,
    exact .pdata bounds at offset 0, budget caps EMITS only, kTargetsSize
    68→69) — the Stage D instrument;
    (b) SERVER: one teardown log line in release_session (`ev=activity
    stage=session_release session=0x...`) — the Stage B instrument.
  - `PROBE_NOTE.md` — the worktree's own provenance note.
- `0001-c2-cross-member-allocation.patch` (repo root) — the C2 arm, also
  unapplied to the nested repo.
- The live tree and the nested repo were left untouched by the interrupted
  session; it verified that itself as its last act.

## THE NEXT MOVE: ONE PAIRED BOOT, THREE ARMS (all pre-named)

1. Stage D — run apply_p2211.py in the fork-p2211 worktree, build, deploy the
   client. READOUT: `recv_root calls=0` → nothing dispatches entry 0; the
   question moves to what owns/dispatches the table. `calls>0` → it runs and
   bails, and the bail is a static read of a readable 1181-byte function —
   likely server-satisfiable. Either value is progress.
2. Stage B — the same boot's server logs `stage=session_release` per teardown:
   WHO tears the mac's activity sessions down, and when.
3. C2 (optional third arm; apply the patch + flip
   gameplay.entity_index_allocation_cross_member) — pre-named: mgr_free stays
   >0 past landing; the site-214 `failed to create 'player_broadcast' entity`
   count falls from 17-80 toward 0; pushes read members=N, N = session members.
   ABSENCE NEGATIVE: members=N ships and mgr_free still reads 0 → the client is
   not consuming cross-member rows (a real verdict, not a null).

Do not boot without: gate_boot.py on a written brief (it MUST carry the
archive-completeness check — 20.336 R1 cost three boots their evidence),
preflight, and the SETTINGS DELTA named (C2 default OFF; flip only if the arm
is included).

## DO NOT (the earned list, carried forward)

- Assert the root is never dispatched (unmeasured either way — 20.338 R3).
- Loosen same_account (a join transient) or raise the 30s duty cycle
  (`paced` is not a failure — 20.337 R3's inverted verdict came from exactly
  this reading).
- Run the p2(42) self-peer-row arm PAIRED (solo-only, 20.295 R3).
- Cite 20.53 as a render control (retracted); re-census ent instances with
  handler-address needles (vtable bases only — 20.336 R4).
- Trust a null readout without checking the server log archive is complete.

## READING ORDER FOR THE NEXT SESSION

STATE.md header → this file → FRONT_multiplayer-chain.md (Stage B warning block
+ the three line families) → FINDINGS 20.336-20.338 → the staged
apply_p2211.py itself (read what produces each line before trusting it —
09-03 postmortem).
