# FINDINGS — 2026-08-18

## 11.1 THE COMMUNITY-FORK BREAKTHROUGH: THE 801 = THE SUBCLASS SELECTION (2026-08-18 ~00:1x)

- THE DISCOVERY (from community_forks/Sunrise-master, the user's diff-only request): the
  fork implements the opcode-801 as THE SUBCLASS ABILITY-NODE SELECTION — the payload =
  {u64 subclassInstanceSoid, u8 socketEntry (biased +0x80)}. Our server answered the 801s
  with the generic echo; the fork's mutate_subclass_selection parses them + routes the entry
  to the right ability field. The boot-11/12 log decode confirmed: our 801 payloads
  60000000000000328900 → the entry 9, …8800 → 8, …9400 → 20, …8F00 → 15 — the grenade/path
  node picks the user clicked.
- THE FORK'S MACHINERY (read end-to-end): the opcode801 codec (47 lines), the
  socket_entry_buckets domain {socketEntryListIndex, buckets[36]} (the per-entry →
  destination-ability-bucket map, extracted from the selector chains), the
  stage_subclass_selection (the bucket-routed field update + the Attunement wide-group
  bundle handling), the prepare/preview/commit (the staleness-guarded publish). The fork's
  ability fields live on the subclass ITEM; ours live on the CHARACTER — the port adapted
  the target.
- THE 2100 CORRECTION (the earlier work): the 2100's hashes (0x841B4EF7 / 0xAB28899E) =
  the SCENARIO domain's content hashes (the orbit_d2 / ambient_city_tow rows' hash lists),
  NOT the ability picks — the 2100 = a scenario-state announcement, and the reverse map for
  it = the remaining parked piece.

## 11.2 THE 801 PORT (2026-08-18 ~00:2x-~01:0x)

- THE PORTED FILES: middleware/web_service/messages/opcode801.{h,codec.cpp} (the direct
  port), state/build_data/socket_entry_buckets/{definition.h,catalog.h,catalog.cpp} (the
  catalog, the Table pattern), the state_account_runtime's stage_subclass_selection +
  prepare/commit (the bucket-routed routing onto the character-level fields, the bundle
  handling), the web_service_runtime's 801 branch, the queuez staging's subclass-selection
  branch (the commit → the persist → the restamp → the banner refresh), the loader's 6th
  JSON domain (the socketEntryBuckets).
- THE EXTRACTION: build_socket_entry_buckets.py = the 14-list per-entry selector-chain
  destinations → s1_domains_socketEntryBuckets.json (the lists 1-11 = the 15-19 routing
  entries each; the lists 0/13 = the empty non-subclass lists).
- THE FIRST BOOT (the boot-12): the 801s = HANDLED + the commits = SUCCEEDED (the
  subclass_selection result=ok ×4) — but the push = FAILED (the banner_refresh prepare):
  the ability bucket catalog = held only the 9 authored rows, so the new selections'
  bucket rows = missing → the banner re-encode = the fail.

## 11.3 THE ALTERNATIVE CATALOG + THE CACHE SAGA (2026-08-18 ~01:0x-~03:0x)

- THE ENUMERATION: the stage-mirror (the Python replica of the C++ routing) = the 54
  reachable selections per list × 9 lists = the 486 rows. The capacity: the abilities'
  kDefinitionCapacity = 16 → 512.
- THE CACHE REBUILD (the rosterfix machinery vs the live v24): the traps hit + fixed, in
  order: (1) the stack overflow (0xC00000FD) — the Domains aggregate + the loader's JSON
  arena both moved off the stack (the static storage + the 8-MB stack reserve); (2) the
  header model — the Header = 100 B with the 8-B InvestmentConstants (the stat-row count =
  6, not 5); (3) the checksum — the reader's model = the constants + the ENTIRE payload
  stream (the per-domain boundaries = the unnecessary; the rosterfix's record sizes = the
  stale for the spawn domains); (4) the double-splice corruption (the clean backup
  .bak_altrows = the source of truth).
- THE VERIFICATION TOOL: the --cache-check harness mode (the server/persistence/cache_check
  .cpp) = the reader's exact model applied to the deployed cache with every check printed —
  the cache = now the reader-valid (the checksum = the byte-exact match).
- THE RESULT: the server booted with the 486-row catalog (the abilityBuckets json=486
  cache=486 = the match) + the socketEntryBuckets 14-row domain.

## 11.4 THE WEASEL — THE NONCE DOUBLE-ADVANCE (2026-08-18 ~09:1x)

- THE BOOT (the user's morning test): the ability swap → the WEASEL. The server log = the
  swap = fully handled (the banner_refresh result=ok + the subclass_selection result=ok) —
  the client's log = the '_connection_failure_bad_signature'.
- THE ROOT: the append_banner_refresh_notification = advances the push nonce INTERNALLY
  (the queuez_banner_push.cpp's advance at the end); my ability-change + subclass-selection
  branches = added a manual advance on top = the +2 for the one push → the crypto desync →
  the client's signature rejection.
- THE FIX: the removed the manual advances (both branches). The next boot = the clean (the
  three 801 picks = the committed + pushed with the correct signatures).

## 11.5 THE PORPOISE — THE CROSS-LIST STALE PICKS (2026-08-18 ~09:2x)

- THE BOOT: the ability-node picks = the WORKED (the entries 9/8/20 = the commits + the
  pushes); the subclass equip (the 403) → the PORPOISE — the equip's banner_refresh =
  the fail (the reason=prepare): the equip carried the OLD subclass's pick indices onto the
  NEW subclass's list → the catalog had no row for the cross-list selection.
- THE FIX: the equip_subclass_item = the resets the five ability picks to the defaults on
  the equip (the retail-ish "each subclass starts fresh"; our character-level model resets
  instead of remembering per subclass).

## 11.6 THE CHARACTER-CREATION REGRESSION + THE FALLBACK (2026-08-18 ~09:3x)

- THE BOOT: the client = landed on the CHARACTER CREATION screen — the roster snapshot =
  the encode failed. The DB check: the character 0's persisted picks = {6, 8, 10, 21, 2}
  (the entry 21 = the bundle member) — a combination the compute's strict build_ability
  _buckets rejects (the assign_kinds' duplicate-bucket check) and the enumeration skipped.
- THE HOLISTIC FIX: the character_appearance_abilities' apply_ability_buckets = the
  graceful fallback — the find_ability_buckets miss → the retry with the list's DEFAULT
  selection (always in the catalog) → the banner/roster can never fail an encode over a
  stale pick combination.
- THE KNOWN FOLLOW-UPS (the parked): (1) the bucket builder's strictness (the loosening so
  the catalog holds every reachable selection — the fallback's proper successor); (2) the
  2100's reverse map (the scenario-hash family → the ability pick — the last piece of the
  ability swap).

## 11.7 THE CURRENT STATE + THE ESCALATION (2026-08-18 ~09:4x)

- THE WORKING: the 801 ability-node picks (the commit + the push), the subclass equip (the
  state applies; the picks reset), the banner/roster (the never-fail fallback), the server
  (the 486-row catalog, the reader-valid cache).
- THE REMAINING: the ability swap's in-place commit = still the rollback — the 2100's
  reverse map = the last named piece (the node picks work; the UI's ability-swap flow
  appears to require the 2100's completion).
- THE ESCALATION: the user = escalating; the package = STATE + the fresh incident (the
  current stack: the server pid 6512, the cache = the reader-valid, the DLL = the 801
  build). The state files = the updated snapshots for the reviewer.

## 11.8 THE REVIEWER'S RULING LANDS: THE 2100 = RETIRED, THE REAL BUG = THE FAMILY-4
##        VERSION DISCIPLINE (2026-08-18 ~10:32)

- THE UNRECORDED CRASH (the ruling's named real bug; INCIDENT_20260818_094056's client
  log): at client-t≈99 s (t=99281) the client logged "server_object queuez version is
  out of order ... family_type ENUM(4) ... version 4, flags 0x00" → "failed to process
  update" → kick_to_bootflow_start ('unavailable') → the fatal
  '_connection_failure_suicide' → a cleanup loop wedged 255+ seconds (the game still
  running at the incident time; the user closed it ~10:2x). BOOT 16's "the swaps = the
  same behavior" UNDERSTATED this — the boot ended in a session-killing rejection, not
  a rollback.
- THE SERVER-SIDE PIN (verified in the live log, lines 487-510): the two 801s
  (t=189969 entry=9, t=191782 entry=7) each logged result=ok with ONLY the
  banner_refresh (family=0) — then the equip (t=193297) delivered family=4 objects=1
  bytes=824. The rejected frame = the subclass-equip increment at version 4; the client
  held v1 and expected v2. The boot offset ≈ 93.5 s (server-t 193297 ≡ client-t 99281).
- THE MECHANISM (code-verified this session): stage_subclass_selection
  (queuez_character_staging.cpp:189) and stage_ability_change (:158) both set
  after.family4Version = before + 1 — but their outcome branches
  (queuez_outcome_staging.cpp:129-233) publish only the family-0 banner refresh. No
  family-4 frame goes out, yet the server's mirror advances. The client requires exactly
  +1 per delivered frame — the community fork's stage_family4_refresh enforces it
  verbatim (family.version != before.family4Version + 1 → refuse,
  queuez_family_refresh_staging.cpp:16). THE ARITHMETIC: v1 + two 801 bumps + the
  equip's +1 = version 4 = exactly the rejected version.
- THE ROOT PORT GAP: the community fork's 801 branch DOES publish the family-4
  increment (push::append_subclass_selection_notification,
  queuez_select_character.cpp:170 — one upsert object at the staged version, the
  flags-0 family). OUR PORT DROPPED IT (the banner-only branch) — the bug = a port
  omission, not a design error. THE FIX = the equip branch's exact shape (the
  re-encoded record, flags 0, one object, at selection.after.family4Version) published
  BEFORE the banner refresh, mirroring the equip's ordering; the same for the
  ability-change branch if its mutate ever succeeds. Do NOT take the drop-the-bump
  alternative — that re-creates the stale-record rollback.
- THE 2100 = RETIRED (the ruling, verified): the community fork has NO 2100 handler —
  the opcode appears only in opcode_routes.cpp's kStatusOnlyOpcodes (the 5-bit status
  answer). The fork's swaps work without any 2100 semantics. Our apply_ability_change's
  mutate-fail early-out (the definitionHash == 0 / kNoPlugSource guard; the live log's
  three "result=fail step=mutate" lines = the scenario hashes correctly refused) = the
  correct shape — the scenario hashes will never be plug hashes. NO reverse-map work.
- THE ONE-BUG-TWO-MYSTERIES VERDICT: the in-place rollback = the client's family-4
  record never receives the 801s' updates, so the UI's optimistic apply reverts; the
  crash = the equip increment landing with skipped versions. The weasel (the nonce
  double-advance) = the same bug class on a different counter — both now fixed on the
  counter discipline.
- FRONT 2 (the dictionary) = AUDIT-CLEAN: the four spaces separated and labeled, the
  shift map verified (26 clean knots + the churn regime, 21,512/21,569 resolved), the
  record bridge proven on four independent sources (objectiveValues @+0xA438 keyed by
  the manifest objective index; the 25/25 map), the string join honest (72 HIGH / 50
  MEDIUM / 41 NONE — the order test refuted, the identity-join principle), the elite-4
  correctly isolated as the only bank-reachable client-inserted rows inside the
  veteran's range. THE PLAN CHANGE: the trigger = a flag byte in 47..3933 (the record
  space = definitively severed — the MoT cross); the ranked next test = flag[1682] +
  the elite-4, superseding the blind halving; the census's resolve hook = the better
  first move (one boot names the veteran if the recap plays). The DB = already the
  staged fold (the first 1,549 suppressed, the second half reverted).
- FRONT 3 (the census) = APPROVED with two corrections: (a) the spec's "existing 3" =
  STALE — the deployed gate_trace module installs SEVEN observers (char/rollback/
  acquire/poll/session/session_wrap/bit_prim; verified in gate_trace_observer.cpp's
  install()); the instruction = "extend the batch, touch nothing existing"; (b) the
  boot protocol's "the server = the current live stack" = SUPERSEDED — the census boot
  MUST ride the fixed server (the spec's step-6 swap attempt would kick the client
  mid-tour on the unfixed build). THE INTERACTION, answered: the census hooks and the
  801 port share the tree and the DLL — that's fine (the observers = exonerated for the
  crash), and the census boot and the fix-validation boot = THE SAME BOOT (the fix's
  swap window IS the census's most valuable window).
- THE CONSOLIDATED ORDERING (the kick-off, the user's go): (1) COMMIT THE TREE first
  (24 modified + the new opcode801/gate_trace/cache-check/equip-diff/socket-entry-
  buckets files; the build_out_*.txt logs = excluded noise) — a WIP commit is fine,
  the unreproducible-stack risk is real; (2) the family-4 fix + the harness assertion
  (ZERO boots: the replay subscription → 801×2 → 403 with the strictly-consecutive
  delivered family-4 versions + the deferred-repush-after-advance case); (3) the census
  hooks (the DLL, same worktree, /m:1); (4) BOOT A — one boot, three payloads: the
  fixed server + the census DLL + the already-staged DB fold (the swap = the money; the
  recap fold observation; the liveness census + the correspondence pairs; does NOT
  test the bucket loosening, any 2100 semantics, any new suppression). Then per
  outcome: the swap commits → apply_subclass_inventory.py + the verb harvest (the
  visible win); the veteran named → the dictionary completes the entry; neither → the
  flag1682+elite-4 boot, then the halving.
- THE DO-NOT LIST (binding): no 2100 reverse-map work; no swap attempts on the unfixed
  server (the kick mid-tour); no bucket-builder loosening before the swap commits (the
  fallback covers it); the 0.3.2 folds and the gate_trace observers = EXONERATED for
  the crash (the mechanism is complete without them; the observers are compiled into
  the deployed DLL by design).
- THE STATE PEN CORRECTIONS (applied this entry's siblings): STATE's known-gap 1 (the
  2100 reverse map) = RETIRED with the fork's status-only evidence; boot 16's "same
  behavior" = corrected to the session-killing crash; the census spec's existing-hooks
  staleness = noted (the spec file carries the amendment); the dictionary's headline +
  the JSON pointer = consolidated into STATE's flag-map section.

## 11.9 THE RULING'S ACTIONS 1-3 LAND: THE COMMIT, THE FAMILY-4 FIX + THE HARNESS GATE
##        (EXIT 0, 39/39), THE CENSUS HOOKS, THE BOOT-A DEPLOY (2026-08-18 ~11:40)

- ACTION 1 — THE COMMIT: the tree committed in two steps: 84acc9c (the WIP: the 801
  port + the observers + the harnesses — 24 modified + the new modules; the
  build_out_*.txt logs = gitignored, build_out.txt committed as-is) + 7ef8363 + d0d1c73
  (below). The deployed exe/DLL = now reproducible from the tree.
- ACTION 2 — THE FAMILY-4 FIX (commit 7ef8363): the shared prepare_character_republish
  (family4_character_update.cpp) mirrors prepare_subclass_equip exactly (the re-encoded
  character record, flags 0, one object, at the staged after.family4Version);
  append_subclass_selection_notification + append_ability_change_notification
  (queuez_subclass_selection.cpp) mirror the equip push; BOTH outcome branches
  (queuez_outcome_staging.cpp) publish the family-4 increment BEFORE the banner refresh
  with the advance_nonce between (the equip branch's exact ordering). THE REPUSH GUARD
  (queuez_deferred_push.cpp): the delayed family-4 re-push now disarms + skips when the
  mirror's version advanced past the initial (a version-zero re-snapshot would regress
  the client's held version — the crash's sibling). THE HARNESS
  (--selection-version-test, selection_version_test.cpp): the subscription replay →
  801×2 → 403 with the delivered family-4 versions asserted strictly consecutive, the
  frame shapes asserted (flags 0, one object, the version match), the full append path
  exercised, and the deferred-repush cases (the replay refused after the advance; the
  guard skips with no frame, no scratch, the mirror not regressed, disarmed). EXIT 0,
  39/39 ASSERTIONS, ZERO BOOTS. The two harness-env fixes on the way: the
  socketEntryBuckets domain JSON was missing from the harness env's content dir
  (copied from s1_accept), and the harness loads oo2core_3_win64.dll itself (the test
  blocks run before the boot's initialize_oodle; the prepare's compress_installed
  needs the module).
- ACTION 3 — THE CENSUS HOOKS (commit d0d1c73): five new gate_trace detours per the
  spec — resolve 0x50F4C0 (the vtable+0x6F8 hash→index resolver, the correspondence
  instrument), expr_eval 0x548E00 (the 7-arg evaluator, the completion marker at
  outEval+0x14dfc), stamp_consume 0xA42FD0, stamp_gate 0xE82AB0, ui_refresh 0xE80FC0
  (the thunk, raw regs) — expr_vm NOT attached (the ruling's default). THE SPEC'S 1.4
  OVERLAP RESOLVED: the MARKER_POLL target (0xC8F1C0) = ALREADY deployed as the
  bit_prim observer (same function, same ABI, same per-index surface); a second
  detour would nest a Detours chain on a hot primitive and violate touch-nothing-
  existing, so the bit_prim line = renamed to stage=marker_poll in the parser's
  schema shape (log-line-only; nothing else consumed bit_poll — verified by grep).
  The batch = 12 specs (7 existing + 5 new); the install line carries count=12. The
  DLL build = /m:1 clean (40 new functions); the DLL project needed the two new
  server-side push files added to its vcxproj (the DLL shares the queuez sources).
- THE BOOT-A DEPLOY: the server (6512) stopped; the fixed exe → s1_accept
  (backup .bak_bootA) + the census DLL → the dcv build (backup .bak_bootA); the
  s1_accept cache re-stamped to the new exe identity (ts 0x6A84A30C / size
  0x27AE000); incident.py run (INCIDENT_20260818_113931). THE ACCEPTANCE STACK
  (the fresh hashes): server exe 7177635F…, DLL 6F3C6D5C…, s1 cache 544AF66B…,
  s1 settings 238DAF75… (untouched), s1 db FD75C9DF… (the staged fold, untouched),
  dcv cache/settings = byte-exact (untouched). THE BOOT A BRIEF = the next entry's
  gate (the user's arbitration).

## 11.10 THE EQHASH DRIFT INCIDENT + THE REPAIR (2026-08-18 ~12:0x)

- THE FAILURE: the first BOOT-A server start (11:44) FAILED the content swap —
  every domain logged cache=0 (the runtime catalog = empty) → content_swap fail
  → the boot aborted. The --cache-check harness = PASSED (the checksum match, the
  identity values) — the reader's model was fine; the failure = the boot's
  identity GATE: cachedBuild {ts, size, eqHash} != expectedBuild
  {the live exe's ts/size + configured_hash(the persisted account)}.
- THE MECHANISM (the boot-16 WAL + the crash): the boot-16 session's 801s + the
  equip persisted to the DB THROUGH THE SQLITE WAL (the state.db mtime stayed
  01:02; the WAL carried the mutations). My Stop-Process -Force at the deploy
  left the WAL uncheckpointed but INTACT — the first boot's SQLite open replayed
  it (state.db mtime → 11:45). The DB = the post-boot-16 account (char 0's picks
  = {6,7,10,21,2} — the entry-9/entry-7 mutations landed). THE CACHE'S eqHash =
  0x9EF65EB9BD334F2C = the last restamp that LANDED mid-session (09:36, the
  second 801's); the equip's final restamp never landed (the crash interrupted
  the flow — the restamp failure = a warn, not a stop). THE DRIFT:
  configured_hash(post-boot-16 DB) = 0xE494E7605C06672C ≠ the cache's → the
  gate refused → empty domains. THE L5 open item 6 (the shutdown eqHash
  re-stamp hook) = EXACTLY the gap that bit us, confirmed live.
- THE REPAIR (repair_eqhash_bootA.py, read-only DB + one header write): the
  exact configured_hash port (the armor-repick lineage) over the live DB →
  0xE494E7605C06672C; the cache header @20 re-stamped (the ts/size = already
  the exe's). The checksum = untouched (it covers the constants + the payload,
  never the identity fields — re-verified by the reader: match=yes). THE
  SECOND BOOT = CLEAN: all six domains match (49/14/9/486/88 + the
  socketEntryBuckets 14-row JSON), the bootstrap token, the listeners (30975/
  3074/3075/443), the oodle, the cache ok, initialize ok (pid 29096). THE
  ACCEPTANCE STACK (re-hashed): exe 7177635F…, DLL 6F3C6D5C…, s1 cache
  BA004019…, s1 settings 238DAF75…, s1 db D9C6018F… (the WAL now checkpointed —
  the post-boot-16 account = durable), dcv cache/settings = byte-exact.
- THE LESSON (recorded for every future deploy): after ANY session whose
  mutations persisted (the 801s, the equips, any equipment change), the cache's
  eqHash = potentially stale vs the DB — the repair script's DRY RUN (the
  read-only hash compare) = part of the boot-cycle gate from now on, right
  after incident.py. The proper fix = the L5 shutdown re-stamp hook (parked,
  not this lane).

## 11.11 THE BOOT-A HARVEST: THE VERSION DISCIPLINE HOLDS, THE CENSUS WINS, THE
##        DISPLAY'S SOURCE = NAMED (2026-08-18 ~12:13)

- THE USER'S VERDICT: "the same behavior as before... nothing's different" — the
  UI's ability nodes still cue-then-revert. THE MONEY = NEGATIVE at the UI level,
  but the harvest = the biggest single-boot payload yet:
- THE VERSION DISCIPLINE = VALIDATED LIVE: all four 801s (t=236672 entry=9,
  t=238703 entry=11, t=240719 entry=15, t=244265 entry=5) delivered the family-4
  character after-image (subclass_select family=4 objects=1 bytes=824 each)
  + the equip at t=246656 (family=4 v5) — and the client ACCEPTED every frame
  (no out-of-order, no kick, no suicide — the boot-16 crash = gone). The fix's
  core claim = proven on the wire.
- THE CENSUS (the parse gates EXACT: char_test 8015 / flag_test 13874 = the
  boot-7 shape; the installs ok; 22,575 events; zero anomalies):
  (1) THE CORRESPONDENCE = CONFIRMED LIVE: 0x428899AE → 583 + 0x0FCFFEF9 → 581
  (the platform pair, exactly the predicted registry indices) — the bank↔
  manifest correspondence question = ANSWERED (the vt+0x6F8 resolver's output =
  the runtime investment registry index; the dictionary's shift map applies).
  (2) THE DICTIONARY'S SHIFT MAP = VALIDATED on 4 more hashes live: the
  swap-time cluster's inputs returned exactly the dictionary-predicted registry
  indices (0xCF6479B3→9680, 0xCC3FCAC3→9681, 0x8A2B0E56→11552, 0x280C0F22→
  12825 = manifest 9675/9676/11547 + a beyond-bank registry row). All unnamed.
  (3) THE SWAP'S GATE CANDIDATES = NAMED: at the third verb (t=60593, the
  0xAB28899E emit) the client evaluated FOUR requirement expressions in one
  shot (the swap's gate set) + 2 resolver misses (0x621B5FE9, 0x6430B910).
  (4) THE STAMP CHAIN = LIVE: two fires (t=24937 the boot's, t=85328 ≈ 1.5 s
  AFTER the equip — the content stamp @0xB74C now triggers the marker banks +
  the UI refresh on the equipment change). (5) THE ROLLBACK EXECUTOR = fired
  ONCE, at t=33453 (the early boot) — NOT in the swap window: the UI's revert
  = NOT via FUN_140e078f0; the queuez-side apply = accepted. (6) The periodic
  requirement session (caller 0x555649, ~15-s cadence) = the tour's steady
  evaluations (12243-12246, 13479-13481, 15065, 5622 + 2 misses).
- THE DISPLAY'S SOURCE = NAMED (the revert's mechanism, code-traced): the
  ability picks reach the wire ONLY through the subclass item's SOCKET STATE —
  resolve_socket_states (subclass_socket_selection.cpp) → the instance record's
  36-slot socketEntryStates @+247 (instance/layout.h) → the ITEM INSTANCE
  record. The item records = published ONLY in the boot-time full snapshot;
  the 801 increments deliver the CHARACTER record (per the ruling's shape) —
  the client's item record keeps the boot-time sockets → the node display =
  the old picks → the cue-then-revert. THE FORK'S SHAPE = the proof: the
  community fork's append_subclass_selection_notification publishes the
  SUBCLASS ITEM UPSERT (one object, the item's instance record with the
  mutated sockets) — our port diverged (the picks live on our character model,
  so the port published the character). THE CONTRACT-FIRST RULING: the fork =
  the proven reference; the divergence = a bug until proven otherwise. THE
  NEXT FIX = add the subclass item upsert to the 801's family-4 frame (the
  fork's exact shape, encoded via our instance encoder with the post-selection
  socket states) — the harness extends to assert the item object, then the
  boot retests. (The 2100 ability-change branch = the same gap, but its mutate
  = currently the correct fail — no live impact.)
- THE OPEN: the recap observation (did the montage play? — the user's report
  covered the swap; the resolve stream shows NO in-range (47..3933)
  resolutions, so the recap's D1-family evaluation did not resolve through
  0x6F8 this boot — the fold direction = pending the user's answer); the
  two swap-time resolver misses = unclassified (not in the registry — the
  record-space or the package-only family).

## 11.12 THE USER'S BOOT-A OBSERVATIONS + THE ESCALATION PREP (2026-08-18 ~12:22)

- THE USER'S FULL REPORT (post-tour, the escalation's subject): (1) THE SHIP
  DISAPPEARED AGAIN — the previous boot (boot 16) did NOT have this bug; (2) THE
  TOWER LOAD = REQUIRED AN ABILITY-SWAP ATTEMPT — the user "had to try an
  ability swap for me to actually load into the tower" — ALSO new vs the
  previous boot ("they must be related" — the user's own read); (3) the
  ability swap + the subclass swap = no difference (the cue-then-revert
  unchanged). THE RECAP = unobserved/not reported (the fold direction = still
  open).
- THE CHANGED VARIABLES vs the previous boot (the candidate surfaces, for the
  reviewer): (a) the server exe = the family-4 fix + the deferred-repush guard
  (server-side only); (b) the DLL = the census build (5 new log-only detours +
  the bit_prim→marker_poll line rename); (c) the cache = the identity
  re-stamps (ts/size + the eqHash repair; the payload = byte-identical); (d)
  THE DB = now the POST-BOOT-16 state (the boot-16 WAL carried the session's
  persisted mutations — the entry-9/11/15/5 picks + the equip — into the
  durable DB at this boot's first open; the previous boots ran the pre-mutation
  state). THE SHIP = the appearance-family issue (the intermittent history: the
  boots 1/2 invisible → the boot 3 back after the 0x6D6D marker sweep; the
  current encoder = marker-free since 40bf3f4). THE TOWER-GATING = possibly the
  activity-host handoff stall (the user's own community-doc entry: "the client
  waits for a missing peer-side Activity Host handoff") — unverified.
- THE DIVERGENCES FROM THE REVIEWER'S GAME PLAN (the disclosure, for the
  escalation package): (1) THE DEFERRED-REPUSH GUARD — the ruling's action 2
  asked for the harness ASSERTION on the repush case; the code read showed the
  repush path WOULD send a version-zero re-snapshot after the advance (the
  crash's sibling), so I added a guard (skip + disarm once the mirror's version
  advanced) — a behavior change beyond the ruling's harness. (2) THE CENSUS =
  FIVE new detours + a rename, not six — the spec's 1.4 MARKER_POLL (0xC8F1C0)
  = already deployed as the bit_prim observer; a second attach = a nested
  Detours chain on a hot primitive; the bit_prim line = renamed to the parser's
  marker_poll shape (the hook untouched). The batch = 12, not 13. (3) THE EQHASH
  DRIFT + REPAIR (unplanned, FINDINGS 11.10) — the first BOOT-A server start
  failed the identity gate (the boot-16 WAL mutations vs the cache's stale
  eqHash); the repair = the read-only DB hash + one header write; the actual
  BOOT A = the second start. (4) THE HARNESS-ENV FIXES — the socketEntryBuckets
  JSON copied into the harness env + the harness self-loads oo2core (the test
  blocks run before the boot's initialize_oodle). NOT DIVERGED: no DB changes,
  no 2100 work, no bucket loosening, expr_vm not attached, the dcv cache +
  settings = byte-exact.
- THE ESCALATION: the user = escalating before the next work. The package =
  "STATE + incident <ts>" (the fresh incident = generated). THE OPEN QUESTION
  FOR THE REVIEWER (the user's ask): the ship + the tower-gating regressions'
  attribution across the four changed variables.

## 11.13 THE ESCALATION RULING + THE ACTION RESULTS + THE STREAM ALIGNMENT
##        (2026-08-18 ~12:58)

- THE RULING (the package "STATE + incident 20260818_122423"): EXONERATED by
  construction: (b) the census DLL — the log-only detours, the
  run-original-first, the zero census anomalies, the parse gates = the boot-7
  shape exactly; (c) the re-stamps — the payload byte-identical, the identity
  gate passed all six domains at boot, inert afterward.
- REGRESSION 1 (the ship) = (d) THE DURABLE DB, the prime suspect: boot 16's
  session persisted an actual subclass equip — the DB now holds a different
  loadout than every prior clean-ship boot (all ran the pre-equip state; the
  WAL made it durable only at boot A's open). The ship's history = the
  appearance-family; the loadout = the appearance source. THE ZERO-BOOT
  DISCRIMINATOR: rebuild the roster/banner record offline from the
  pre-boot-16 DB via the harness + diff against the current — if the
  ship-relevant appearance rows moved, (d) = confirmed by mechanism.
- REGRESSION 2 (the tower) = (a), REFRAMED: probably the fix working. The
  client log names the stall: activity_session_creation — "Waiting for
  session peer properties to be in the expected state (no AH ids)" +
  "unable to get session keys" (the posse/fireteam sessions
  host-established). The tower was NOT gated by the swap — it was RELEASED
  by the refresh the swap triggers. The bug = the tower join doesn't
  self-produce that refresh. The server's "gameplay stage=advertise
  result=skip reason=not_ready region=48/56" = the matchmaking-seed /
  session-layer work item. The regression + the S3 convergence plan + the
  community's blocker 1 = THE SAME KNOT.
- ACTION 2 (the repush, executed this session): TWO companion/repush pairs —
  the second at t=1052140/1052594 = a RE-SUBSCRIPTION's full snapshot (56
  objects = the post-equip count; recorded=1 = the version-0 stage on a
  fresh mirror). The repush frame = the version-0 full snapshot on the
  fresh-mirror connection = the guard's DESIGNED version-0 arm (the skip =
  only for the advanced mirror). No kick followed. THE GUARD =
  EXONERATED; documented; move on.
- ACTION 3 (the tower pin, executed this session): the world change to
  city_tower_social_d2 = client-t=55109. The 5 s before: the posse session
  keys arriving (t=49062, after the t=49031 failure) + the AH join to OUR
  server's activity host (t=49219, AH 9EAA3001:00200002) + the two swap 2100s
  (t=49234/50250 — both correctly refused by the mutate-fail, ZERO frames) +
  the bubbleauth force (t=50234) → the orbit_outro (4.8 s) → the world
  change. THE REFINEMENT TO THE RULING'S MECHANISM: the release = the session
  layer's own completion (the posse keys + the AH join + the bubble grant) —
  NOT the swap's traffic (the 801s' family-4 increments at t≈73872+ and the
  stamp-chain fire at t=85328 came AFTER the release; the 2100s = no frames
  at all). The user's causal read = the timing coincidence of two swap
  attempts during the stall's tail. THE RULING'S DEEPER CLAIM = UNSHAKEN:
  the stall = the session layer (the AH-id wait) = the S3 knot; the join not
  self-producing its refresh = the fix-forward.
- THE STREAM ALIGNMENT (the user's question, answered): ALIGNED EXACTLY on
  the next fix — the ruling's action 4 (the fork-shaped item upsert) = my
  full-stream stage-4 frame divergence (two independent passes, the same
  conclusion; the fork publishes the subclass ITEM's record, we published the
  character's); ALIGNED on the S3 knot (my stage-6 context = the same
  neighborhood); REFINED (not conflicted) on the tower's release mechanism
  (above); SETTLED by the analysis on the repush (the version-0 arm);
  COMPLEMENTARY on the ship (outside the swap stream the analysis covered).
- THE DO-NOT (binding): no revert of the guard or the fix (both regressions
  point forward — the seed + the refresh-on-join, not back); no chase of the
  suicide line (it's the quit path).
- THE QUEUED WORK (the bounded actions, after this recording): (1) the ship
  discriminator (zero boots — the harness rebuild + diff); (2) THE ITEM
  UPSERT (the ruling's action 4 — the swap's named next fix: the 801's
  family-4 frame carries the subclass item's instance record, the fork's
  exact shape, via the existing append_items + the loadout resolve) + the
  harness extension; (3) one boot when ready (the upsert retest + the ship
  test).

## 11.14 THE ITEM UPSERT LANDS + THE SHIP DISCRIMINATOR'S ANSWER + THE BOOT-B
##        DEPLOY (2026-08-18 ~13:1x)

- THE ITEM UPSERT (commit 6692c07): prepare_item_republish replaces the
  character shape — the 801's family-4 frame now carries the subclass ITEM's
  instance record (one object, keyed {itemInstanceDefinitionId,
  subclassInstanceSoid}, the sockets re-resolved from the committed mutation
  via the existing append_items + the loadout resolve). The ability-change
  branch shares the shape via the equipped subclass slot. THE HARNESS: two
  new asserts (the frame's object = the item — the id/version keying). EXIT
  0, 41/41 (the item asserts included). Both projects build clean /m:1.
- THE SHIP DISCRIMINATOR (the ruling's action 1, executed zero-boot): a
  throwaway harness env with the CURRENT DB (the WAL included) + the cache
  eqHash re-stamped → the --s1-test records → the diff vs the harness env's
  pre-boot-16 artifacts. THE CHARACTER RECORD = 11 differing bytes, ALL in
  the known equip delta (the two inventory rows @0xE00-0xE40, the subclass
  slot @0x2CC0 (0x0C→0x32), the summary indices @0xDF8/0x2D60/0x2E00, the
  stamp @0xB74C/D) — ZERO bytes in the head/appearance region (0→0xDF8),
  ZERO in the flags, ZERO in the vehicle slot. THE SHIP'S ROWS DID NOT MOVE
  → the ruling's (d) mechanism = NOT confirmed at the character-record level
  (the remaining surface = the roster/banner encodes; the ship's intermittent
  appearance-family history = the alternative). THE ACCOUNT RECORD = 12,113
  differing bytes = the fold's flag-bank bytes (@0x742C+, the expected
  pre-fold-vs-fold confound) + one summary byte. THE BONUS FINDING: the
  character record carries NO ability picks at all (the picks changed
  {6,8,10,21,2}→{4,8,20,21,2} with zero corresponding record bytes) — the
  node display's source CAN ONLY be the item's sockets, which is exactly
  what the upsert now publishes. The fix = not just the fork's shape; it's
  the only record that can drive the display.
- THE BOOT-B DEPLOY: the server stopped; the item-upsert exe → s1_accept
  (.bak_bootB) + the rebuilt DLL → the dcv build (.bak_bootB); THE EQHASH
  DRIFT re-appeared (the BOOT-A session's own persist-restamps lagged the
  final state — the known flakiness family) → the repair re-stamped (ts
  0x6A84BB65 / the live DB's hash 0xBC6A3C3C97BDC06C); incident.py run
  (INCIDENT_20260818_131523). THE BOOT = CLEAN (pid 35556): all six domains
  match (49/14/9/486/88 + the 14-row socketEntryBuckets), the cache ok, the
  listeners up. THE DB = untouched (the post-BOOT-A state + the staged fold).
  THE BOOT B BRIEF = the next gate (the user's arbitration).

## 11.15 THE BOOT-B HARVEST: THE ABILITY SWAP COMMITS — THE SWAP FRONT CLOSES
##        (2026-08-18 ~13:25)

- THE MONEY = LANDED (the user): "ABILITY SWAP WORKS!!!!!!" — the item upsert
  = the final piece. THE SERVER LOG (the confirmation): four 801s delivered
  the ITEM frames (subclass_select family=4 objects=1 bytes=189/190/188/186 —
  the item records, vs the old 824-B character shape) → the equip (824 B).
  All accepted, no kick, no suicide. THE CENSUS (the parse gates EXACT:
  8015/13874, the installs): the working swap visibly drives the client —
  stamp_consume 87 fires (vs 2 in BOOT A), marker_poll 650 (vs 258), resolve
  645 (vs 375), sync_header 10 (vs 5); the correspondence re-confirmed
  (0x428899AE→583, 0x0FCFFEF9→581, 46 fires each); rollback_apply = 1 (the
  boot-time fire only — the revert executor never touched the swap).
- THE USER'S FULL REPORT: (1) THE ABILITY SWAP = WORKS. (2) NO RECAP MONTAGE
  ("haven't seen that for a while") → THE VETERAN FOLD DIRECTION = SETTLED:
  the recap GONE with the kept half (the first 1,549 of the gap-order
  47..3933) suppressed → THE VETERAN ∈ THE KEPT HALF (the 0=suppress
  semantics, the boot-6 precedent). The ranked next test = flag[1682] + the
  elite-4 (the ruling's Front 2), then the halving of the kept half. (3) THE
  SHIP = BACK AND RENDERING — the ship regression = closed (the intermittent
  appearance-family flake; the discriminator's record-level verdict (d)
  unconfirmed = consistent). (4) THE SUBCLASS EQUIP's display refresh =
  still needs the inventory exit/re-entry + still triggers the flag pop-ups
  (the indices 1..13 talk-backs) — THE REMAINING SWAP-FRONT UX GAP: the
  403's family-4 frame = the character record only; the equip's pick-reset
  changes the same item sockets the 801 now republishes → THE NEXT
  CANDIDATE = the equip's item republish (the same shape; the fork's equip =
  a generalized item-state update). (5) THE TOWER = loaded right in (no
  stall, no swap needed) — consistent with the session-layer flake verdict.
- THE NEXT STEPS (the user's arbitration): (a) apply_subclass_inventory.py —
  the post-commit script = WRITTEN FOR THE AUTHORED STATE; the current DB's
  equipped rows = drifted (the user's live picks) → the script's
  verify_plan = would REFUSE. The options: update the script's expected
  picks to the current equipped rows + run, or run after the next equip
  cycle. THE RUN = after the server stops (the DB lock) + the eqHash
  re-check (the script adds the non-equipped items → the configured hash =
  UNCHANGED (the hash covers the EQUIPPED slots only) — verify). (b) The
  equip's item republish (the remaining UX gap). (c) The veteran hunt:
  flag[1682] + the elite-4 (the fold direction = now settled).

## 11.16 THE BOOT-C/D/E SAGA: THE EQUIP'S DISPLAY = STILL BROKEN, THE POP-UPS =
##        NOT THE 1..13 — THE ESCALATION PREP (2026-08-18 ~14:4x)

- BOOT C (the equip's item republish, commit 4bf7632): the 403's frame = the
  character + the item (2 objects, the harness 42/42). THE USER: the same
  outcome — the pop-ups + the exit/re-entry + "the ui icons for the
  subclasses disappear" + "I can only do one subclass tree swap at a time, I
  can't swap back and forth" (the other picks — the grenades/jump/class —
  work fine).
- BOOT D (the in-place family-0 refresh, commit 9f52ba5): the banner refresh =
  the record-only upsert (flags 0, no release — the fork's shape, the
  release-and-re-add = the "tears down the ship/banner binding" pattern the
  fork's own comment names). THE USER: "i was able to swap the subclass trees
  maybe twice before it stopped responding. as for the ui changes, no
  difference." THE LOGS: the frames = delivered + accepted (the banner =
  family=0 objects=1, the equip = family=4 objects=2, no client-side
  out-of-order, the connection = alive throughout) — the stall = the
  CLIENT's UI stopped sending (the selector's death), not the wire.
- BOOT E (the flags revert — the head block 1..13 → 2, the boot-E prep): the
  hypothesis = the pop-ups + the selector's death = the 1..13 suppression
  (the boot-3/4 inference: the Crucible/Bray pop-ups = the head-block zeros;
  the bisection = long past the head, the suppression = overdue). THE USER:
  "everything remained the same" — THE HYPOTHESIS = REFUTED: the pop-ups +
  the icons + the re-entry = UNCHANGED with the 1..13 = 2 (the DB-verified
  revert: 13 rows → 2, the kept fold's 1,549 zeros = untouched). THE
  CORRECTED SUSPECTS: (a) the pop-ups = the KEPT FOLD's 1,549 zeros (the
  active bisection — the swap-time requirement evaluations hitting the
  suppressed indices; the boot-5-era fold = present in every pop-up boot);
  (b) the selector's icons + the re-entry = the family-3 ROSTER refresh (the
  fork's THIRD equip publication — "family three owns the orbit roster and a
  separate copy of the same appearance record" — our equip publishes the
  family-4 pair + the family-0 in-place, but NOT the roster); (c) the
  pop-ups = a non-bank surface (the 2100 status, the family5, the
  entitlements) — unproven.
- THE ESCALATION: the user = escalating. The package = "STATE + incident
  <ts>". THE QUESTION FOR THE REVIEWER: the pop-ups' + the selector's source
  across (a) the fold zeros, (b) the missing roster refresh, (c) the
  un-mapped surfaces — with the boots C/D/E's empirical matrix (the frames =
  accepted; the behavior = unchanged across three server-side changes).

## 11.17 THE LAYER-1 LIVE CONSOLE LANDS (2026-08-18 ~14:59)

- THE DIRECTIVE (the user's, while the escalation runs): amend the
  outward-facing live tools per the reviewer's game plan — the Layer 1 build
  first (the merged live tail + the trap rules), preserving the
  recording-version tools as the fallback.
- THE BUILD: RE_scripts/live_console.py (the single Python console, ~300
  lines) + the live_console.ps1 launcher. THE FEATURES, per the reviewer's
  spec: (1) THE MERGED TAIL — both logs polled (the seek-based follower with
  the rotation handling), parsed (the ev=/stage= grammar), interleaved in the
  real-time arrival order (the natural alignment of the two clocks); (2) THE
  LADDER PANEL — the family-4 / family-0 version mirrors TRACKED from the
  frame lines (each delivered family-4 increment = +1, the snapshot/
  companion/repush replays = the v0; the banner_refresh/move = the family-0
  +1) + the pushed-frame count (the nonce's visible proxy) — the three
  counters the weasel/porpoise/out-of-order crashes all formed in, now
  visible as they move; (3) THE BOOTFLOW STRIP — the client's
  world-controller states on a 40-cell bar (the state names compressed to
  the codes: ASC/MM/AH/ORBOUT/XIT/SLICE/PHYS/WRLD/KICK + the TOWER/ORBIT
  world changes — "stuck" = the bar stops moving); (4) THE TRAP
  TRANSLATIONS — the nine-rule signature set from the FINDINGS corpus (the
  out-of-order → "FAMILY-4 VERSION REJECTED", the kick, the suicide (the
  kick-precursor-aware gray), the cache=0 → "IDENTITY GATE — run the eqHash
  dry-run", the not_ready → "the seed work item", the 2100's mutate-fail →
  "correctly refused (expected)", the install count → "the census armed",
  the fail/skip fallbacks) — each firing = a red/yellow banner in the stream
  + the last trap pinned in the panel; (5) THE CENSUS FEED — the resolve
  pairs (the hash→idx, deduped into the pair table), the marker polls, the
  stamp fires, the 2100 verbs, the expr evals, the rollback applies — the
  live counters in the panel. ZERO server/client changes, zero boots; the
  smoke = the py_compile clean + the live loop run.
- THE FALLBACK: live_server_tail.ps1.v1 + live_client_tail.ps1.v1 = the
  recording-version tails (the one-window-per-log views the Discord session
  used) — kept byte-exact.
- THE QUEUED LAYERS (the reviewer's build order, not started — the
  escalation's lane-hold stands): Layer 2 (the server read/write verbs + the
  journal — GET /state, /ladder, /flags, /journal + POST /suppress, /restore,
  /restamp, /repush with the enforced bookkeeping), Layer 3 (the RPM
  client-mirror watcher — the held family-4 version polled at 1-2 Hz next to
  the server's), Layer 4 (the boot-report.py — the quit-time acceptance
  report + the FINDINGS skeleton).

## 11.18 THE LAYER-2 ADMIN SURFACE LANDS + THE VERBS VERIFIED LIVE (2026-08-18 ~15:3x)

- THE BUILD (commits 8794cf8 + 24f64af): the loopback admin HTTP listener
  (127.0.0.1:8099, the Winsock TCP + the select-loop thread, the discovery
  listener's pattern) with the read verbs + the write verbs, every write
  journaled. THE READS: GET /state (the per-session queuez mirrors via the
  new bap::ladder_snapshot), GET /ladder (the family-4/family-0 versions —
  the Layer-1 panel's ground truth), GET /flags?scope= (the bank summary +
  the zero runs), GET /journal. THE WRITES: POST /suppress + /restore (one
  flag range → the state::unlocks::mutate_flags (the live table) + the
  persistence::update_flag_range (the DB) + a journal entry), POST /restamp
  (the eqHash repair as a verb: the configured hash + the cache restamp +
  the journal — the identity-pair bookkeeping enforced by code), POST
  /repush (the bap::arm_deferred_repush — the manual tower-release test).
- THE VERIFICATION (live, no boot): the boot logged ev=admin stage=listen
  result=ok port=8099; GET /flags = the exact bisection state (12,300
  total, 1,549 zeros, 10,751 twos, the runs all ∈ 47..3933, the head block
  clean); POST /restamp = {"ok":true,"eqHash":"0x42796371CDD0062B"}; POST
  /repush = {"ok":true,"armed":0} (no sessions); GET /journal = both entries
  timestamped. THE ONE BUG + THE FIX: the journal path doubled the Sunrise
  suffix (the artifact directory already carries it) → the entries silently
  dropped → the append corrected + the entries verified.
- THE FALLBACK: the .v1 tails + the Layer-1 console = untouched; the admin
  surface = the separate port (the layers test independently).
- THE RULING'S ACTIONS (the user's escalation, landed): action 1 = the
  zero-boot popup grep (next); action 2 = the complete-fork-shape equip (the
  family-3 roster refresh + the delayed ability-refresh arm); action 3 =
  BOOT F. THE DO-NOT: no reverts, the fold's 1,549 stays, no popup
  suppression boots, no new surfaces, and the boots C/D/E = partial states
  of the fork's atomic transaction, not refutations (the record carries this
  correction).

## 11.19 THE RULING'S ACTION 1 LANDS + THE ACTION-2 PORT BEGINS (2026-08-18 ~15:4x)

- THE RULING DIGEST (the user's escalation, the package STATE + incident
  20260818_145049): the audit = clean (the boots C/D/E = one-variable-each
  discipline upheld). THE REFRAME: the fork's equip = an ATOMIC transaction
  of three publications (the character, the family-0 appearance, the
  family-3 roster) + the deferred ability-rebuild arm; the boots C/D/E each
  moved ONE member of the set — three null results on partial states of an
  atomic shape = the expected outcome, not evidence against it. THE TWO
  UNTESTED DIVERGENCES: (1) the family-3 roster refresh (never ported) —
  the symptom match for the disappearing icons + the re-entry; (2) the
  delayed ability-refresh arm (never ported; ours rebuilds inline) — the
  symptom match for "~2 swaps then the selector dies" (the race the fork's
  deferral exists to avoid). THE POP-UPS: the 1..13 attribution = dead twice
  over; the dictionary's own §3 warning predicted it — the popup's home =
  the ACQUIRED-CONTENT id space (the 127-record diff → FUN_140e07510), and
  the boots 1/2-vs-3 correlation = confounded (no mid-session family-4
  republish existed pre-boot-3). THE RANKING: (i) the acquired/record-state
  diff firing on the equip's republish (the acquire_check hook = the
  observer); (ii) the fold's 1,549 zeros; (c) the unmapped surfaces = last.
- ACTION 1 (the zero-boot grep, executed): the boot-D + boot-E client logs
  (the .old + the current — the boot-C's = rotated away) — acquire_check =
  ZERO in both (no record-state diff fired, in-window or anywhere) → THE
  MECHANISM (i) = NOT CONFIRMED; (ii) = primary per the ruling. THE
  POPUP-TIME EVALUATIONS: the boot-D's equip window (t=96,000-108,000) =
  only the steady periodic resolve set (the ~15-s requirement session's
  regular hashes, the caller 0x555649) — no popup-specific burst; the
  boot-E's window = empty. THE STAMP CHAIN = one swap-era fire per boot (the
  gate + the refresh + the consume) + the steady idle consumes (~1.06-s
  cadence, result=0) — the deployed machinery = working as designed.
- ACTION 2 (the complete-fork-shape equip, in progress): the family-3
  roster refresh (the fork's third publication, the exact position +
  ordering) + the delayed ability-refresh arm (defer the bucket
  re-derivation off the inline path) + the harness asserts (the three-frame
  transaction + the arm). One build, /m:1.

## 11.20 ACTION 2 LANDS: THE COMPLETE-FORK-SHAPE EQUIP + THE BOOT-F DEPLOY
##        (2026-08-18 ~16:1x)

- THE PORT (commit c2637b3): the SessionState's family-3 ladder (the
  root/version/active — the fork's fields), the subscription tracking (the
  fork's logic verbatim), stage_roster_appearance_refresh (the +1),
  prepare_roster_appearance_refresh (the encode_family3 record + the
  optional encode_roster — the fork's verbatim), append_roster_refresh_
  notification, and the equip branch's third publication in the fork's
  exact position (after the family-0 refresh, before the publication). THE
  DELAYED ABILITY-REFRESH ARM: the StagedPublication's armsAbilityRefresh →
  the Session's abilityRefreshArmed/DueTick (the 500-ms settle window, the
  fork's constant) → consume_ability_refresh (the delayed re-send of the
  appearance + the roster pair — the fork's deferral: the inline rebuild
  races the client's refresh; the ~2-swap selector death = the race the
  deferral exists to avoid). THE HARNESS: the family-0 + the family-3
  subscriptions staged into the chain, then the three-frame transaction
  asserts (the family-4 pair + the family-0 in-place + the family-3 roster,
  each at exactly +1, the correct frame shapes). EXIT 0, 49/49. THE HARNESS
  ENV = refreshed to the 486-row era (the stale cache/content blocked the
  family-3 encode — the env's cache + the content JSONs = the deployed
  copies, the identity + the eqHash re-stamped).
- THE BOOT-F DEPLOY: the exe + the DLL deployed (.bak_bootF), the cache
  re-stamped (ts 0x6A84E50E / eqHash 0x42796371CDD0062B), incident.py
  (INCIDENT_20260818_160954). THE BOOT = CLEAN (pid 30540): all six domains
  match (49/14/9/486/88 + the 14-row socketEntryBuckets), the admin port
  live (ev=admin stage=listen result=ok port=8099), the cache ok. THE DB =
  untouched (the head-block-reverted + the kept fold). BOOT F = the ruling's
  money boot — the first boot where the equip actually matches the
  reference implementation's full shape.

## 11.21 BOOT F: THE COMPLETE TRANSACTION DELIVERED, THE BEHAVIOR = UNCHANGED —
##        THE RULING'S CONTINGENCY (2026-08-18 ~19:09)

- THE WIRE = THE FORK'S FULL SHAPE (the server log, verified): TWO equips
  (the user's swap + the swap-back) each delivered the complete atomic
  transaction — the family-4 pair (objects=2), the family-0 in-place
  (objects=1), the family-3 equip_roster (objects=2, 1155/1176 B), then the
  DELAYED PAIR ~547 ms later (the banner + the equip_roster + the
  ability_refresh report). The arm fired exactly as designed. The client
  accepted every frame (no out-of-order, no rejections).
- THE USER: "the behavior is the same as previous, banner flags still got
  triggered, ui still glitches, have to leave inventory and go back to see
  the changes." The pop-ups + the icons + the re-entry = UNCHANGED with the
  complete fork shape on the wire. THE CENSUS (the parse gates pass; the
  flag_test = 13,861 with pass 10,763 = the +13 over boot A = the head-block
  revert visible in the walk): the stamp_consume = 17 (the idle zeros + the
  boot's single fire — no swap-era stamp burst this boot), the acquire_check
  = ZERO again (the record-state diff never fires), the expr_eval = 21, the
  resolve = 422 (the steady periodic set).
- THE RULING'S CONTINGENCY (the action 4, now in force): "if popups persist
  past (i) ruled in and fixed: the fold-zero test folds into the veteran
  hunt's next boot anyway (flag1682 + elite-4 + the halving) — no dedicated
  popup boots." THE NEXT BOOT = the veteran hunt's ranked test: flag[1682]
  (the record-bridge candidate) + the elite-4 (the bank 340/341/951/952) —
  the same DB-flag surface as the popup suspect (ii) (the fold's zeros), so
  one boot tests both fronts. The direction = still settled (the veteran ∈
  the kept half).

## 11.22 BOOT H: BOTH FIXES VERIFIED LIVE, THE SHIP RETURNS, THE EQUIP'S
##        DISPLAY REMAINS — THE ESCALATION PREP (2026-08-18 ~22:32)

- FIX A (the family-3 ladder survives the family-4 staging, commit 3a8dd9b) =
  VERIFIED LIVE: the boot-H subscribed in the boot-G's exact order (the
  family-3 first, then the companion) and the equip delivered the complete
  trio — the family-4 pair + the banner + the equip_roster result=ok (the
  family=3 objects=2) + the delayed pair 516 ms later. The refusal is dead;
  the reversed-order harness gate = EXIT 0.
- FIX B (the join's own refresh, the same commit) = VERIFIED LIVE, with the
  cleanest evidence yet: the boot-G's slice-set stall (the in_world at
  t=90234, the normal_z_leg at t=143265 — 52.7 s later) vs the boot-H (the
  in_world AND the normal_z_leg at the SAME instant, t=88078). The join's
  repush woke the evaluator and the playable region streamed immediately.
  The tower loads on its own — no swap required.
- THE SHIP = BACK — with the five candidate flags STILL suppressed. The
  boot-G flag-trip attribution = WEAKENED: the ship's breakage may have been
  the same intermittent render flake as the boot-A's, and the return's
  plausible mechanism = the Fix-B join refresh (the family-4 re-delivery at
  the render's moment). The narrow-down test = now lower priority; if the
  ship stays back with the five zeroed, the flag theory dies.
- THE SUBCLASS EQUIP = STILL THE SAME: the pop-ups still fire and the UI
  still needs the inventory exit/re-entry — despite the COMPLETE
  transaction delivering and the client accepting every frame (no
  rejections, the census clean). The wire = complete; the display's
  problem = definitively NOT the delivery — it is what the client's UI does
  (or fails to do) with the frames it accepts.
- THE ESCALATION: the user = escalating. The package = "STATE + incident
  <ts>". THE QUESTION FOR THE REVIEWER: the equip's display + the pop-ups —
  with the delivery proven complete and accepted, what surface remains? The
  standing candidates: (a) the fold's zeros (the ruling's suspect ii — the
  halving = the test); (b) a client-side apply surface the census hasn't
  hooked (the UI's re-entry re-reads something the frames don't touch); (c)
  a remaining fork-shape gap. The ruling's contingency stands: the fold-zero
  test folds into the veteran hunt's next boot (the halving).

## 11.8 THE COMMUNITY S3 ATLAS: THE WOW.MD DOCUMENT (2026-08-18 ~10:29)

- THE SOURCE: community_forks/Wow.md — a shared community doc by "George Ratington" (+ the
  contributors @isinternet + @T14D3), read end-to-end this session. THE FULL ELABORATION =
  THE FILE ITSELF (the tables, the RVA lists, the pipeline notes — everything below = the
  distillation; the file = the authority). It maps the ACTIVITY-SELECTION / MISSION
  MACHINERY on the SAME PINNED ARRIVALS EXE we target.
- WHAT THEY ACHIEVED (their frontier): the LAUNCH PIPELINE = cracked — the native CHOSEN
  282 (0x011A) → mission_towerfall 266 (0x010A) rewrite before the client commits the
  route, the real 0x1B0 authored publication descriptor (the 0x330 = 0x1B0+0x180 reading =
  corrected; +0xBE0 = the next record), the PUB48.48 slice precaching, the citizen/activity
  join, the Homecoming world LOADING. THEIR LIMIT (their own honest statement): no opening
  cinematic, no authored objectives, no dialogue, no AI, no encounters, no markers, no
  mission progression — "not a complete playable mission yet." THE WALL AFTER OURS, MAPPED
  BY SOMEONE WHO GOT THERE FIRST.
- THE VOCABULARY COMPLETIONS (zero boots, pure absorption — the immediate value):
  (1) THE BAP SERVICE MAP: 6→7 (the activity host manager, carries the activity
  selection), 8→9 (the activity-message request + the uncorrelated push), 16→17 (the
  activity host), 42→43 (matchmaking/advertisement), + 14/18/23/32/44/110/121/302/304/306.
  (2) THE ACTIVITY MESSAGE TYPES: 0 (entity-slot notification), 1 (the global
  activity-state selection push), 3/4 (join request/result), 5 (sensor/authority), 12
  (replicated membership), 16 (keepalive), 52 (patch epoch), 54 (bubble/activity-host
  state) — NOTE: OUR OWN TRAFFIC IS NOW NAMED: our server pushes types 1/5/12 (the
  incident-log activity pushes), + the recurring svc=8 rsp=none frames = the
  activity-message service. (3) THE AUTHORITY SLOT TYPES: 13 (participation), 17
  (lifetime), 18 (activity script), 35 (mission director) beyond our served
  config/package/queue/spawn set. (4) THE GAMEPLAY GROUP-MESSAGE IDs (11/12/13/15-18/26/
  29/30/31/34/36/38/39/40 — the PEER/GROUP channel, not BAP): the vocabulary for the
  gameplay/DTLS subsystem folded (dormant) into our integration — message 36 = decoded as
  PLAYER REMOVE (a solved bug we inherit).
- THE RVA ATLAS (the future S3 census targets, OUR exe): the selection pump 0x175E520
  (branches on the descriptor's +0x12 route byte: 0 = local mode 6, nonzero = authored
  mode 1), the local manager initializer 0x1772440 (mode 6), the authored initializer
  0x1773200 (mode 1), the authored receiver creator 0x17B8C50, the component dispatch
  0x1766A30, the publisher 0x17ADA60, the ingest boundary 0x175B8F0 (vtable +0xB8), +
  the negative results = AS VALUABLE AS THE POSITIVES: never force 0x1751F50 (the
  route-commit; "bad bad bad things happen"), 0x1757600 (the local route-lane
  constructor) NEVER FIRED in the failing path, + PUBLISHING THE AUTHORITY OBJECTS ALONE
  DID NOT PRODUCE AUTHORED MODE 1 — a saved month: that would have been our first S3
  instinct too.
- THE CONVERGENCE (our answers for their doc): (1) THEIR "0x811C9DC5 SENTINEL repeatedly
  observed in the default/generic descriptor" = THE FNV-1A 32-BIT OFFSET BASIS — the
  initialization constant of the engine's hash function (our anchor work watched the
  client clear the cached definition hash to EXACTLY this value, anchor-closeout-
  region-derivation.md phase 3): their sentinel = an UN-HASHED FNV DEFAULT, not a magic
  constant. A genuine giveback. (2) Their bootflow observations (object 282 advancing
  31→38, the cleanup states) = our 40-state bootflow map + the world_controller cleanup
  lines in our own incident logs — two independent observers, the same state machine.
  (3) Their content tags (mission_towerfall / CHOSEN / PUB48.48 / the scenario tags) =
  mechanically verifiable against our extracted corpus (1,106 activity defs + 61,508
  children) — the cross-verification validates both pipelines at once.
- HOW IT FITS (the ruling's integration): (1) the S2 carrier hunt's next probe set = NOW
  LABELED (the entity-slot family 0/20/21, type 54, the patch epoch 52); (2) the gameplay
  channel's message vocabulary = banked for when it lights up; (3) the S3 front = mapped
  but NOT OPENED (the swap remains one family-4 publish away; the census boot = the next
  boot, UNCHANGED); (4) their architecture (the embedded BAP server + the two-phase roster
  + the post-join seeding) = the independent convergence on our shape — the client's
  stall ("waits for a missing peer-side Activity Host handoff") = EXACTLY the thing our
  standalone server exists to provide.
- THE BOUNDED USES (queued, zero boots): (a) the vocabulary bank = a future
  claims/community-s3-vocabulary.md (the service/message/slot/group-ID tables + the RVA
  atlas, each entry labeled confirmed-vs-pending — their own pending list is unverified);
  (b) the cross-verification lane: their tags vs our extracted defs; (c) the giveback
  items when the user chooses to publish: the 0x811C9DC5 answer, the 2100-is-status-only
  finding (the fork implements NO 2100 handler — saves anyone chasing ability-swap
  semantics), eventually the flag dictionary.
- THE DO-NOT: no S3 front now; no Homecoming chase (their own doc dead-ends it at
  gameplay); no trusting the pending RVAs; NO CHANGE to the escalation ruling's ordering
  (commit the tree → the family-4 version fix → the census hooks → BOOT A).

## ORDERING-QUIRK NOTICE (2026-08-19 ~10:5x, the pen-fix pass)

The "## 11.8 THE COMMUNITY S3 ATLAS: THE WOW.MD DOCUMENT (2026-08-18 ~10:29)" heading
(line 779) sits AFTER "## 11.22 BOOT H..." (line 743) — an out-of-order append from
the 08-18 session (the S3-atlas work was written after boot H's entry but numbered
11.8). KNOWN QUIRK, NOT FIXED: the file is append-only; the entries are not moved.
The duplicated "11.8" number (the ruling at line 109 vs the S3 atlas at line 779)
is a naming conflict — the S3-atlas entry would be "11.23" if renumbered; it keeps
its label as recorded so cross-references stay stable. State the number, don't
trust the line order, when citing either.
