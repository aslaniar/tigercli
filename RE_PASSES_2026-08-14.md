# RE PASSES — 2026-08-14 (evening): parallel waves, cost-aware, GLM-5.3 A/B

Directive: further client-side RE in parallel lanes, mechanically verified, cost-aware.
Secondary objective: **A/B GLM 5.3 vs DeepSeek V4 Flash** on identical evidence-gated
briefs to measure which model runs our RE machine better. Local-only for now (no
upstream PRs; the Discord watch informs, doesn't drive).

## Lanes (P0-P3). Free lanes always run first and in parallel; LLM spend only where
## deterministic tooling cannot do the job. **All LLM lanes run on GLM 5.3** — this
## wave doubles as a qualitative capability read vs the v4-flash waves that produced
## the existing five specs (user judges by eye; no formal benchmark).

### P0 — FREE / deterministic (no LLM cost)
- **P0.a Sunrise local build** (S0 prereq): clone fresh (upstream moved to 0.2.1),
  build with v143 override (vcxproj asks v145), produce .map → symbolize the Detours
  far-stub replacement addresses (locks hook semantics before any fork).
- **P0.b Cold-run warming** (user-in-the-loop, near-zero cost): existing scripts
  (dump_sunrise_memory.py + compare_text_pages.py). Untested states that might execute
  the two encrypted runs (rva 0x5D0D000-0x609C000, 0x896A000-0x8A5E000): Crucible
  private match, raid arrival override, Director heavy use, character creation.
  Result either way is a findings line (definitive negative already recorded for
  menu/destination/orbit).
- **P0.c .pkg reader port** (S1/S2 prereq, my scripting): Python pkg-header parser
  (port tiger-pkg header logic) + decompression through the game's own
  oo2core_3_win64.dll via ctypes (Sunrise's Oodle trick). Target: walk ONE
  destination's scenario/spawn tables (start: w64_pvp_* or the Tower's
  city_tower_social_d2 packages) and dump raw spawn-point rows. Feeds P3.

### P1 — LLM, single agent, flash (cheap, no A/B needed)
- **Entity baseline schema**: with the tagReflection entry layout (0x28B) and the
  DAT_142439C70 resolver known, one agent digests the schema entries for entity/sobject
  types → names the per-archetype field layout (position, body_vitality,
  damage_sections, model ref). Output = "what bytes make a combatant," feeding S2/S3.
  Corpus: existing phase3-6 + strings. May need one small deterministic Ghidra dump
  (schema-entry region for entity types) — prep by main session only if the agent
  requests it (REQUEST gate).

### P2 — THE GLM 5.3 LANES (2 agent runs, one per task)
- **Task A: family-3 roster mapping** (never dug; mirrors the family-4 arc).
  Entry anchors: dispatch table entries for s->ws types (0x141FBF2F8=type100,
  0x141FBF300=102), the DAT_142808A70 runtime registry notes, queuez store model.
- **Task B: unmapped web-service opcode sweep** — verify 1820/0x71C and 1901/0x76D
  handlers; map 701/0x2BD and the 1200-1399/0x4B0-0x577 band. Method: grep the
  decompiled corpus for the hex immediates; known family-5 dispatch =
  FUN_140e0e4e0 + 15-entry table DAT_141FBCB60; find sibling handlers.
- **Agent files**: family3-roster.md / opcode-sweep.md — both
  model: opencode-go/glm-5.3, reasoningEffort: max.
- **Fair-corpus rule**: both lanes use the SAME frozen corpus (phases 3-6 + strings +
  census + anchors report). No new dumps mid-run.

### P2 QUALITY READ (informal, user-judged)
When the lanes land, compare against the equivalent v4-flash outputs from the
prior waves (family4-decoders.md as the family-3 yardstick): claim density,
evidence quality, correction rate caught by the Phase-7 gate, FINAL-section
usefulness. No scoreboard, no cost math — just "which one's map would you rather
build on."

### P3 — deferred until P0.c yields data
- Spawn-set tag analysis of extracted destination data (which tag class 0x80809162
  rows look like; spawn-point 48B structure confirmation; per-destination inventory).

## Cost guardrails
- Step caps 40 everywhere; briefs scoped to finish in-cap; write-early-and-often rule
  in every brief (family4's silent step-cap failure is the cautionary tale).
- Deterministic-first: P0 lanes consume zero model budget.
- A/B budget: exactly 4 agent runs. No 5-lane swarm until the A/B picks the model.
- Escalation per workspace AGENTS.md: flash/glm tiers dig; escalate only on a NAMED
  reasoning failure, not vibes.

## Execution order
1. [me] Create 4 A/B agent files (project + global .opencode/agent dirs).
2. [me] Optionally kick P0.a build prep (fresh clone) and P0.c parser skeleton.
3. [user] Restart opencode (agent registry picks up the new files).
4. [launch] P2: 4 background agents (2 tasks × 2 models).
5. [verify] Phase-7 pass when all four land → scoreboard → FINDINGS update +
   model decision for future swarms.

## Success criteria (this wave)
- Family-3 mapped to the same standard as family-4.
- Opcode band 1200-1399 at least half-identified with addresses.
- A/B scoreboard complete; a standing "which model digs better" answer.
- P0.a build green OR its blockers documented; P0.c parsing at least one package.
