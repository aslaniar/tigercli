# STATE-inventory — the inventory-relevant snapshot (namespaced parallel-work file)

Updated: 2026-08-19 ~10:5x by the main session (the pen-fix pass: the header
rewritten per the current truth — the family-4 version discipline = boot-validated
for two days, the swap front = narrowed to the equip's kind question; the parked
state, the pending boot L, and the opcode front carry below). The main session's
full snapshot = STATE.md (root). TIMESTAMP RULE: every new recording = the exact
time.

## CURRENT STATE (one paragraph)

THE SWAP FRONT: the ability swap COMMITS + PUSHES (the item upsert, boot-B's
"ABILITY SWAP WORKS"; the family-4 version discipline = LANDED + boot-validated in
boot-A — four 801s + the equip delivered +1 frames, all accepted, no kick, no
suicide). THE ONE OPEN ITEM = the SUBCLASS EQUIP's DISPLAY (the pop-ups + the
re-entry + the icons): the wire story = PROVEN COMPLETE (the 403's frame carries
the character + the item; the boot-K census refuted the BAP-channel theory — the
equip's frames DO take the WS wire), the panel trigger = NAMED (FUN_140a43300 +
the kind-1 trio), the remaining question = the walker's KIND CLASSIFICATION (why
the equip's objects don't take the stamp-compare kind 1) — the next lane. The
pop-ups = DEAD (the stamp revert); the tower fix = the join's refresh (3a8dd9b) +
the hot-path trap (the ws_wire hook = to be stripped). The 2100 = RETIRED. The
486-row alternative catalog + the reader-valid cache = the deployed.

## THE SWAP-FRONT STATUS (post-BOOT-K: the parked state)

- WORKING: the 403 equip (the record changes, the harness-verified), the 801 node
  picks (the commit + the persist + the banner push), the equip's pick-reset, the
  roster/banner fallback, THE ABILITY SWAP COMMITS (the item upsert — boot-B).
- THE FAMILY-4 VERSION DISCIPLINE = LANDED + boot-validated (boot-A: four 801s +
  the equip delivered +1 frames, all accepted, no kick, no suicide; the harness
  --selection-version-test = EXIT 0, 39/39). No longer an open item.
- THE ONE OPEN FRONT = THE SUBCLASS EQUIP's DISPLAY: the wire story = PROVEN
  COMPLETE (the 403 frame = character + item, harness 42/42; the boot-K census =
  the equip = ON THE WS WIRE, the BAP-channel theory refuted); the panel trigger =
  NAMED (FUN_140a43300 + the kind-1 trio, lane_panel_trigger.md); THE NEXT =
  the walker's kind classification (the cool-path hook on FUN_140e002d0's kind
  lookup) — why the equip's objects don't take the stamp-compare kind.
- THE 2100 = RETIRED: the fork implements no 2100 handler (the status-only
  opcode); our apply_ability_change's mutate-fail early-out = the correct shape;
  no reverse map.
- THE KNOWN FOLLOW-UP (parked): the bucket builder's strictness (the bundle-member
  combos = the rejected → the fallback covers).

## THE BANKED ASSETS (do not re-derive)

- The 801 port (the integration worktree, uncommitted): the codec, the
  socket_entry_buckets domain, the stage/commit, the staging branches, the loader's
  6th domain, the equip-reset, the fallback.
- The extraction scripts: build_socket_entry_buckets.py +
  build_alternative_ability_rows.py (the stage-mirror enumeration) +
  verify_checksum*.py + the --cache-check harness mode.
- The domain JSONs: s1_domains_socketEntryBuckets.json (14 rows) +
  s1_domains_abilityBuckets.json (486 rows).
- The cache: the reader-valid 486-row build (the .bak_altrows = the clean 9-row
  source; the .bak_801_pre/.bak_altrows backups).

## OPEN ITEMS (ranked — post-BOOT-K: the parked state)

1. THE WS_WIRE HOOK = TO STRIP (the hot-path trap) — the batch 14 → 13,
   keeping the bap_apply. The BOOT L = the tower-load re-observation.
2. THE EQUIP'S KIND QUESTION — the next lane: why the wire-path frames don't
   take the compare kind (the cool-path hook on the walker's kind lookup).
3. THE VETERAN HUNT — the halving of the kept fold (the Layer-2 /restore) =
   the next boot's rider.
4. THE OPCODE SESSION = the user's parallel work.
5. The bucket builder's strictness (parked) + the dll-decapi items (parked).

## THE OPCODE FRONT (2026-08-19, the fusion day: B/C/D landed, E = resume-running)

- LANE D (vendor-verb inference) LANDED + VERIFIED: the 1200-1399 band has NO emitter in this
  build (whole-corpus scan + the result-enum valediction); the storefront verbs that exist =
  1820/904/1901/801-804/403/601/701/702 + BAP purchased-offers; the 24-entry `_result_` enum at
  0x141BDCD70 (index 1 = success) names the outcomes. The ranked boot list (P1-1 vendor open =
  the storefront-round-trip test, settle GAP-4). Deliverable = claims/vendor-verb-inference.md.
- LANE B LANDED SILENT: the opcode-switch hypothesis DISPROVEN — FUN_140DFEC50 = pure vtable
  dispatch (BAP type 11 → R19 0x141C3D2A8 + txnId correlation); the semantics live in the
  per-opcode response decoders. Deliverable = claims/opcode-decoder-map.md.
- LANE C LANDED (death-at-report-back, recovered): the 237 empty decoder slots are structurally
  impossible — one factory (FUN_140E75790) fills 71 from a static table at static-init.
  Deliverable = claims/decoder-registry-fill-order.md.
- LANE E = LANDED (resume ses_fe6c39a59f): 9 opcodes named by the upstream codecs (402/403/404/
  406/801/901/903/1820/1901), all portable; 403+801 VERIFIED in the client's traffic (byte-exact).
  Deliverable = claims/upstream-opcode-codec-diff.md. CORRECTION (the 2026-08-19 audit, FINDINGS
  12.8): the "16-file codec MERGE" = ~90% PHANTOM — the codecs already rode the 8/16 upstream
  merge into the worktree (git: b1c366d/60c3a44/f8e4a67) + our 801 port (84acc9c); lane E compared
  against MASTER (a worktree error). WHAT REMAINS = the external-server vcxproj entries + the
  consume() branches (only 403-inline + 801 exist today) + the 403 reconciliation, gated AFTER
  boot L. The P1-1 vendor-open boot (lane D's #1) = the next boot candidate.
- PROCESS CORRECTION (binding): check disk + opencode.db session_message BEFORE any re-spawn
  (FINDINGS 12.6).

## Workstream rules (inherited, abbreviated)

No game boots without the main session's go (hashes + incident.py; the cache/settings
re-hash first — the stamp-on-persist). The fork = the shared repo; implementation work
= the integration worktree. The deliverable rule, the lane death-safety rule, the
Ghidra-Python rule, the shell-layout rule, the timestamp rule, the boot-brief rule —
all inherited from AGENTS.md, unchanged.
